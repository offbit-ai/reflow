#![allow(clippy::all, unused_imports, dead_code)]

//! Auto-generated API actors for Jira
//!
//! Service: Jira (jira)
//! Issue and project tracking software
//!
//! Required env var: JIRA_API_KEY (OAuth2 access token)
//!
//! Generated by api-schema-gen codegen — do not edit manually.

use crate::{Actor, ActorBehavior, Message, Port};
use actor_macro::actor;
use anyhow::{Error, Result};
use reflow_actor::{message::EncodableValue, ActorContext};
use serde_json::{json, Value};
use std::collections::HashMap;
use std::time::Duration;

const BASE_URL: &str = "https://your-domain.atlassian.net";
const ENV_KEY: &str = "JIRA_API_KEY";

/// Apply authentication to the request builder.
fn apply_auth(
    config: &reflow_actor::ActorConfig,
    mut builder: reqwest::RequestBuilder,
) -> Result<reqwest::RequestBuilder> {
    let credential = config
        .get_config_or_env(ENV_KEY)
        .ok_or_else(|| anyhow::anyhow!("Missing env var: {}", ENV_KEY))?;
    builder = builder.header("Authorization", format!("Bearer {}", credential));
    Ok(builder)
}

/// create issue via Jira API
///
/// Method: POST /issue
#[actor(
    JiraCreateIssueActor,
    inports::<100>(fields),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/issue".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("fields") {
        body.insert("fields".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /issue failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// search issues via Jira API
///
/// Method: GET /search
#[actor(
    JiraSearchIssuesActor,
    inports::<100>(jql),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_issues(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("jql") {
        query_pairs.push(("jql", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// update issue via Jira API
///
/// Method: PUT /issue/{issueIdOrKey}
#[actor(
    JiraUpdateIssueActor,
    inports::<100>(issueIdOrKey, fields),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/issue/{issueIdOrKey}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("fields") {
        body.insert("fields".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /issue/{{issueIdOrKey}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get announcement banner configuration
///
/// Method: GET /rest/api/3/announcementBanner
#[actor(
    JiraReadBannerActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_banner(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/announcementBanner".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/announcementBanner failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update announcement banner configuration
///
/// Method: PUT /rest/api/3/announcementBanner
#[actor(
    JiraUpdateAnnouncementBannerActor,
    inports::<100>(message, isEnabled, visibility, isDismissible),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_announcement_banner(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/announcementBanner".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("message") {
        body.insert("message".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("isEnabled") {
        body.insert("isEnabled".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("visibility") {
        body.insert("visibility".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("isDismissible") {
        body.insert("isDismissible".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/announcementBanner failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk get custom field configurations
///
/// Method: POST /rest/api/3/app/field/context/configuration/list
#[actor(
    JiraReadCustomFieldsConfigurationsActor,
    inports::<100>(id, fieldContextId, issueId, projectKeyOrId, issueTypeId, startAt, maxResults, fieldIdsOrKeys),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_custom_fields_configurations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/app/field/context/configuration/list".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fieldContextId") {
        query_pairs.push(("fieldContextId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueId") {
        query_pairs.push(("issueId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectKeyOrId") {
        query_pairs.push(("projectKeyOrId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueTypeId") {
        query_pairs.push(("issueTypeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("fieldIdsOrKeys") {
        body.insert("fieldIdsOrKeys".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/app/field/context/configuration/list failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update custom fields
///
/// Method: POST /rest/api/3/app/field/value
#[actor(
    JiraUpdateMultipleCustomFieldValuesActor,
    inports::<100>(generateChangelog, updates),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_multiple_custom_field_values(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/app/field/value".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("generateChangelog") {
        query_pairs.push(("generateChangelog", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("updates") {
        body.insert("updates".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/app/field/value failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get custom field configurations
///
/// Method: GET /rest/api/3/app/field/{fieldIdOrKey}/context/configuration
#[actor(
    JiraReadCustomFieldConfigurationActor,
    inports::<100>(fieldIdOrKey, id, fieldContextId, issueId, projectKeyOrId, issueTypeId, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_custom_field_configuration(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/app/field/{fieldIdOrKey}/context/configuration".to_string();
    if let Some(val) = inputs.get("fieldIdOrKey") {
        endpoint = endpoint.replace("{{fieldIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fieldContextId") {
        query_pairs.push(("fieldContextId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueId") {
        query_pairs.push(("issueId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectKeyOrId") {
        query_pairs.push(("projectKeyOrId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueTypeId") {
        query_pairs.push(("issueTypeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/app/field/{{fieldIdOrKey}}/context/configuration failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update custom field configurations
///
/// Method: PUT /rest/api/3/app/field/{fieldIdOrKey}/context/configuration
#[actor(
    JiraUpdateCustomFieldConfigurationActor,
    inports::<100>(fieldIdOrKey, configurations),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_custom_field_configuration(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/app/field/{fieldIdOrKey}/context/configuration".to_string();
    if let Some(val) = inputs.get("fieldIdOrKey") {
        endpoint = endpoint.replace("{{fieldIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("configurations") {
        body.insert("configurations".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/app/field/{{fieldIdOrKey}}/context/configuration failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update custom field value
///
/// Method: PUT /rest/api/3/app/field/{fieldIdOrKey}/value
#[actor(
    JiraUpdateCustomFieldValueActor,
    inports::<100>(fieldIdOrKey, generateChangelog, updates),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_custom_field_value(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/app/field/{fieldIdOrKey}/value".to_string();
    if let Some(val) = inputs.get("fieldIdOrKey") {
        endpoint = endpoint.replace("{{fieldIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("generateChangelog") {
        query_pairs.push(("generateChangelog", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("updates") {
        body.insert("updates".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/app/field/{{fieldIdOrKey}}/value failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get application property
///
/// Method: GET /rest/api/3/application-properties
#[actor(
    JiraReadApplicationPropertyActor,
    inports::<100>(key, permissionLevel, keyFilter),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_application_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/application-properties".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("permissionLevel") {
        query_pairs.push(("permissionLevel", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("keyFilter") {
        query_pairs.push(("keyFilter", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/application-properties failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get advanced settings
///
/// Method: GET /rest/api/3/application-properties/advanced-settings
#[actor(
    JiraReadAdvancedSettingsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_advanced_settings(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/application-properties/advanced-settings".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/application-properties/advanced-settings failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set application property
///
/// Method: PUT /rest/api/3/application-properties/{id}
#[actor(
    JiraUpdateJiraSettingsActor,
    inports::<100>(id, value),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_jira_settings(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/application-properties/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("value") {
        body.insert("value".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/application-properties/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all application roles
///
/// Method: GET /rest/api/3/applicationrole
#[actor(
    JiraReadAllApplicationRolesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_application_roles(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/applicationrole".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/applicationrole failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get application role
///
/// Method: GET /rest/api/3/applicationrole/{key}
#[actor(
    JiraReadApplicationRoleActor,
    inports::<100>(key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_application_role(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/applicationrole/{key}".to_string();
    if let Some(val) = inputs.get("key") {
        endpoint = endpoint.replace("{{key}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/applicationrole/{{key}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get attachment content
///
/// Method: GET /rest/api/3/attachment/content/{id}
#[actor(
    JiraReadAttachmentContentActor,
    inports::<100>(id, redirect),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_attachment_content(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/attachment/content/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("redirect") {
        query_pairs.push(("redirect", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/attachment/content/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get Jira attachment settings
///
/// Method: GET /rest/api/3/attachment/meta
#[actor(
    JiraReadAttachmentMetaActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_attachment_meta(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/attachment/meta".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/attachment/meta failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get attachment thumbnail
///
/// Method: GET /rest/api/3/attachment/thumbnail/{id}
#[actor(
    JiraReadAttachmentThumbnailActor,
    inports::<100>(id, redirect, fallbackToDefault, width, height),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_attachment_thumbnail(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/attachment/thumbnail/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("redirect") {
        query_pairs.push(("redirect", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fallbackToDefault") {
        query_pairs.push(("fallbackToDefault", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("width") {
        query_pairs.push(("width", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("height") {
        query_pairs.push(("height", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/attachment/thumbnail/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get attachment metadata
///
/// Method: GET /rest/api/3/attachment/{id}
#[actor(
    JiraReadAttachmentActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_attachment(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/attachment/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/attachment/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete attachment
///
/// Method: DELETE /rest/api/3/attachment/{id}
#[actor(
    JiraDeleteAttachmentActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_attachment(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/attachment/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/attachment/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all metadata for an expanded attachment
///
/// Method: GET /rest/api/3/attachment/{id}/expand/human
#[actor(
    JiraReadIssueAttachmentsActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_attachments(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/attachment/{id}/expand/human".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/attachment/{{id}}/expand/human failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get audit records
///
/// Method: GET /rest/api/3/auditing/record
#[actor(
    JiraReadAuditRecordsActor,
    inports::<100>(offset, limit, filter, from, to),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_audit_records(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/auditing/record".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("offset") {
        query_pairs.push(("offset", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("limit") {
        query_pairs.push(("limit", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("filter") {
        query_pairs.push(("filter", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("from") {
        query_pairs.push(("from", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("to") {
        query_pairs.push(("to", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/auditing/record failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get system avatars by type
///
/// Method: GET /rest/api/3/avatar/{type}/system
#[actor(
    JiraReadAllSystemAvatarsActor,
    inports::<100>(type_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_system_avatars(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/avatar/{type}/system".to_string();
    if let Some(val) = inputs.get("type_") {
        endpoint = endpoint.replace("{{type}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/avatar/{{type}}/system failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk delete issues
///
/// Method: POST /rest/api/3/bulk/issues/delete
#[actor(
    JiraCreateIssueBulkOperationsActor,
    inports::<100>(sendBulkNotification, selectedIssueIdsOrKeys),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_bulk_operations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/bulk/issues/delete".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("sendBulkNotification") {
        body.insert("sendBulkNotification".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("selectedIssueIdsOrKeys") {
        body.insert("selectedIssueIdsOrKeys".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/bulk/issues/delete failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get bulk editable fields
///
/// Method: GET /rest/api/3/bulk/issues/fields
#[actor(
    JiraReadBulkEditableFieldsActor,
    inports::<100>(issueIdsOrKeys, searchText, endingBefore, startingAfter),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_bulk_editable_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/bulk/issues/fields".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("issueIdsOrKeys") {
        query_pairs.push(("issueIdsOrKeys", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("searchText") {
        query_pairs.push(("searchText", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("endingBefore") {
        query_pairs.push(("endingBefore", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startingAfter") {
        query_pairs.push(("startingAfter", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/bulk/issues/fields failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk move issues
///
/// Method: POST /rest/api/3/bulk/issues/move
#[actor(
    JiraMoveIssueBulkOperationsActor,
    inports::<100>(sendBulkNotification, targetToSourcesMapping),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_issue_bulk_operations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/bulk/issues/move".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("sendBulkNotification") {
        body.insert("sendBulkNotification".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("targetToSourcesMapping") {
        body.insert("targetToSourcesMapping".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/bulk/issues/move failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get available transitions
///
/// Method: GET /rest/api/3/bulk/issues/transition
#[actor(
    JiraReadAvailableTransitionsActor,
    inports::<100>(issueIdsOrKeys, endingBefore, startingAfter),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_available_transitions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/bulk/issues/transition".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("issueIdsOrKeys") {
        query_pairs.push(("issueIdsOrKeys", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("endingBefore") {
        query_pairs.push(("endingBefore", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startingAfter") {
        query_pairs.push(("startingAfter", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/bulk/issues/transition failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk unwatch issues
///
/// Method: POST /rest/api/3/bulk/issues/unwatch
#[actor(
    JiraUnwatchIssueBulkOperationsActor,
    inports::<100>(selectedIssueIdsOrKeys),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_unwatch_issue_bulk_operations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/bulk/issues/unwatch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("selectedIssueIdsOrKeys") {
        body.insert("selectedIssueIdsOrKeys".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/bulk/issues/unwatch failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk watch issues
///
/// Method: POST /rest/api/3/bulk/issues/watch
#[actor(
    JiraWatchIssueBulkOperationsActor,
    inports::<100>(selectedIssueIdsOrKeys),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_watch_issue_bulk_operations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/bulk/issues/watch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("selectedIssueIdsOrKeys") {
        body.insert("selectedIssueIdsOrKeys".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/bulk/issues/watch failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get bulk issue operation progress
///
/// Method: GET /rest/api/3/bulk/queue/{taskId}
#[actor(
    JiraReadBulkOperationProgressActor,
    inports::<100>(taskId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_bulk_operation_progress(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/bulk/queue/{taskId}".to_string();
    if let Some(val) = inputs.get("taskId") {
        endpoint = endpoint.replace("{{taskId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/bulk/queue/{{taskId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk fetch changelogs
///
/// Method: POST /rest/api/3/changelog/bulkfetch
#[actor(
    JiraReadBulkChangelogsActor,
    inports::<100>(fieldIds, maxResults, issueIdsOrKeys, nextPageToken),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_bulk_changelogs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/changelog/bulkfetch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("fieldIds") {
        body.insert("fieldIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("maxResults") {
        body.insert("maxResults".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueIdsOrKeys") {
        body.insert("issueIdsOrKeys".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("nextPageToken") {
        body.insert("nextPageToken".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/changelog/bulkfetch failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all classification levels
///
/// Method: GET /rest/api/3/classification-levels
#[actor(
    JiraReadAllUserDataClassificationLevelsActor,
    inports::<100>(status, orderBy),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_user_data_classification_levels(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/classification-levels".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("status") {
        query_pairs.push(("status", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/classification-levels failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get comments by IDs
///
/// Method: POST /rest/api/3/comment/list
#[actor(
    JiraReadCommentsByIdsActor,
    inports::<100>(expand, ids),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_comments_by_ids(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/comment/list".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("ids") {
        body.insert("ids".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/comment/list failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get comment property keys
///
/// Method: GET /rest/api/3/comment/{commentId}/properties
#[actor(
    JiraReadCommentPropertyKeysActor,
    inports::<100>(commentId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_comment_property_keys(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/comment/{commentId}/properties".to_string();
    if let Some(val) = inputs.get("commentId") {
        endpoint = endpoint.replace("{{commentId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/comment/{{commentId}}/properties failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete comment property
///
/// Method: DELETE /rest/api/3/comment/{commentId}/properties/{propertyKey}
#[actor(
    JiraDeleteCommentPropertyActor,
    inports::<100>(commentId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_comment_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/comment/{commentId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("commentId") {
        endpoint = endpoint.replace("{{commentId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/comment/{{commentId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get comment property
///
/// Method: GET /rest/api/3/comment/{commentId}/properties/{propertyKey}
#[actor(
    JiraReadCommentPropertyActor,
    inports::<100>(commentId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_comment_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/comment/{commentId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("commentId") {
        endpoint = endpoint.replace("{{commentId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/comment/{{commentId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Set comment property
///
/// Method: PUT /rest/api/3/comment/{commentId}/properties/{propertyKey}
#[actor(
    JiraUpdateIssueCommentPropertiesActor,
    inports::<100>(commentId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_comment_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/comment/{commentId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("commentId") {
        endpoint = endpoint.replace("{{commentId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/comment/{{commentId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create component
///
/// Method: POST /rest/api/3/component
#[actor(
    JiraCreateComponentActor,
    inports::<100>(ari, realAssigneeType, assignee, name, project, self_, description, lead, leadUserName, isAssigneeTypeValid, projectId, leadAccountId, metadata, assigneeType, id, realAssignee),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_component(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/component".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("ari") {
        body.insert("ari".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("realAssigneeType") {
        body.insert("realAssigneeType".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("assignee") {
        body.insert("assignee".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("project") {
        body.insert("project".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lead") {
        body.insert("lead".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("leadUserName") {
        body.insert("leadUserName".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("isAssigneeTypeValid") {
        body.insert("isAssigneeTypeValid".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectId") {
        body.insert("projectId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("leadAccountId") {
        body.insert("leadAccountId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("metadata") {
        body.insert("metadata".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("assigneeType") {
        body.insert("assigneeType".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("realAssignee") {
        body.insert("realAssignee".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/component failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Find components for projects
///
/// Method: GET /rest/api/3/component
#[actor(
    JiraSearchComponentsForProjectsActor,
    inports::<100>(projectIdsOrKeys, startAt, maxResults, orderBy, query),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_components_for_projects(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/component".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectIdsOrKeys") {
        query_pairs.push(("projectIdsOrKeys", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/component failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete component
///
/// Method: DELETE /rest/api/3/component/{id}
#[actor(
    JiraDeleteComponentActor,
    inports::<100>(id, moveIssuesTo),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_component(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/component/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("moveIssuesTo") {
        query_pairs.push(("moveIssuesTo", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/component/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get component
///
/// Method: GET /rest/api/3/component/{id}
#[actor(
    JiraReadComponentActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_component(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/component/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/component/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update component
///
/// Method: PUT /rest/api/3/component/{id}
#[actor(
    JiraUpdateComponentActor,
    inports::<100>(id, assigneeType, realAssignee, description, realAssigneeType, isAssigneeTypeValid, name, lead, self_, leadAccountId, metadata, assignee, project, projectId, ari, leadUserName),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_component(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/component/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("assigneeType") {
        body.insert("assigneeType".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("realAssignee") {
        body.insert("realAssignee".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("realAssigneeType") {
        body.insert("realAssigneeType".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("isAssigneeTypeValid") {
        body.insert("isAssigneeTypeValid".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lead") {
        body.insert("lead".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("leadAccountId") {
        body.insert("leadAccountId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("metadata") {
        body.insert("metadata".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("assignee") {
        body.insert("assignee".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("project") {
        body.insert("project".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectId") {
        body.insert("projectId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("ari") {
        body.insert("ari".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("leadUserName") {
        body.insert("leadUserName".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/component/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get component issues count
///
/// Method: GET /rest/api/3/component/{id}/relatedIssueCounts
#[actor(
    JiraReadComponentRelatedIssuesActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_component_related_issues(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/component/{id}/relatedIssueCounts".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/component/{{id}}/relatedIssueCounts failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get field schemes
///
/// Method: GET /rest/api/3/config/fieldschemes
#[actor(
    JiraReadFieldAssociationSchemesActor,
    inports::<100>(projectId, query, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_field_association_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/config/fieldschemes".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/config/fieldschemes failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create field scheme
///
/// Method: POST /rest/api/3/config/fieldschemes
#[actor(
    JiraCreateFieldAssociationSchemeActor,
    inports::<100>(name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_field_association_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/config/fieldschemes".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/config/fieldschemes failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update fields associated with field schemes
///
/// Method: PUT /rest/api/3/config/fieldschemes/fields
#[actor(
    JiraUpdateFieldsAssociatedWithSchemesActor,
    inports::<100>(body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_fields_associated_with_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/config/fieldschemes/fields".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/config/fieldschemes/fields failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove fields associated with field schemes
///
/// Method: DELETE /rest/api/3/config/fieldschemes/fields
#[actor(
    JiraDeleteFieldsAssociatedWithSchemesActor,
    inports::<100>(body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_fields_associated_with_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/config/fieldschemes/fields".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/config/fieldschemes/fields failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove field parameters
///
/// Method: DELETE /rest/api/3/config/fieldschemes/fields/parameters
#[actor(
    JiraDeleteFieldAssociationSchemeItemParametersActor,
    inports::<100>(body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_field_association_scheme_item_parameters(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/config/fieldschemes/fields/parameters".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/config/fieldschemes/fields/parameters failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update field parameters
///
/// Method: PUT /rest/api/3/config/fieldschemes/fields/parameters
#[actor(
    JiraUpdateFieldAssociationSchemeItemParametersActor,
    inports::<100>(body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_field_association_scheme_item_parameters(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/config/fieldschemes/fields/parameters".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/config/fieldschemes/fields/parameters failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get projects with field schemes
///
/// Method: GET /rest/api/3/config/fieldschemes/projects
#[actor(
    JiraReadProjectsWithFieldSchemesActor,
    inports::<100>(startAt, maxResults, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_projects_with_field_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/config/fieldschemes/projects".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/config/fieldschemes/projects failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Associate projects to field schemes
///
/// Method: PUT /rest/api/3/config/fieldschemes/projects
#[actor(
    JiraUpdateFieldSchemesActor,
    inports::<100>(body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_field_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/config/fieldschemes/projects".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/config/fieldschemes/projects failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete a field scheme
///
/// Method: DELETE /rest/api/3/config/fieldschemes/{id}
#[actor(
    JiraDeleteFieldAssociationSchemeActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_field_association_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/config/fieldschemes/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/config/fieldschemes/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get field scheme
///
/// Method: GET /rest/api/3/config/fieldschemes/{id}
#[actor(
    JiraReadFieldAssociationSchemeByIdActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_field_association_scheme_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/config/fieldschemes/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/config/fieldschemes/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update field scheme
///
/// Method: PUT /rest/api/3/config/fieldschemes/{id}
#[actor(
    JiraUpdateFieldAssociationSchemeActor,
    inports::<100>(id, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_field_association_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/config/fieldschemes/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/config/fieldschemes/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Clone field scheme
///
/// Method: POST /rest/api/3/config/fieldschemes/{id}/clone
#[actor(
    JiraCreateFieldSchemesActor,
    inports::<100>(id, name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_field_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/config/fieldschemes/{id}/clone".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/config/fieldschemes/{{id}}/clone failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Search field scheme fields
///
/// Method: GET /rest/api/3/config/fieldschemes/{id}/fields
#[actor(
    JiraSearchFieldAssociationSchemeFieldsActor,
    inports::<100>(startAt, maxResults, fieldId, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_field_association_scheme_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/config/fieldschemes/{id}/fields".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fieldId") {
        query_pairs.push(("fieldId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/config/fieldschemes/{{id}}/fields failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get field parameters
///
/// Method: GET /rest/api/3/config/fieldschemes/{id}/fields/{fieldId}/parameters
#[actor(
    JiraReadFieldAssociationSchemeItemParametersActor,
    inports::<100>(id, fieldId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_field_association_scheme_item_parameters(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/config/fieldschemes/{id}/fields/{fieldId}/parameters".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/config/fieldschemes/{{id}}/fields/{{fieldId}}/parameters failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Search field scheme projects
///
/// Method: GET /rest/api/3/config/fieldschemes/{id}/projects
#[actor(
    JiraSearchFieldAssociationSchemeProjectsActor,
    inports::<100>(startAt, maxResults, projectId, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_field_association_scheme_projects(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/config/fieldschemes/{id}/projects".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/config/fieldschemes/{{id}}/projects failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get global settings
///
/// Method: GET /rest/api/3/configuration
#[actor(
    JiraReadConfigurationActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_configuration(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/configuration".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/configuration failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Select time tracking provider
///
/// Method: PUT /rest/api/3/configuration/timetracking
#[actor(
    JiraUpdateTimeTrackingActor,
    inports::<100>(url, name, key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_time_tracking(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/configuration/timetracking".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("url") {
        body.insert("url".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("key") {
        body.insert("key".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/configuration/timetracking failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get selected time tracking provider
///
/// Method: GET /rest/api/3/configuration/timetracking
#[actor(
    JiraReadSelectedTimeTrackingImplementationActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_selected_time_tracking_implementation(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/configuration/timetracking".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/configuration/timetracking failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all time tracking providers
///
/// Method: GET /rest/api/3/configuration/timetracking/list
#[actor(
    JiraReadAvailableTimeTrackingImplementationsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_available_time_tracking_implementations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/configuration/timetracking/list".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/configuration/timetracking/list failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get time tracking settings
///
/// Method: GET /rest/api/3/configuration/timetracking/options
#[actor(
    JiraReadSharedTimeTrackingConfigurationActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_shared_time_tracking_configuration(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/configuration/timetracking/options".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/configuration/timetracking/options failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get custom field option
///
/// Method: GET /rest/api/3/customFieldOption/{id}
#[actor(
    JiraReadCustomFieldOptionActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_custom_field_option(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/customFieldOption/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/customFieldOption/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all dashboards
///
/// Method: GET /rest/api/3/dashboard
#[actor(
    JiraReadAllDashboardsActor,
    inports::<100>(filter, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_dashboards(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/dashboard".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("filter") {
        query_pairs.push(("filter", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/dashboard failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create dashboard
///
/// Method: POST /rest/api/3/dashboard
#[actor(
    JiraCreateDashboardActor,
    inports::<100>(extendAdminPermissions, editPermissions, name, description, sharePermissions),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_dashboard(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/dashboard".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("extendAdminPermissions") {
        query_pairs.push(("extendAdminPermissions", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("editPermissions") {
        body.insert("editPermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("sharePermissions") {
        body.insert("sharePermissions".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/dashboard failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk edit dashboards
///
/// Method: PUT /rest/api/3/dashboard/bulk/edit
#[actor(
    JiraUpdateDashboardsActor,
    inports::<100>(entityIds, action, extendAdminPermissions, changeOwnerDetails, permissionDetails),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_dashboards(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/dashboard/bulk/edit".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("entityIds") {
        body.insert("entityIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("action") {
        body.insert("action".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("extendAdminPermissions") {
        body.insert("extendAdminPermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("changeOwnerDetails") {
        body.insert("changeOwnerDetails".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("permissionDetails") {
        body.insert("permissionDetails".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/dashboard/bulk/edit failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get available gadgets
///
/// Method: GET /rest/api/3/dashboard/gadgets
#[actor(
    JiraReadAllAvailableDashboardGadgetsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_available_dashboard_gadgets(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/dashboard/gadgets".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/dashboard/gadgets failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Search for dashboards
///
/// Method: GET /rest/api/3/dashboard/search
#[actor(
    JiraReadDashboardsPaginatedActor,
    inports::<100>(dashboardName, accountId, owner, groupname, groupId, projectId, orderBy, startAt, maxResults, status, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_dashboards_paginated(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/dashboard/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("dashboardName") {
        query_pairs.push(("dashboardName", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("owner") {
        query_pairs.push(("owner", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupname") {
        query_pairs.push(("groupname", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("status") {
        query_pairs.push(("status", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/dashboard/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Add gadget to dashboard
///
/// Method: POST /rest/api/3/dashboard/{dashboardId}/gadget
#[actor(
    JiraCreateDashboardsActor,
    inports::<100>(dashboardId, position, ignoreUriAndModuleKeyValidation, moduleKey, color, title, uri),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_dashboards(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{dashboardId}/gadget".to_string();
    if let Some(val) = inputs.get("dashboardId") {
        endpoint = endpoint.replace("{{dashboardId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("position") {
        body.insert("position".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("ignoreUriAndModuleKeyValidation") {
        body.insert(
            "ignoreUriAndModuleKeyValidation".to_string(),
            val.clone().into(),
        );
    }
    if let Some(val) = inputs.get("moduleKey") {
        body.insert("moduleKey".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("color") {
        body.insert("color".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("title") {
        body.insert("title".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("uri") {
        body.insert("uri".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/dashboard/{{dashboardId}}/gadget failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get gadgets
///
/// Method: GET /rest/api/3/dashboard/{dashboardId}/gadget
#[actor(
    JiraReadAllGadgetsActor,
    inports::<100>(dashboardId, moduleKey, uri, gadgetId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_gadgets(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{dashboardId}/gadget".to_string();
    if let Some(val) = inputs.get("dashboardId") {
        endpoint = endpoint.replace("{{dashboardId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("moduleKey") {
        query_pairs.push(("moduleKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("uri") {
        query_pairs.push(("uri", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("gadgetId") {
        query_pairs.push(("gadgetId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/dashboard/{{dashboardId}}/gadget failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove gadget from dashboard
///
/// Method: DELETE /rest/api/3/dashboard/{dashboardId}/gadget/{gadgetId}
#[actor(
    JiraDeleteGadgetActor,
    inports::<100>(dashboardId, gadgetId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_gadget(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{dashboardId}/gadget/{gadgetId}".to_string();
    if let Some(val) = inputs.get("dashboardId") {
        endpoint = endpoint.replace("{{dashboardId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("gadgetId") {
        endpoint = endpoint.replace("{{gadgetId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/dashboard/{{dashboardId}}/gadget/{{gadgetId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update gadget on dashboard
///
/// Method: PUT /rest/api/3/dashboard/{dashboardId}/gadget/{gadgetId}
#[actor(
    JiraUpdateGadgetActor,
    inports::<100>(dashboardId, gadgetId, title, color, position),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_gadget(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{dashboardId}/gadget/{gadgetId}".to_string();
    if let Some(val) = inputs.get("dashboardId") {
        endpoint = endpoint.replace("{{dashboardId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("gadgetId") {
        endpoint = endpoint.replace("{{gadgetId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("title") {
        body.insert("title".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("color") {
        body.insert("color".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("position") {
        body.insert("position".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/dashboard/{{dashboardId}}/gadget/{{gadgetId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get dashboard item property keys
///
/// Method: GET /rest/api/3/dashboard/{dashboardId}/items/{itemId}/properties
#[actor(
    JiraReadDashboardItemPropertyKeysActor,
    inports::<100>(dashboardId, itemId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_dashboard_item_property_keys(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{dashboardId}/items/{itemId}/properties".to_string();
    if let Some(val) = inputs.get("dashboardId") {
        endpoint = endpoint.replace("{{dashboardId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("itemId") {
        endpoint = endpoint.replace("{{itemId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/dashboard/{{dashboardId}}/items/{{itemId}}/properties failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete dashboard item property
///
/// Method: DELETE /rest/api/3/dashboard/{dashboardId}/items/{itemId}/properties/{propertyKey}
#[actor(
    JiraDeleteDashboardItemPropertyActor,
    inports::<100>(dashboardId, itemId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_dashboard_item_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/dashboard/{dashboardId}/items/{itemId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("dashboardId") {
        endpoint = endpoint.replace("{{dashboardId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("itemId") {
        endpoint = endpoint.replace("{{itemId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/dashboard/{{dashboardId}}/items/{{itemId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get dashboard item property
///
/// Method: GET /rest/api/3/dashboard/{dashboardId}/items/{itemId}/properties/{propertyKey}
#[actor(
    JiraReadDashboardItemPropertyActor,
    inports::<100>(dashboardId, itemId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_dashboard_item_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/dashboard/{dashboardId}/items/{itemId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("dashboardId") {
        endpoint = endpoint.replace("{{dashboardId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("itemId") {
        endpoint = endpoint.replace("{{itemId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/dashboard/{{dashboardId}}/items/{{itemId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete dashboard
///
/// Method: DELETE /rest/api/3/dashboard/{id}
#[actor(
    JiraDeleteDashboardActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_dashboard(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/dashboard/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get dashboard
///
/// Method: GET /rest/api/3/dashboard/{id}
#[actor(
    JiraReadDashboardActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_dashboard(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/dashboard/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update dashboard
///
/// Method: PUT /rest/api/3/dashboard/{id}
#[actor(
    JiraUpdateDashboardActor,
    inports::<100>(id, extendAdminPermissions, editPermissions, name, sharePermissions, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_dashboard(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("extendAdminPermissions") {
        query_pairs.push(("extendAdminPermissions", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("editPermissions") {
        body.insert("editPermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("sharePermissions") {
        body.insert("sharePermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/dashboard/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Copy dashboard
///
/// Method: POST /rest/api/3/dashboard/{id}/copy
#[actor(
    JiraCopyDashboardsActor,
    inports::<100>(id, extendAdminPermissions, editPermissions, description, name, sharePermissions),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_copy_dashboards(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/dashboard/{id}/copy".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("extendAdminPermissions") {
        query_pairs.push(("extendAdminPermissions", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("editPermissions") {
        body.insert("editPermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("sharePermissions") {
        body.insert("sharePermissions".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/dashboard/{{id}}/copy failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get data policy for the workspace
///
/// Method: GET /rest/api/3/data-policy
#[actor(
    JiraReadPolicyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_policy(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/data-policy".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/data-policy failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get data policy for projects
///
/// Method: GET /rest/api/3/data-policy/project
#[actor(
    JiraReadPoliciesActor,
    inports::<100>(ids),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_policies(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/data-policy/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("ids") {
        query_pairs.push(("ids", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/data-policy/project failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get events
///
/// Method: GET /rest/api/3/events
#[actor(
    JiraReadEventsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_events(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/events".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/events failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Analyse Jira expression
///
/// Method: POST /rest/api/3/expression/analyse
#[actor(
    JiraCreateJiraExpressionsActor,
    inports::<100>(check, contextVariables, expressions),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_jira_expressions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/expression/analyse".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("check") {
        query_pairs.push(("check", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("contextVariables") {
        body.insert("contextVariables".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("expressions") {
        body.insert("expressions".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/expression/analyse failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create custom field
///
/// Method: POST /rest/api/3/field
#[actor(
    JiraCreateCustomFieldActor,
    inports::<100>(description, searcherKey, name, type_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_custom_field(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/field".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("searcherKey") {
        body.insert("searcherKey".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("type_") {
        body.insert("type".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/field failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get fields
///
/// Method: GET /rest/api/3/field
#[actor(
    JiraReadFieldsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_fields(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/field".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/field failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Remove associations
///
/// Method: DELETE /rest/api/3/field/association
#[actor(
    JiraDeleteAssociationsActor,
    inports::<100>(associationContexts, fields),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_associations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/field/association".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/field/association failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create associations
///
/// Method: PUT /rest/api/3/field/association
#[actor(
    JiraCreateAssociationsActor,
    inports::<100>(fields, associationContexts),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_associations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/field/association".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("fields") {
        body.insert("fields".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("associationContexts") {
        body.insert("associationContexts".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/field/association failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get fields paginated
///
/// Method: GET /rest/api/3/field/search
#[actor(
    JiraReadFieldsPaginatedActor,
    inports::<100>(startAt, maxResults, type_, id, query, orderBy, expand, projectIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_fields_paginated(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/field/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("type_") {
        query_pairs.push(("type", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectIds") {
        query_pairs.push(("projectIds", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/field/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get fields in trash paginated
///
/// Method: GET /rest/api/3/field/search/trashed
#[actor(
    JiraReadTrashedFieldsPaginatedActor,
    inports::<100>(startAt, maxResults, id, query, expand, orderBy),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_trashed_fields_paginated(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/field/search/trashed".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/field/search/trashed failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update custom field
///
/// Method: PUT /rest/api/3/field/{fieldId}
#[actor(
    JiraUpdateCustomFieldActor,
    inports::<100>(fieldId, searcherKey, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_custom_field(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("searcherKey") {
        body.insert("searcherKey".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/field/{{fieldId}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create custom field context
///
/// Method: POST /rest/api/3/field/{fieldId}/context
#[actor(
    JiraCreateCustomFieldContextActor,
    inports::<100>(fieldId, projectIds, issueTypeIds, id, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_custom_field_context(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("projectIds") {
        body.insert("projectIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypeIds") {
        body.insert("issueTypeIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/field/{{fieldId}}/context failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get custom field contexts
///
/// Method: GET /rest/api/3/field/{fieldId}/context
#[actor(
    JiraReadContextsForFieldActor,
    inports::<100>(fieldId, isAnyIssueType, isGlobalContext, contextId, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_contexts_for_field(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("isAnyIssueType") {
        query_pairs.push(("isAnyIssueType", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("isGlobalContext") {
        query_pairs.push(("isGlobalContext", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("contextId") {
        query_pairs.push(("contextId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/field/{{fieldId}}/context failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue types for custom field context
///
/// Method: GET /rest/api/3/field/{fieldId}/context/issuetypemapping
#[actor(
    JiraReadIssueTypeMappingsForContextsActor,
    inports::<100>(fieldId, contextId, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type_mappings_for_contexts(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/issuetypemapping".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("contextId") {
        query_pairs.push(("contextId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/field/{{fieldId}}/context/issuetypemapping failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get custom field contexts for projects and issue types
///
/// Method: POST /rest/api/3/field/{fieldId}/context/mapping
#[actor(
    JiraReadCustomFieldContextsForProjectsAndIssueTypesActor,
    inports::<100>(fieldId, startAt, maxResults, mappings),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_custom_field_contexts_for_projects_and_issue_types(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/mapping".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("mappings") {
        body.insert("mappings".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/field/{{fieldId}}/context/mapping failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project mappings for custom field context
///
/// Method: GET /rest/api/3/field/{fieldId}/context/projectmapping
#[actor(
    JiraReadProjectContextMappingActor,
    inports::<100>(fieldId, contextId, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_context_mapping(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/projectmapping".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("contextId") {
        query_pairs.push(("contextId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/field/{{fieldId}}/context/projectmapping failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update custom field context
///
/// Method: PUT /rest/api/3/field/{fieldId}/context/{contextId}
#[actor(
    JiraUpdateCustomFieldContextActor,
    inports::<100>(fieldId, contextId, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_custom_field_context(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/{contextId}".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/field/{{fieldId}}/context/{{contextId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete custom field context
///
/// Method: DELETE /rest/api/3/field/{fieldId}/context/{contextId}
#[actor(
    JiraDeleteCustomFieldContextActor,
    inports::<100>(fieldId, contextId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_custom_field_context(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/{contextId}".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/field/{{fieldId}}/context/{{contextId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add issue types to context
///
/// Method: PUT /rest/api/3/field/{fieldId}/context/{contextId}/issuetype
#[actor(
    JiraUpdateIssueCustomFieldContextsActor,
    inports::<100>(fieldId, contextId, issueTypeIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_custom_field_contexts(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/{contextId}/issuetype".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypeIds") {
        body.insert("issueTypeIds".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/field/{{fieldId}}/context/{{contextId}}/issuetype failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Remove issue types from context
///
/// Method: POST /rest/api/3/field/{fieldId}/context/{contextId}/issuetype/remove
#[actor(
    JiraDeleteIssueTypesFromContextActor,
    inports::<100>(fieldId, contextId, issueTypeIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_types_from_context(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/field/{fieldId}/context/{contextId}/issuetype/remove".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypeIds") {
        body.insert("issueTypeIds".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /rest/api/3/field/{{fieldId}}/context/{{contextId}}/issuetype/remove failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get custom field options (context)
///
/// Method: GET /rest/api/3/field/{fieldId}/context/{contextId}/option
#[actor(
    JiraReadOptionsForContextActor,
    inports::<100>(fieldId, contextId, optionId, onlyOptions, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_options_for_context(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/{contextId}/option".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("optionId") {
        query_pairs.push(("optionId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("onlyOptions") {
        query_pairs.push(("onlyOptions", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/field/{{fieldId}}/context/{{contextId}}/option failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create custom field options (context)
///
/// Method: POST /rest/api/3/field/{fieldId}/context/{contextId}/option
#[actor(
    JiraCreateCustomFieldOptionActor,
    inports::<100>(fieldId, contextId, options),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_custom_field_option(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/{contextId}/option".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("options") {
        body.insert("options".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /rest/api/3/field/{{fieldId}}/context/{{contextId}}/option failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update custom field options (context)
///
/// Method: PUT /rest/api/3/field/{fieldId}/context/{contextId}/option
#[actor(
    JiraUpdateCustomFieldOptionActor,
    inports::<100>(fieldId, contextId, options),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_custom_field_option(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/{contextId}/option".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("options") {
        body.insert("options".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/field/{{fieldId}}/context/{{contextId}}/option failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Reorder custom field options (context)
///
/// Method: PUT /rest/api/3/field/{fieldId}/context/{contextId}/option/move
#[actor(
    JiraMoveIssueCustomFieldOptionsActor,
    inports::<100>(fieldId, contextId, after, customFieldOptionIds, position),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_issue_custom_field_options(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/{contextId}/option/move".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("after") {
        body.insert("after".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("customFieldOptionIds") {
        body.insert("customFieldOptionIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("position") {
        body.insert("position".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/field/{{fieldId}}/context/{{contextId}}/option/move failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete custom field options (context)
///
/// Method: DELETE /rest/api/3/field/{fieldId}/context/{contextId}/option/{optionId}
#[actor(
    JiraDeleteCustomFieldOptionActor,
    inports::<100>(fieldId, contextId, optionId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_custom_field_option(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/field/{fieldId}/context/{contextId}/option/{optionId}".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("optionId") {
        endpoint = endpoint.replace("{{optionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/field/{{fieldId}}/context/{{contextId}}/option/{{optionId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Replace custom field options
///
/// Method: DELETE /rest/api/3/field/{fieldId}/context/{contextId}/option/{optionId}/issue
#[actor(
    JiraDeleteIssueCustomFieldOptionsActor,
    inports::<100>(replaceWith, jql, fieldId, optionId, contextId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_custom_field_options(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/field/{fieldId}/context/{contextId}/option/{optionId}/issue".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("optionId") {
        endpoint = endpoint.replace("{{optionId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("replaceWith") {
        query_pairs.push(("replaceWith", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("jql") {
        query_pairs.push(("jql", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/field/{{fieldId}}/context/{{contextId}}/option/{{optionId}}/issue failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Remove custom field context from projects
///
/// Method: POST /rest/api/3/field/{fieldId}/context/{contextId}/project/remove
#[actor(
    JiraDeleteCustomFieldContextFromProjectsActor,
    inports::<100>(fieldId, contextId, projectIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_custom_field_context_from_projects(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/context/{contextId}/project/remove".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("contextId") {
        endpoint = endpoint.replace("{{contextId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("projectIds") {
        body.insert("projectIds".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /rest/api/3/field/{{fieldId}}/context/{{contextId}}/project/remove failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get screens for a field
///
/// Method: GET /rest/api/3/field/{fieldId}/screens
#[actor(
    JiraReadScreensForFieldActor,
    inports::<100>(fieldId, startAt, maxResults, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_screens_for_field(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldId}/screens".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/field/{{fieldId}}/screens failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all issue field options
///
/// Method: GET /rest/api/3/field/{fieldKey}/option
#[actor(
    JiraReadAllIssueFieldOptionsActor,
    inports::<100>(startAt, maxResults, fieldKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_issue_field_options(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldKey}/option".to_string();
    if let Some(val) = inputs.get("fieldKey") {
        endpoint = endpoint.replace("{{fieldKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/field/{{fieldKey}}/option failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create issue field option
///
/// Method: POST /rest/api/3/field/{fieldKey}/option
#[actor(
    JiraCreateIssueFieldOptionActor,
    inports::<100>(fieldKey, properties, value, config),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_field_option(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldKey}/option".to_string();
    if let Some(val) = inputs.get("fieldKey") {
        endpoint = endpoint.replace("{{fieldKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("properties") {
        body.insert("properties".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("value") {
        body.insert("value".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("config") {
        body.insert("config".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/field/{{fieldKey}}/option failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get selectable issue field options
///
/// Method: GET /rest/api/3/field/{fieldKey}/option/suggestions/edit
#[actor(
    JiraReadSelectableIssueFieldOptionsActor,
    inports::<100>(startAt, maxResults, projectId, fieldKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_selectable_issue_field_options(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldKey}/option/suggestions/edit".to_string();
    if let Some(val) = inputs.get("fieldKey") {
        endpoint = endpoint.replace("{{fieldKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/field/{{fieldKey}}/option/suggestions/edit failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get visible issue field options
///
/// Method: GET /rest/api/3/field/{fieldKey}/option/suggestions/search
#[actor(
    JiraReadVisibleIssueFieldOptionsActor,
    inports::<100>(startAt, maxResults, projectId, fieldKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_visible_issue_field_options(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldKey}/option/suggestions/search".to_string();
    if let Some(val) = inputs.get("fieldKey") {
        endpoint = endpoint.replace("{{fieldKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/field/{{fieldKey}}/option/suggestions/search failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue field option
///
/// Method: GET /rest/api/3/field/{fieldKey}/option/{optionId}
#[actor(
    JiraReadIssueFieldOptionActor,
    inports::<100>(fieldKey, optionId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_field_option(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldKey}/option/{optionId}".to_string();
    if let Some(val) = inputs.get("fieldKey") {
        endpoint = endpoint.replace("{{fieldKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("optionId") {
        endpoint = endpoint.replace("{{optionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/field/{{fieldKey}}/option/{{optionId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update issue field option
///
/// Method: PUT /rest/api/3/field/{fieldKey}/option/{optionId}
#[actor(
    JiraUpdateIssueFieldOptionActor,
    inports::<100>(fieldKey, optionId, value, id, config, properties),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_field_option(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldKey}/option/{optionId}".to_string();
    if let Some(val) = inputs.get("fieldKey") {
        endpoint = endpoint.replace("{{fieldKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("optionId") {
        endpoint = endpoint.replace("{{optionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("value") {
        body.insert("value".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("config") {
        body.insert("config".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("properties") {
        body.insert("properties".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/field/{{fieldKey}}/option/{{optionId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete issue field option
///
/// Method: DELETE /rest/api/3/field/{fieldKey}/option/{optionId}
#[actor(
    JiraDeleteIssueFieldOptionActor,
    inports::<100>(fieldKey, optionId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_field_option(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldKey}/option/{optionId}".to_string();
    if let Some(val) = inputs.get("fieldKey") {
        endpoint = endpoint.replace("{{fieldKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("optionId") {
        endpoint = endpoint.replace("{{optionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/field/{{fieldKey}}/option/{{optionId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Replace issue field option
///
/// Method: DELETE /rest/api/3/field/{fieldKey}/option/{optionId}/issue
#[actor(
    JiraDeleteIssueActor,
    inports::<100>(replaceWith, jql, overrideScreenSecurity, overrideEditableFlag, fieldKey, optionId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{fieldKey}/option/{optionId}/issue".to_string();
    if let Some(val) = inputs.get("fieldKey") {
        endpoint = endpoint.replace("{{fieldKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("optionId") {
        endpoint = endpoint.replace("{{optionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("replaceWith") {
        query_pairs.push(("replaceWith", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("jql") {
        query_pairs.push(("jql", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideScreenSecurity") {
        query_pairs.push(("overrideScreenSecurity", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideEditableFlag") {
        query_pairs.push(("overrideEditableFlag", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/field/{{fieldKey}}/option/{{optionId}}/issue failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete custom field
///
/// Method: DELETE /rest/api/3/field/{id}
#[actor(
    JiraDeleteCustomFieldActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_custom_field(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/field/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Restore custom field from trash
///
/// Method: POST /rest/api/3/field/{id}/restore
#[actor(
    JiraRestoreIssueFieldsActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_restore_issue_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{id}/restore".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/field/{{id}}/restore failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Move custom field to trash
///
/// Method: POST /rest/api/3/field/{id}/trash
#[actor(
    JiraCreateIssueFieldsActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/field/{id}/trash".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/field/{{id}}/trash failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create filter
///
/// Method: POST /rest/api/3/filter
#[actor(
    JiraCreateFilterActor,
    inports::<100>(expand, overrideSharePermissions, editPermissions, name, id, subscriptions, jql, description, favourite, sharedUsers, owner, approximateLastUsed, favouritedCount, viewUrl, sharePermissions, searchUrl, self_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_filter(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/filter".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideSharePermissions") {
        query_pairs.push(("overrideSharePermissions", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("editPermissions") {
        body.insert("editPermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("subscriptions") {
        body.insert("subscriptions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("jql") {
        body.insert("jql".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("favourite") {
        body.insert("favourite".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("sharedUsers") {
        body.insert("sharedUsers".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("owner") {
        body.insert("owner".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("approximateLastUsed") {
        body.insert("approximateLastUsed".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("favouritedCount") {
        body.insert("favouritedCount".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("viewUrl") {
        body.insert("viewUrl".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("sharePermissions") {
        body.insert("sharePermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("searchUrl") {
        body.insert("searchUrl".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/filter failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get default share scope
///
/// Method: GET /rest/api/3/filter/defaultShareScope
#[actor(
    JiraReadDefaultShareScopeActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_default_share_scope(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/filter/defaultShareScope".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/filter/defaultShareScope failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set default share scope
///
/// Method: PUT /rest/api/3/filter/defaultShareScope
#[actor(
    JiraUpdateFilterSharingActor,
    inports::<100>(scope),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_filter_sharing(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/filter/defaultShareScope".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("scope") {
        body.insert("scope".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/filter/defaultShareScope failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get favorite filters
///
/// Method: GET /rest/api/3/filter/favourite
#[actor(
    JiraReadFavouriteFiltersActor,
    inports::<100>(expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_favourite_filters(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/filter/favourite".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/filter/favourite failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get my filters
///
/// Method: GET /rest/api/3/filter/my
#[actor(
    JiraReadMyFiltersActor,
    inports::<100>(expand, includeFavourites),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_my_filters(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/filter/my".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("includeFavourites") {
        query_pairs.push(("includeFavourites", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/filter/my failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Search for filters
///
/// Method: GET /rest/api/3/filter/search
#[actor(
    JiraReadFiltersPaginatedActor,
    inports::<100>(filterName, accountId, owner, groupname, groupId, projectId, id, orderBy, startAt, maxResults, expand, overrideSharePermissions, isSubstringMatch),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_filters_paginated(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/filter/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("filterName") {
        query_pairs.push(("filterName", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("owner") {
        query_pairs.push(("owner", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupname") {
        query_pairs.push(("groupname", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideSharePermissions") {
        query_pairs.push(("overrideSharePermissions", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("isSubstringMatch") {
        query_pairs.push(("isSubstringMatch", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/filter/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update filter
///
/// Method: PUT /rest/api/3/filter/{id}
#[actor(
    JiraUpdateFilterActor,
    inports::<100>(id, expand, overrideSharePermissions, editPermissions, favouritedCount, approximateLastUsed, viewUrl, sharedUsers, description, searchUrl, favourite, owner, name, sharePermissions, self_, subscriptions, jql),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_filter(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideSharePermissions") {
        query_pairs.push(("overrideSharePermissions", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("editPermissions") {
        body.insert("editPermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("favouritedCount") {
        body.insert("favouritedCount".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("approximateLastUsed") {
        body.insert("approximateLastUsed".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("viewUrl") {
        body.insert("viewUrl".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("sharedUsers") {
        body.insert("sharedUsers".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("searchUrl") {
        body.insert("searchUrl".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("favourite") {
        body.insert("favourite".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("owner") {
        body.insert("owner".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("sharePermissions") {
        body.insert("sharePermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("subscriptions") {
        body.insert("subscriptions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("jql") {
        body.insert("jql".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/filter/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete filter
///
/// Method: DELETE /rest/api/3/filter/{id}
#[actor(
    JiraDeleteFilterActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_filter(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/filter/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get filter
///
/// Method: GET /rest/api/3/filter/{id}
#[actor(
    JiraReadFilterActor,
    inports::<100>(id, expand, overrideSharePermissions),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_filter(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideSharePermissions") {
        query_pairs.push(("overrideSharePermissions", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/filter/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Set columns
///
/// Method: PUT /rest/api/3/filter/{id}/columns
#[actor(
    JiraUpdateFiltersActor,
    inports::<100>(id, columns),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_filters(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}/columns".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("columns") {
        body.insert("columns".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/filter/{{id}}/columns failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get columns
///
/// Method: GET /rest/api/3/filter/{id}/columns
#[actor(
    JiraReadColumnsActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_columns(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}/columns".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/filter/{{id}}/columns failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Reset columns
///
/// Method: DELETE /rest/api/3/filter/{id}/columns
#[actor(
    JiraDeleteFiltersActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_filters(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}/columns".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/filter/{{id}}/columns failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove filter as favorite
///
/// Method: DELETE /rest/api/3/filter/{id}/favourite
#[actor(
    JiraDeleteFavouriteForFilterActor,
    inports::<100>(id, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_favourite_for_filter(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}/favourite".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/filter/{{id}}/favourite failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get share permissions
///
/// Method: GET /rest/api/3/filter/{id}/permission
#[actor(
    JiraReadSharePermissionsActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_share_permissions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}/permission".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/filter/{{id}}/permission failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add share permission
///
/// Method: POST /rest/api/3/filter/{id}/permission
#[actor(
    JiraCreateFilterSharingActor,
    inports::<100>(id, groupId, accountId, projectId, projectRoleId, rights, type_, groupname),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_filter_sharing(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}/permission".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("groupId") {
        body.insert("groupId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("accountId") {
        body.insert("accountId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectId") {
        body.insert("projectId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectRoleId") {
        body.insert("projectRoleId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("rights") {
        body.insert("rights".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("type_") {
        body.insert("type".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("groupname") {
        body.insert("groupname".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/filter/{{id}}/permission failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete share permission
///
/// Method: DELETE /rest/api/3/filter/{id}/permission/{permissionId}
#[actor(
    JiraDeleteSharePermissionActor,
    inports::<100>(id, permissionId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_share_permission(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}/permission/{permissionId}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("permissionId") {
        endpoint = endpoint.replace("{{permissionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/filter/{{id}}/permission/{{permissionId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get share permission
///
/// Method: GET /rest/api/3/filter/{id}/permission/{permissionId}
#[actor(
    JiraReadSharePermissionActor,
    inports::<100>(id, permissionId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_share_permission(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/filter/{id}/permission/{permissionId}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("permissionId") {
        endpoint = endpoint.replace("{{permissionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/filter/{{id}}/permission/{{permissionId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk pin or unpin issue panel to projects
///
/// Method: POST /rest/api/3/forge/panel/action/bulk/async
#[actor(
    JiraCreateIssuePanelsActor,
    inports::<100>(moduleId, projectList),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_panels(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/forge/panel/action/bulk/async".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("moduleId") {
        body.insert("moduleId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectList") {
        body.insert("projectList".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/forge/panel/action/bulk/async failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove group
///
/// Method: DELETE /rest/api/3/group
#[actor(
    JiraDeleteGroupActor,
    inports::<100>(groupname, groupId, swapGroup, swapGroupId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_group(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/group".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("groupname") {
        query_pairs.push(("groupname", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("swapGroup") {
        query_pairs.push(("swapGroup", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("swapGroupId") {
        query_pairs.push(("swapGroupId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/group failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create group
///
/// Method: POST /rest/api/3/group
#[actor(
    JiraCreateGroupActor,
    inports::<100>(name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_group(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/group".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/group failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk get groups
///
/// Method: GET /rest/api/3/group/bulk
#[actor(
    JiraListGroupsActor,
    inports::<100>(startAt, maxResults, groupId, groupName, accessType, applicationKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_list_groups(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/group/bulk".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupName") {
        query_pairs.push(("groupName", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accessType") {
        query_pairs.push(("accessType", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("applicationKey") {
        query_pairs.push(("applicationKey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/group/bulk failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get users from group
///
/// Method: GET /rest/api/3/group/member
#[actor(
    JiraReadUsersFromGroupActor,
    inports::<100>(groupname, groupId, includeInactiveUsers, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_users_from_group(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/group/member".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("groupname") {
        query_pairs.push(("groupname", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("includeInactiveUsers") {
        query_pairs.push(("includeInactiveUsers", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/group/member failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Remove user from group
///
/// Method: DELETE /rest/api/3/group/user
#[actor(
    JiraDeleteUserFromGroupActor,
    inports::<100>(groupname, groupId, username, accountId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_user_from_group(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/group/user".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("groupname") {
        query_pairs.push(("groupname", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/group/user failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Add user to group
///
/// Method: POST /rest/api/3/group/user
#[actor(
    JiraCreateGroupsActor,
    inports::<100>(groupname, groupId, accountId, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_groups(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/group/user".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("groupname") {
        query_pairs.push(("groupname", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("accountId") {
        body.insert("accountId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/group/user failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Find groups
///
/// Method: GET /rest/api/3/groups/picker
#[actor(
    JiraSearchGroupsActor,
    inports::<100>(accountId, query, exclude, excludeId, maxResults, caseInsensitive, userName),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_groups(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/groups/picker".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("exclude") {
        query_pairs.push(("exclude", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("excludeId") {
        query_pairs.push(("excludeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("caseInsensitive") {
        query_pairs.push(("caseInsensitive", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("userName") {
        query_pairs.push(("userName", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/groups/picker failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Find users and groups
///
/// Method: GET /rest/api/3/groupuserpicker
#[actor(
    JiraSearchUsersAndGroupsActor,
    inports::<100>(query, maxResults, showAvatar, fieldId, projectId, issueTypeId, avatarSize, caseInsensitive, excludeConnectAddons),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_users_and_groups(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/groupuserpicker".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("showAvatar") {
        query_pairs.push(("showAvatar", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fieldId") {
        query_pairs.push(("fieldId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueTypeId") {
        query_pairs.push(("issueTypeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("avatarSize") {
        query_pairs.push(("avatarSize", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("caseInsensitive") {
        query_pairs.push(("caseInsensitive", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("excludeConnectAddons") {
        query_pairs.push(("excludeConnectAddons", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/groupuserpicker failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get license
///
/// Method: GET /rest/api/3/instance/license
#[actor(
    JiraReadLicenseActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_license(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/instance/license".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/instance/license failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Archive issue(s) by JQL
///
/// Method: POST /rest/api/3/issue/archive
#[actor(
    JiraArchiveIssuesActor,
    inports::<100>(jql),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_archive_issues(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issue/archive".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("jql") {
        body.insert("jql".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/issue/archive failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk create issue
///
/// Method: POST /rest/api/3/issue/bulk
#[actor(
    JiraCreateIssuesActor,
    inports::<100>(issueUpdates),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issues(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issue/bulk".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueUpdates") {
        body.insert("issueUpdates".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/issue/bulk failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get create metadata issue types for a project
///
/// Method: GET /rest/api/3/issue/createmeta/{projectIdOrKey}/issuetypes
#[actor(
    JiraReadCreateIssueMetaIssueTypesActor,
    inports::<100>(projectIdOrKey, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_create_issue_meta_issue_types(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/createmeta/{projectIdOrKey}/issuetypes".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/createmeta/{{projectIdOrKey}}/issuetypes failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get create field metadata for a project and issue type id
///
/// Method: GET /rest/api/3/issue/createmeta/{projectIdOrKey}/issuetypes/{issueTypeId}
#[actor(
    JiraReadCreateIssueMetaIssueTypeIdActor,
    inports::<100>(projectIdOrKey, issueTypeId, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_create_issue_meta_issue_type_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issue/createmeta/{projectIdOrKey}/issuetypes/{issueTypeId}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issueTypeId") {
        endpoint = endpoint.replace("{{issueTypeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/issue/createmeta/{{projectIdOrKey}}/issuetypes/{{issueTypeId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get issue limit report
///
/// Method: GET /rest/api/3/issue/limit/report
#[actor(
    JiraReadIssueLimitReportActor,
    inports::<100>(isReturningKeys),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_limit_report(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issue/limit/report".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("isReturningKeys") {
        query_pairs.push(("isReturningKeys", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/issue/limit/report failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get issue picker suggestions
///
/// Method: GET /rest/api/3/issue/picker
#[actor(
    JiraReadIssuePickerResourceActor,
    inports::<100>(query, currentJQL, currentIssueKey, currentProjectId, showSubTasks, showSubTaskParent),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_picker_resource(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issue/picker".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("currentJQL") {
        query_pairs.push(("currentJQL", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("currentIssueKey") {
        query_pairs.push(("currentIssueKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("currentProjectId") {
        query_pairs.push(("currentProjectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("showSubTasks") {
        query_pairs.push(("showSubTasks", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("showSubTaskParent") {
        query_pairs.push(("showSubTaskParent", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/issue/picker failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk set issues properties by list
///
/// Method: POST /rest/api/3/issue/properties
#[actor(
    JiraCreateIssuePropertiesActor,
    inports::<100>(entitiesIds, properties),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issue/properties".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("entitiesIds") {
        body.insert("entitiesIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("properties") {
        body.insert("properties".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/issue/properties failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk delete issue property
///
/// Method: DELETE /rest/api/3/issue/properties/{propertyKey}
#[actor(
    JiraDeleteIssuePropertiesActor,
    inports::<100>(propertyKey, entityIds, currentValue),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issue/properties/{{propertyKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk set issue property
///
/// Method: PUT /rest/api/3/issue/properties/{propertyKey}
#[actor(
    JiraUpdateIssuePropertiesActor,
    inports::<100>(propertyKey, filter, value, expression),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("filter") {
        body.insert("filter".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("value") {
        body.insert("value".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("expression") {
        body.insert("expression".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/issue/properties/{{propertyKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Unarchive issue(s) by issue keys/ID
///
/// Method: PUT /rest/api/3/issue/unarchive
#[actor(
    JiraUpdateIssuesActor,
    inports::<100>(issueIdsOrKeys),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issues(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issue/unarchive".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueIdsOrKeys") {
        body.insert("issueIdsOrKeys".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/issue/unarchive failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get is watching issue bulk
///
/// Method: POST /rest/api/3/issue/watching
#[actor(
    JiraReadIsWatchingIssueBulkActor,
    inports::<100>(issueIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_is_watching_issue_bulk(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issue/watching".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueIds") {
        body.insert("issueIds".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/issue/watching failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get issue
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}
#[actor(
    JiraReadIssueActor,
    inports::<100>(issueIdOrKey, fields, fieldsByKeys, expand, properties, updateHistory, failFast),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("fields") {
        query_pairs.push(("fields", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fieldsByKeys") {
        query_pairs.push(("fieldsByKeys", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("properties") {
        query_pairs.push(("properties", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("updateHistory") {
        query_pairs.push(("updateHistory", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("failFast") {
        query_pairs.push(("failFast", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issue/{{issueIdOrKey}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add attachment
///
/// Method: POST /rest/api/3/issue/{issueIdOrKey}/attachments
#[actor(
    JiraCreateIssueAttachmentsActor,
    inports::<100>(issueIdOrKey, body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_attachments(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/attachments".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/issue/{{issueIdOrKey}}/attachments failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get changelogs
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/changelog
#[actor(
    JiraReadChangeLogsActor,
    inports::<100>(issueIdOrKey, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_change_logs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/changelog".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/changelog failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get changelogs by IDs
///
/// Method: POST /rest/api/3/issue/{issueIdOrKey}/changelog/list
#[actor(
    JiraReadChangeLogsByIdsActor,
    inports::<100>(issueIdOrKey, changelogIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_change_logs_by_ids(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/changelog/list".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("changelogIds") {
        body.insert("changelogIds".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/issue/{{issueIdOrKey}}/changelog/list failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add comment
///
/// Method: POST /rest/api/3/issue/{issueIdOrKey}/comment
#[actor(
    JiraCreateIssueCommentsActor,
    inports::<100>(issueIdOrKey, expand, updated, renderedBody, jsdAuthorCanSeeRequest, author, updateAuthor, properties, jsdPublic, body, self_, visibility, created, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_comments(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/comment".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("updated") {
        body.insert("updated".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("renderedBody") {
        body.insert("renderedBody".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("jsdAuthorCanSeeRequest") {
        body.insert("jsdAuthorCanSeeRequest".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("author") {
        body.insert("author".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateAuthor") {
        body.insert("updateAuthor".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("properties") {
        body.insert("properties".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("jsdPublic") {
        body.insert("jsdPublic".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("visibility") {
        body.insert("visibility".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("created") {
        body.insert("created".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/issue/{{issueIdOrKey}}/comment failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get comments
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/comment
#[actor(
    JiraReadCommentsActor,
    inports::<100>(issueIdOrKey, startAt, maxResults, orderBy, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/comment".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/comment failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete comment
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/comment/{id}
#[actor(
    JiraDeleteCommentActor,
    inports::<100>(issueIdOrKey, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_comment(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/comment/{id}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issue/{{issueIdOrKey}}/comment/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update comment
///
/// Method: PUT /rest/api/3/issue/{issueIdOrKey}/comment/{id}
#[actor(
    JiraUpdateCommentActor,
    inports::<100>(issueIdOrKey, id, notifyUsers, overrideEditableFlag, expand, self_, body, jsdAuthorCanSeeRequest, properties, author, renderedBody, jsdPublic, created, updated, updateAuthor, visibility),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_comment(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/comment/{id}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("notifyUsers") {
        query_pairs.push(("notifyUsers", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideEditableFlag") {
        query_pairs.push(("overrideEditableFlag", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("jsdAuthorCanSeeRequest") {
        body.insert("jsdAuthorCanSeeRequest".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("properties") {
        body.insert("properties".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("author") {
        body.insert("author".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("renderedBody") {
        body.insert("renderedBody".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("jsdPublic") {
        body.insert("jsdPublic".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("created") {
        body.insert("created".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updated") {
        body.insert("updated".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateAuthor") {
        body.insert("updateAuthor".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("visibility") {
        body.insert("visibility".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/issue/{{issueIdOrKey}}/comment/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get comment
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/comment/{id}
#[actor(
    JiraReadCommentActor,
    inports::<100>(issueIdOrKey, id, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_comment(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/comment/{id}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/comment/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get edit issue metadata
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/editmeta
#[actor(
    JiraReadEditIssueMetaActor,
    inports::<100>(issueIdOrKey, overrideScreenSecurity, overrideEditableFlag),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_edit_issue_meta(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/editmeta".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("overrideScreenSecurity") {
        query_pairs.push(("overrideScreenSecurity", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideEditableFlag") {
        query_pairs.push(("overrideEditableFlag", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/editmeta failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue property keys
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/properties
#[actor(
    JiraReadIssuePropertyKeysActor,
    inports::<100>(issueIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_property_keys(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/properties".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/properties failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete issue property
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/properties/{propertyKey}
#[actor(
    JiraDeleteIssuePropertyActor,
    inports::<100>(issueIdOrKey, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/issue/{{issueIdOrKey}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get issue property
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/properties/{propertyKey}
#[actor(
    JiraReadIssuePropertyActor,
    inports::<100>(issueIdOrKey, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/issue/{{issueIdOrKey}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete remote issue link by global ID
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/remotelink
#[actor(
    JiraDeleteRemoteIssueLinkByGlobalIdActor,
    inports::<100>(issueIdOrKey, globalId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_remote_issue_link_by_global_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/remotelink".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("globalId") {
        query_pairs.push(("globalId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issue/{{issueIdOrKey}}/remotelink failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get remote issue links
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/remotelink
#[actor(
    JiraReadRemoteIssueLinksActor,
    inports::<100>(issueIdOrKey, globalId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_remote_issue_links(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/remotelink".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("globalId") {
        query_pairs.push(("globalId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/remotelink failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create or update remote issue link
///
/// Method: POST /rest/api/3/issue/{issueIdOrKey}/remotelink
#[actor(
    JiraCreateOrUpdateRemoteIssueLinkActor,
    inports::<100>(issueIdOrKey, object, globalId, application, relationship),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_or_update_remote_issue_link(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/remotelink".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("object") {
        body.insert("object".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("globalId") {
        body.insert("globalId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("application") {
        body.insert("application".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("relationship") {
        body.insert("relationship".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/issue/{{issueIdOrKey}}/remotelink failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get remote issue link by ID
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/remotelink/{linkId}
#[actor(
    JiraReadRemoteIssueLinkByIdActor,
    inports::<100>(issueIdOrKey, linkId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_remote_issue_link_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/remotelink/{linkId}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("linkId") {
        endpoint = endpoint.replace("{{linkId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/remotelink/{{linkId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update remote issue link by ID
///
/// Method: PUT /rest/api/3/issue/{issueIdOrKey}/remotelink/{linkId}
#[actor(
    JiraUpdateRemoteIssueLinkActor,
    inports::<100>(issueIdOrKey, linkId, object, relationship, application, globalId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_remote_issue_link(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/remotelink/{linkId}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("linkId") {
        endpoint = endpoint.replace("{{linkId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("object") {
        body.insert("object".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("relationship") {
        body.insert("relationship".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("application") {
        body.insert("application".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("globalId") {
        body.insert("globalId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/issue/{{issueIdOrKey}}/remotelink/{{linkId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete remote issue link by ID
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/remotelink/{linkId}
#[actor(
    JiraDeleteRemoteIssueLinkByIdActor,
    inports::<100>(issueIdOrKey, linkId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_remote_issue_link_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/remotelink/{linkId}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("linkId") {
        endpoint = endpoint.replace("{{linkId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/issue/{{issueIdOrKey}}/remotelink/{{linkId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get transitions
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/transitions
#[actor(
    JiraReadTransitionsActor,
    inports::<100>(issueIdOrKey, expand, transitionId, skipRemoteOnlyCondition, includeUnavailableTransitions, sortByOpsBarAndStatus),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_transitions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/transitions".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("transitionId") {
        query_pairs.push(("transitionId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("skipRemoteOnlyCondition") {
        query_pairs.push(("skipRemoteOnlyCondition", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("includeUnavailableTransitions") {
        query_pairs.push(("includeUnavailableTransitions", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sortByOpsBarAndStatus") {
        query_pairs.push(("sortByOpsBarAndStatus", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/transitions failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete vote
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/votes
#[actor(
    JiraDeleteVoteActor,
    inports::<100>(issueIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_vote(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/votes".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issue/{{issueIdOrKey}}/votes failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get votes
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/votes
#[actor(
    JiraReadVotesActor,
    inports::<100>(issueIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_votes(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/votes".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issue/{{issueIdOrKey}}/votes failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add vote
///
/// Method: POST /rest/api/3/issue/{issueIdOrKey}/votes
#[actor(
    JiraCreateIssueVotesActor,
    inports::<100>(issueIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_votes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/votes".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/issue/{{issueIdOrKey}}/votes failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add watcher
///
/// Method: POST /rest/api/3/issue/{issueIdOrKey}/watchers
#[actor(
    JiraCreateIssueWatchersActor,
    inports::<100>(issueIdOrKey, body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_watchers(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/watchers".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/issue/{{issueIdOrKey}}/watchers failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue watchers
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/watchers
#[actor(
    JiraReadIssueWatchersActor,
    inports::<100>(issueIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_watchers(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/watchers".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/watchers failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete watcher
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/watchers
#[actor(
    JiraDeleteWatcherActor,
    inports::<100>(issueIdOrKey, username, accountId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_watcher(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/watchers".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issue/{{issueIdOrKey}}/watchers failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue worklogs
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/worklog
#[actor(
    JiraReadIssueWorklogActor,
    inports::<100>(issueIdOrKey, startAt, maxResults, startedAfter, startedBefore, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_worklog(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/worklog".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startedAfter") {
        query_pairs.push(("startedAfter", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startedBefore") {
        query_pairs.push(("startedBefore", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/worklog failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add worklog
///
/// Method: POST /rest/api/3/issue/{issueIdOrKey}/worklog
#[actor(
    JiraCreateIssueWorklogsActor,
    inports::<100>(issueIdOrKey, notifyUsers, adjustEstimate, newEstimate, reduceBy, expand, overrideEditableFlag, timeSpentSeconds, visibility, updateAuthor, updated, comment, issueId, created, self_, author, started, properties, timeSpent, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_worklogs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/worklog".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("notifyUsers") {
        query_pairs.push(("notifyUsers", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("adjustEstimate") {
        query_pairs.push(("adjustEstimate", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("newEstimate") {
        query_pairs.push(("newEstimate", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("reduceBy") {
        query_pairs.push(("reduceBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideEditableFlag") {
        query_pairs.push(("overrideEditableFlag", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("timeSpentSeconds") {
        body.insert("timeSpentSeconds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("visibility") {
        body.insert("visibility".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateAuthor") {
        body.insert("updateAuthor".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updated") {
        body.insert("updated".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("comment") {
        body.insert("comment".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueId") {
        body.insert("issueId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("created") {
        body.insert("created".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("author") {
        body.insert("author".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("started") {
        body.insert("started".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("properties") {
        body.insert("properties".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("timeSpent") {
        body.insert("timeSpent".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/issue/{{issueIdOrKey}}/worklog failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk delete worklogs
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/worklog
#[actor(
    JiraDeleteIssueWorklogsActor,
    inports::<100>(issueIdOrKey, adjustEstimate, overrideEditableFlag, ids),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_worklogs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/worklog".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("adjustEstimate") {
        query_pairs.push(("adjustEstimate", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideEditableFlag") {
        query_pairs.push(("overrideEditableFlag", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issue/{{issueIdOrKey}}/worklog failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk move worklogs
///
/// Method: POST /rest/api/3/issue/{issueIdOrKey}/worklog/move
#[actor(
    JiraMoveIssueWorklogsActor,
    inports::<100>(issueIdOrKey, adjustEstimate, overrideEditableFlag, ids),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_issue_worklogs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/worklog/move".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("adjustEstimate") {
        query_pairs.push(("adjustEstimate", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideEditableFlag") {
        query_pairs.push(("overrideEditableFlag", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("ids") {
        body.insert("ids".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueIdOrKey") {
        body.insert("issueIdOrKey".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/issue/{{issueIdOrKey}}/worklog/move failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get worklog
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/worklog/{id}
#[actor(
    JiraReadWorklogActor,
    inports::<100>(issueIdOrKey, id, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_worklog(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/worklog/{id}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issue/{{issueIdOrKey}}/worklog/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update worklog
///
/// Method: PUT /rest/api/3/issue/{issueIdOrKey}/worklog/{id}
#[actor(
    JiraUpdateWorklogActor,
    inports::<100>(issueIdOrKey, id, notifyUsers, adjustEstimate, newEstimate, expand, overrideEditableFlag, author, issueId, created, timeSpentSeconds, updateAuthor, started, updated, visibility, timeSpent, properties, comment, self_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_worklog(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/worklog/{id}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("notifyUsers") {
        query_pairs.push(("notifyUsers", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("adjustEstimate") {
        query_pairs.push(("adjustEstimate", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("newEstimate") {
        query_pairs.push(("newEstimate", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideEditableFlag") {
        query_pairs.push(("overrideEditableFlag", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("author") {
        body.insert("author".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueId") {
        body.insert("issueId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("created") {
        body.insert("created".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("timeSpentSeconds") {
        body.insert("timeSpentSeconds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateAuthor") {
        body.insert("updateAuthor".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("started") {
        body.insert("started".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updated") {
        body.insert("updated".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("visibility") {
        body.insert("visibility".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("timeSpent") {
        body.insert("timeSpent".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("properties") {
        body.insert("properties".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("comment") {
        body.insert("comment".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/issue/{{issueIdOrKey}}/worklog/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete worklog
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/worklog/{id}
#[actor(
    JiraDeleteWorklogActor,
    inports::<100>(issueIdOrKey, id, notifyUsers, adjustEstimate, newEstimate, increaseBy, overrideEditableFlag),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_worklog(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issue/{issueIdOrKey}/worklog/{id}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("notifyUsers") {
        query_pairs.push(("notifyUsers", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("adjustEstimate") {
        query_pairs.push(("adjustEstimate", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("newEstimate") {
        query_pairs.push(("newEstimate", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("increaseBy") {
        query_pairs.push(("increaseBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("overrideEditableFlag") {
        query_pairs.push(("overrideEditableFlag", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issue/{{issueIdOrKey}}/worklog/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get worklog property keys
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}/properties
#[actor(
    JiraReadWorklogPropertyKeysActor,
    inports::<100>(issueIdOrKey, worklogId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_worklog_property_keys(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}/properties".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("worklogId") {
        endpoint = endpoint.replace("{{worklogId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/issue/{{issueIdOrKey}}/worklog/{{worklogId}}/properties failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete worklog property
///
/// Method: DELETE /rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}/properties/{propertyKey}
#[actor(
    JiraDeleteWorklogPropertyActor,
    inports::<100>(issueIdOrKey, worklogId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_worklog_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("worklogId") {
        endpoint = endpoint.replace("{{worklogId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/issue/{{issueIdOrKey}}/worklog/{{worklogId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get worklog property
///
/// Method: GET /rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}/properties/{propertyKey}
#[actor(
    JiraReadWorklogPropertyActor,
    inports::<100>(issueIdOrKey, worklogId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_worklog_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("worklogId") {
        endpoint = endpoint.replace("{{worklogId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/issue/{{issueIdOrKey}}/worklog/{{worklogId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Set worklog property
///
/// Method: PUT /rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}/properties/{propertyKey}
#[actor(
    JiraUpdateIssueWorklogPropertiesActor,
    inports::<100>(issueIdOrKey, worklogId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_worklog_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("issueIdOrKey") {
        endpoint = endpoint.replace("{{issueIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("worklogId") {
        endpoint = endpoint.replace("{{worklogId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/issue/{{issueIdOrKey}}/worklog/{{worklogId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create issue link
///
/// Method: POST /rest/api/3/issueLink
#[actor(
    JiraCreateIssueLinksActor,
    inports::<100>(outwardIssue, inwardIssue, comment, type_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_links(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issueLink".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("outwardIssue") {
        body.insert("outwardIssue".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("inwardIssue") {
        body.insert("inwardIssue".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("comment") {
        body.insert("comment".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("type_") {
        body.insert("type".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/issueLink failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete issue link
///
/// Method: DELETE /rest/api/3/issueLink/{linkId}
#[actor(
    JiraDeleteIssueLinkActor,
    inports::<100>(linkId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_link(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issueLink/{linkId}".to_string();
    if let Some(val) = inputs.get("linkId") {
        endpoint = endpoint.replace("{{linkId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/issueLink/{{linkId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue link
///
/// Method: GET /rest/api/3/issueLink/{linkId}
#[actor(
    JiraReadIssueLinkActor,
    inports::<100>(linkId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_link(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issueLink/{linkId}".to_string();
    if let Some(val) = inputs.get("linkId") {
        endpoint = endpoint.replace("{{linkId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issueLink/{{linkId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue link types
///
/// Method: GET /rest/api/3/issueLinkType
#[actor(
    JiraReadIssueLinkTypesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_link_types(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issueLinkType".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/issueLinkType failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create issue link type
///
/// Method: POST /rest/api/3/issueLinkType
#[actor(
    JiraCreateIssueLinkTypeActor,
    inports::<100>(inward, name, id, outward, self_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_link_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issueLinkType".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("inward") {
        body.insert("inward".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("outward") {
        body.insert("outward".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/issueLinkType failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete issue link type
///
/// Method: DELETE /rest/api/3/issueLinkType/{issueLinkTypeId}
#[actor(
    JiraDeleteIssueLinkTypeActor,
    inports::<100>(issueLinkTypeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_link_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issueLinkType/{issueLinkTypeId}".to_string();
    if let Some(val) = inputs.get("issueLinkTypeId") {
        endpoint = endpoint.replace("{{issueLinkTypeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issueLinkType/{{issueLinkTypeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update issue link type
///
/// Method: PUT /rest/api/3/issueLinkType/{issueLinkTypeId}
#[actor(
    JiraUpdateIssueLinkTypeActor,
    inports::<100>(issueLinkTypeId, self_, name, id, inward, outward),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_link_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issueLinkType/{issueLinkTypeId}".to_string();
    if let Some(val) = inputs.get("issueLinkTypeId") {
        endpoint = endpoint.replace("{{issueLinkTypeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("inward") {
        body.insert("inward".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("outward") {
        body.insert("outward".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/issueLinkType/{{issueLinkTypeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue link type
///
/// Method: GET /rest/api/3/issueLinkType/{issueLinkTypeId}
#[actor(
    JiraReadIssueLinkTypeActor,
    inports::<100>(issueLinkTypeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_link_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issueLinkType/{issueLinkTypeId}".to_string();
    if let Some(val) = inputs.get("issueLinkTypeId") {
        endpoint = endpoint.replace("{{issueLinkTypeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issueLinkType/{{issueLinkTypeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Export archived issue(s)
///
/// Method: PUT /rest/api/3/issues/archive/export
#[actor(
    JiraExportIssuesActor,
    inports::<100>(issueTypes, projects, archivedBy, archivedDateRange, reporters),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_export_issues(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issues/archive/export".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypes") {
        body.insert("issueTypes".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projects") {
        body.insert("projects".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("archivedBy") {
        body.insert("archivedBy".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("archivedDateRange") {
        body.insert("archivedDateRange".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("reporters") {
        body.insert("reporters".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/issues/archive/export failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue security schemes
///
/// Method: GET /rest/api/3/issuesecurityschemes
#[actor(
    JiraReadIssueSecuritySchemesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_security_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuesecurityschemes".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issuesecurityschemes failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create issue security scheme
///
/// Method: POST /rest/api/3/issuesecurityschemes
#[actor(
    JiraCreateIssueSecuritySchemeActor,
    inports::<100>(levels, name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_security_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuesecurityschemes".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("levels") {
        body.insert("levels".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/issuesecurityschemes failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue security levels
///
/// Method: GET /rest/api/3/issuesecurityschemes/level
#[actor(
    JiraReadSecurityLevelsActor,
    inports::<100>(startAt, maxResults, id, schemeId, onlyDefault),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_security_levels(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuesecurityschemes/level".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("schemeId") {
        query_pairs.push(("schemeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("onlyDefault") {
        query_pairs.push(("onlyDefault", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issuesecurityschemes/level failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set default issue security levels
///
/// Method: PUT /rest/api/3/issuesecurityschemes/level/default
#[actor(
    JiraUpdateIssueSecuritySchemesActor,
    inports::<100>(defaultValues),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_security_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuesecurityschemes/level/default".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("defaultValues") {
        body.insert("defaultValues".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/issuesecurityschemes/level/default failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue security level members
///
/// Method: GET /rest/api/3/issuesecurityschemes/level/member
#[actor(
    JiraReadSecurityLevelMembersActor,
    inports::<100>(startAt, maxResults, id, schemeId, levelId, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_security_level_members(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuesecurityschemes/level/member".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("schemeId") {
        query_pairs.push(("schemeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("levelId") {
        query_pairs.push(("levelId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issuesecurityschemes/level/member failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get projects using issue security schemes
///
/// Method: GET /rest/api/3/issuesecurityschemes/project
#[actor(
    JiraSearchProjectsUsingSecuritySchemesActor,
    inports::<100>(startAt, maxResults, issueSecuritySchemeId, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_projects_using_security_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuesecurityschemes/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueSecuritySchemeId") {
        query_pairs.push(("issueSecuritySchemeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issuesecurityschemes/project failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Search issue security schemes
///
/// Method: GET /rest/api/3/issuesecurityschemes/search
#[actor(
    JiraSearchSecuritySchemesActor,
    inports::<100>(startAt, maxResults, id, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_security_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuesecurityschemes/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issuesecurityschemes/search failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue security scheme
///
/// Method: GET /rest/api/3/issuesecurityschemes/{id}
#[actor(
    JiraReadIssueSecuritySchemeActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_security_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuesecurityschemes/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issuesecurityschemes/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update issue security scheme
///
/// Method: PUT /rest/api/3/issuesecurityschemes/{id}
#[actor(
    JiraUpdateIssueSecuritySchemeActor,
    inports::<100>(id, name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_security_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuesecurityschemes/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/issuesecurityschemes/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue security level members by issue security scheme
///
/// Method: GET /rest/api/3/issuesecurityschemes/{issueSecuritySchemeId}/members
#[actor(
    JiraReadIssueSecurityLevelMembersActor,
    inports::<100>(issueSecuritySchemeId, startAt, maxResults, issueSecurityLevelId, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_security_level_members(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issuesecurityschemes/{issueSecuritySchemeId}/members".to_string();
    if let Some(val) = inputs.get("issueSecuritySchemeId") {
        endpoint = endpoint.replace("{{issueSecuritySchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueSecurityLevelId") {
        query_pairs.push(("issueSecurityLevelId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/issuesecurityschemes/{{issueSecuritySchemeId}}/members failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete issue security scheme
///
/// Method: DELETE /rest/api/3/issuesecurityschemes/{schemeId}
#[actor(
    JiraDeleteSecuritySchemeActor,
    inports::<100>(schemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_security_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuesecurityschemes/{schemeId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issuesecurityschemes/{{schemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove issue security level
///
/// Method: DELETE /rest/api/3/issuesecurityschemes/{schemeId}/level/{levelId}
#[actor(
    JiraDeleteLevelActor,
    inports::<100>(schemeId, levelId, replaceWith),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_level(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuesecurityschemes/{schemeId}/level/{levelId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("levelId") {
        endpoint = endpoint.replace("{{levelId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("replaceWith") {
        query_pairs.push(("replaceWith", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/issuesecurityschemes/{{schemeId}}/level/{{levelId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update issue security level
///
/// Method: PUT /rest/api/3/issuesecurityschemes/{schemeId}/level/{levelId}
#[actor(
    JiraUpdateSecurityLevelActor,
    inports::<100>(schemeId, levelId, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_security_level(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuesecurityschemes/{schemeId}/level/{levelId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("levelId") {
        endpoint = endpoint.replace("{{levelId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/issuesecurityschemes/{{schemeId}}/level/{{levelId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Remove member from issue security level
///
/// Method: DELETE /rest/api/3/issuesecurityschemes/{schemeId}/level/{levelId}/member/{memberId}
#[actor(
    JiraDeleteMemberFromSecurityLevelActor,
    inports::<100>(schemeId, levelId, memberId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_member_from_security_level(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issuesecurityschemes/{schemeId}/level/{levelId}/member/{memberId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("levelId") {
        endpoint = endpoint.replace("{{levelId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("memberId") {
        endpoint = endpoint.replace("{{memberId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/issuesecurityschemes/{{schemeId}}/level/{{levelId}}/member/{{memberId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get all issue types for user
///
/// Method: GET /rest/api/3/issuetype
#[actor(
    JiraReadIssueAllTypesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_all_types(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetype".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/issuetype failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create issue type
///
/// Method: POST /rest/api/3/issuetype
#[actor(
    JiraCreateIssueTypeActor,
    inports::<100>(type_, description, name, hierarchyLevel),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetype".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("type_") {
        body.insert("type".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("hierarchyLevel") {
        body.insert("hierarchyLevel".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/issuetype failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get issue types for project
///
/// Method: GET /rest/api/3/issuetype/project
#[actor(
    JiraReadIssueTypesForProjectActor,
    inports::<100>(projectId, level),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_types_for_project(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetype/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("level") {
        query_pairs.push(("level", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/issuetype/project failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get issue type
///
/// Method: GET /rest/api/3/issuetype/{id}
#[actor(
    JiraReadIssueTypeActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/issuetype/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete issue type
///
/// Method: DELETE /rest/api/3/issuetype/{id}
#[actor(
    JiraDeleteIssueTypeActor,
    inports::<100>(id, alternativeIssueTypeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("alternativeIssueTypeId") {
        query_pairs.push(("alternativeIssueTypeId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/issuetype/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update issue type
///
/// Method: PUT /rest/api/3/issuetype/{id}
#[actor(
    JiraUpdateIssueTypeActor,
    inports::<100>(id, description, avatarId, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("avatarId") {
        body.insert("avatarId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/issuetype/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get alternative issue types
///
/// Method: GET /rest/api/3/issuetype/{id}/alternatives
#[actor(
    JiraReadAlternativeIssueTypesActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_alternative_issue_types(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{id}/alternatives".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issuetype/{{id}}/alternatives failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Load issue type avatar
///
/// Method: POST /rest/api/3/issuetype/{id}/avatar2
#[actor(
    JiraCreateIssueTypeAvatarActor,
    inports::<100>(id, x, y, size),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_type_avatar(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{id}/avatar2".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("x") {
        query_pairs.push(("x", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("y") {
        query_pairs.push(("y", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("size") {
        query_pairs.push(("size", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/issuetype/{{id}}/avatar2 failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue type property keys
///
/// Method: GET /rest/api/3/issuetype/{issueTypeId}/properties
#[actor(
    JiraReadIssueTypePropertyKeysActor,
    inports::<100>(issueTypeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type_property_keys(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{issueTypeId}/properties".to_string();
    if let Some(val) = inputs.get("issueTypeId") {
        endpoint = endpoint.replace("{{issueTypeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issuetype/{{issueTypeId}}/properties failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete issue type property
///
/// Method: DELETE /rest/api/3/issuetype/{issueTypeId}/properties/{propertyKey}
#[actor(
    JiraDeleteIssueTypePropertyActor,
    inports::<100>(issueTypeId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_type_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{issueTypeId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("issueTypeId") {
        endpoint = endpoint.replace("{{issueTypeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/issuetype/{{issueTypeId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get issue type property
///
/// Method: GET /rest/api/3/issuetype/{issueTypeId}/properties/{propertyKey}
#[actor(
    JiraReadIssueTypePropertyActor,
    inports::<100>(issueTypeId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{issueTypeId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("issueTypeId") {
        endpoint = endpoint.replace("{{issueTypeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/issuetype/{{issueTypeId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Set issue type property
///
/// Method: PUT /rest/api/3/issuetype/{issueTypeId}/properties/{propertyKey}
#[actor(
    JiraUpdateIssueTypePropertiesActor,
    inports::<100>(issueTypeId, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_type_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetype/{issueTypeId}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("issueTypeId") {
        endpoint = endpoint.replace("{{issueTypeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/issuetype/{{issueTypeId}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get all issue type schemes
///
/// Method: GET /rest/api/3/issuetypescheme
#[actor(
    JiraReadAllIssueTypeSchemesActor,
    inports::<100>(startAt, maxResults, id, orderBy, expand, queryString),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_issue_type_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("queryString") {
        query_pairs.push(("queryString", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/issuetypescheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create issue type scheme
///
/// Method: POST /rest/api/3/issuetypescheme
#[actor(
    JiraCreateIssueTypeSchemeActor,
    inports::<100>(description, name, issueTypeIds, defaultIssueTypeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_type_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypeIds") {
        body.insert("issueTypeIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("defaultIssueTypeId") {
        body.insert("defaultIssueTypeId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/issuetypescheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get issue type scheme items
///
/// Method: GET /rest/api/3/issuetypescheme/mapping
#[actor(
    JiraReadIssueTypeSchemesMappingActor,
    inports::<100>(startAt, maxResults, issueTypeSchemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type_schemes_mapping(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescheme/mapping".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueTypeSchemeId") {
        query_pairs.push(("issueTypeSchemeId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issuetypescheme/mapping failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Assign issue type scheme to project
///
/// Method: PUT /rest/api/3/issuetypescheme/project
#[actor(
    JiraUpdateIssueTypeSchemesActor,
    inports::<100>(issueTypeSchemeId, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_type_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescheme/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypeSchemeId") {
        body.insert("issueTypeSchemeId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectId") {
        body.insert("projectId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/issuetypescheme/project failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue type schemes for projects
///
/// Method: GET /rest/api/3/issuetypescheme/project
#[actor(
    JiraReadIssueTypeSchemeForProjectsActor,
    inports::<100>(startAt, maxResults, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type_scheme_for_projects(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescheme/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issuetypescheme/project failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete issue type scheme
///
/// Method: DELETE /rest/api/3/issuetypescheme/{issueTypeSchemeId}
#[actor(
    JiraDeleteIssueTypeSchemeActor,
    inports::<100>(issueTypeSchemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_type_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetypescheme/{issueTypeSchemeId}".to_string();
    if let Some(val) = inputs.get("issueTypeSchemeId") {
        endpoint = endpoint.replace("{{issueTypeSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/issuetypescheme/{{issueTypeSchemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update issue type scheme
///
/// Method: PUT /rest/api/3/issuetypescheme/{issueTypeSchemeId}
#[actor(
    JiraUpdateIssueTypeSchemeActor,
    inports::<100>(issueTypeSchemeId, defaultIssueTypeId, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_type_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetypescheme/{issueTypeSchemeId}".to_string();
    if let Some(val) = inputs.get("issueTypeSchemeId") {
        endpoint = endpoint.replace("{{issueTypeSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("defaultIssueTypeId") {
        body.insert("defaultIssueTypeId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/issuetypescheme/{{issueTypeSchemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Change order of issue types
///
/// Method: PUT /rest/api/3/issuetypescheme/{issueTypeSchemeId}/issuetype/move
#[actor(
    JiraMoveIssueTypeSchemesActor,
    inports::<100>(issueTypeSchemeId, position, issueTypeIds, after),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_issue_type_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetypescheme/{issueTypeSchemeId}/issuetype/move".to_string();
    if let Some(val) = inputs.get("issueTypeSchemeId") {
        endpoint = endpoint.replace("{{issueTypeSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("position") {
        body.insert("position".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypeIds") {
        body.insert("issueTypeIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("after") {
        body.insert("after".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/issuetypescheme/{{issueTypeSchemeId}}/issuetype/move failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Remove issue type from issue type scheme
///
/// Method: DELETE /rest/api/3/issuetypescheme/{issueTypeSchemeId}/issuetype/{issueTypeId}
#[actor(
    JiraDeleteIssueTypeFromIssueTypeSchemeActor,
    inports::<100>(issueTypeSchemeId, issueTypeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_type_from_issue_type_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issuetypescheme/{issueTypeSchemeId}/issuetype/{issueTypeId}".to_string();
    if let Some(val) = inputs.get("issueTypeSchemeId") {
        endpoint = endpoint.replace("{{issueTypeSchemeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issueTypeId") {
        endpoint = endpoint.replace("{{issueTypeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/issuetypescheme/{{issueTypeSchemeId}}/issuetype/{{issueTypeId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get issue type screen schemes
///
/// Method: GET /rest/api/3/issuetypescreenscheme
#[actor(
    JiraReadIssueTypeScreenSchemesActor,
    inports::<100>(startAt, maxResults, id, queryString, orderBy, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type_screen_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescreenscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("queryString") {
        query_pairs.push(("queryString", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/issuetypescreenscheme failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create issue type screen scheme
///
/// Method: POST /rest/api/3/issuetypescreenscheme
#[actor(
    JiraCreateIssueTypeScreenSchemeActor,
    inports::<100>(description, issueTypeMappings, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_type_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescreenscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypeMappings") {
        body.insert("issueTypeMappings".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/issuetypescreenscheme failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue type screen scheme items
///
/// Method: GET /rest/api/3/issuetypescreenscheme/mapping
#[actor(
    JiraReadIssueTypeScreenSchemeMappingsActor,
    inports::<100>(startAt, maxResults, issueTypeScreenSchemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type_screen_scheme_mappings(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescreenscheme/mapping".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueTypeScreenSchemeId") {
        query_pairs.push(("issueTypeScreenSchemeId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issuetypescreenscheme/mapping failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Assign issue type screen scheme to project
///
/// Method: PUT /rest/api/3/issuetypescreenscheme/project
#[actor(
    JiraUpdateIssueTypeScreenSchemesActor,
    inports::<100>(issueTypeScreenSchemeId, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_type_screen_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescreenscheme/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypeScreenSchemeId") {
        body.insert("issueTypeScreenSchemeId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectId") {
        body.insert("projectId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/issuetypescreenscheme/project failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue type screen schemes for projects
///
/// Method: GET /rest/api/3/issuetypescreenscheme/project
#[actor(
    JiraReadIssueTypeScreenSchemeProjectAssociationsActor,
    inports::<100>(startAt, maxResults, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_type_screen_scheme_project_associations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/issuetypescreenscheme/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/issuetypescreenscheme/project failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete issue type screen scheme
///
/// Method: DELETE /rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}
#[actor(
    JiraDeleteIssueTypeScreenSchemeActor,
    inports::<100>(issueTypeScreenSchemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_issue_type_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}".to_string();
    if let Some(val) = inputs.get("issueTypeScreenSchemeId") {
        endpoint = endpoint.replace("{{issueTypeScreenSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/issuetypescreenscheme/{{issueTypeScreenSchemeId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update issue type screen scheme
///
/// Method: PUT /rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}
#[actor(
    JiraUpdateIssueTypeScreenSchemeActor,
    inports::<100>(issueTypeScreenSchemeId, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_type_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}".to_string();
    if let Some(val) = inputs.get("issueTypeScreenSchemeId") {
        endpoint = endpoint.replace("{{issueTypeScreenSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/issuetypescreenscheme/{{issueTypeScreenSchemeId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update issue type screen scheme default screen scheme
///
/// Method: PUT /rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/mapping/default
#[actor(
    JiraUpdateDefaultScreenSchemeActor,
    inports::<100>(issueTypeScreenSchemeId, screenSchemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_default_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/mapping/default".to_string();
    if let Some(val) = inputs.get("issueTypeScreenSchemeId") {
        endpoint = endpoint.replace("{{issueTypeScreenSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("screenSchemeId") {
        body.insert("screenSchemeId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/issuetypescreenscheme/{{issueTypeScreenSchemeId}}/mapping/default failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Remove mappings from issue type screen scheme
///
/// Method: POST /rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/mapping/remove
#[actor(
    JiraDeleteMappingsFromIssueTypeScreenSchemeActor,
    inports::<100>(issueTypeScreenSchemeId, issueTypeIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_mappings_from_issue_type_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/mapping/remove".to_string();
    if let Some(val) = inputs.get("issueTypeScreenSchemeId") {
        endpoint = endpoint.replace("{{issueTypeScreenSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypeIds") {
        body.insert("issueTypeIds".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /rest/api/3/issuetypescreenscheme/{{issueTypeScreenSchemeId}}/mapping/remove failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get issue type screen scheme projects
///
/// Method: GET /rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/project
#[actor(
    JiraReadProjectsForIssueTypeScreenSchemeActor,
    inports::<100>(issueTypeScreenSchemeId, startAt, maxResults, query),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_projects_for_issue_type_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/project".to_string();
    if let Some(val) = inputs.get("issueTypeScreenSchemeId") {
        endpoint = endpoint.replace("{{issueTypeScreenSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/issuetypescreenscheme/{{issueTypeScreenSchemeId}}/project failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get field reference data (GET)
///
/// Method: GET /rest/api/3/jql/autocompletedata
#[actor(
    JiraReadAutoCompleteActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_auto_complete(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/jql/autocompletedata".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/jql/autocompletedata failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get field reference data (POST)
///
/// Method: POST /rest/api/3/jql/autocompletedata
#[actor(
    JiraReadAutoCompletePostActor,
    inports::<100>(includeCollapsedFields, projectIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_auto_complete_post(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/jql/autocompletedata".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("includeCollapsedFields") {
        body.insert("includeCollapsedFields".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectIds") {
        body.insert("projectIds".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/jql/autocompletedata failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get field auto complete suggestions
///
/// Method: GET /rest/api/3/jql/autocompletedata/suggestions
#[actor(
    JiraReadFieldAutoCompleteForQueryStringActor,
    inports::<100>(fieldName, fieldValue, predicateName, predicateValue),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_field_auto_complete_for_query_string(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/jql/autocompletedata/suggestions".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("fieldName") {
        query_pairs.push(("fieldName", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fieldValue") {
        query_pairs.push(("fieldValue", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("predicateName") {
        query_pairs.push(("predicateName", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("predicateValue") {
        query_pairs.push(("predicateValue", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/jql/autocompletedata/suggestions failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get precomputations (apps)
///
/// Method: GET /rest/api/3/jql/function/computation
#[actor(
    JiraReadPrecomputationsActor,
    inports::<100>(functionKey, startAt, maxResults, orderBy),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_precomputations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/jql/function/computation".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("functionKey") {
        query_pairs.push(("functionKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/jql/function/computation failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update precomputations (apps)
///
/// Method: POST /rest/api/3/jql/function/computation
#[actor(
    JiraUpdatePrecomputationsActor,
    inports::<100>(skipNotFoundPrecomputations, values),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_precomputations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/jql/function/computation".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("skipNotFoundPrecomputations") {
        query_pairs.push(("skipNotFoundPrecomputations", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("values") {
        body.insert("values".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/jql/function/computation failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get precomputations by ID (apps)
///
/// Method: POST /rest/api/3/jql/function/computation/search
#[actor(
    JiraReadPrecomputationsByIdActor,
    inports::<100>(orderBy, precomputationIDs),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_precomputations_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/jql/function/computation/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("precomputationIDs") {
        body.insert("precomputationIDs".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/jql/function/computation/search failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Check issues against JQL
///
/// Method: POST /rest/api/3/jql/match
#[actor(
    JiraCreateIssueSearchActor,
    inports::<100>(issueIds, jqls),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_search(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/jql/match".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueIds") {
        body.insert("issueIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("jqls") {
        body.insert("jqls".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/jql/match failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Parse JQL query
///
/// Method: POST /rest/api/3/jql/parse
#[actor(
    JiraCreateJqlActor,
    inports::<100>(validation, queries),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_jql(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/jql/parse".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("validation") {
        query_pairs.push(("validation", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("queries") {
        body.insert("queries".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/jql/parse failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all labels
///
/// Method: GET /rest/api/3/label
#[actor(
    JiraReadAllLabelsActor,
    inports::<100>(startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_labels(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/label".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/label failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get approximate license count
///
/// Method: GET /rest/api/3/license/approximateLicenseCount
#[actor(
    JiraReadApproximateLicenseCountActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_approximate_license_count(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/license/approximateLicenseCount".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/license/approximateLicenseCount failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get approximate application license count
///
/// Method: GET /rest/api/3/license/approximateLicenseCount/product/{applicationKey}
#[actor(
    JiraReadApproximateApplicationLicenseCountActor,
    inports::<100>(applicationKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_approximate_application_license_count(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/license/approximateLicenseCount/product/{applicationKey}".to_string();
    if let Some(val) = inputs.get("applicationKey") {
        endpoint = endpoint.replace("{{applicationKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/license/approximateLicenseCount/product/{{applicationKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get my permissions
///
/// Method: GET /rest/api/3/mypermissions
#[actor(
    JiraReadMyPermissionsActor,
    inports::<100>(projectKey, projectId, issueKey, issueId, permissions, projectUuid, projectConfigurationUuid, commentId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_my_permissions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/mypermissions".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectKey") {
        query_pairs.push(("projectKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueKey") {
        query_pairs.push(("issueKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueId") {
        query_pairs.push(("issueId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("permissions") {
        query_pairs.push(("permissions", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectUuid") {
        query_pairs.push(("projectUuid", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectConfigurationUuid") {
        query_pairs.push(("projectConfigurationUuid", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("commentId") {
        query_pairs.push(("commentId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/mypermissions failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get preference
///
/// Method: GET /rest/api/3/mypreferences
#[actor(
    JiraReadPreferenceActor,
    inports::<100>(key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_preference(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/mypreferences".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/mypreferences failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Set preference
///
/// Method: PUT /rest/api/3/mypreferences
#[actor(
    JiraUpdateMyselfActor,
    inports::<100>(key, body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_myself(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/mypreferences".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/mypreferences failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete preference
///
/// Method: DELETE /rest/api/3/mypreferences
#[actor(
    JiraDeletePreferenceActor,
    inports::<100>(key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_preference(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/mypreferences".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/mypreferences failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get locale
///
/// Method: GET /rest/api/3/mypreferences/locale
#[actor(
    JiraReadLocaleActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_locale(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/mypreferences/locale".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/mypreferences/locale failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get current user
///
/// Method: GET /rest/api/3/myself
#[actor(
    JiraReadCurrentUserActor,
    inports::<100>(expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_current_user(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/myself".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/myself failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create notification scheme
///
/// Method: POST /rest/api/3/notificationscheme
#[actor(
    JiraCreateNotificationSchemeActor,
    inports::<100>(notificationSchemeEvents, name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_notification_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/notificationscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("notificationSchemeEvents") {
        body.insert("notificationSchemeEvents".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/notificationscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get notification schemes paginated
///
/// Method: GET /rest/api/3/notificationscheme
#[actor(
    JiraReadNotificationSchemesActor,
    inports::<100>(startAt, maxResults, id, projectId, onlyDefault, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_notification_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/notificationscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("onlyDefault") {
        query_pairs.push(("onlyDefault", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/notificationscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get projects using notification schemes paginated
///
/// Method: GET /rest/api/3/notificationscheme/project
#[actor(
    JiraReadNotificationSchemeToProjectMappingsActor,
    inports::<100>(startAt, maxResults, notificationSchemeId, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_notification_scheme_to_project_mappings(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/notificationscheme/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("notificationSchemeId") {
        query_pairs.push(("notificationSchemeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/notificationscheme/project failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update notification scheme
///
/// Method: PUT /rest/api/3/notificationscheme/{id}
#[actor(
    JiraUpdateNotificationSchemeActor,
    inports::<100>(id, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_notification_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/notificationscheme/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/notificationscheme/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get notification scheme
///
/// Method: GET /rest/api/3/notificationscheme/{id}
#[actor(
    JiraReadNotificationSchemeActor,
    inports::<100>(id, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_notification_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/notificationscheme/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/notificationscheme/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add notifications to notification scheme
///
/// Method: PUT /rest/api/3/notificationscheme/{id}/notification
#[actor(
    JiraUpdateIssueNotificationSchemesActor,
    inports::<100>(id, notificationSchemeEvents),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_notification_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/notificationscheme/{id}/notification".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("notificationSchemeEvents") {
        body.insert("notificationSchemeEvents".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/notificationscheme/{{id}}/notification failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete notification scheme
///
/// Method: DELETE /rest/api/3/notificationscheme/{notificationSchemeId}
#[actor(
    JiraDeleteNotificationSchemeActor,
    inports::<100>(notificationSchemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_notification_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/notificationscheme/{notificationSchemeId}".to_string();
    if let Some(val) = inputs.get("notificationSchemeId") {
        endpoint = endpoint.replace("{{notificationSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/notificationscheme/{{notificationSchemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove notification from notification scheme
///
/// Method: DELETE /rest/api/3/notificationscheme/{notificationSchemeId}/notification/{notificationId}
#[actor(
    JiraDeleteNotificationFromNotificationSchemeActor,
    inports::<100>(notificationSchemeId, notificationId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_notification_from_notification_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/notificationscheme/{notificationSchemeId}/notification/{notificationId}"
            .to_string();
    if let Some(val) = inputs.get("notificationSchemeId") {
        endpoint = endpoint.replace("{{notificationSchemeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("notificationId") {
        endpoint = endpoint.replace("{{notificationId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/notificationscheme/{{notificationSchemeId}}/notification/{{notificationId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get all permissions
///
/// Method: GET /rest/api/3/permissions
#[actor(
    JiraReadAllPermissionsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_permissions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/permissions".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/permissions failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get bulk permissions
///
/// Method: POST /rest/api/3/permissions/check
#[actor(
    JiraReadBulkPermissionsActor,
    inports::<100>(projectPermissions, accountId, globalPermissions),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_bulk_permissions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/permissions/check".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("projectPermissions") {
        body.insert("projectPermissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("accountId") {
        body.insert("accountId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("globalPermissions") {
        body.insert("globalPermissions".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/permissions/check failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get permitted projects
///
/// Method: POST /rest/api/3/permissions/project
#[actor(
    JiraReadPermittedProjectsActor,
    inports::<100>(permissions),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_permitted_projects(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/permissions/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("permissions") {
        body.insert("permissions".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/permissions/project failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create permission scheme
///
/// Method: POST /rest/api/3/permissionscheme
#[actor(
    JiraCreatePermissionSchemeActor,
    inports::<100>(expand, description, scope, self_, permissions, name, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_permission_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/permissionscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("scope") {
        body.insert("scope".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("permissions") {
        body.insert("permissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("expand") {
        body.insert("expand".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/permissionscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all permission schemes
///
/// Method: GET /rest/api/3/permissionscheme
#[actor(
    JiraReadAllPermissionSchemesActor,
    inports::<100>(expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_permission_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/permissionscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/permissionscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete permission scheme
///
/// Method: DELETE /rest/api/3/permissionscheme/{schemeId}
#[actor(
    JiraDeletePermissionSchemeActor,
    inports::<100>(schemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_permission_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/permissionscheme/{schemeId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/permissionscheme/{{schemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get permission scheme
///
/// Method: GET /rest/api/3/permissionscheme/{schemeId}
#[actor(
    JiraReadPermissionSchemeActor,
    inports::<100>(schemeId, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_permission_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/permissionscheme/{schemeId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/permissionscheme/{{schemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update permission scheme
///
/// Method: PUT /rest/api/3/permissionscheme/{schemeId}
#[actor(
    JiraUpdatePermissionSchemeActor,
    inports::<100>(schemeId, expand, self_, id, permissions, description, name, scope),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_permission_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/permissionscheme/{schemeId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("expand") {
        body.insert("expand".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("permissions") {
        body.insert("permissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("scope") {
        body.insert("scope".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/permissionscheme/{{schemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create permission grant
///
/// Method: POST /rest/api/3/permissionscheme/{schemeId}/permission
#[actor(
    JiraCreatePermissionGrantActor,
    inports::<100>(schemeId, expand, id, permission, holder, self_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_permission_grant(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/permissionscheme/{schemeId}/permission".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("permission") {
        body.insert("permission".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("holder") {
        body.insert("holder".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/permissionscheme/{{schemeId}}/permission failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get permission scheme grants
///
/// Method: GET /rest/api/3/permissionscheme/{schemeId}/permission
#[actor(
    JiraReadPermissionSchemeGrantsActor,
    inports::<100>(schemeId, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_permission_scheme_grants(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/permissionscheme/{schemeId}/permission".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/permissionscheme/{{schemeId}}/permission failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get permission scheme grant
///
/// Method: GET /rest/api/3/permissionscheme/{schemeId}/permission/{permissionId}
#[actor(
    JiraReadPermissionSchemeGrantActor,
    inports::<100>(schemeId, permissionId, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_permission_scheme_grant(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/permissionscheme/{schemeId}/permission/{permissionId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("permissionId") {
        endpoint = endpoint.replace("{{permissionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/permissionscheme/{{schemeId}}/permission/{{permissionId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete permission scheme grant
///
/// Method: DELETE /rest/api/3/permissionscheme/{schemeId}/permission/{permissionId}
#[actor(
    JiraDeletePermissionSchemeEntityActor,
    inports::<100>(schemeId, permissionId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_permission_scheme_entity(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/permissionscheme/{schemeId}/permission/{permissionId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("permissionId") {
        endpoint = endpoint.replace("{{permissionId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/permissionscheme/{{schemeId}}/permission/{{permissionId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get plans paginated
///
/// Method: GET /rest/api/3/plans/plan
#[actor(
    JiraReadPlansActor,
    inports::<100>(includeTrashed, includeArchived, cursor, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_plans(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/plans/plan".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("includeTrashed") {
        query_pairs.push(("includeTrashed", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("includeArchived") {
        query_pairs.push(("includeArchived", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("cursor") {
        query_pairs.push(("cursor", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/plans/plan failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create plan
///
/// Method: POST /rest/api/3/plans/plan
#[actor(
    JiraCreatePlanActor,
    inports::<100>(useGroupId, crossProjectReleases, customFields, leadAccountId, name, exclusionRules, permissions, scheduling, issueSources),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_plan(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/plans/plan".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("useGroupId") {
        query_pairs.push(("useGroupId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("crossProjectReleases") {
        body.insert("crossProjectReleases".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("customFields") {
        body.insert("customFields".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("leadAccountId") {
        body.insert("leadAccountId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("exclusionRules") {
        body.insert("exclusionRules".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("permissions") {
        body.insert("permissions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("scheduling") {
        body.insert("scheduling".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueSources") {
        body.insert("issueSources".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/plans/plan failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update plan
///
/// Method: PUT /rest/api/3/plans/plan/{planId}
#[actor(
    JiraUpdatePlanActor,
    inports::<100>(planId, useGroupId, body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_plan(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("useGroupId") {
        query_pairs.push(("useGroupId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/plans/plan/{{planId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get plan
///
/// Method: GET /rest/api/3/plans/plan/{planId}
#[actor(
    JiraReadPlanActor,
    inports::<100>(planId, useGroupId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_plan(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("useGroupId") {
        query_pairs.push(("useGroupId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/plans/plan/{{planId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Archive plan
///
/// Method: PUT /rest/api/3/plans/plan/{planId}/archive
#[actor(
    JiraArchivePlansActor,
    inports::<100>(planId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_archive_plans(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/archive".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/plans/plan/{{planId}}/archive failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Duplicate plan
///
/// Method: POST /rest/api/3/plans/plan/{planId}/duplicate
#[actor(
    JiraCreatePlansActor,
    inports::<100>(planId, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_plans(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/duplicate".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/plans/plan/{{planId}}/duplicate failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get teams in plan paginated
///
/// Method: GET /rest/api/3/plans/plan/{planId}/team
#[actor(
    JiraReadTeamsActor,
    inports::<100>(planId, cursor, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_teams(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/team".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("cursor") {
        query_pairs.push(("cursor", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/plans/plan/{{planId}}/team failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add Atlassian team to plan
///
/// Method: POST /rest/api/3/plans/plan/{planId}/team/atlassian
#[actor(
    JiraCreateTeamsInPlanActor,
    inports::<100>(planId, sprintLength, id, planningStyle, issueSourceId, capacity),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_teams_in_plan(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/team/atlassian".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("sprintLength") {
        body.insert("sprintLength".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("planningStyle") {
        body.insert("planningStyle".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueSourceId") {
        body.insert("issueSourceId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("capacity") {
        body.insert("capacity".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/plans/plan/{{planId}}/team/atlassian failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update Atlassian team in plan
///
/// Method: PUT /rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}
#[actor(
    JiraUpdateAtlassianTeamActor,
    inports::<100>(planId, atlassianTeamId, body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_atlassian_team(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("atlassianTeamId") {
        endpoint = endpoint.replace("{{atlassianTeamId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/plans/plan/{{planId}}/team/atlassian/{{atlassianTeamId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Remove Atlassian team from plan
///
/// Method: DELETE /rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}
#[actor(
    JiraDeleteAtlassianTeamActor,
    inports::<100>(planId, atlassianTeamId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_atlassian_team(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("atlassianTeamId") {
        endpoint = endpoint.replace("{{atlassianTeamId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/plans/plan/{{planId}}/team/atlassian/{{atlassianTeamId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get Atlassian team in plan
///
/// Method: GET /rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}
#[actor(
    JiraReadAtlassianTeamActor,
    inports::<100>(planId, atlassianTeamId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_atlassian_team(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("atlassianTeamId") {
        endpoint = endpoint.replace("{{atlassianTeamId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/plans/plan/{{planId}}/team/atlassian/{{atlassianTeamId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create plan-only team
///
/// Method: POST /rest/api/3/plans/plan/{planId}/team/planonly
#[actor(
    JiraCreatePlanOnlyTeamActor,
    inports::<100>(planId, issueSourceId, name, sprintLength, planningStyle, memberAccountIds, capacity),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_plan_only_team(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/team/planonly".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueSourceId") {
        body.insert("issueSourceId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("sprintLength") {
        body.insert("sprintLength".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("planningStyle") {
        body.insert("planningStyle".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("memberAccountIds") {
        body.insert("memberAccountIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("capacity") {
        body.insert("capacity".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/plans/plan/{{planId}}/team/planonly failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update plan-only team
///
/// Method: PUT /rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}
#[actor(
    JiraUpdatePlanOnlyTeamActor,
    inports::<100>(planId, planOnlyTeamId, body),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_plan_only_team(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("planOnlyTeamId") {
        endpoint = endpoint.replace("{{planOnlyTeamId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("body") {
        body.insert("body".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/plans/plan/{{planId}}/team/planonly/{{planOnlyTeamId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete plan-only team
///
/// Method: DELETE /rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}
#[actor(
    JiraDeletePlanOnlyTeamActor,
    inports::<100>(planId, planOnlyTeamId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_plan_only_team(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("planOnlyTeamId") {
        endpoint = endpoint.replace("{{planOnlyTeamId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/plans/plan/{{planId}}/team/planonly/{{planOnlyTeamId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get plan-only team
///
/// Method: GET /rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}
#[actor(
    JiraReadPlanOnlyTeamActor,
    inports::<100>(planId, planOnlyTeamId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_plan_only_team(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("planOnlyTeamId") {
        endpoint = endpoint.replace("{{planOnlyTeamId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/plans/plan/{{planId}}/team/planonly/{{planOnlyTeamId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Trash plan
///
/// Method: PUT /rest/api/3/plans/plan/{planId}/trash
#[actor(
    JiraUpdatePlansActor,
    inports::<100>(planId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_plans(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/plans/plan/{planId}/trash".to_string();
    if let Some(val) = inputs.get("planId") {
        endpoint = endpoint.replace("{{planId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/plans/plan/{{planId}}/trash failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set default priority
///
/// Method: PUT /rest/api/3/priority/default
#[actor(
    JiraUpdateIssuePrioritiesActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_priorities(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/priority/default".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/priority/default failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Move priorities
///
/// Method: PUT /rest/api/3/priority/move
#[actor(
    JiraMoveIssuePrioritiesActor,
    inports::<100>(after, position, ids),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_issue_priorities(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/priority/move".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("after") {
        body.insert("after".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("position") {
        body.insert("position".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("ids") {
        body.insert("ids".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/priority/move failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get priority
///
/// Method: GET /rest/api/3/priority/{id}
#[actor(
    JiraReadPriorityActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_priority(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/priority/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/priority/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete priority
///
/// Method: DELETE /rest/api/3/priority/{id}
#[actor(
    JiraDeletePriorityActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_priority(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/priority/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/priority/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get priority schemes
///
/// Method: GET /rest/api/3/priorityscheme
#[actor(
    JiraReadPrioritySchemesActor,
    inports::<100>(startAt, maxResults, priorityId, schemeId, schemeName, onlyDefault, orderBy, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_priority_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/priorityscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("priorityId") {
        query_pairs.push(("priorityId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("schemeId") {
        query_pairs.push(("schemeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("schemeName") {
        query_pairs.push(("schemeName", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("onlyDefault") {
        query_pairs.push(("onlyDefault", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/priorityscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create priority scheme
///
/// Method: POST /rest/api/3/priorityscheme
#[actor(
    JiraCreatePrioritySchemeActor,
    inports::<100>(defaultPriorityId, mappings, projectIds, priorityIds, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_priority_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/priorityscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("defaultPriorityId") {
        body.insert("defaultPriorityId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("mappings") {
        body.insert("mappings".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectIds") {
        body.insert("projectIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("priorityIds") {
        body.insert("priorityIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/priorityscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Suggested priorities for mappings
///
/// Method: POST /rest/api/3/priorityscheme/mappings
#[actor(
    JiraCreatePrioritySchemesActor,
    inports::<100>(projects, priorities, schemeId, maxResults, startAt),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_priority_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/priorityscheme/mappings".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("projects") {
        body.insert("projects".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("priorities") {
        body.insert("priorities".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("schemeId") {
        body.insert("schemeId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("maxResults") {
        body.insert("maxResults".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("startAt") {
        body.insert("startAt".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/priorityscheme/mappings failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get available priorities by priority scheme
///
/// Method: GET /rest/api/3/priorityscheme/priorities/available
#[actor(
    JiraReadAvailablePrioritiesByPrioritySchemeActor,
    inports::<100>(startAt, maxResults, query, schemeId, exclude),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_available_priorities_by_priority_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/priorityscheme/priorities/available".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("schemeId") {
        query_pairs.push(("schemeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("exclude") {
        query_pairs.push(("exclude", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/priorityscheme/priorities/available failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete priority scheme
///
/// Method: DELETE /rest/api/3/priorityscheme/{schemeId}
#[actor(
    JiraDeletePrioritySchemeActor,
    inports::<100>(schemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_priority_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/priorityscheme/{schemeId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/priorityscheme/{{schemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update priority scheme
///
/// Method: PUT /rest/api/3/priorityscheme/{schemeId}
#[actor(
    JiraUpdatePrioritySchemeActor,
    inports::<100>(schemeId, mappings, defaultPriorityId, name, projects, priorities, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_priority_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/priorityscheme/{schemeId}".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("mappings") {
        body.insert("mappings".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("defaultPriorityId") {
        body.insert("defaultPriorityId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projects") {
        body.insert("projects".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("priorities") {
        body.insert("priorities".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/priorityscheme/{{schemeId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get priorities by priority scheme
///
/// Method: GET /rest/api/3/priorityscheme/{schemeId}/priorities
#[actor(
    JiraReadPrioritiesByPrioritySchemeActor,
    inports::<100>(startAt, maxResults, schemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_priorities_by_priority_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/priorityscheme/{schemeId}/priorities".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/priorityscheme/{{schemeId}}/priorities failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get projects by priority scheme
///
/// Method: GET /rest/api/3/priorityscheme/{schemeId}/projects
#[actor(
    JiraReadProjectsByPrioritySchemeActor,
    inports::<100>(startAt, maxResults, projectId, schemeId, query),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_projects_by_priority_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/priorityscheme/{schemeId}/projects".to_string();
    if let Some(val) = inputs.get("schemeId") {
        endpoint = endpoint.replace("{{schemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/priorityscheme/{{schemeId}}/projects failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create project
///
/// Method: POST /rest/api/3/project
#[actor(
    JiraCreateProjectActor,
    inports::<100>(issueTypeScheme, assigneeType, key, lead, projectTemplateKey, workflowScheme, description, avatarId, categoryId, notificationScheme, fieldConfigurationScheme, issueTypeScreenScheme, projectTypeKey, issueSecurityScheme, leadAccountId, name, url, permissionScheme),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_project(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypeScheme") {
        body.insert("issueTypeScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("assigneeType") {
        body.insert("assigneeType".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("key") {
        body.insert("key".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lead") {
        body.insert("lead".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectTemplateKey") {
        body.insert("projectTemplateKey".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflowScheme") {
        body.insert("workflowScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("avatarId") {
        body.insert("avatarId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("categoryId") {
        body.insert("categoryId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("notificationScheme") {
        body.insert("notificationScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("fieldConfigurationScheme") {
        body.insert("fieldConfigurationScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypeScreenScheme") {
        body.insert("issueTypeScreenScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectTypeKey") {
        body.insert("projectTypeKey".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueSecurityScheme") {
        body.insert("issueSecurityScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("leadAccountId") {
        body.insert("leadAccountId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("url") {
        body.insert("url".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("permissionScheme") {
        body.insert("permissionScheme".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/project failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create custom project
///
/// Method: POST /rest/api/3/project-template
#[actor(
    JiraCreateProjectWithCustomTemplateActor,
    inports::<100>(details, template),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_project_with_custom_template(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project-template".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("details") {
        body.insert("details".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("template") {
        body.insert("template".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/project-template failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Edit a custom project template
///
/// Method: PUT /rest/api/3/project-template/edit-template
#[actor(
    JiraUpdateProjectTemplatesActor,
    inports::<100>(templateName, templateGenerationOptions, templateKey, templateDescription),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_templates(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project-template/edit-template".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("templateName") {
        body.insert("templateName".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("templateGenerationOptions") {
        body.insert("templateGenerationOptions".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("templateKey") {
        body.insert("templateKey".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("templateDescription") {
        body.insert("templateDescription".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/project-template/edit-template failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Gets a custom project template
///
/// Method: GET /rest/api/3/project-template/live-template
#[actor(
    JiraListProjectTemplatesActor,
    inports::<100>(projectId, templateKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_list_project_templates(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project-template/live-template".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("templateKey") {
        query_pairs.push(("templateKey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project-template/live-template failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Deletes a custom project template
///
/// Method: DELETE /rest/api/3/project-template/remove-template
#[actor(
    JiraDeleteTemplateActor,
    inports::<100>(templateKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_template(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project-template/remove-template".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("templateKey") {
        query_pairs.push(("templateKey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/project-template/remove-template failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Save a custom project template
///
/// Method: POST /rest/api/3/project-template/save-template
#[actor(
    JiraCreateProjectTemplatesActor,
    inports::<100>(templateFromProjectRequest, templateName, templateDescription),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_project_templates(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project-template/save-template".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("templateFromProjectRequest") {
        body.insert("templateFromProjectRequest".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("templateName") {
        body.insert("templateName".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("templateDescription") {
        body.insert("templateDescription".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/project-template/save-template failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get recent projects
///
/// Method: GET /rest/api/3/project/recent
#[actor(
    JiraReadRecentActor,
    inports::<100>(expand, properties),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_recent(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project/recent".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("properties") {
        query_pairs.push(("properties", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/project/recent failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get projects paginated
///
/// Method: GET /rest/api/3/project/search
#[actor(
    JiraSearchProjectsActor,
    inports::<100>(startAt, maxResults, orderBy, id, keys, query, typeKey, categoryId, action, expand, status, properties, propertyQuery),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_projects(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("keys") {
        query_pairs.push(("keys", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("typeKey") {
        query_pairs.push(("typeKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("categoryId") {
        query_pairs.push(("categoryId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("action") {
        query_pairs.push(("action", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("status") {
        query_pairs.push(("status", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("properties") {
        query_pairs.push(("properties", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("propertyQuery") {
        query_pairs.push(("propertyQuery", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/project/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all project types
///
/// Method: GET /rest/api/3/project/type
#[actor(
    JiraReadAllProjectTypesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_project_types(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project/type".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/project/type failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get licensed project types
///
/// Method: GET /rest/api/3/project/type/accessible
#[actor(
    JiraReadAllAccessibleProjectTypesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_accessible_project_types(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/project/type/accessible".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/project/type/accessible failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project type by key
///
/// Method: GET /rest/api/3/project/type/{projectTypeKey}
#[actor(
    JiraReadProjectTypeByKeyActor,
    inports::<100>(projectTypeKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_type_by_key(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/type/{projectTypeKey}".to_string();
    if let Some(val) = inputs.get("projectTypeKey") {
        endpoint = endpoint.replace("{{projectTypeKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/type/{{projectTypeKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get accessible project type by key
///
/// Method: GET /rest/api/3/project/type/{projectTypeKey}/accessible
#[actor(
    JiraReadAccessibleProjectTypeByKeyActor,
    inports::<100>(projectTypeKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_accessible_project_type_by_key(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/type/{projectTypeKey}/accessible".to_string();
    if let Some(val) = inputs.get("projectTypeKey") {
        endpoint = endpoint.replace("{{projectTypeKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/type/{{projectTypeKey}}/accessible failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete project
///
/// Method: DELETE /rest/api/3/project/{projectIdOrKey}
#[actor(
    JiraDeleteProjectActor,
    inports::<100>(projectIdOrKey, enableUndo),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_project(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("enableUndo") {
        query_pairs.push(("enableUndo", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/project/{{projectIdOrKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}
#[actor(
    JiraReadProjectActor,
    inports::<100>(projectIdOrKey, expand, properties),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("properties") {
        query_pairs.push(("properties", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/project/{{projectIdOrKey}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update project
///
/// Method: PUT /rest/api/3/project/{projectIdOrKey}
#[actor(
    JiraUpdateProjectActor,
    inports::<100>(projectIdOrKey, expand, assigneeType, description, issueSecurityScheme, name, categoryId, notificationScheme, lead, avatarId, leadAccountId, releasedProjectKeys, url, permissionScheme, key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("assigneeType") {
        body.insert("assigneeType".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueSecurityScheme") {
        body.insert("issueSecurityScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("categoryId") {
        body.insert("categoryId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("notificationScheme") {
        body.insert("notificationScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lead") {
        body.insert("lead".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("avatarId") {
        body.insert("avatarId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("leadAccountId") {
        body.insert("leadAccountId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("releasedProjectKeys") {
        body.insert("releasedProjectKeys".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("url") {
        body.insert("url".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("permissionScheme") {
        body.insert("permissionScheme".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("key") {
        body.insert("key".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/project/{{projectIdOrKey}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Archive project
///
/// Method: POST /rest/api/3/project/{projectIdOrKey}/archive
#[actor(
    JiraArchiveProjectsActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_archive_projects(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/archive".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/project/{{projectIdOrKey}}/archive failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set project avatar
///
/// Method: PUT /rest/api/3/project/{projectIdOrKey}/avatar
#[actor(
    JiraUpdateProjectAvatarActor,
    inports::<100>(projectIdOrKey, owner, isSelected, urls, fileName, id, isDeletable, isSystemAvatar),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_avatar(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/avatar".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("owner") {
        body.insert("owner".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("isSelected") {
        body.insert("isSelected".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("urls") {
        body.insert("urls".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("fileName") {
        body.insert("fileName".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("isDeletable") {
        body.insert("isDeletable".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("isSystemAvatar") {
        body.insert("isSystemAvatar".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/project/{{projectIdOrKey}}/avatar failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete project avatar
///
/// Method: DELETE /rest/api/3/project/{projectIdOrKey}/avatar/{id}
#[actor(
    JiraDeleteProjectAvatarActor,
    inports::<100>(projectIdOrKey, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_project_avatar(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/avatar/{id}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/project/{{projectIdOrKey}}/avatar/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Load project avatar
///
/// Method: POST /rest/api/3/project/{projectIdOrKey}/avatar2
#[actor(
    JiraCreateProjectAvatarActor,
    inports::<100>(projectIdOrKey, x, y, size),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_project_avatar(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/avatar2".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("x") {
        query_pairs.push(("x", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("y") {
        query_pairs.push(("y", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("size") {
        query_pairs.push(("size", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/project/{{projectIdOrKey}}/avatar2 failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all project avatars
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/avatars
#[actor(
    JiraReadAllProjectAvatarsActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_project_avatars(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/avatars".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/avatars failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get the classification configuration for a project
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/classification-config
#[actor(
    JiraReadProjectClassificationConfigActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_classification_config(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/classification-config".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/project/{{projectIdOrKey}}/classification-config failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Remove the default data classification level from a project
///
/// Method: DELETE /rest/api/3/project/{projectIdOrKey}/classification-level/default
#[actor(
    JiraDeleteDefaultProjectClassificationActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_default_project_classification(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/project/{projectIdOrKey}/classification-level/default".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/project/{{projectIdOrKey}}/classification-level/default failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get the default data classification level of a project
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/classification-level/default
#[actor(
    JiraReadDefaultProjectClassificationActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_default_project_classification(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/project/{projectIdOrKey}/classification-level/default".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/project/{{projectIdOrKey}}/classification-level/default failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update the default data classification level of a project
///
/// Method: PUT /rest/api/3/project/{projectIdOrKey}/classification-level/default
#[actor(
    JiraUpdateDefaultProjectClassificationActor,
    inports::<100>(projectIdOrKey, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_default_project_classification(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/project/{projectIdOrKey}/classification-level/default".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/project/{{projectIdOrKey}}/classification-level/default failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get project components paginated
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/component
#[actor(
    JiraReadProjectComponentsPaginatedActor,
    inports::<100>(projectIdOrKey, startAt, maxResults, orderBy, componentSource, query),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_components_paginated(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/component".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("componentSource") {
        query_pairs.push(("componentSource", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/component failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project components
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/components
#[actor(
    JiraReadProjectComponentsActor,
    inports::<100>(projectIdOrKey, componentSource),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_components(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/components".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("componentSource") {
        query_pairs.push(("componentSource", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/components failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete project asynchronously
///
/// Method: POST /rest/api/3/project/{projectIdOrKey}/delete
#[actor(
    JiraDeleteProjectAsynchronouslyActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_project_asynchronously(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/delete".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/project/{{projectIdOrKey}}/delete failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project features
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/features
#[actor(
    JiraReadFeaturesForProjectActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_features_for_project(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/features".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/features failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set project feature state
///
/// Method: PUT /rest/api/3/project/{projectIdOrKey}/features/{featureKey}
#[actor(
    JiraUpdateProjectFeaturesActor,
    inports::<100>(projectIdOrKey, featureKey, state),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_features(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/features/{featureKey}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("featureKey") {
        endpoint = endpoint.replace("{{featureKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("state") {
        body.insert("state".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/project/{{projectIdOrKey}}/features/{{featureKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get project property keys
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/properties
#[actor(
    JiraReadProjectPropertyKeysActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_property_keys(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/properties".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/properties failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set project property
///
/// Method: PUT /rest/api/3/project/{projectIdOrKey}/properties/{propertyKey}
#[actor(
    JiraUpdateProjectPropertiesActor,
    inports::<100>(projectIdOrKey, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/project/{{projectIdOrKey}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get project property
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/properties/{propertyKey}
#[actor(
    JiraReadProjectPropertyActor,
    inports::<100>(projectIdOrKey, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/project/{{projectIdOrKey}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete project property
///
/// Method: DELETE /rest/api/3/project/{projectIdOrKey}/properties/{propertyKey}
#[actor(
    JiraDeleteProjectPropertyActor,
    inports::<100>(projectIdOrKey, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_project_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/project/{{projectIdOrKey}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Restore deleted or archived project
///
/// Method: POST /rest/api/3/project/{projectIdOrKey}/restore
#[actor(
    JiraRestoreProjectsActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_restore_projects(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/restore".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/project/{{projectIdOrKey}}/restore failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project roles for project
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/role
#[actor(
    JiraReadProjectRolesActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_roles(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/role".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/role failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project role for project
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/role/{id}
#[actor(
    JiraReadProjectRoleActor,
    inports::<100>(projectIdOrKey, id, excludeInactiveUsers),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_role(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/role/{id}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("excludeInactiveUsers") {
        query_pairs.push(("excludeInactiveUsers", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/role/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add actors to project role
///
/// Method: POST /rest/api/3/project/{projectIdOrKey}/role/{id}
#[actor(
    JiraCreateProjectRoleActorsActor,
    inports::<100>(projectIdOrKey, id, group, groupId, user),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_project_role_actors(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/role/{id}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("group") {
        body.insert("group".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("groupId") {
        body.insert("groupId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("user") {
        body.insert("user".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/project/{{projectIdOrKey}}/role/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete actors from project role
///
/// Method: DELETE /rest/api/3/project/{projectIdOrKey}/role/{id}
#[actor(
    JiraDeleteActorActor,
    inports::<100>(projectIdOrKey, id, user, group, groupId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_actor(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/role/{id}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("user") {
        query_pairs.push(("user", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("group") {
        query_pairs.push(("group", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/project/{{projectIdOrKey}}/role/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set actors for project role
///
/// Method: PUT /rest/api/3/project/{projectIdOrKey}/role/{id}
#[actor(
    JiraUpdateProjectRoleActorsActor,
    inports::<100>(projectIdOrKey, id, categorisedActors),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_role_actors(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/role/{id}".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("categorisedActors") {
        body.insert("categorisedActors".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/project/{{projectIdOrKey}}/role/{{id}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project role details
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/roledetails
#[actor(
    JiraReadProjectRoleDetailsActor,
    inports::<100>(projectIdOrKey, currentMember, excludeConnectAddons, excludeOtherServiceRoles),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_role_details(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/roledetails".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("currentMember") {
        query_pairs.push(("currentMember", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("excludeConnectAddons") {
        query_pairs.push(("excludeConnectAddons", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("excludeOtherServiceRoles") {
        query_pairs.push(("excludeOtherServiceRoles", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/roledetails failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all statuses for project
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/statuses
#[actor(
    JiraReadAllStatusesActor,
    inports::<100>(projectIdOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_statuses(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/statuses".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/statuses failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project versions paginated
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/version
#[actor(
    JiraReadProjectVersionsPaginatedActor,
    inports::<100>(projectIdOrKey, startAt, maxResults, orderBy, query, status, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_versions_paginated(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/version".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("status") {
        query_pairs.push(("status", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/version failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project versions
///
/// Method: GET /rest/api/3/project/{projectIdOrKey}/versions
#[actor(
    JiraReadProjectVersionsActor,
    inports::<100>(projectIdOrKey, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_versions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectIdOrKey}/versions".to_string();
    if let Some(val) = inputs.get("projectIdOrKey") {
        endpoint = endpoint.replace("{{projectIdOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectIdOrKey}}/versions failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set project's sender email
///
/// Method: PUT /rest/api/3/project/{projectId}/email
#[actor(
    JiraUpdateProjectEmailActor,
    inports::<100>(projectId, emailAddress, emailAddressStatus),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_email(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectId}/email".to_string();
    if let Some(val) = inputs.get("projectId") {
        endpoint = endpoint.replace("{{projectId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("emailAddress") {
        body.insert("emailAddress".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("emailAddressStatus") {
        body.insert("emailAddressStatus".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/project/{{projectId}}/email failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project's sender email
///
/// Method: GET /rest/api/3/project/{projectId}/email
#[actor(
    JiraReadProjectEmailActor,
    inports::<100>(projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_email(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectId}/email".to_string();
    if let Some(val) = inputs.get("projectId") {
        endpoint = endpoint.replace("{{projectId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/project/{{projectId}}/email failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project issue type hierarchy
///
/// Method: GET /rest/api/3/project/{projectId}/hierarchy
#[actor(
    JiraReadHierarchyActor,
    inports::<100>(projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_hierarchy(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectId}/hierarchy".to_string();
    if let Some(val) = inputs.get("projectId") {
        endpoint = endpoint.replace("{{projectId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectId}}/hierarchy failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project issue security scheme
///
/// Method: GET /rest/api/3/project/{projectKeyOrId}/issuesecuritylevelscheme
#[actor(
    JiraReadProjectIssueSecuritySchemeActor,
    inports::<100>(projectKeyOrId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_issue_security_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectKeyOrId}/issuesecuritylevelscheme".to_string();
    if let Some(val) = inputs.get("projectKeyOrId") {
        endpoint = endpoint.replace("{{projectKeyOrId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/project/{{projectKeyOrId}}/issuesecuritylevelscheme failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get project notification scheme
///
/// Method: GET /rest/api/3/project/{projectKeyOrId}/notificationscheme
#[actor(
    JiraReadNotificationSchemeForProjectActor,
    inports::<100>(projectKeyOrId, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_notification_scheme_for_project(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectKeyOrId}/notificationscheme".to_string();
    if let Some(val) = inputs.get("projectKeyOrId") {
        endpoint = endpoint.replace("{{projectKeyOrId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectKeyOrId}}/notificationscheme failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get assigned permission scheme
///
/// Method: GET /rest/api/3/project/{projectKeyOrId}/permissionscheme
#[actor(
    JiraReadAssignedPermissionSchemeActor,
    inports::<100>(projectKeyOrId, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_assigned_permission_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectKeyOrId}/permissionscheme".to_string();
    if let Some(val) = inputs.get("projectKeyOrId") {
        endpoint = endpoint.replace("{{projectKeyOrId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectKeyOrId}}/permissionscheme failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Assign permission scheme
///
/// Method: PUT /rest/api/3/project/{projectKeyOrId}/permissionscheme
#[actor(
    JiraUpdateProjectPermissionSchemesActor,
    inports::<100>(projectKeyOrId, expand, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_permission_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectKeyOrId}/permissionscheme".to_string();
    if let Some(val) = inputs.get("projectKeyOrId") {
        endpoint = endpoint.replace("{{projectKeyOrId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/project/{{projectKeyOrId}}/permissionscheme failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project issue security levels
///
/// Method: GET /rest/api/3/project/{projectKeyOrId}/securitylevel
#[actor(
    JiraReadSecurityLevelsForProjectActor,
    inports::<100>(projectKeyOrId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_security_levels_for_project(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/project/{projectKeyOrId}/securitylevel".to_string();
    if let Some(val) = inputs.get("projectKeyOrId") {
        endpoint = endpoint.replace("{{projectKeyOrId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/project/{{projectKeyOrId}}/securitylevel failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create project category
///
/// Method: POST /rest/api/3/projectCategory
#[actor(
    JiraCreateProjectCategoryActor,
    inports::<100>(name, description, id, self_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_project_category(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/projectCategory".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/projectCategory failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all project categories
///
/// Method: GET /rest/api/3/projectCategory
#[actor(
    JiraReadAllProjectCategoriesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_project_categories(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/projectCategory".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/projectCategory failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update project category
///
/// Method: PUT /rest/api/3/projectCategory/{id}
#[actor(
    JiraUpdateProjectCategoryActor,
    inports::<100>(id, description, name, self_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_category(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/projectCategory/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/projectCategory/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete project category
///
/// Method: DELETE /rest/api/3/projectCategory/{id}
#[actor(
    JiraDeleteProjectCategoryActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_project_category(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/projectCategory/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/projectCategory/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get project category by ID
///
/// Method: GET /rest/api/3/projectCategory/{id}
#[actor(
    JiraReadProjectCategoryByIdActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_category_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/projectCategory/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/projectCategory/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get fields for projects
///
/// Method: GET /rest/api/3/projects/fields
#[actor(
    JiraReadProjectFieldsActor,
    inports::<100>(startAt, maxResults, projectId, workTypeId, fieldId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/projects/fields".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("workTypeId") {
        query_pairs.push(("workTypeId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fieldId") {
        query_pairs.push(("fieldId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/projects/fields failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Validate project key
///
/// Method: GET /rest/api/3/projectvalidate/key
#[actor(
    JiraListKeyActor,
    inports::<100>(key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_list_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/projectvalidate/key".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/projectvalidate/key failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get valid project key
///
/// Method: GET /rest/api/3/projectvalidate/validProjectKey
#[actor(
    JiraReadValidProjectKeyActor,
    inports::<100>(key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_valid_project_key(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/projectvalidate/validProjectKey".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/projectvalidate/validProjectKey failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get valid project name
///
/// Method: GET /rest/api/3/projectvalidate/validProjectName
#[actor(
    JiraReadValidProjectNameActor,
    inports::<100>(name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_valid_project_name(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/projectvalidate/validProjectName".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("name") {
        query_pairs.push(("name", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/projectvalidate/validProjectName failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Redact
///
/// Method: POST /rest/api/3/redact
#[actor(
    JiraCreateIssueRedactionActor,
    inports::<100>(redactions),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_issue_redaction(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/redact".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("redactions") {
        body.insert("redactions".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/redact failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get redaction status
///
/// Method: GET /rest/api/3/redact/status/{jobId}
#[actor(
    JiraReadRedactionStatusActor,
    inports::<100>(jobId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_redaction_status(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/redact/status/{jobId}".to_string();
    if let Some(val) = inputs.get("jobId") {
        endpoint = endpoint.replace("{{jobId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/redact/status/{{jobId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create resolution
///
/// Method: POST /rest/api/3/resolution
#[actor(
    JiraCreateResolutionActor,
    inports::<100>(description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_resolution(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/resolution".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/resolution failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Set default resolution
///
/// Method: PUT /rest/api/3/resolution/default
#[actor(
    JiraUpdateIssueResolutionsActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_resolutions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/resolution/default".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/resolution/default failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Move resolutions
///
/// Method: PUT /rest/api/3/resolution/move
#[actor(
    JiraMoveIssueResolutionsActor,
    inports::<100>(after, ids, position),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_issue_resolutions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/resolution/move".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("after") {
        body.insert("after".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("ids") {
        body.insert("ids".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("position") {
        body.insert("position".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/resolution/move failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Search resolutions
///
/// Method: GET /rest/api/3/resolution/search
#[actor(
    JiraSearchResolutionsActor,
    inports::<100>(startAt, maxResults, id, onlyDefault),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_resolutions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/resolution/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("onlyDefault") {
        query_pairs.push(("onlyDefault", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/resolution/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update resolution
///
/// Method: PUT /rest/api/3/resolution/{id}
#[actor(
    JiraUpdateResolutionActor,
    inports::<100>(id, name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_resolution(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/resolution/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/resolution/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete resolution
///
/// Method: DELETE /rest/api/3/resolution/{id}
#[actor(
    JiraDeleteResolutionActor,
    inports::<100>(id, replaceWith),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_resolution(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/resolution/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("replaceWith") {
        query_pairs.push(("replaceWith", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/resolution/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get resolution
///
/// Method: GET /rest/api/3/resolution/{id}
#[actor(
    JiraReadResolutionActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_resolution(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/resolution/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/resolution/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create project role
///
/// Method: POST /rest/api/3/role
#[actor(
    JiraCreateProjectRoleActor,
    inports::<100>(name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_project_role(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/role".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/role failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all project roles
///
/// Method: GET /rest/api/3/role
#[actor(
    JiraReadAllProjectRolesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_project_roles(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/role".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/role failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get project role by ID
///
/// Method: GET /rest/api/3/role/{id}
#[actor(
    JiraReadProjectRoleByIdActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_role_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/role/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/role/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Fully update project role
///
/// Method: PUT /rest/api/3/role/{id}
#[actor(
    JiraUpdateProjectRolesActor,
    inports::<100>(id, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_roles(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/role/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/role/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Partial update project role
///
/// Method: POST /rest/api/3/role/{id}
#[actor(
    JiraCreateProjectRolesActor,
    inports::<100>(id, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_project_roles(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/role/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/role/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete project role
///
/// Method: DELETE /rest/api/3/role/{id}
#[actor(
    JiraDeleteProjectRoleActor,
    inports::<100>(id, swap),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_project_role(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/role/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("swap") {
        query_pairs.push(("swap", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/role/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get default actors for project role
///
/// Method: GET /rest/api/3/role/{id}/actors
#[actor(
    JiraReadProjectRoleActorsForRoleActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_role_actors_for_role(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/role/{id}/actors".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/role/{{id}}/actors failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete default actors from project role
///
/// Method: DELETE /rest/api/3/role/{id}/actors
#[actor(
    JiraDeleteProjectRoleActorsFromRoleActor,
    inports::<100>(id, user, groupId, group),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_project_role_actors_from_role(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/role/{id}/actors".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("user") {
        query_pairs.push(("user", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("groupId") {
        query_pairs.push(("groupId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("group") {
        query_pairs.push(("group", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/role/{{id}}/actors failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get screens
///
/// Method: GET /rest/api/3/screens
#[actor(
    JiraReadScreensActor,
    inports::<100>(startAt, maxResults, id, queryString, scope, orderBy),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_screens(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/screens".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("queryString") {
        query_pairs.push(("queryString", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("scope") {
        query_pairs.push(("scope", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/screens failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create screen
///
/// Method: POST /rest/api/3/screens
#[actor(
    JiraCreateScreenActor,
    inports::<100>(name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_screen(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/screens".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/screens failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Add field to default screen
///
/// Method: POST /rest/api/3/screens/addToDefault/{fieldId}
#[actor(
    JiraCreateScreensActor,
    inports::<100>(fieldId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_screens(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/addToDefault/{fieldId}".to_string();
    if let Some(val) = inputs.get("fieldId") {
        endpoint = endpoint.replace("{{fieldId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/screens/addToDefault/{{fieldId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get bulk screen tabs
///
/// Method: GET /rest/api/3/screens/tabs
#[actor(
    JiraReadBulkScreenTabsActor,
    inports::<100>(screenId, tabId, startAt, maxResult),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_bulk_screen_tabs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/screens/tabs".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("screenId") {
        query_pairs.push(("screenId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("tabId") {
        query_pairs.push(("tabId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResult") {
        query_pairs.push(("maxResult", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/screens/tabs failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update screen
///
/// Method: PUT /rest/api/3/screens/{screenId}
#[actor(
    JiraUpdateScreenActor,
    inports::<100>(screenId, name, description),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_screen(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/screens/{{screenId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete screen
///
/// Method: DELETE /rest/api/3/screens/{screenId}
#[actor(
    JiraDeleteScreenActor,
    inports::<100>(screenId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_screen(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/screens/{{screenId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get available screen fields
///
/// Method: GET /rest/api/3/screens/{screenId}/availableFields
#[actor(
    JiraReadAvailableScreenFieldsActor,
    inports::<100>(screenId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_available_screen_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/availableFields".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/screens/{{screenId}}/availableFields failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all screen tabs
///
/// Method: GET /rest/api/3/screens/{screenId}/tabs
#[actor(
    JiraReadAllScreenTabsActor,
    inports::<100>(screenId, projectKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_screen_tabs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectKey") {
        query_pairs.push(("projectKey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/screens/{{screenId}}/tabs failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create screen tab
///
/// Method: POST /rest/api/3/screens/{screenId}/tabs
#[actor(
    JiraCreateScreenTabsActor,
    inports::<100>(screenId, id, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_screen_tabs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/screens/{{screenId}}/tabs failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete screen tab
///
/// Method: DELETE /rest/api/3/screens/{screenId}/tabs/{tabId}
#[actor(
    JiraDeleteScreenTabActor,
    inports::<100>(screenId, tabId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_screen_tab(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs/{tabId}".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("tabId") {
        endpoint = endpoint.replace("{{tabId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/screens/{{screenId}}/tabs/{{tabId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update screen tab
///
/// Method: PUT /rest/api/3/screens/{screenId}/tabs/{tabId}
#[actor(
    JiraUpdateScreenTabsActor,
    inports::<100>(screenId, tabId, id, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_screen_tabs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs/{tabId}".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("tabId") {
        endpoint = endpoint.replace("{{tabId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/screens/{{screenId}}/tabs/{{tabId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all screen tab fields
///
/// Method: GET /rest/api/3/screens/{screenId}/tabs/{tabId}/fields
#[actor(
    JiraReadAllScreenTabFieldsActor,
    inports::<100>(screenId, tabId, projectKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_screen_tab_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs/{tabId}/fields".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("tabId") {
        endpoint = endpoint.replace("{{tabId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectKey") {
        query_pairs.push(("projectKey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/screens/{{screenId}}/tabs/{{tabId}}/fields failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Add screen tab field
///
/// Method: POST /rest/api/3/screens/{screenId}/tabs/{tabId}/fields
#[actor(
    JiraCreateScreenTabFieldsActor,
    inports::<100>(screenId, tabId, fieldId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_screen_tab_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs/{tabId}/fields".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("tabId") {
        endpoint = endpoint.replace("{{tabId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("fieldId") {
        body.insert("fieldId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/screens/{{screenId}}/tabs/{{tabId}}/fields failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove screen tab field
///
/// Method: DELETE /rest/api/3/screens/{screenId}/tabs/{tabId}/fields/{id}
#[actor(
    JiraDeleteScreenTabFieldActor,
    inports::<100>(screenId, tabId, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_screen_tab_field(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs/{tabId}/fields/{id}".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("tabId") {
        endpoint = endpoint.replace("{{tabId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/screens/{{screenId}}/tabs/{{tabId}}/fields/{{id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Move screen tab field
///
/// Method: POST /rest/api/3/screens/{screenId}/tabs/{tabId}/fields/{id}/move
#[actor(
    JiraMoveScreenTabFieldsActor,
    inports::<100>(screenId, tabId, id, position, after),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_screen_tab_fields(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs/{tabId}/fields/{id}/move".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("tabId") {
        endpoint = endpoint.replace("{{tabId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("position") {
        body.insert("position".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("after") {
        body.insert("after".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /rest/api/3/screens/{{screenId}}/tabs/{{tabId}}/fields/{{id}}/move failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Move screen tab
///
/// Method: POST /rest/api/3/screens/{screenId}/tabs/{tabId}/move/{pos}
#[actor(
    JiraMoveScreenTabsActor,
    inports::<100>(screenId, tabId, pos),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_screen_tabs(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screens/{screenId}/tabs/{tabId}/move/{pos}".to_string();
    if let Some(val) = inputs.get("screenId") {
        endpoint = endpoint.replace("{{screenId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("tabId") {
        endpoint = endpoint.replace("{{tabId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pos") {
        endpoint = endpoint.replace("{{pos}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /rest/api/3/screens/{{screenId}}/tabs/{{tabId}}/move/{{pos}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get screen schemes
///
/// Method: GET /rest/api/3/screenscheme
#[actor(
    JiraReadScreenSchemesActor,
    inports::<100>(startAt, maxResults, id, expand, queryString, orderBy),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_screen_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/screenscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("queryString") {
        query_pairs.push(("queryString", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/screenscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create screen scheme
///
/// Method: POST /rest/api/3/screenscheme
#[actor(
    JiraCreateScreenSchemeActor,
    inports::<100>(screens, description, name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/screenscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("screens") {
        body.insert("screens".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/screenscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete screen scheme
///
/// Method: DELETE /rest/api/3/screenscheme/{screenSchemeId}
#[actor(
    JiraDeleteScreenSchemeActor,
    inports::<100>(screenSchemeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screenscheme/{screenSchemeId}".to_string();
    if let Some(val) = inputs.get("screenSchemeId") {
        endpoint = endpoint.replace("{{screenSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/screenscheme/{{screenSchemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update screen scheme
///
/// Method: PUT /rest/api/3/screenscheme/{screenSchemeId}
#[actor(
    JiraUpdateScreenSchemeActor,
    inports::<100>(screenSchemeId, name, description, screens),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_screen_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/screenscheme/{screenSchemeId}".to_string();
    if let Some(val) = inputs.get("screenSchemeId") {
        endpoint = endpoint.replace("{{screenSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("screens") {
        body.insert("screens".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/screenscheme/{{screenSchemeId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Search for issues using JQL enhanced search (POST)
///
/// Method: POST /rest/api/3/search/jql
#[actor(
    JiraSearchAndReconsileIssuesUsingJqlPostActor,
    inports::<100>(fieldsByKeys, maxResults, expand, properties, fields, reconcileIssues, jql, nextPageToken),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_and_reconsile_issues_using_jql_post(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/search/jql".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("fieldsByKeys") {
        body.insert("fieldsByKeys".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("maxResults") {
        body.insert("maxResults".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("expand") {
        body.insert("expand".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("properties") {
        body.insert("properties".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("fields") {
        body.insert("fields".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("reconcileIssues") {
        body.insert("reconcileIssues".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("jql") {
        body.insert("jql".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("nextPageToken") {
        body.insert("nextPageToken".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/search/jql failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Search for issues using JQL enhanced search (GET)
///
/// Method: GET /rest/api/3/search/jql
#[actor(
    JiraSearchAndReconsileIssuesUsingJqlActor,
    inports::<100>(jql, nextPageToken, maxResults, fields, expand, properties, fieldsByKeys, failFast, reconcileIssues),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_and_reconsile_issues_using_jql(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/search/jql".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("jql") {
        query_pairs.push(("jql", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("nextPageToken") {
        query_pairs.push(("nextPageToken", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fields") {
        query_pairs.push(("fields", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("properties") {
        query_pairs.push(("properties", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fieldsByKeys") {
        query_pairs.push(("fieldsByKeys", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("failFast") {
        query_pairs.push(("failFast", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("reconcileIssues") {
        query_pairs.push(("reconcileIssues", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/search/jql failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get issue security level
///
/// Method: GET /rest/api/3/securitylevel/{id}
#[actor(
    JiraReadIssueSecurityLevelActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_security_level(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/securitylevel/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/securitylevel/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get Jira instance info
///
/// Method: GET /rest/api/3/serverInfo
#[actor(
    JiraReadServerInfoActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_server_info(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/serverInfo".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/serverInfo failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Set issue navigator default columns
///
/// Method: PUT /rest/api/3/settings/columns
#[actor(
    JiraUpdateIssueNavigatorSettingsActor,
    inports::<100>(columns),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_issue_navigator_settings(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/settings/columns".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("columns") {
        body.insert("columns".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/settings/columns failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get issue navigator default columns
///
/// Method: GET /rest/api/3/settings/columns
#[actor(
    JiraReadIssueNavigatorDefaultColumnsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_issue_navigator_default_columns(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/settings/columns".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/settings/columns failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all statuses
///
/// Method: GET /rest/api/3/status
#[actor(
    JiraReadStatusesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_statuses(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/status".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/status failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get status
///
/// Method: GET /rest/api/3/status/{idOrName}
#[actor(
    JiraReadStatusActor,
    inports::<100>(idOrName),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_status(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/status/{idOrName}".to_string();
    if let Some(val) = inputs.get("idOrName") {
        endpoint = endpoint.replace("{{idOrName}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/status/{{idOrName}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all status categories
///
/// Method: GET /rest/api/3/statuscategory
#[actor(
    JiraReadStatusCategoriesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_status_categories(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/statuscategory".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/statuscategory failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get status category
///
/// Method: GET /rest/api/3/statuscategory/{idOrKey}
#[actor(
    JiraReadStatusCategoryActor,
    inports::<100>(idOrKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_status_category(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/statuscategory/{idOrKey}".to_string();
    if let Some(val) = inputs.get("idOrKey") {
        endpoint = endpoint.replace("{{idOrKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/statuscategory/{{idOrKey}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk delete Statuses
///
/// Method: DELETE /rest/api/3/statuses
#[actor(
    JiraDeleteStatusesByIdActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_statuses_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/statuses".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/statuses failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk get statuses
///
/// Method: GET /rest/api/3/statuses
#[actor(
    JiraReadStatusesByIdActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_statuses_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/statuses".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("id") {
        query_pairs.push(("id", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/statuses failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk create statuses
///
/// Method: POST /rest/api/3/statuses
#[actor(
    JiraCreateStatusesActor,
    inports::<100>(scope, statuses),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_statuses(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/statuses".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("scope") {
        body.insert("scope".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("statuses") {
        body.insert("statuses".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/statuses failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk update statuses
///
/// Method: PUT /rest/api/3/statuses
#[actor(
    JiraUpdateStatusesActor,
    inports::<100>(statuses),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_statuses(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/statuses".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("statuses") {
        body.insert("statuses".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/statuses failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk get statuses by name
///
/// Method: GET /rest/api/3/statuses/byNames
#[actor(
    JiraReadStatusesByNameActor,
    inports::<100>(name, projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_statuses_by_name(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/statuses/byNames".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("name") {
        query_pairs.push(("name", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/statuses/byNames failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Search statuses paginated
///
/// Method: GET /rest/api/3/statuses/search
#[actor(
    JiraSearchStatusActor,
    inports::<100>(projectId, startAt, maxResults, searchString, statusCategory),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_status(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/statuses/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("searchString") {
        query_pairs.push(("searchString", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("statusCategory") {
        query_pairs.push(("statusCategory", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/statuses/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get issue type usages by status and project
///
/// Method: GET /rest/api/3/statuses/{statusId}/project/{projectId}/issueTypeUsages
#[actor(
    JiraReadProjectIssueTypeUsagesForStatusActor,
    inports::<100>(statusId, projectId, nextPageToken, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_issue_type_usages_for_status(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/statuses/{statusId}/project/{projectId}/issueTypeUsages".to_string();
    if let Some(val) = inputs.get("statusId") {
        endpoint = endpoint.replace("{{statusId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("projectId") {
        endpoint = endpoint.replace("{{projectId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("nextPageToken") {
        query_pairs.push(("nextPageToken", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/statuses/{{statusId}}/project/{{projectId}}/issueTypeUsages failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get project usages by status
///
/// Method: GET /rest/api/3/statuses/{statusId}/projectUsages
#[actor(
    JiraReadProjectUsagesForStatusActor,
    inports::<100>(statusId, nextPageToken, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_usages_for_status(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/statuses/{statusId}/projectUsages".to_string();
    if let Some(val) = inputs.get("statusId") {
        endpoint = endpoint.replace("{{statusId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("nextPageToken") {
        query_pairs.push(("nextPageToken", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/statuses/{{statusId}}/projectUsages failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get workflow usages by status
///
/// Method: GET /rest/api/3/statuses/{statusId}/workflowUsages
#[actor(
    JiraReadWorkflowUsagesForStatusActor,
    inports::<100>(statusId, nextPageToken, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_usages_for_status(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/statuses/{statusId}/workflowUsages".to_string();
    if let Some(val) = inputs.get("statusId") {
        endpoint = endpoint.replace("{{statusId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("nextPageToken") {
        query_pairs.push(("nextPageToken", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/statuses/{{statusId}}/workflowUsages failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get task
///
/// Method: GET /rest/api/3/task/{taskId}
#[actor(
    JiraReadTaskActor,
    inports::<100>(taskId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_task(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/task/{taskId}".to_string();
    if let Some(val) = inputs.get("taskId") {
        endpoint = endpoint.replace("{{taskId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/task/{{taskId}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Cancel task
///
/// Method: POST /rest/api/3/task/{taskId}/cancel
#[actor(
    JiraCancelTaskActor,
    inports::<100>(taskId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_cancel_task(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/task/{taskId}/cancel".to_string();
    if let Some(val) = inputs.get("taskId") {
        endpoint = endpoint.replace("{{taskId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/task/{{taskId}}/cancel failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get UI modifications
///
/// Method: GET /rest/api/3/uiModifications
#[actor(
    JiraReadUiModificationsActor,
    inports::<100>(startAt, maxResults, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_ui_modifications(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/uiModifications".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/uiModifications failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create UI modification
///
/// Method: POST /rest/api/3/uiModifications
#[actor(
    JiraCreateUiModificationActor,
    inports::<100>(description, data, name, contexts),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_ui_modification(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/uiModifications".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("data") {
        body.insert("data".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("contexts") {
        body.insert("contexts".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/uiModifications failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete UI modification
///
/// Method: DELETE /rest/api/3/uiModifications/{uiModificationId}
#[actor(
    JiraDeleteUiModificationActor,
    inports::<100>(uiModificationId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_ui_modification(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/uiModifications/{uiModificationId}".to_string();
    if let Some(val) = inputs.get("uiModificationId") {
        endpoint = endpoint.replace("{{uiModificationId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/uiModifications/{{uiModificationId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update UI modification
///
/// Method: PUT /rest/api/3/uiModifications/{uiModificationId}
#[actor(
    JiraUpdateUiModificationActor,
    inports::<100>(uiModificationId, contexts, name, description, data),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_ui_modification(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/uiModifications/{uiModificationId}".to_string();
    if let Some(val) = inputs.get("uiModificationId") {
        endpoint = endpoint.replace("{{uiModificationId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("contexts") {
        body.insert("contexts".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("data") {
        body.insert("data".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/uiModifications/{{uiModificationId}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get avatars
///
/// Method: GET /rest/api/3/universal_avatar/type/{type}/owner/{entityId}
#[actor(
    JiraReadAvatarsActor,
    inports::<100>(type_, entityId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_avatars(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/universal_avatar/type/{type}/owner/{entityId}".to_string();
    if let Some(val) = inputs.get("type_") {
        endpoint = endpoint.replace("{{type}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("entityId") {
        endpoint = endpoint.replace("{{entityId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/universal_avatar/type/{{type}}/owner/{{entityId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Load avatar
///
/// Method: POST /rest/api/3/universal_avatar/type/{type}/owner/{entityId}
#[actor(
    JiraCreateAvatarsActor,
    inports::<100>(type_, entityId, x, y, size),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_avatars(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/universal_avatar/type/{type}/owner/{entityId}".to_string();
    if let Some(val) = inputs.get("type_") {
        endpoint = endpoint.replace("{{type}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("entityId") {
        endpoint = endpoint.replace("{{entityId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("x") {
        query_pairs.push(("x", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("y") {
        query_pairs.push(("y", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("size") {
        query_pairs.push(("size", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /rest/api/3/universal_avatar/type/{{type}}/owner/{{entityId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete avatar
///
/// Method: DELETE /rest/api/3/universal_avatar/type/{type}/owner/{owningObjectId}/avatar/{id}
#[actor(
    JiraDeleteAvatarActor,
    inports::<100>(type_, owningObjectId, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_avatar(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/universal_avatar/type/{type}/owner/{owningObjectId}/avatar/{id}".to_string();
    if let Some(val) = inputs.get("type_") {
        endpoint = endpoint.replace("{{type}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("owningObjectId") {
        endpoint = endpoint.replace("{{owningObjectId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/universal_avatar/type/{{type}}/owner/{{owningObjectId}}/avatar/{{id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get avatar image by type
///
/// Method: GET /rest/api/3/universal_avatar/view/type/{type}
#[actor(
    JiraReadAvatarImageByTypeActor,
    inports::<100>(type_, size, format),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_avatar_image_by_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/universal_avatar/view/type/{type}".to_string();
    if let Some(val) = inputs.get("type_") {
        endpoint = endpoint.replace("{{type}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("size") {
        query_pairs.push(("size", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("format") {
        query_pairs.push(("format", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/universal_avatar/view/type/{{type}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get avatar image by ID
///
/// Method: GET /rest/api/3/universal_avatar/view/type/{type}/avatar/{id}
#[actor(
    JiraReadAvatarImageByIdActor,
    inports::<100>(type_, id, size, format),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_avatar_image_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/universal_avatar/view/type/{type}/avatar/{id}".to_string();
    if let Some(val) = inputs.get("type_") {
        endpoint = endpoint.replace("{{type}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("size") {
        query_pairs.push(("size", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("format") {
        query_pairs.push(("format", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/universal_avatar/view/type/{{type}}/avatar/{{id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get avatar image by owner
///
/// Method: GET /rest/api/3/universal_avatar/view/type/{type}/owner/{entityId}
#[actor(
    JiraReadAvatarImageByOwnerActor,
    inports::<100>(type_, entityId, size, format),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_avatar_image_by_owner(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/universal_avatar/view/type/{type}/owner/{entityId}".to_string();
    if let Some(val) = inputs.get("type_") {
        endpoint = endpoint.replace("{{type}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("entityId") {
        endpoint = endpoint.replace("{{entityId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("size") {
        query_pairs.push(("size", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("format") {
        query_pairs.push(("format", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/universal_avatar/view/type/{{type}}/owner/{{entityId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create user
///
/// Method: POST /rest/api/3/user
#[actor(
    JiraCreateUserActor,
    inports::<100>(password, emailAddress, displayName, name, applicationKeys, products, key, self_),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("password") {
        body.insert("password".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("emailAddress") {
        body.insert("emailAddress".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("displayName") {
        body.insert("displayName".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("applicationKeys") {
        body.insert("applicationKeys".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("products") {
        body.insert("products".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("key") {
        body.insert("key".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/user failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete user
///
/// Method: DELETE /rest/api/3/user
#[actor(
    JiraDeleteUserActor,
    inports::<100>(accountId, username, key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/user failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get user
///
/// Method: GET /rest/api/3/user
#[actor(
    JiraReadUserActor,
    inports::<100>(accountId, username, key, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Find users assignable to projects
///
/// Method: GET /rest/api/3/user/assignable/multiProjectSearch
#[actor(
    JiraSearchBulkAssignableUsersActor,
    inports::<100>(query, username, accountId, projectKeys, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_bulk_assignable_users(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/assignable/multiProjectSearch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectKeys") {
        query_pairs.push(("projectKeys", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/user/assignable/multiProjectSearch failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Find users assignable to issues
///
/// Method: GET /rest/api/3/user/assignable/search
#[actor(
    JiraSearchAssignableUsersActor,
    inports::<100>(query, sessionId, username, accountId, project, issueKey, issueId, startAt, maxResults, actionDescriptorId, recommend, accountType, appType),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_assignable_users(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/assignable/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sessionId") {
        query_pairs.push(("sessionId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("project") {
        query_pairs.push(("project", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueKey") {
        query_pairs.push(("issueKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueId") {
        query_pairs.push(("issueId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("actionDescriptorId") {
        query_pairs.push(("actionDescriptorId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("recommend") {
        query_pairs.push(("recommend", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountType") {
        query_pairs.push(("accountType", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("appType") {
        query_pairs.push(("appType", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/user/assignable/search failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk get users
///
/// Method: GET /rest/api/3/user/bulk
#[actor(
    JiraListUsersActor,
    inports::<100>(startAt, maxResults, username, key, accountId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_list_users(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/bulk".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/bulk failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Set user default columns
///
/// Method: PUT /rest/api/3/user/columns
#[actor(
    JiraUpdateUsersActor,
    inports::<100>(accountId, columns),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_users(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/columns".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("columns") {
        body.insert("columns".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/user/columns failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get user default columns
///
/// Method: GET /rest/api/3/user/columns
#[actor(
    JiraReadUserDefaultColumnsActor,
    inports::<100>(accountId, username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_user_default_columns(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/columns".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/columns failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Reset user default columns
///
/// Method: DELETE /rest/api/3/user/columns
#[actor(
    JiraDeleteUsersActor,
    inports::<100>(accountId, username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_users(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/columns".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/user/columns failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get user email
///
/// Method: GET /rest/api/3/user/email
#[actor(
    JiraReadUserEmailActor,
    inports::<100>(accountId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_user_email(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/email".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/email failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get user email bulk
///
/// Method: GET /rest/api/3/user/email/bulk
#[actor(
    JiraReadUserEmailBulkActor,
    inports::<100>(accountId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_user_email_bulk(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/email/bulk".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/email/bulk failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get user groups
///
/// Method: GET /rest/api/3/user/groups
#[actor(
    JiraReadUserGroupsActor,
    inports::<100>(accountId, username, key),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_user_groups(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/groups".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("key") {
        query_pairs.push(("key", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/groups failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Find users with permissions
///
/// Method: GET /rest/api/3/user/permission/search
#[actor(
    JiraSearchUsersWithAllPermissionsActor,
    inports::<100>(query, username, accountId, permissions, issueKey, projectKey, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_users_with_all_permissions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/permission/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("permissions") {
        query_pairs.push(("permissions", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueKey") {
        query_pairs.push(("issueKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectKey") {
        query_pairs.push(("projectKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/user/permission/search failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Find users for picker
///
/// Method: GET /rest/api/3/user/picker
#[actor(
    JiraSearchUsersForPickerActor,
    inports::<100>(query, maxResults, showAvatar, exclude, excludeAccountIds, avatarSize, excludeConnectUsers),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_users_for_picker(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/picker".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("showAvatar") {
        query_pairs.push(("showAvatar", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("exclude") {
        query_pairs.push(("exclude", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("excludeAccountIds") {
        query_pairs.push(("excludeAccountIds", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("avatarSize") {
        query_pairs.push(("avatarSize", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("excludeConnectUsers") {
        query_pairs.push(("excludeConnectUsers", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/picker failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get user property keys
///
/// Method: GET /rest/api/3/user/properties
#[actor(
    JiraReadUserPropertyKeysActor,
    inports::<100>(accountId, userKey, username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_user_property_keys(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/properties".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("userKey") {
        query_pairs.push(("userKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/properties failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete user property
///
/// Method: DELETE /rest/api/3/user/properties/{propertyKey}
#[actor(
    JiraDeleteUserPropertyActor,
    inports::<100>(accountId, userKey, username, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_user_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/user/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("userKey") {
        query_pairs.push(("userKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/user/properties/{{propertyKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set user property
///
/// Method: PUT /rest/api/3/user/properties/{propertyKey}
#[actor(
    JiraUpdateUserPropertiesActor,
    inports::<100>(accountId, userKey, username, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_user_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/user/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("userKey") {
        query_pairs.push(("userKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/user/properties/{{propertyKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get user property
///
/// Method: GET /rest/api/3/user/properties/{propertyKey}
#[actor(
    JiraReadUserPropertyActor,
    inports::<100>(accountId, userKey, username, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_user_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/user/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("userKey") {
        query_pairs.push(("userKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/user/properties/{{propertyKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Find users
///
/// Method: GET /rest/api/3/user/search
#[actor(
    JiraSearchUsersActor,
    inports::<100>(query, username, accountId, startAt, maxResults, property),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_users(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("property") {
        query_pairs.push(("property", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Find users by query
///
/// Method: GET /rest/api/3/user/search/query
#[actor(
    JiraSearchUsersByQueryActor,
    inports::<100>(query, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_users_by_query(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/search/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/user/search/query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Find user keys by query
///
/// Method: GET /rest/api/3/user/search/query/key
#[actor(
    JiraSearchUserKeysByQueryActor,
    inports::<100>(query, startAt, maxResult),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_user_keys_by_query(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/search/query/key".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResult") {
        query_pairs.push(("maxResult", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/user/search/query/key failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Find users with browse permission
///
/// Method: GET /rest/api/3/user/viewissue/search
#[actor(
    JiraSearchUsersWithBrowsePermissionActor,
    inports::<100>(query, username, accountId, issueKey, projectKey, startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_users_with_browse_permission(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/user/viewissue/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("query") {
        query_pairs.push(("query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("username") {
        query_pairs.push(("username", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("accountId") {
        query_pairs.push(("accountId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueKey") {
        query_pairs.push(("issueKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectKey") {
        query_pairs.push(("projectKey", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/user/viewissue/search failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get all users default
///
/// Method: GET /rest/api/3/users
#[actor(
    JiraReadAllUsersDefaultActor,
    inports::<100>(startAt, maxResults, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_users_default(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/users".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/users failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all users
///
/// Method: GET /rest/api/3/users/search
#[actor(
    JiraReadAllUsersActor,
    inports::<100>(startAt, maxResults, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_users(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/users/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/users/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create version
///
/// Method: POST /rest/api/3/version
#[actor(
    JiraCreateVersionActor,
    inports::<100>(overdue, driver, archived, issuesStatusForFixVersion, description, releaseDate, project, userReleaseDate, operations, startDate, name, userStartDate, approvers, self_, expand, moveUnfixedIssuesTo, projectId, id, released),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_version(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/version".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("overdue") {
        body.insert("overdue".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("driver") {
        body.insert("driver".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("archived") {
        body.insert("archived".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issuesStatusForFixVersion") {
        body.insert("issuesStatusForFixVersion".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("releaseDate") {
        body.insert("releaseDate".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("project") {
        body.insert("project".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("userReleaseDate") {
        body.insert("userReleaseDate".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("operations") {
        body.insert("operations".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("startDate") {
        body.insert("startDate".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("userStartDate") {
        body.insert("userStartDate".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("approvers") {
        body.insert("approvers".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("expand") {
        body.insert("expand".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("moveUnfixedIssuesTo") {
        body.insert("moveUnfixedIssuesTo".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectId") {
        body.insert("projectId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("released") {
        body.insert("released".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/version failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get version
///
/// Method: GET /rest/api/3/version/{id}
#[actor(
    JiraReadVersionActor,
    inports::<100>(id, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_version(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/version/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Update version
///
/// Method: PUT /rest/api/3/version/{id}
#[actor(
    JiraUpdateVersionActor,
    inports::<100>(id, overdue, approvers, project, description, archived, self_, expand, issuesStatusForFixVersion, name, operations, released, startDate, userReleaseDate, driver, userStartDate, projectId, releaseDate, moveUnfixedIssuesTo),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_version(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("overdue") {
        body.insert("overdue".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("approvers") {
        body.insert("approvers".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("project") {
        body.insert("project".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("archived") {
        body.insert("archived".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("expand") {
        body.insert("expand".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issuesStatusForFixVersion") {
        body.insert("issuesStatusForFixVersion".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("operations") {
        body.insert("operations".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("released") {
        body.insert("released".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("startDate") {
        body.insert("startDate".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("userReleaseDate") {
        body.insert("userReleaseDate".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("driver") {
        body.insert("driver".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("userStartDate") {
        body.insert("userStartDate".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("projectId") {
        body.insert("projectId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("releaseDate") {
        body.insert("releaseDate".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("moveUnfixedIssuesTo") {
        body.insert("moveUnfixedIssuesTo".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/version/{{id}} failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Merge versions
///
/// Method: PUT /rest/api/3/version/{id}/mergeto/{moveIssuesTo}
#[actor(
    JiraUpdateProjectVersionsActor,
    inports::<100>(id, moveIssuesTo),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_project_versions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}/mergeto/{moveIssuesTo}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("moveIssuesTo") {
        endpoint = endpoint.replace("{{moveIssuesTo}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/version/{{id}}/mergeto/{{moveIssuesTo}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Move version
///
/// Method: POST /rest/api/3/version/{id}/move
#[actor(
    JiraMoveProjectVersionsActor,
    inports::<100>(id, after, position),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_move_project_versions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}/move".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("after") {
        body.insert("after".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("position") {
        body.insert("position".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/version/{{id}}/move failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get version's related issues count
///
/// Method: GET /rest/api/3/version/{id}/relatedIssueCounts
#[actor(
    JiraReadVersionRelatedIssuesActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_version_related_issues(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}/relatedIssueCounts".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/version/{{id}}/relatedIssueCounts failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get related work
///
/// Method: GET /rest/api/3/version/{id}/relatedwork
#[actor(
    JiraReadRelatedWorkActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_related_work(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}/relatedwork".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/version/{{id}}/relatedwork failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create related work
///
/// Method: POST /rest/api/3/version/{id}/relatedwork
#[actor(
    JiraCreateRelatedWorkActor,
    inports::<100>(id, url, issueId, category, title, relatedWorkId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_related_work(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}/relatedwork".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("url") {
        body.insert("url".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueId") {
        body.insert("issueId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("category") {
        body.insert("category".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("title") {
        body.insert("title".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("relatedWorkId") {
        body.insert("relatedWorkId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/version/{{id}}/relatedwork failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update related work
///
/// Method: PUT /rest/api/3/version/{id}/relatedwork
#[actor(
    JiraUpdateRelatedWorkActor,
    inports::<100>(id, category, issueId, relatedWorkId, url, title),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_related_work(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}/relatedwork".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("category") {
        body.insert("category".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueId") {
        body.insert("issueId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("relatedWorkId") {
        body.insert("relatedWorkId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("url") {
        body.insert("url".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("title") {
        body.insert("title".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/version/{{id}}/relatedwork failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete and replace version
///
/// Method: POST /rest/api/3/version/{id}/removeAndSwap
#[actor(
    JiraDeleteAndReplaceVersionActor,
    inports::<100>(id, moveAffectedIssuesTo, customFieldReplacementList, moveFixIssuesTo),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_and_replace_version(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}/removeAndSwap".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("moveAffectedIssuesTo") {
        body.insert("moveAffectedIssuesTo".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("customFieldReplacementList") {
        body.insert("customFieldReplacementList".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("moveFixIssuesTo") {
        body.insert("moveFixIssuesTo".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/version/{{id}}/removeAndSwap failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get version's unresolved issues count
///
/// Method: GET /rest/api/3/version/{id}/unresolvedIssueCount
#[actor(
    JiraReadVersionUnresolvedIssuesActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_version_unresolved_issues(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{id}/unresolvedIssueCount".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/version/{{id}}/unresolvedIssueCount failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete related work
///
/// Method: DELETE /rest/api/3/version/{versionId}/relatedwork/{relatedWorkId}
#[actor(
    JiraDeleteRelatedWorkActor,
    inports::<100>(versionId, relatedWorkId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_related_work(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/version/{versionId}/relatedwork/{relatedWorkId}".to_string();
    if let Some(val) = inputs.get("versionId") {
        endpoint = endpoint.replace("{{versionId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("relatedWorkId") {
        endpoint = endpoint.replace("{{relatedWorkId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/version/{{versionId}}/relatedwork/{{relatedWorkId}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get dynamic webhooks for app
///
/// Method: GET /rest/api/3/webhook
#[actor(
    JiraReadDynamicWebhooksForAppActor,
    inports::<100>(startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_dynamic_webhooks_for_app(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/webhook".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/webhook failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Delete webhooks by ID
///
/// Method: DELETE /rest/api/3/webhook
#[actor(
    JiraDeleteWebhookByIdActor,
    inports::<100>(webhookIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_webhook_by_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/webhook".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("DELETE /rest/api/3/webhook failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Register dynamic webhooks
///
/// Method: POST /rest/api/3/webhook
#[actor(
    JiraCreateWebhooksActor,
    inports::<100>(url, webhooks),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_webhooks(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/webhook".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("url") {
        body.insert("url".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("webhooks") {
        body.insert("webhooks".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/webhook failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get failed webhooks
///
/// Method: GET /rest/api/3/webhook/failed
#[actor(
    JiraReadFailedWebhooksActor,
    inports::<100>(maxResults, after),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_failed_webhooks(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/webhook/failed".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("after") {
        query_pairs.push(("after", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/webhook/failed failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Extend webhook life
///
/// Method: PUT /rest/api/3/webhook/refresh
#[actor(
    JiraRefreshWebhooksActor,
    inports::<100>(webhookIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_refresh_webhooks(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/webhook/refresh".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("webhookIds") {
        body.insert("webhookIds".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("PUT /rest/api/3/webhook/refresh failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Read workflow version from history
///
/// Method: POST /rest/api/3/workflow/history
#[actor(
    JiraCreateWorkflowsActor,
    inports::<100>(version, workflowId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_workflows(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflow/history".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("version") {
        body.insert("version".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflowId") {
        body.insert("workflowId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/workflow/history failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// List workflow history entries
///
/// Method: POST /rest/api/3/workflow/history/list
#[actor(
    JiraListWorkflowHistoryActor,
    inports::<100>(expand, workflowId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_list_workflow_history(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflow/history/list".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("workflowId") {
        body.insert("workflowId".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/workflow/history/list failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get workflow transition rule configurations
///
/// Method: GET /rest/api/3/workflow/rule/config
#[actor(
    JiraReadWorkflowTransitionRuleConfigurationsActor,
    inports::<100>(startAt, maxResults, types, keys, workflowNames, withTags, draft, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_transition_rule_configurations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflow/rule/config".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("types") {
        query_pairs.push(("types", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("keys") {
        query_pairs.push(("keys", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("workflowNames") {
        query_pairs.push(("workflowNames", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("withTags") {
        query_pairs.push(("withTags", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("draft") {
        query_pairs.push(("draft", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/workflow/rule/config failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update workflow transition rule configurations
///
/// Method: PUT /rest/api/3/workflow/rule/config
#[actor(
    JiraUpdateWorkflowTransitionRuleConfigurationsActor,
    inports::<100>(workflows),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_workflow_transition_rule_configurations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflow/rule/config".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("workflows") {
        body.insert("workflows".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/workflow/rule/config failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete workflow transition rule configurations
///
/// Method: PUT /rest/api/3/workflow/rule/config/delete
#[actor(
    JiraDeleteWorkflowTransitionRuleConfigurationsActor,
    inports::<100>(workflows),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_workflow_transition_rule_configurations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflow/rule/config/delete".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("workflows") {
        body.insert("workflows".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/workflow/rule/config/delete failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete inactive workflow
///
/// Method: DELETE /rest/api/3/workflow/{entityId}
#[actor(
    JiraDeleteInactiveWorkflowActor,
    inports::<100>(entityId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_inactive_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflow/{entityId}".to_string();
    if let Some(val) = inputs.get("entityId") {
        endpoint = endpoint.replace("{{entityId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/workflow/{{entityId}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue types in a project that are using a given workflow
///
/// Method: GET /rest/api/3/workflow/{workflowId}/project/{projectId}/issueTypeUsages
#[actor(
    JiraReadWorkflowProjectIssueTypeUsagesActor,
    inports::<100>(workflowId, projectId, nextPageToken, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_project_issue_type_usages(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/api/3/workflow/{workflowId}/project/{projectId}/issueTypeUsages".to_string();
    if let Some(val) = inputs.get("workflowId") {
        endpoint = endpoint.replace("{{workflowId}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("projectId") {
        endpoint = endpoint.replace("{{projectId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("nextPageToken") {
        query_pairs.push(("nextPageToken", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/workflow/{{workflowId}}/project/{{projectId}}/issueTypeUsages failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get projects using a given workflow
///
/// Method: GET /rest/api/3/workflow/{workflowId}/projectUsages
#[actor(
    JiraReadProjectUsagesForWorkflowActor,
    inports::<100>(workflowId, nextPageToken, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_usages_for_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflow/{workflowId}/projectUsages".to_string();
    if let Some(val) = inputs.get("workflowId") {
        endpoint = endpoint.replace("{{workflowId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("nextPageToken") {
        query_pairs.push(("nextPageToken", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/workflow/{{workflowId}}/projectUsages failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get workflow schemes which are using a given workflow
///
/// Method: GET /rest/api/3/workflow/{workflowId}/workflowSchemes
#[actor(
    JiraReadWorkflowSchemeUsagesForWorkflowActor,
    inports::<100>(workflowId, nextPageToken, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_scheme_usages_for_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflow/{workflowId}/workflowSchemes".to_string();
    if let Some(val) = inputs.get("workflowId") {
        endpoint = endpoint.replace("{{workflowId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("nextPageToken") {
        query_pairs.push(("nextPageToken", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/workflow/{{workflowId}}/workflowSchemes failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get available workflow capabilities
///
/// Method: GET /rest/api/3/workflows/capabilities
#[actor(
    JiraListWorkflowsActor,
    inports::<100>(workflowId, projectId, issueTypeId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_list_workflows(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflows/capabilities".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("workflowId") {
        query_pairs.push(("workflowId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("issueTypeId") {
        query_pairs.push(("issueTypeId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/workflows/capabilities failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get the user's default workflow editor
///
/// Method: GET /rest/api/3/workflows/defaultEditor
#[actor(
    JiraReadDefaultEditorActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_default_editor(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflows/defaultEditor".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/workflows/defaultEditor failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Search workflows
///
/// Method: GET /rest/api/3/workflows/search
#[actor(
    JiraSearchWorkflowsActor,
    inports::<100>(startAt, maxResults, expand, queryString, orderBy, scope, isActive),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_workflows(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflows/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("queryString") {
        query_pairs.push(("queryString", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("orderBy") {
        query_pairs.push(("orderBy", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("scope") {
        query_pairs.push(("scope", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("isActive") {
        query_pairs.push(("isActive", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/workflows/search failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Bulk update workflows
///
/// Method: POST /rest/api/3/workflows/update
#[actor(
    JiraUpdateWorkflowsActor,
    inports::<100>(statuses, workflows),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_workflows(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflows/update".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("statuses") {
        body.insert("statuses".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflows") {
        body.insert("workflows".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/workflows/update failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Create workflow scheme
///
/// Method: POST /rest/api/3/workflowscheme
#[actor(
    JiraCreateWorkflowSchemeActor,
    inports::<100>(issueTypeMappings, defaultWorkflow, id, updateDraftIfNeeded, lastModified, originalDefaultWorkflow, issueTypes, lastModifiedUser, draft, self_, name, description, originalIssueTypeMappings),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_workflow_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflowscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypeMappings") {
        body.insert("issueTypeMappings".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("defaultWorkflow") {
        body.insert("defaultWorkflow".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lastModified") {
        body.insert("lastModified".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("originalDefaultWorkflow") {
        body.insert("originalDefaultWorkflow".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypes") {
        body.insert("issueTypes".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lastModifiedUser") {
        body.insert("lastModifiedUser".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("draft") {
        body.insert("draft".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("originalIssueTypeMappings") {
        body.insert("originalIssueTypeMappings".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/workflowscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get all workflow schemes
///
/// Method: GET /rest/api/3/workflowscheme
#[actor(
    JiraReadAllWorkflowSchemesActor,
    inports::<100>(startAt, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_all_workflow_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflowscheme".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("startAt") {
        query_pairs.push(("startAt", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/workflowscheme failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get workflow scheme project associations
///
/// Method: GET /rest/api/3/workflowscheme/project
#[actor(
    JiraReadWorkflowSchemeProjectAssociationsActor,
    inports::<100>(projectId),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_scheme_project_associations(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflowscheme/project".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("projectId") {
        query_pairs.push(("projectId", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/workflowscheme/project failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Switch workflow scheme for project
///
/// Method: POST /rest/api/3/workflowscheme/project/switch
#[actor(
    JiraCreateWorkflowSchemesActor,
    inports::<100>(projectId, targetSchemeId, mappingsByIssueTypeOverride),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_workflow_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflowscheme/project/switch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("projectId") {
        body.insert("projectId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("targetSchemeId") {
        body.insert("targetSchemeId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("mappingsByIssueTypeOverride") {
        body.insert(
            "mappingsByIssueTypeOverride".to_string(),
            val.clone().into(),
        );
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/workflowscheme/project/switch failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update workflow scheme
///
/// Method: POST /rest/api/3/workflowscheme/update
#[actor(
    JiraUpdateSchemesActor,
    inports::<100>(name, statusMappingsByIssueTypeOverride, defaultWorkflowId, description, version, id, statusMappingsByWorkflows, workflowsForIssueTypes),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_schemes(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflowscheme/update".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("statusMappingsByIssueTypeOverride") {
        body.insert(
            "statusMappingsByIssueTypeOverride".to_string(),
            val.clone().into(),
        );
    }
    if let Some(val) = inputs.get("defaultWorkflowId") {
        body.insert("defaultWorkflowId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("version") {
        body.insert("version".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("statusMappingsByWorkflows") {
        body.insert("statusMappingsByWorkflows".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflowsForIssueTypes") {
        body.insert("workflowsForIssueTypes".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/api/3/workflowscheme/update failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get required status mappings for workflow scheme update
///
/// Method: POST /rest/api/3/workflowscheme/update/mappings
#[actor(
    JiraReadRequiredWorkflowSchemeMappingsActor,
    inports::<100>(defaultWorkflowId, workflowsForIssueTypes, id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_required_workflow_scheme_mappings(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/workflowscheme/update/mappings".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("defaultWorkflowId") {
        body.insert("defaultWorkflowId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflowsForIssueTypes") {
        body.insert("workflowsForIssueTypes".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/workflowscheme/update/mappings failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete workflow scheme
///
/// Method: DELETE /rest/api/3/workflowscheme/{id}
#[actor(
    JiraDeleteWorkflowSchemeActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_workflow_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("DELETE /rest/api/3/workflowscheme/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get workflow scheme
///
/// Method: GET /rest/api/3/workflowscheme/{id}
#[actor(
    JiraReadWorkflowSchemeActor,
    inports::<100>(id, returnDraftIfExists),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("returnDraftIfExists") {
        query_pairs.push(("returnDraftIfExists", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/workflowscheme/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Classic update workflow scheme
///
/// Method: PUT /rest/api/3/workflowscheme/{id}
#[actor(
    JiraUpdateWorkflowSchemeActor,
    inports::<100>(id, self_, updateDraftIfNeeded, lastModified, originalIssueTypeMappings, issueTypes, draft, description, issueTypeMappings, lastModifiedUser, name, defaultWorkflow, originalDefaultWorkflow),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_workflow_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lastModified") {
        body.insert("lastModified".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("originalIssueTypeMappings") {
        body.insert("originalIssueTypeMappings".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypes") {
        body.insert("issueTypes".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("draft") {
        body.insert("draft".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypeMappings") {
        body.insert("issueTypeMappings".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lastModifiedUser") {
        body.insert("lastModifiedUser".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("defaultWorkflow") {
        body.insert("defaultWorkflow".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("originalDefaultWorkflow") {
        body.insert("originalDefaultWorkflow".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/workflowscheme/{{id}} failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Create draft workflow scheme
///
/// Method: POST /rest/api/3/workflowscheme/{id}/createdraft
#[actor(
    JiraCreateWorkflowSchemeDraftFromParentActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_workflow_scheme_draft_from_parent(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/createdraft".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/workflowscheme/{{id}}/createdraft failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update default workflow
///
/// Method: PUT /rest/api/3/workflowscheme/{id}/default
#[actor(
    JiraUpdateDefaultWorkflowActor,
    inports::<100>(id, updateDraftIfNeeded, workflow),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_default_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/default".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflow") {
        body.insert("workflow".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/workflowscheme/{{id}}/default failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete default workflow
///
/// Method: DELETE /rest/api/3/workflowscheme/{id}/default
#[actor(
    JiraDeleteDefaultWorkflowActor,
    inports::<100>(id, updateDraftIfNeeded),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_default_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/default".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        query_pairs.push(("updateDraftIfNeeded", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/workflowscheme/{{id}}/default failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get default workflow
///
/// Method: GET /rest/api/3/workflowscheme/{id}/default
#[actor(
    JiraReadDefaultWorkflowActor,
    inports::<100>(id, returnDraftIfExists),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_default_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/default".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("returnDraftIfExists") {
        query_pairs.push(("returnDraftIfExists", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/workflowscheme/{{id}}/default failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete draft workflow scheme
///
/// Method: DELETE /rest/api/3/workflowscheme/{id}/draft
#[actor(
    JiraDeleteWorkflowSchemeDraftActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_workflow_scheme_draft(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/workflowscheme/{{id}}/draft failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get draft workflow scheme
///
/// Method: GET /rest/api/3/workflowscheme/{id}/draft
#[actor(
    JiraReadWorkflowSchemeDraftActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_scheme_draft(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("GET /rest/api/3/workflowscheme/{{id}}/draft failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update draft workflow scheme
///
/// Method: PUT /rest/api/3/workflowscheme/{id}/draft
#[actor(
    JiraUpdateWorkflowSchemeDraftActor,
    inports::<100>(id, issueTypes, description, originalIssueTypeMappings, self_, defaultWorkflow, lastModified, lastModifiedUser, draft, issueTypeMappings, name, originalDefaultWorkflow, updateDraftIfNeeded),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_workflow_scheme_draft(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("issueTypes") {
        body.insert("issueTypes".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("id") {
        body.insert("id".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("description") {
        body.insert("description".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("originalIssueTypeMappings") {
        body.insert("originalIssueTypeMappings".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("self_") {
        body.insert("self".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("defaultWorkflow") {
        body.insert("defaultWorkflow".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lastModified") {
        body.insert("lastModified".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("lastModifiedUser") {
        body.insert("lastModifiedUser".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("draft") {
        body.insert("draft".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypeMappings") {
        body.insert("issueTypeMappings".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("originalDefaultWorkflow") {
        body.insert("originalDefaultWorkflow".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("PUT /rest/api/3/workflowscheme/{{id}}/draft failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get draft default workflow
///
/// Method: GET /rest/api/3/workflowscheme/{id}/draft/default
#[actor(
    JiraReadDraftDefaultWorkflowActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_draft_default_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/default".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/workflowscheme/{{id}}/draft/default failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Update draft default workflow
///
/// Method: PUT /rest/api/3/workflowscheme/{id}/draft/default
#[actor(
    JiraUpdateDraftDefaultWorkflowActor,
    inports::<100>(id, updateDraftIfNeeded, workflow),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_draft_default_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/default".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflow") {
        body.insert("workflow".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/workflowscheme/{{id}}/draft/default failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete draft default workflow
///
/// Method: DELETE /rest/api/3/workflowscheme/{id}/draft/default
#[actor(
    JiraDeleteDraftDefaultWorkflowActor,
    inports::<100>(id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_draft_default_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/default".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/workflowscheme/{{id}}/draft/default failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete workflow for issue type in draft workflow scheme
///
/// Method: DELETE /rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}
#[actor(
    JiraDeleteWorkflowSchemeDraftIssueTypeActor,
    inports::<100>(id, issueType),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_workflow_scheme_draft_issue_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issueType") {
        endpoint = endpoint.replace("{{issueType}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/workflowscheme/{{id}}/draft/issuetype/{{issueType}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Set workflow for issue type in draft workflow scheme
///
/// Method: PUT /rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}
#[actor(
    JiraUpdateWorkflowSchemeDraftsActor,
    inports::<100>(id, issueType, updateDraftIfNeeded, workflow),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_workflow_scheme_drafts(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issueType") {
        endpoint = endpoint.replace("{{issueType}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflow") {
        body.insert("workflow".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueType") {
        body.insert("issueType".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/api/3/workflowscheme/{{id}}/draft/issuetype/{{issueType}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get workflow for issue type in draft workflow scheme
///
/// Method: GET /rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}
#[actor(
    JiraReadWorkflowSchemeDraftIssueTypeActor,
    inports::<100>(id, issueType),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_scheme_draft_issue_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issueType") {
        endpoint = endpoint.replace("{{issueType}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/workflowscheme/{{id}}/draft/issuetype/{{issueType}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Publish draft workflow scheme
///
/// Method: POST /rest/api/3/workflowscheme/{id}/draft/publish
#[actor(
    JiraPublishWorkflowSchemeDraftsActor,
    inports::<100>(id, validateOnly, statusMappings),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_publish_workflow_scheme_drafts(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/publish".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("validateOnly") {
        query_pairs.push(("validateOnly", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("statusMappings") {
        body.insert("statusMappings".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/api/3/workflowscheme/{{id}}/draft/publish failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set issue types for workflow in workflow scheme
///
/// Method: PUT /rest/api/3/workflowscheme/{id}/draft/workflow
#[actor(
    JiraUpdateDraftWorkflowMappingActor,
    inports::<100>(id, workflowName, workflow, issueTypes, updateDraftIfNeeded, defaultMapping),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_draft_workflow_mapping(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/workflow".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("workflowName") {
        query_pairs.push(("workflowName", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("workflow") {
        body.insert("workflow".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypes") {
        body.insert("issueTypes".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("defaultMapping") {
        body.insert("defaultMapping".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/workflowscheme/{{id}}/draft/workflow failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete issue types for workflow in draft workflow scheme
///
/// Method: DELETE /rest/api/3/workflowscheme/{id}/draft/workflow
#[actor(
    JiraDeleteDraftWorkflowMappingActor,
    inports::<100>(id, workflowName),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_draft_workflow_mapping(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/workflow".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("workflowName") {
        query_pairs.push(("workflowName", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/workflowscheme/{{id}}/draft/workflow failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue types for workflows in draft workflow scheme
///
/// Method: GET /rest/api/3/workflowscheme/{id}/draft/workflow
#[actor(
    JiraReadDraftWorkflowActor,
    inports::<100>(id, workflowName),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_draft_workflow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/draft/workflow".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("workflowName") {
        query_pairs.push(("workflowName", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/workflowscheme/{{id}}/draft/workflow failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete workflow for issue type in workflow scheme
///
/// Method: DELETE /rest/api/3/workflowscheme/{id}/issuetype/{issueType}
#[actor(
    JiraDeleteWorkflowSchemeIssueTypeActor,
    inports::<100>(id, issueType, updateDraftIfNeeded),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_workflow_scheme_issue_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/issuetype/{issueType}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issueType") {
        endpoint = endpoint.replace("{{issueType}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        query_pairs.push(("updateDraftIfNeeded", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/api/3/workflowscheme/{{id}}/issuetype/{{issueType}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get workflow for issue type in workflow scheme
///
/// Method: GET /rest/api/3/workflowscheme/{id}/issuetype/{issueType}
#[actor(
    JiraReadWorkflowSchemeIssueTypeActor,
    inports::<100>(id, issueType, returnDraftIfExists),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow_scheme_issue_type(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/issuetype/{issueType}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issueType") {
        endpoint = endpoint.replace("{{issueType}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("returnDraftIfExists") {
        query_pairs.push(("returnDraftIfExists", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/workflowscheme/{{id}}/issuetype/{{issueType}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set workflow for issue type in workflow scheme
///
/// Method: PUT /rest/api/3/workflowscheme/{id}/issuetype/{issueType}
#[actor(
    JiraUpdateWorkflowSchemesActor,
    inports::<100>(id, issueType, workflow, updateDraftIfNeeded),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_workflow_schemes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/issuetype/{issueType}".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issueType") {
        endpoint = endpoint.replace("{{issueType}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("workflow") {
        body.insert("workflow".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueType") {
        body.insert("issueType".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/workflowscheme/{{id}}/issuetype/{{issueType}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get issue types for workflows in workflow scheme
///
/// Method: GET /rest/api/3/workflowscheme/{id}/workflow
#[actor(
    JiraReadWorkflowActor,
    inports::<100>(id, workflowName, returnDraftIfExists),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_workflow(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/workflow".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("workflowName") {
        query_pairs.push(("workflowName", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("returnDraftIfExists") {
        query_pairs.push(("returnDraftIfExists", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/api/3/workflowscheme/{{id}}/workflow failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Set issue types for workflow in workflow scheme
///
/// Method: PUT /rest/api/3/workflowscheme/{id}/workflow
#[actor(
    JiraUpdateWorkflowMappingActor,
    inports::<100>(id, workflowName, defaultMapping, issueTypes, updateDraftIfNeeded, workflow),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_workflow_mapping(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/workflow".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("workflowName") {
        query_pairs.push(("workflowName", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("defaultMapping") {
        body.insert("defaultMapping".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issueTypes") {
        body.insert("issueTypes".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        body.insert("updateDraftIfNeeded".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflow") {
        body.insert("workflow".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/api/3/workflowscheme/{{id}}/workflow failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete issue types for workflow in workflow scheme
///
/// Method: DELETE /rest/api/3/workflowscheme/{id}/workflow
#[actor(
    JiraDeleteWorkflowMappingActor,
    inports::<100>(id, workflowName, updateDraftIfNeeded),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_workflow_mapping(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{id}/workflow".to_string();
    if let Some(val) = inputs.get("id") {
        endpoint = endpoint.replace("{{id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("workflowName") {
        query_pairs.push(("workflowName", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("updateDraftIfNeeded") {
        query_pairs.push(("updateDraftIfNeeded", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/api/3/workflowscheme/{{id}}/workflow failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get projects which are using a given workflow scheme
///
/// Method: GET /rest/api/3/workflowscheme/{workflowSchemeId}/projectUsages
#[actor(
    JiraReadProjectUsagesForWorkflowSchemeActor,
    inports::<100>(workflowSchemeId, nextPageToken, maxResults),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_project_usages_for_workflow_scheme(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/api/3/workflowscheme/{workflowSchemeId}/projectUsages".to_string();
    if let Some(val) = inputs.get("workflowSchemeId") {
        endpoint = endpoint.replace("{{workflowSchemeId}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("nextPageToken") {
        query_pairs.push(("nextPageToken", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maxResults") {
        query_pairs.push(("maxResults", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /rest/api/3/workflowscheme/{{workflowSchemeId}}/projectUsages failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Get IDs of deleted worklogs
///
/// Method: GET /rest/api/3/worklog/deleted
#[actor(
    JiraReadIdsOfWorklogsDeletedSinceActor,
    inports::<100>(since),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_ids_of_worklogs_deleted_since(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/worklog/deleted".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("since") {
        query_pairs.push(("since", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/worklog/deleted failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get worklogs
///
/// Method: POST /rest/api/3/worklog/list
#[actor(
    JiraReadWorklogsForIdsActor,
    inports::<100>(expand, ids),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_worklogs_for_ids(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/worklog/list".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("ids") {
        body.insert("ids".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("POST /rest/api/3/worklog/list failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get IDs of updated worklogs
///
/// Method: GET /rest/api/3/worklog/updated
#[actor(
    JiraReadIdsOfWorklogsModifiedSinceActor,
    inports::<100>(since, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_ids_of_worklogs_modified_since(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/api/3/worklog/updated".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("since") {
        query_pairs.push(("since", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("expand") {
        query_pairs.push(("expand", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/api/3/worklog/updated failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get app properties
///
/// Method: GET /rest/atlassian-connect/1/addons/{addonKey}/properties
#[actor(
    JiraReadAppPropertiesActor,
    inports::<100>(addonKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_app_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/atlassian-connect/1/addons/{addonKey}/properties".to_string();
    if let Some(val) = inputs.get("addonKey") {
        endpoint = endpoint.replace("{{addonKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/atlassian-connect/1/addons/{{addonKey}}/properties failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete app property
///
/// Method: DELETE /rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}
#[actor(
    JiraDeleteAppPropertiesActor,
    inports::<100>(addonKey, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_app_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("addonKey") {
        endpoint = endpoint.replace("{{addonKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /rest/atlassian-connect/1/addons/{{addonKey}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Set app property
///
/// Method: PUT /rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}
#[actor(
    JiraUpdateAppPropertiesActor,
    inports::<100>(addonKey, propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_app_properties(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint =
        "/rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("addonKey") {
        endpoint = endpoint.replace("{{addonKey}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /rest/atlassian-connect/1/addons/{{addonKey}}/properties/{{propertyKey}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Register modules
///
/// Method: POST /rest/atlassian-connect/1/app/module/dynamic
#[actor(
    JiraCreateDynamicModulesActor,
    inports::<100>(modules),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_create_dynamic_modules(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/atlassian-connect/1/app/module/dynamic".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("modules") {
        body.insert("modules".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/atlassian-connect/1/app/module/dynamic failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Remove modules
///
/// Method: DELETE /rest/atlassian-connect/1/app/module/dynamic
#[actor(
    JiraDeleteDynamicModulesActor,
    inports::<100>(moduleKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_dynamic_modules(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/atlassian-connect/1/app/module/dynamic".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("moduleKey") {
        query_pairs.push(("moduleKey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/atlassian-connect/1/app/module/dynamic failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get modules
///
/// Method: GET /rest/atlassian-connect/1/app/module/dynamic
#[actor(
    JiraListDynamicModulesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_list_dynamic_modules(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/atlassian-connect/1/app/module/dynamic".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/atlassian-connect/1/app/module/dynamic failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Bulk update custom field value
///
/// Method: PUT /rest/atlassian-connect/1/migration/field
#[actor(
    JiraUpdateAppMigrationActor,
    inports::<100>(Atlassian_Transfer_Id, updateValueList),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_app_migration(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/atlassian-connect/1/migration/field".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    if let Some(val) = inputs.get("Atlassian_Transfer_Id") {
        builder = builder.header("Atlassian-Transfer-Id", super::message_to_str(val));
    }
    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("updateValueList") {
        body.insert("updateValueList".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/atlassian-connect/1/migration/field failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get workflow transition rule configurations
///
/// Method: POST /rest/atlassian-connect/1/migration/workflow/rule/search
#[actor(
    JiraSearchAppMigrationActor,
    inports::<100>(Atlassian_Transfer_Id, ruleIds, workflowEntityId, expand),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_search_app_migration(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/atlassian-connect/1/migration/workflow/rule/search".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    if let Some(val) = inputs.get("Atlassian_Transfer_Id") {
        builder = builder.header("Atlassian-Transfer-Id", super::message_to_str(val));
    }
    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("ruleIds") {
        body.insert("ruleIds".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("workflowEntityId") {
        body.insert("workflowEntityId".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("expand") {
        body.insert("expand".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "POST /rest/atlassian-connect/1/migration/workflow/rule/search failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Retrieve the attributes of service registries
///
/// Method: GET /rest/atlassian-connect/1/service-registry
#[actor(
    JiraListServiceRegistryActor,
    inports::<100>(serviceIds),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_list_service_registry(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/atlassian-connect/1/service-registry".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("serviceIds") {
        query_pairs.push(("serviceIds", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/atlassian-connect/1/service-registry failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get app property keys (Forge)
///
/// Method: GET /rest/forge/1/app/properties
#[actor(
    JiraReadForgeAppPropertyKeysActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_forge_app_property_keys(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/forge/1/app/properties".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /rest/forge/1/app/properties failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Set app property (Forge)
///
/// Method: PUT /rest/forge/1/app/properties/{propertyKey}
#[actor(
    JiraUpdateForgeAppPropertyActor,
    inports::<100>(propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_update_forge_app_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/forge/1/app/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "PUT /rest/forge/1/app/properties/{{propertyKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Delete app property (Forge)
///
/// Method: DELETE /rest/forge/1/app/properties/{propertyKey}
#[actor(
    JiraDeleteForgeAppPropertyActor,
    inports::<100>(propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_delete_forge_app_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/forge/1/app/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "DELETE /rest/forge/1/app/properties/{{propertyKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get app property (Forge)
///
/// Method: GET /rest/forge/1/app/properties/{propertyKey}
#[actor(
    JiraReadForgeAppPropertyActor,
    inports::<100>(propertyKey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_forge_app_property(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/rest/forge/1/app/properties/{propertyKey}".to_string();
    if let Some(val) = inputs.get("propertyKey") {
        endpoint = endpoint.replace("{{propertyKey}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!(
                        "GET /rest/forge/1/app/properties/{{propertyKey}} failed: {}",
                        e
                    )
                    .into(),
                ),
            );
        }
    }

    Ok(output)
}

/// Get worklogs by issue id and worklog id
///
/// Method: POST /rest/internal/api/latest/worklog/bulk
#[actor(
    JiraReadWorklogsByIssueIdAndWorklogIdActor,
    inports::<100>(requests),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn jira_read_worklogs_by_issue_id_and_worklog_id(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/rest/internal/api/latest/worklog/bulk".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("requests") {
        body.insert("requests".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(
                    format!("POST /rest/internal/api/latest/worklog/bulk failed: {}", e).into(),
                ),
            );
        }
    }

    Ok(output)
}
