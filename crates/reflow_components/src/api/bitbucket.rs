#![allow(clippy::all, unused_imports, dead_code)]

//! Auto-generated API actors for Bitbucket
//!
//! Service: Bitbucket (bitbucket)
//! Git repository hosting and CI/CD platform
//!
//! Required env var: BITBUCKET_API_KEY (OAuth2 access token)
//!
//! Generated by api-schema-gen codegen — do not edit manually.

use crate::{Actor, ActorBehavior, Message, Port};
use actor_macro::actor;
use anyhow::{Error, Result};
use reflow_actor::{message::EncodableValue, ActorContext};
use serde_json::{json, Value};
use std::collections::HashMap;
use std::time::Duration;

const BASE_URL: &str = "https://api.bitbucket.org/2.0";
const ENV_KEY: &str = "BITBUCKET_API_KEY";

/// Apply authentication to the request builder.
fn apply_auth(config: &reflow_actor::ActorConfig, mut builder: reqwest::RequestBuilder) -> Result<reqwest::RequestBuilder> {
    let credential = config.get_config_or_env(ENV_KEY)
        .ok_or_else(|| anyhow::anyhow!("Missing env var: {}", ENV_KEY))?;
    builder = builder.header("Authorization", format!("Bearer {}", credential));
    Ok(builder)
}

/// list repositories via Bitbucket API
///
/// Method: GET /repositories/{workspace}
#[actor(
    BitbucketListRepositoriesActor,
    inports::<100>(workspace),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_list_repositories(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}".to_string();
    if let Some(val) = inputs.get("workspace") {
        endpoint = endpoint.replace("{{workspace}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// create issue via Bitbucket API
///
/// Method: POST /repositories/{workspace}/{repo_slug}/issues
#[actor(
    BitbucketCreateIssueActor,
    inports::<100>(workspace, repo_slug, title),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_issue(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/issues".to_string();
    if let Some(val) = inputs.get("workspace") {
        endpoint = endpoint.replace("{{workspace}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("title") {
        body.insert("title".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{workspace}}/{{repo_slug}}/issues failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: PUT /addon
#[actor(
    BitbucketUpdateAddonActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_addon(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /addon failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: DELETE /addon
#[actor(
    BitbucketDeleteAddonActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_addon(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /addon failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: GET /addon/linkers
#[actor(
    BitbucketReadAddonLinkersActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_addon_linkers(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon/linkers".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /addon/linkers failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: GET /addon/linkers/{linker_key}
#[actor(
    BitbucketReadAddonLinkersByLinkerKeyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_addon_linkers_by_linker_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon/linkers/{linker_key}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /addon/linkers/{{linker_key}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: GET /addon/linkers/{linker_key}/values
#[actor(
    BitbucketReadAddonLinkersByLinkerKeyValuesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_addon_linkers_by_linker_key_values(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon/linkers/{linker_key}/values".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /addon/linkers/{{linker_key}}/values failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: DELETE /addon/linkers/{linker_key}/values
#[actor(
    BitbucketDeleteAddonLinkersByLinkerKeyValuesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_addon_linkers_by_linker_key_values(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon/linkers/{linker_key}/values".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /addon/linkers/{{linker_key}}/values failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: POST /addon/linkers/{linker_key}/values
#[actor(
    BitbucketCreateAddonLinkersByLinkerKeyValuesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_addon_linkers_by_linker_key_values(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon/linkers/{linker_key}/values".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /addon/linkers/{{linker_key}}/values failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: PUT /addon/linkers/{linker_key}/values
#[actor(
    BitbucketUpdateAddonLinkersByLinkerKeyValuesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_addon_linkers_by_linker_key_values(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon/linkers/{linker_key}/values".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /addon/linkers/{{linker_key}}/values failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// POST a new custom event.\n\nThe data within the event body will be hydrated by Bitbucket. For example, the following event\nsubmission would result in subscribers for the event receiving the full repository object\ncorresponding to the UUID.\n\n```\n$ curl -X POST -H \"Content-Type: application/json\" -d '{\n    \"mynumdata\": \"12345\",\n    \"repository\": {\n        \"type\": \"repository\",\n        \"uuid\": \"{be95aa1f-c0b2-47f6-99d1-bf5d3a0f850f}\"\n}}' https://api.bitbucket.org/2.0/addon/users/myuser/events/com.example.app%3Amyevent\n```\n\nUse the optional `fields` property of the custom event Connect\nmodule where the event is defined to add additional\nfields to the expanded payload sent to listeners.\n\nFor example, the `customEvents` module in the app descriptor for the previous example would look like this:\n\n```\n'modules': {\n    'customEvents': {\n        'com.example.app:myevent': {\n            'schema': {\n                'properties': {\n                    'mynumdata': {'type': 'number'},\n                    'repository': {'$ref': '#/definitions/repository'}\n                }\n            },\n            'fields': ['repository.owner']\n        }\n    }\n}\n```\n\nBy specifying fields as above, the repository owner\nwill also be sent to subscribers of the event.
///
/// Method: POST /addon/users/{target_user}/events/{event_key}
#[actor(
    BitbucketCreateAddonUsersByTargetUserEventsByEventKeyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_addon_users_by_target_user_events_by_event_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/addon/users/{target_user}/events/{event_key}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /addon/users/{{target_user}}/events/{{event_key}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the webhook resource or subject types on which webhooks can\nbe registered.\n\nEach resource/subject type contains an `events` link that returns the\npaginated list of specific events each individual subject type can\nemit.\n\nThis endpoint is publicly accessible and does not require\nauthentication or scopes.\n\nExample:\n\n```\n$ curl https://api.bitbucket.org/2.0/hook_events\n\n{\n    \"repository\": {\n        \"links\": {\n            \"events\": {\n                \"href\": \"https://api.bitbucket.org/2.0/hook_events/repository\"\n            }\n        }\n    },\n    \"team\": {\n        \"links\": {\n            \"events\": {\n                \"href\": \"https://api.bitbucket.org/2.0/hook_events/team\"\n            }\n        }\n    },\n    \"user\": {\n        \"links\": {\n            \"events\": {\n                \"href\": \"https://api.bitbucket.org/2.0/hook_events/user\"\n            }\n        }\n    }\n}\n```
///
/// Method: GET /hook_events
#[actor(
    BitbucketReadHookEventsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_hook_events(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/hook_events".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /hook_events failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of all valid webhook events for the\nspecified entity.\n\nThis is public data that does not require any scopes or authentication.\n\nExample:\n\nNOTE: The following example is a truncated response object for the `team` `subject_type`.\nWe return the same structure for the other `subject_type` objects.\n\n```\n$ curl https://api.bitbucket.org/2.0/hook_events/team\n{\n    \"page\": 1,\n    \"pagelen\": 30,\n    \"size\": 21,\n    \"values\": [\n        {\n            \"category\": \"Repository\",\n            \"description\": \"Whenever a repository push occurs\",\n            \"event\": \"repo:push\",\n            \"label\": \"Push\"\n        },\n        {\n            \"category\": \"Repository\",\n            \"description\": \"Whenever a repository fork occurs\",\n            \"event\": \"repo:fork\",\n            \"label\": \"Fork\"\n        },\n        ...\n        {\n            \"category\": \"Repository\",\n            \"description\": \"Whenever a repository import occurs\",\n            \"event\": \"repo:imported\",\n            \"label\": \"Import\"\n        }\n    ]\n}\n```
///
/// Method: GET /hook_events/{subject_type}
#[actor(
    BitbucketReadHookEventsBySubjectTypeActor,
    inports::<100>(subject_type),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_hook_events_by_subject_type(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/hook_events/{subject_type}".to_string();
    if let Some(val) = inputs.get("subject_type") {
        endpoint = endpoint.replace("{{subject_type}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /hook_events/{{subject_type}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all pull requests authored by the specified user.\n\nBy default only open pull requests are returned. This can be controlled\nusing the `state` query parameter. To retrieve pull requests that are\nin one of multiple states, repeat the `state` parameter for each\nindividual state.\n\nThis endpoint also supports filtering and sorting of the results. See\n[filtering and sorting](../../../../meta/filtering) for more details.
///
/// Method: GET /pullrequests/{target_user}
#[actor(
    BitbucketReadPullrequestsByTargetUserActor,
    inports::<100>(username, state),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pullrequests_by_target_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/pullrequests/{target_user}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("state") {
        query_pairs.push(("state", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /pullrequests/{{target_user}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of all public repositories.\n\nThis endpoint also supports filtering and sorting of the results. See\n[filtering and sorting](../meta/filtering) for more details.
///
/// Method: GET /repositories
#[actor(
    BitbucketReadRepositoriesActor,
    inports::<100>(after),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("after") {
        query_pairs.push(("after", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of all repositories owned by the specified\naccount or UUID.\n\nThe result can be narrowed down based on the authenticated user's role.\n\nE.g. with `?role=contributor`, only those repositories that the\nauthenticated user has write access to are returned (this includes any\nrepo the user is an admin on, as that implies write access).\n\nThis endpoint also supports filtering and sorting of the results. See\n[filtering and sorting](../../meta/filtering) for more details.
///
/// Method: GET /repositories/{username}
#[actor(
    BitbucketReadRepositoriesByUsernameActor,
    inports::<100>(role),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("role") {
        query_pairs.push(("role", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Since this endpoint can be used to both update and to create a\nrepository, the request body depends on the intent.\n\n### Creation\n\nSee the POST documentation for the repository endpoint for an example\nof the request body.\n\n### Update\n\nNote: Changing the `name` of the repository will cause the location to\nbe changed. This is because the URL of the repo is derived from the\nname (a process called slugification). In such a scenario, it is\npossible for the request to fail if the newly created slug conflicts\nwith an existing repository's slug. But if there is no conflict,\nthe new location will be returned in the `Location` header of the\nresponse.
///
/// Method: PUT /repositories/{username}/{repo_slug}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the object describing this repository.
///
/// Method: GET /repositories/{username}/{repo_slug}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes the repository. This is an irreversible operation.\n\nThis does not affect its forks.
///
/// Method: DELETE /repositories/{username}/{repo_slug}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugActor,
    inports::<100>(redirect_to),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("redirect_to") {
        query_pairs.push(("redirect_to", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new repository.\n\nNote: In order to set the project for the newly created repository,\npass in either the project key or the project UUID as part of the\nrequest body as shown in the examples below:\n\n```\n$ curl -X POST -H \"Content-Type: application/json\" -d '{\n    \"scm\": \"git\",\n    \"project\": {\n        \"key\": \"MARS\"\n    }\n}' https://api.bitbucket.org/2.0/repositories/teamsinspace/hablanding\n```\n\nor\n\n```\n$ curl -X POST -H \"Content-Type: application/json\" -d '{\n    \"scm\": \"git\",\n    \"project\": {\n        \"key\": \"{ba516952-992a-4c2d-acbd-17d502922f96}\"\n    }\n}' https://api.bitbucket.org/2.0/repositories/teamsinspace/hablanding\n```\n\nThe project must only be assigned for repositories belonging to a team.\nIf the repository owner is a team and the project is not provided, the\nrepository is automatically assigned to the oldest project in the team.\n\nNote: In the examples above, the username `teamsinspace`,\nand/or the repository name `hablanding` can be replaced by UUIDs.
///
/// Method: POST /repositories/{username}/{repo_slug}
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new branch restriction rule for a repository.\n\n`kind` describes what will be restricted. Allowed values include:\n`push`, `force`, `delete` and `restrict_merges`.\n\nDifferent kinds of branch restrictions have different requirements:\n\n* `push` and `restrict_merges` require `users` and `groups` to be\n  specified. Empty lists are allowed, in which case permission is\n  denied for everybody.\n* `force` can not be specified in a Mercurial repository.\n\nThe restriction applies to all branches that match. There are\ntwo ways to match a branch. It is configured in `branch_match_kind`:\n\n1. `glob`: Matches a branch against the `pattern`. A `'*'` in\n   `pattern` will expand to match zero or more characters, and every\n   other character matches itself. For example, `'foo*'` will match\n   `'foo'` and `'foobar'`, but not `'barfoo'`. `'*'` will match all\n   branches.\n2. `branching_model`: Matches a branch against the repository's\n   branching model. The `branch_type` controls the type of branch\n   to match. Allowed values include: `production`, `development`,\n   `bugfix`, `release`, `feature` and `hotfix`.\n\nThe combination of `kind` and match must be unique. This means that\ntwo `glob` restrictions in a repository cannot have the same `kind` and\n`pattern`. Additionally, two `branching_model` restrictions in a\nrepository cannot have the same `kind` and `branch_type`.\n\n`users` and `groups` are lists of users and groups that are except from\nthe restriction. They can only be configured in `push` and\n`restrict_merges` restrictions. The `push` restriction stops a user\npushing to matching branches unless that user is in `users` or is a\nmember of a group in `groups`. The `restrict_merges` stops a user\nmerging pull requests to matching branches unless that user is in\n`users` or is a member of a group in `groups`. Adding new users or\ngroups to an existing restriction should be done via `PUT`.\n\nNote that branch restrictions with overlapping matchers is allowed,\nbut the resulting behavior may be surprising.
///
/// Method: POST /repositories/{username}/{repo_slug}/branch-restrictions
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugBranchrestrictionsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_branchrestrictions(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/branch-restrictions".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/branch-restrictions failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of all branch restrictions on the\nrepository.
///
/// Method: GET /repositories/{username}/{repo_slug}/branch-restrictions
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugBranchrestrictionsActor,
    inports::<100>(kind, pattern),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_branchrestrictions(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/branch-restrictions".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("kind") {
        query_pairs.push(("kind", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("pattern") {
        query_pairs.push(("pattern", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/branch-restrictions failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a specific branch restriction rule.
///
/// Method: GET /repositories/{username}/{repo_slug}/branch-restrictions/{id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugBranchrestrictionsByIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_branchrestrictions_by_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/branch-restrictions/{id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/branch-restrictions/{{id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes an existing branch restriction rule.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/branch-restrictions/{id}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugBranchrestrictionsByIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_branchrestrictions_by_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/branch-restrictions/{id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/branch-restrictions/{{id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Updates an existing branch restriction rule.\n\nFields not present in the request body are ignored.\n\nSee [`POST`](../branch-restrictions#post) for details.
///
/// Method: PUT /repositories/{username}/{repo_slug}/branch-restrictions/{id}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugBranchrestrictionsByIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_branchrestrictions_by_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/branch-restrictions/{id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/branch-restrictions/{{id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Return the branching model as applied to the repository. This view is\nread-only. The branching model settings can be changed using the\n[settings](branching-model/settings#get) API.\n\nThe returned object:\n\n1. Always has a `development` property. `development.branch` contains\n   the actual repository branch object that is considered to be the\n   `development` branch. `development.branch` will not be present\n   if it does not exist.\n2. Might have a `production` property. `production` will not\n   be present when `production` is disabled.\n   `production.branch` contains the actual branch object that is\n   considered to be the `production` branch. `production.branch` will\n   not be present if it does not exist.\n3. Always has a `branch_types` array which contains all enabled branch\n   types.\n\nExample body:\n\n```\n{\n  \"development\": {\n    \"name\": \"master\",\n    \"branch\": {\n      \"type\": \"branch\",\n      \"name\": \"master\",\n      \"target\": {\n        \"hash\": \"16dffcb0de1b22e249db6799532074cf32efe80f\"\n      }\n    },\n    \"use_mainbranch\": true\n  },\n  \"production\": {\n    \"name\": \"production\",\n    \"branch\": {\n      \"type\": \"branch\",\n      \"name\": \"production\",\n      \"target\": {\n        \"hash\": \"16dffcb0de1b22e249db6799532074cf32efe80f\"\n      }\n    },\n    \"use_mainbranch\": false\n  },\n  \"branch_types\": [\n    {\n      \"kind\": \"release\",\n      \"prefix\": \"release/\"\n    },\n    {\n      \"kind\": \"hotfix\",\n      \"prefix\": \"hotfix/\"\n    },\n    {\n      \"kind\": \"feature\",\n      \"prefix\": \"feature/\"\n    },\n    {\n      \"kind\": \"bugfix\",\n      \"prefix\": \"bugfix/\"\n    }\n  ],\n  \"type\": \"branching_model\",\n  \"links\": {\n    \"self\": {\n      \"href\": \"https://api.bitbucket.org/.../branching-model\"\n    }\n  }\n}\n```
///
/// Method: GET /repositories/{username}/{repo_slug}/branching-model
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugBranchingmodelActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_branchingmodel(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/branching-model".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/branching-model failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update the branching model configuration for a repository.\n\nThe `development` branch can be configured to a specific branch or to\ntrack the main branch. When set to a specific branch it must\ncurrently exist. Only the passed properties will be updated. The\nproperties not passed will be left unchanged. A request without a\n`development` property will leave the development branch unchanged.\n\nIt is possible for the `development` branch to be invalid. This\nhappens when it points at a specific branch that has been\ndeleted. This is indicated in the `is_valid` field for the branch. It is\nnot possible to update the settings for `development` if that\nwould leave the branch in an invalid state. Such a request will be\nrejected.\n\nThe `production` branch can be a specific branch, the main\nbranch or disabled. When set to a specific branch it must currently\nexist. The `enabled` property can be used to enable (`true`) or\ndisable (`false`) it. Only the passed properties will be updated. The\nproperties not passed will be left unchanged. A request without a\n`production` property will leave the production branch unchanged.\n\nIt is possible for the `production` branch to be invalid. This\nhappens when it points at a specific branch that has been\ndeleted. This is indicated in the `is_valid` field for the branch. A\nrequest that would leave `production` enabled and invalid will be\nrejected. It is possible to update `production` and make it invalid if\nit would also be left disabled.\n\nThe `branch_types` property contains the branch types to be updated.\nOnly the branch types passed will be updated. All updates will be\nrejected if it would leave the branching model in an invalid state.\nFor branch types this means that:\n\n1. The prefixes for all enabled branch types are valid. For example,\n   it is not possible to use '*' inside a Git prefix.\n2. A prefix of an enabled branch type must not be a prefix of another\n   enabled branch type. This is to ensure that a branch can be easily\n   classified by its prefix unambiguously.\n\nIt is possible to store an invalid prefix if that branch type would be\nleft disabled. Only the passed properties will be updated. The\nproperties not passed will be left unchanged. Each branch type must\nhave a `kind` property to identify it.\n\nExample Body:\n\n```\n    {\n      \"development\": {\n        \"use_mainbranch\": true\n      },\n      \"production\": {\n        \"enabled\": true,\n        \"use_mainbranch\": false,\n        \"name\": \"production\"\n      },\n      \"branch_types\": [\n        {\n          \"kind\": \"bugfix\",\n          \"enabled\": true,\n          \"prefix\": \"bugfix/\"\n        },\n        {\n          \"kind\": \"feature\",\n          \"enabled\": true,\n          \"prefix\": \"feature/\"\n        },\n        {\n          \"kind\": \"hotfix\",\n          \"prefix\": \"hotfix/\"\n        },\n        {\n          \"kind\": \"release\",\n          \"enabled\": false,\n        }\n      ]\n    }\n```
///
/// Method: PUT /repositories/{username}/{repo_slug}/branching-model/settings
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugBranchingmodelSettingsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_branchingmodel_settings(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/branching-model/settings".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/branching-model/settings failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Return the branching model configuration for a repository. The returned\nobject:\n\n1. Always has a `development` property for the development branch.\n2. Always a `production` property for the production branch. The\n   production branch can be disabled.\n3. The `branch_types` contains all the branch types.\n\nThis is the raw configuration for the branching model. A client\nwishing to see the branching model with its actual current branches may\nfind the [active model API](../branching-model#get) more useful.\n\nExample body:\n\n```\n{\n  \"development\": {\n    \"is_valid\": true,\n    \"name\": null,\n    \"use_mainbranch\": true\n  },\n  \"production\": {\n    \"is_valid\": true,\n    \"name\": \"production\",\n    \"use_mainbranch\": false,\n    \"enabled\": false\n  },\n  \"branch_types\": [\n    {\n      \"kind\": \"release\",\n      \"enabled\": true,\n      \"prefix\": \"release/\"\n    },\n    {\n      \"kind\": \"hotfix\",\n      \"enabled\": true,\n      \"prefix\": \"hotfix/\"\n    },\n    {\n      \"kind\": \"feature\",\n      \"enabled\": true,\n      \"prefix\": \"feature/\"\n    },\n    {\n      \"kind\": \"bugfix\",\n      \"enabled\": false,\n      \"prefix\": \"bugfix/\"\n    }\n  ],\n  \"type\": \"branching_model_settings\",\n  \"links\": {\n    \"self\": {\n      \"href\": \"https://api.bitbucket.org/.../branching-model/settings\"\n    }\n  }\n}\n```
///
/// Method: GET /repositories/{username}/{repo_slug}/branching-model/settings
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugBranchingmodelSettingsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_branchingmodel_settings(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/branching-model/settings".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/branching-model/settings failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified commit.
///
/// Method: GET /repositories/{username}/{repo_slug}/commit/{node}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugCommitByNodeActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_commit_by_node(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/commit/{{node}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Approve the specified commit as the authenticated user.\n\nThis operation is only available to users that have explicit access to\nthe repository. In contrast, just the fact that a repository is\npublicly accessible to users does not give them the ability to approve\ncommits.
///
/// Method: POST /repositories/{username}/{repo_slug}/commit/{node}/approve
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugCommitByNodeApproveActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_commit_by_node_approve(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/approve".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/approve failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Redact the authenticated user's approval of the specified commit.\n\nThis operation is only available to users that have explicit access to\nthe repository. In contrast, just the fact that a repository is\npublicly accessible to users does not give them the ability to approve\ncommits.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/commit/{node}/approve
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugCommitByNodeApproveActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_commit_by_node_approve(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/approve".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/approve failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the commit's comments.\n\nThis includes both global and inline comments.\n\nThe default sorting is oldest to newest and can be overridden with\nthe `sort` query parameter.
///
/// Method: GET /repositories/{username}/{repo_slug}/commit/{node}/comments
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugCommitByNodeCommentsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_commit_by_node_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/comments".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/comments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates new comment on the specified commit.\n\nTo post a reply to an existing comment, include the `parent.id` field:\n\n```\n$ curl https://api.bitbucket.org/2.0/repositories/atlassian/prlinks/commit/db9ba1e031d07a02603eae0e559a7adc010257fc/comments/ \\\n  -X POST -u evzijst \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"content\": {\"raw\": \"One more thing!\"},\n       \"parent\": {\"id\": 5728901}}'\n```
///
/// Method: POST /repositories/{username}/{repo_slug}/commit/{node}/comments
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugCommitByNodeCommentsActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_commit_by_node_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/comments".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/comments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified commit comment.
///
/// Method: GET /repositories/{username}/{repo_slug}/commit/{node}/comments/{comment_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugCommitByNodeCommentsByCommentIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_commit_by_node_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/comments/{comment_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all statuses (e.g. build results) for a specific commit.
///
/// Method: GET /repositories/{username}/{repo_slug}/commit/{node}/statuses
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugCommitByNodeStatusesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_commit_by_node_statuses(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/statuses".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/statuses failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new build status against the specified commit.\n\nIf the specified key already exists, the existing status object will\nbe overwritten.\n\nWhen creating a new commit status, you can use a URI template for the URL.\nTemplates are URLs that contain variable names that Bitbucket will\nevaluate at runtime whenever the URL is displayed anywhere similar to\nparameter substitution in\n[Bitbucket Connect](https://developer.atlassian.com/bitbucket/concepts/context-parameters.html).\nFor example, one could use `https://foo.com/builds/{repository.full_name}`\nwhich Bitbucket will turn into `https://foo.com/builds/foo/bar` at render time.\nThe context variables available are `repository` and `commit`.
///
/// Method: POST /repositories/{username}/{repo_slug}/commit/{node}/statuses/build
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugCommitByNodeStatusesBuildActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_commit_by_node_statuses_build(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/statuses/build".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/statuses/build failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified build status for a commit.
///
/// Method: GET /repositories/{username}/{repo_slug}/commit/{node}/statuses/build/{key}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugCommitByNodeStatusesBuildByKeyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_commit_by_node_statuses_build_by_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/statuses/build/{key}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/statuses/build/{{key}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Used to update the current status of a build status object on the\nspecific commit.\n\nThis operation can also be used to change other properties of the\nbuild status:\n\n* `state`\n* `name`\n* `description`\n* `url`\n* `refname`\n\nThe `key` cannot be changed.
///
/// Method: PUT /repositories/{username}/{repo_slug}/commit/{node}/statuses/build/{key}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugCommitByNodeStatusesBuildByKeyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_commit_by_node_statuses_build_by_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commit/{node}/statuses/build/{key}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/commit/{{node}}/statuses/build/{{key}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Identical to `GET /repositories/{username}/{repo_slug}/commits`,\nexcept that POST allows clients to place the include and exclude\nparameters in the request body to avoid URL length issues.\n\n**Note that this resource does NOT support new commit creation.**
///
/// Method: POST /repositories/{username}/{repo_slug}/commits
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugCommitsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_commits(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commits".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/commits failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// These are the repository's commits. They are paginated and returned\nin reverse chronological order, similar to the output of `git log` and\n`hg log`. Like these tools, the DAG can be filtered.\n\n## GET /repositories/{username}/{repo_slug}/commits/\n\nReturns all commits in the repo in topological order (newest commit\nfirst). All branches and tags are included (similar to\n`git log --all` and `hg log`).\n\n## GET /repositories/{username}/{repo_slug}/commits/master\n\nReturns all commits on rev `master` (similar to `git log master`,\n`hg log master`).\n\n## GET /repositories/{username}/{repo_slug}/commits/dev?exclude=master\n\nReturns all commits on ref `dev`, except those that are reachable on\n`master` (similar to `git log dev ^master`).\n\n## GET /repositories/{username}/{repo_slug}/commits/?exclude=master\n\nReturns all commits in the repo that are not on master\n(similar to `git log --all ^master`).\n\n## GET /repositories/{username}/{repo_slug}/commits/?include=foo&include=bar&exclude=fu&exclude=fubar\n\nReturns all commits that are on refs `foo` or `bar`, but not on `fu` or\n`fubar` (similar to `git log foo bar ^fu ^fubar`).\n\nAn optional `path` parameter can be specified that will limit the\nresults to commits that affect that path. `path` can either be a file\nor a directory. If a directory is specified, commits are returned that\nhave modified any file in the directory tree rooted by `path`. It is\nimportant to note that if the `path` parameter is specified, the commits\nreturned by this endpoint may no longer be a DAG, parent commits that\ndo not modify the path will be omitted from the response.\n\n## GET /repositories/{username}/{repo_slug}/commits/?path=README.md&include=foo&include=bar&exclude=master\n\nReturns all commits that are on refs `foo` or `bar`, but not on `master`\nthat changed the file README.md.\n\n## GET /repositories/{username}/{repo_slug}/commits/?path=src/&include=foo&include=bar&exclude=master\n\nReturns all commits that are on refs `foo` or `bar`, but not on `master`\nthat changed to a file in any file in the directory src or its children.\n\nBecause the response could include a very large number of commits, it\nis paginated. Follow the 'next' link in the response to navigate to the\nnext page of commits. As with other paginated resources, do not\nconstruct your own links.\n\nWhen the include and exclude parameters are more than can fit in a\nquery string, clients can use a `x-www-form-urlencoded` POST instead.
///
/// Method: GET /repositories/{username}/{repo_slug}/commits
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugCommitsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_commits(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commits".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/commits failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Identical to `GET /repositories/{username}/{repo_slug}/commits`,\nexcept that POST allows clients to place the include and exclude\nparameters in the request body to avoid URL length issues.\n\n**Note that this resource does NOT support new commit creation.**
///
/// Method: POST /repositories/{username}/{repo_slug}/commits/{revision}
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugCommitsByRevisionActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_commits_by_revision(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commits/{revision}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/commits/{{revision}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// These are the repository's commits. They are paginated and returned\nin reverse chronological order, similar to the output of `git log` and\n`hg log`. Like these tools, the DAG can be filtered.\n\n## GET /repositories/{username}/{repo_slug}/commits/\n\nReturns all commits in the repo in topological order (newest commit\nfirst). All branches and tags are included (similar to\n`git log --all` and `hg log`).\n\n## GET /repositories/{username}/{repo_slug}/commits/master\n\nReturns all commits on rev `master` (similar to `git log master`,\n`hg log master`).\n\n## GET /repositories/{username}/{repo_slug}/commits/dev?exclude=master\n\nReturns all commits on ref `dev`, except those that are reachable on\n`master` (similar to `git log dev ^master`).\n\n## GET /repositories/{username}/{repo_slug}/commits/?exclude=master\n\nReturns all commits in the repo that are not on master\n(similar to `git log --all ^master`).\n\n## GET /repositories/{username}/{repo_slug}/commits/?include=foo&include=bar&exclude=fu&exclude=fubar\n\nReturns all commits that are on refs `foo` or `bar`, but not on `fu` or\n`fubar` (similar to `git log foo bar ^fu ^fubar`).\n\nAn optional `path` parameter can be specified that will limit the\nresults to commits that affect that path. `path` can either be a file\nor a directory. If a directory is specified, commits are returned that\nhave modified any file in the directory tree rooted by `path`. It is\nimportant to note that if the `path` parameter is specified, the commits\nreturned by this endpoint may no longer be a DAG, parent commits that\ndo not modify the path will be omitted from the response.\n\n## GET /repositories/{username}/{repo_slug}/commits/?path=README.md&include=foo&include=bar&exclude=master\n\nReturns all commits that are on refs `foo` or `bar`, but not on `master`\nthat changed the file README.md.\n\n## GET /repositories/{username}/{repo_slug}/commits/?path=src/&include=foo&include=bar&exclude=master\n\nReturns all commits that are on refs `foo` or `bar`, but not on `master`\nthat changed to a file in any file in the directory src or its children.\n\nBecause the response could include a very large number of commits, it\nis paginated. Follow the 'next' link in the response to navigate to the\nnext page of commits. As with other paginated resources, do not\nconstruct your own links.\n\nWhen the include and exclude parameters are more than can fit in a\nquery string, clients can use a `x-www-form-urlencoded` POST instead.
///
/// Method: GET /repositories/{username}/{repo_slug}/commits/{revision}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugCommitsByRevisionActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_commits_by_revision(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/commits/{revision}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/commits/{{revision}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the components that have been defined in the issue tracker.\n\nThis resource is only available on repositories that have the issue\ntracker enabled.
///
/// Method: GET /repositories/{username}/{repo_slug}/components
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugComponentsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_components(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/components".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/components failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified issue tracker component object.
///
/// Method: GET /repositories/{username}/{repo_slug}/components/{component_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugComponentsByComponentIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_components_by_component_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/components/{component_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/components/{{component_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the repository's default reviewers.\n\nThese are the users that are automatically added as reviewers on every\nnew pull request that is created.
///
/// Method: GET /repositories/{username}/{repo_slug}/default-reviewers
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugDefaultreviewersActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_defaultreviewers(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/default-reviewers".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/default-reviewers failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Removes a default reviewer from the repository.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/default-reviewers/{target_username}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugDefaultreviewersByTargetUsernameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_defaultreviewers_by_target_username(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/default-reviewers/{target_username}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/default-reviewers/{{target_username}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified reviewer.\n\nThis can be used to test whether a user is among the repository's\ndefault reviewers list. A 404 indicates that that specified user is not\na default reviewer.
///
/// Method: GET /repositories/{username}/{repo_slug}/default-reviewers/{target_username}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugDefaultreviewersByTargetUsernameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_defaultreviewers_by_target_username(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/default-reviewers/{target_username}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/default-reviewers/{{target_username}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Adds the specified user to the repository's list of default\nreviewers.\n\nThis method is idempotent. Adding a user a second time has no effect.
///
/// Method: PUT /repositories/{username}/{repo_slug}/default-reviewers/{target_username}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugDefaultreviewersByTargetUsernameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_defaultreviewers_by_target_username(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/default-reviewers/{target_username}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/default-reviewers/{{target_username}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all deploy-keys belonging to a repository.\n\nExample:\n```\n$ curl -H \"Authorization <auth header>\" \\\nhttps://api.bitbucket.org/2.0/repositories/mleu/test/deploy-keys\n\nOutput:\n{\n    \"pagelen\": 10,\n    \"values\": [\n        {\n            \"id\": 123,\n            \"key\": \"ssh-rsa \",\n            \"label\": \"mykey\",\n            \"type\": \"deploy_key\",\n            \"created_on\": \"2018-08-15T23:50:59.993890+00:00\",\n            \"repository\": {\n                \"full_name\": \"mleu/test\",\n                \"name\": \"test\",\n                \"type\": \"repository\",\n                \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n            },\n            \"links\":{\n                \"self\":{\n                    \"href\": \"https://api.bitbucket.org/2.0/repositories/mleu/test/deploy-keys/123\"\n                }\n            }\n            \"last_used\": null,\n            \"comment\": \"mleu@C02W454JHTD8\"\n        }\n    ],\n    \"page\": 1,\n    \"size\": 1\n}\n```
///
/// Method: GET /repositories/{username}/{repo_slug}/deploy-keys
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugDeploykeysActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_deploykeys(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/deploy-keys".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/deploy-keys failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create a new deploy key in a repository.\n\nExample:\n```\n$ curl -XPOST \\\n-H \"Authorization <auth header>\" \\\n-H \"Content-type: application/json\" \\\nhttps://api.bitbucket.org/2.0/repositories/mleu/test/deploy-keys -d \\\n'{\n    \"key\": \"ssh-rsa  mleu@C02W454JHTD8\",\n    \"label\": \"mydeploykey\"\n}'\n\nOutput:\n{\n    \"id\": 123,\n    \"key\": \"ssh-rsa \",\n    \"label\": \"mydeploykey\",\n    \"type\": \"deploy_key\",\n    \"created_on\": \"2018-08-15T23:50:59.993890+00:00\",\n    \"repository\": {\n        \"full_name\": \"mleu/test\",\n        \"name\": \"test\",\n        \"type\": \"repository\",\n        \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n    },\n    \"links\":{\n        \"self\":{\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/mleu/test/deploy-keys/123\"\n        }\n    }\n    \"last_used\": null,\n    \"comment\": \"mleu@C02W454JHTD8\"\n}\n```
///
/// Method: POST /repositories/{username}/{repo_slug}/deploy-keys
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugDeploykeysActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_deploykeys(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/deploy-keys".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/deploy-keys failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create a new deploy key in a repository.\n\nThe same key needs to be passed in but the comment and label can change.\n\nExample:\n```\n$ curl -XPUT \\\n-H \"Authorization <auth header>\" \\\n-H \"Content-type: application/json\" \\\nhttps://api.bitbucket.org/2.0/repositories/mleu/test/deploy-keys/1234 -d \\\n'{\n    \"label\": \"newlabel\",\n    \"key\": \"ssh-rsa  newcomment\",\n}'\n\nOutput:\n{\n    \"comment\": \"newcomment\",\n    \"last_used\": null,\n    \"links\": {\n        \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/mleu/test/deploy-keys/1234\"\n        }\n    },\n    \"repository\": {\n        \"full_name\": \"mleu/test\",\n        \"name\": \"test\",\n        \"type\": \"repository\",\n        \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n    },\n    \"label\": \"newlabel\",\n    \"created_on\": \"2018-08-15T23:50:59.993890+00:00\",\n    \"key\": \"ssh-rsa \",\n    \"id\": 1234,\n    \"type\": \"deploy_key\"\n}\n```
///
/// Method: PUT /repositories/{username}/{repo_slug}/deploy-keys/{key_id}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugDeploykeysByKeyIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_deploykeys_by_key_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/deploy-keys/{key_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/deploy-keys/{{key_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// This deletes a deploy key from a repository.\n\nExample:\n```\n$ curl -XDELETE \\\n-H \"Authorization <auth header>\" \\\nhttps://api.bitbucket.org/2.0/repositories/mleu/test/deploy-keys/1234\n```
///
/// Method: DELETE /repositories/{username}/{repo_slug}/deploy-keys/{key_id}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugDeploykeysByKeyIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_deploykeys_by_key_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/deploy-keys/{key_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/deploy-keys/{{key_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the deploy key belonging to a specific key.\n\nExample:\n```\n$ curl -H \"Authorization <auth header>\" \\\nhttps://api.bitbucket.org/2.0/repositories/mleu/test/deploy-key/1234\n\nOutput:\n{\n    \"comment\": \"mleu@C02W454JHTD8\",\n    \"last_used\": null,\n    \"links\": {\n        \"self\": {\n            \"href\": https://api.bitbucket.org/2.0/repositories/mleu/test/deploy-key/1234\"\n        }\n    },\n    \"repository\": {\n        \"full_name\": \"mleu/test\",\n        \"name\": \"test\",\n        \"type\": \"repository\",\n        \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n    },\n    \"label\": \"mykey\",\n    \"created_on\": \"2018-08-15T23:50:59.993890+00:00\",\n    \"key\": \"ssh-rsa \",\n    \"id\": 1234,\n    \"type\": \"deploy_key\"\n}\n```
///
/// Method: GET /repositories/{username}/{repo_slug}/deploy-keys/{key_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugDeploykeysByKeyIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_deploykeys_by_key_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/deploy-keys/{key_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/deploy-keys/{{key_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Produces a raw, git-style diff for either a single commit (diffed\nagainst its first parent), or a revspec of 2 commits (e.g.\n`3a8b42..9ff173` where the first commit represents the source and the\nsecond commit the destination).\n\nIn case of the latter (diffing a revspec), a 3-way diff, or merge diff,\nis computed. This shows the changes introduced by the left branch\n(`3a8b42` in our example) as compared againt the right branch\n(`9ff173`).\n\nThis is equivalent to merging the left branch into the right branch and\nthen computing the diff of the merge commit against its first parent\n(the right branch). This follows the same behavior as pull requests\nthat also show this style of 3-way, or merge diff.\n\nWhile similar to patches, diffs:\n\n* Don't have a commit header (username, commit message, etc)\n* Support the optional `path=foo/bar.py` query param to filter\n  the diff to just that one file diff\n\nThe raw diff is returned as-is, in whatever encoding the files in the\nrepository use. It is not decoded into unicode. As such, the\ncontent-type is `text/plain`.
///
/// Method: GET /repositories/{username}/{repo_slug}/diff/{spec}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugDiffBySpecActor,
    inports::<100>(context, path, ignore_whitespace, binary),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_diff_by_spec(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/diff/{spec}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("context") {
        query_pairs.push(("context", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("path") {
        query_pairs.push(("path", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("ignore_whitespace") {
        query_pairs.push(("ignore_whitespace", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("binary") {
        query_pairs.push(("binary", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/diff/{{spec}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the diff stat for the specified commit.\n\nDiff stat responses contain a record for every path modified by the\ncommit and lists the number of lines added and removed for each file.\n\n\nExample:\n```\ncurl https://api.bitbucket.org/2.0/repositories/bitbucket/geordi/diffstat/d222fa2..e174964\n{\n    \"pagelen\": 500,\n    \"values\": [\n        {\n            \"type\": \"diffstat\",\n            \"status\": \"modified\",\n            \"lines_removed\": 1,\n            \"lines_added\": 2,\n            \"old\": {\n                \"path\": \"setup.py\",\n                \"type\": \"commit_file\",\n                \"links\": {\n                    \"self\": {\n                        \"href\": \"https://api.bitbucket.org/2.0/repositories/bitbucket/geordi/src/e1749643d655d7c7014001a6c0f58abaf42ad850/setup.py\"\n                    }\n                }\n            },\n            \"new\": {\n                \"path\": \"setup.py\",\n                \"type\": \"commit_file\",\n                \"links\": {\n                    \"self\": {\n                        \"href\": \"https://api.bitbucket.org/2.0/repositories/bitbucket/geordi/src/d222fa235229c55dad20b190b0b571adf737d5a6/setup.py\"\n                    }\n                }\n            }\n        }\n    ],\n    \"page\": 1,\n    \"size\": 1\n}\n```
///
/// Method: GET /repositories/{username}/{repo_slug}/diffstat/{spec}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugDiffstatBySpecActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_diffstat_by_spec(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/diffstat/{spec}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/diffstat/{{spec}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a list of download links associated with the repository.
///
/// Method: GET /repositories/{username}/{repo_slug}/downloads
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugDownloadsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_downloads(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/downloads".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/downloads failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Upload new download artifacts.\n\nTo upload files, perform a `multipart/form-data` POST containing one\nor more `files` fields:\n\n    $ echo Hello World > hello.txt\n    $ curl -s -u evzijst -X POST https://api.bitbucket.org/2.0/repositories/evzijst/git-tests/downloads -F files=@hello.txt\n\nWhen a file is uploaded with the same name as an existing artifact,\nthen the existing file will be replaced.
///
/// Method: POST /repositories/{username}/{repo_slug}/downloads
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugDownloadsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_downloads(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/downloads".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/downloads failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes the specified download artifact from the repository.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/downloads/{filename}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugDownloadsByFilenameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_downloads_by_filename(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/downloads/{filename}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/downloads/{{filename}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Return a redirect to the contents of a download artifact.\n\nThis endpoint returns the actual file contents and not the artifact's\nmetadata.\n\n    $ curl -s -L https://api.bitbucket.org/2.0/repositories/evzijst/git-tests/downloads/hello.txt\n    Hello World
///
/// Method: GET /repositories/{username}/{repo_slug}/downloads/{filename}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugDownloadsByFilenameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_downloads_by_filename(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/downloads/{filename}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/downloads/{{filename}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of commits that modified the specified file.\n\nCommits are returned in reverse chronological order. This is roughly\nequivalent to the following commands:\n\n    $ git log --follow --date-order <sha> <path>\n\n    $ hg log --follow <path>\n\nBy default, Bitbucket will follow renames and the path name in the\nreturned entries reflects that. This can be turned off using the\n`?renames=false` query parameter.\n\nResults are returned in descending chronological order by default, and\nlike most endpoints you can\n[filter and sort](../../../../../../meta/filtering) the response to\nonly provide exactly the data you want.\n\nFor example, if you wanted to find commits made before 2011-05-18\nagainst a file named `README.rst`, but you only wanted the path and\ndate, your query would look like this:\n\n```\n$ curl 'https://api.bitbucket.org/2.0/repositories/evzijst/dogslow/filehistory/master/README.rst'\\\n  '?fields=values.next,values.path,values.commit.date&q=commit.date<=2011-05-18'\n{\n  \"values\": [\n    {\n      \"commit\": {\n        \"date\": \"2011-05-17T07:32:09+00:00\"\n      },\n      \"path\": \"README.rst\"\n    },\n    {\n      \"commit\": {\n        \"date\": \"2011-05-16T06:33:28+00:00\"\n      },\n      \"path\": \"README.txt\"\n    },\n    {\n      \"commit\": {\n        \"date\": \"2011-05-16T06:15:39+00:00\"\n      },\n      \"path\": \"README.txt\"\n    }\n  ]\n}\n```\n\nIn the response you can see that the file was renamed to `README.rst`\nby the commit made on 2011-05-16, and was previously named `README.txt`.
///
/// Method: GET /repositories/{username}/{repo_slug}/filehistory/{node}/{path}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugFilehistoryByNodeByPathActor,
    inports::<100>(renames, q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_filehistory_by_node_by_path(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/filehistory/{node}/{path}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("renames") {
        query_pairs.push(("renames", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/filehistory/{{node}}/{{path}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new fork of the specified repository.\n\nBy default, forks are created under the authenticated user's account\nwith the same name and slug of the original repository.\n\n```\n$ curl -X POST -u jdoe https://api.bitbucket.org/2.0/repositories/evzijst/dogslow/forks\n\n{\n  \"scm\": \"git\",\n  \"full_name\": \"jdoe/dogslow\",\n  \"parent\": {\n    \"full_name\": \"evzijst/dogslow\",\n    ...\n  },\n  ...\n}\n```\n\nIf you already have a repo with that name, then a 400 is returned and\nyou'll need to specify a custom name for the new fork:\n\n```\n$ curl -X POST -u jdoe https://api.bitbucket.org/2.0/repositories/evzijst/dogslow/forks \\\n  -H 'Content-Type: application/json' -d '{\n    \"name\": \"dogslow_fork\"\n}'\n```\n\nWhen you specify a value for `name`, it will also affect the `slug`.\nThe `slug` is reflected in the repository URL of the new fork. It is\nderived from `name` by substituting non-ASCII characters, removes\nwhitespace, and changes characters to lower case. For example,\n`My repo` would turn into `my_repo`.\n\n## Forking a repository into a team account\n\nTo create a fork into a team account, specify the new owner's account\nexplicitly as part of the request body. This prevents forked\nrepositories from being owned by the authenticated user submitting the\nrequest:\n\n```\n$ curl -X POST -u jdoe https://api.bitbucket.org/2.0/repositories/atlassian/bbql/forks \\\n  -H 'Content-Type: application/json' -d '{\n    \"name\": \"bbql_fork\",\n    \"owner\": {\n      \"username\": \"atlassian\"\n    }\n}'\n```\n\nTo fork a repository into the same team, also specify a new `name`.\n\nYou need contributor access to create new forks within a team account.\n\n\n## Change the properties of a new fork\n\nBy default the fork inherits most of its properties from the parent.\nHowever, since the optional POST body document follows the normal\n`repository` JSON schema and you can override the new fork's\nproperties.\n\nProperties that can be overridden include:\n\n* description\n* fork_policy\n* language\n* mainbranch\n* is_private (note that a private repo's fork_policy might prohibit\n  the creation of public forks, in which `is_private=False` would fail)\n* has_issues (to initialize or disable the new repo's issue tracker --\n  note that the actual contents of the parent repository's issue\n  tracker are not copied during forking)\n* has_wiki (to initialize or disable the new repo's wiki --\n  note that the actual contents of the parent repository's wiki are not\n  copied during forking)\n* project (only teams have projects and when forking into a private\n  project, the fork's `is_private` must be `true`)\n\nProperties that cannot be modified include:\n\n* scm\n* parent\n* full_name
///
/// Method: POST /repositories/{username}/{repo_slug}/forks
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugForksActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_forks(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/forks".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/forks failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of all the forks of the specified\nrepository.
///
/// Method: GET /repositories/{username}/{repo_slug}/forks
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugForksActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_forks(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/forks".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/forks failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of webhooks installed on this repository.
///
/// Method: GET /repositories/{username}/{repo_slug}/hooks
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugHooksActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_hooks(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/hooks".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/hooks failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new webhook on the specified repository.\n\nExample:\n\n```\n$ curl -X POST -u credentials -H 'Content-Type: application/json'           https://api.bitbucket.org/2.0/repositories/username/slug/hooks           -d '\n    {\n      \"description\": \"Webhook Description\",\n      \"url\": \"https://example.com/\",\n      \"active\": true,\n      \"events\": [\n        \"repo:push\",\n        \"issue:created\",\n        \"issue:updated\"\n      ]\n    }'\n```\n\nNote that this call requires the webhook scope, as well as any scope\nthat applies to the events that the webhook subscribes to. In the\nexample above that means: `webhook`, `repository` and `issue`.\n\nAlso note that the `url` must properly resolve and cannot be an\ninternal, non-routed address.
///
/// Method: POST /repositories/{username}/{repo_slug}/hooks
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugHooksActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_hooks(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/hooks".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/hooks failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Updates the specified webhook subscription.\n\nThe following properties can be mutated:\n\n* `description`\n* `url`\n* `active`\n* `events`
///
/// Method: PUT /repositories/{username}/{repo_slug}/hooks/{uid}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the webhook with the specified id installed on the specified\nrepository.
///
/// Method: GET /repositories/{username}/{repo_slug}/hooks/{uid}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes the specified webhook subscription from the given\nrepository.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/hooks/{uid}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new issue.\n\nThis call requires authentication. Private repositories or private\nissue trackers require the caller to authenticate with an account that\nhas appropriate authorization.\n\nThe authenticated user is used for the issue's `reporter` field.
///
/// Method: POST /repositories/{username}/{repo_slug}/issues
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugIssuesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_issues(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/issues failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the issues in the issue tracker.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// A POST request to this endpoint initiates a new background celery task that archives the repo's issues.\n\nFor example, you can run:\n\ncurl -u <username> -X POST http://api.bitbucket.org/2.0/repositories/<owner_username>/<repo_slug>/\nissues/export\n\nWhen the job has been accepted, it will return a 202 (Accepted) along with a unique url to this job in the\n'Location' response header. This url is the endpoint for where the user can obtain their zip files.\"
///
/// Method: POST /repositories/{username}/{repo_slug}/issues/export
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugIssuesExportActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_issues_export(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/export".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/issues/export failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// This endpoint is used to poll for the progress of an issue export\njob and return the zip file after the job is complete.\nAs long as the job is running, this will return a 200 response\nwith in the response body a description of the current status.\n\nAfter the job has been scheduled, but before it starts executing, this\nendpoint's response is:\n\n{\n \"type\": \"issue_job_status\",\n \"status\": \"ACCEPTED\",\n \"phase\": \"Initializing\",\n \"total\": 0,\n \"count\": 0,\n \"pct\": 0\n}\n\n\nThen once it starts running, it becomes:\n\n{\n \"type\": \"issue_job_status\",\n \"status\": \"STARTED\",\n \"phase\": \"Attachments\",\n \"total\": 15,\n \"count\": 11,\n \"pct\": 73\n}\n\nOnce the job has successfully completed, it returns a stream of the zip file.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/export/{repo_name}-issues-{task_id}.zip
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesExportByRepoNameissuesbyTaskIdZipActor,
    inports::<100>(username, repo_slug, repo_name, task_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_export_by_repo_nameissuesby_task_id_zip(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/export/{repo_name}-issues-{task_id}.zip".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_name") {
        endpoint = endpoint.replace("{{repo_name}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("task_id") {
        endpoint = endpoint.replace("{{task_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/export/{{repo_name}}-issues-{{task_id}}.zip failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// When using GET, this endpoint reports the status of the current import task. Request example:\n\n```\n$ curl -u <username> -X GET https://api.bitbucket.org/2.0/repositories/<owner_username>/<repo_slug>/issues/import\n```\n\nAfter the job has been scheduled, but before it starts executing, this endpoint's response is:\n\n```\n< HTTP/1.1 202 Accepted\n{\n    \"type\": \"issue_job_status\",\n    \"status\": \"PENDING\",\n    \"phase\": \"Attachments\",\n    \"total\": 15,\n    \"count\": 0,\n    \"percent\": 0\n}\n```\n\nOnce it starts running, it is a 202 response with status STARTED and progress filled.\n\nAfter it is finished, it becomes a 200 response with status SUCCESS or FAILURE.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/import
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesImportActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_import(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/import".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/import failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// A POST request to this endpoint will import the zip file given by the archive parameter into the repository. All\nexisting issues will be deleted and replaced by the contents of the imported zip file.\n\nImports are done through a multipart/form-data POST. There is one valid and required form field, with the name\n\"archive,\" which needs to be a file field:\n\n```\n$ curl -u <username> -X POST -F archive=@/path/to/file.zip https://api.bitbucket.org/2.0/repositories/<owner_username>/<repo_slug>/issues/import\n```\n\nWhen the import job is accepted, here is example output:\n\n```\n< HTTP/1.1 202 Accepted\n\n{\n    \"type\": \"issue_job_status\",\n    \"status\": \"ACCEPTED\",\n    \"phase\": \"Attachments\",\n    \"total\": 15,\n    \"count\": 0,\n    \"percent\": 0\n}\n```
///
/// Method: POST /repositories/{username}/{repo_slug}/issues/import
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugIssuesImportActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_issues_import(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/import".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/issues/import failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified issue.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes the specified issue. This requires write access to the\nrepository.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/issues/{issue_id}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugIssuesByIssueIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_issues_by_issue_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Modifies the issue.\n\n```\n$ curl https://api.bitbucket.org/2.0/repostories/evzijst/dogslow/issues/123 \\\n  -u evzijst -s -X PUT -H 'Content-Type: application/json' \\\n  -d '{\n  \"title\": \"Updated title\",\n  \"assignee\": {\n    \"username\": \"evzijst\"\n  },\n  \"priority\": \"minor\",\n  \"version\": {\n    \"name\": \"1.0\"\n  },\n  \"component\": null\n}'\n```\n\nThis example changes the `title`, `assignee`, `priority` and the\n`version`. It also removes the value of the `component` from the issue\nby setting the field to `null`. Any field not present keeps its existing\nvalue.\n\nEach time an issue is edited in the UI or through the API, an immutable\nchange record is created under the `/issues/123/changes` endpoint. It\nalso has a comment associated with the change.
///
/// Method: PUT /repositories/{username}/{repo_slug}/issues/{issue_id}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugIssuesByIssueIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_issues_by_issue_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Upload new issue attachments.\n\nTo upload files, perform a `multipart/form-data` POST containing one\nor more file fields.\n\nWhen a file is uploaded with the same name as an existing attachment,\nthen the existing file will be replaced.
///
/// Method: POST /repositories/{username}/{repo_slug}/issues/{issue_id}/attachments
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugIssuesByIssueIdAttachmentsActor,
    inports::<100>(issue_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_issues_by_issue_id_attachments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/attachments".to_string();
    if let Some(val) = inputs.get("issue_id") {
        endpoint = endpoint.replace("{{issue_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/attachments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all attachments for this issue.\n\nThis returns the files' meta data. This does not return the files'\nactual contents.\n\nThe files are always ordered by their upload date.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}/attachments
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdAttachmentsActor,
    inports::<100>(issue_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id_attachments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/attachments".to_string();
    if let Some(val) = inputs.get("issue_id") {
        endpoint = endpoint.replace("{{issue_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/attachments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the contents of the specified file attachment.\n\nNote that this endpoint does not return a JSON response, but instead\nreturns a redirect pointing to the actual file that in turn will return\nthe raw contents.\n\nThe redirect URL contains a one-time token that has a limited lifetime.\nAs a result, the link should not be persisted, stored, or shared.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}/attachments/{path}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdAttachmentsByPathActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id_attachments_by_path(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/attachments/{path}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/attachments/{{path}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes an attachment.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/issues/{issue_id}/attachments/{path}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugIssuesByIssueIdAttachmentsByPathActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_issues_by_issue_id_attachments_by_path(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/attachments/{path}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/attachments/{{path}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Makes a change to the specified issue.\n\nFor example, to change an issue's state and assignee, create a new\nchange object that modifies these fields:\n\n```\ncurl https://api.bitbucket.org/2.0/site/master/issues/1234/changes \\\n  -s -u evzijst -X POST -H \"Content-Type: application/json\" \\\n  -d '{\n    \"changes\": {\n      \"assignee_account_id\": {\n        \"new\": \"557058:c0b72ad0-1cb5-4018-9cdc-0cde8492c443\"\n      },\n      \"state\": {\n        \"new\": 'resolved\"\n      }\n    }\n    \"message\": {\n      \"raw\": \"This is now resolved.\"\n    }\n  }'\n```\n\nThe above example also includes a custom comment to go alongside the\nchange. This comment will also be visible on the issue page in the UI.\n\nThe fields of the `changes` object are strings, not objects. This\nallows for immutable change log records, even after user accounts,\nmilestones, or other objects recorded in a change entry, get renamed or\ndeleted.\n\nThe `assignee_account_id` field stores the account id. When POSTing a\nnew change and changing the assignee, the client should therefore use\nthe user's account_id in the `changes.assignee_account_id.new` field.\n\nThis call requires authentication. Private repositories or private\nissue trackers require the caller to authenticate with an account that\nhas appropriate authorization.
///
/// Method: POST /repositories/{username}/{repo_slug}/issues/{issue_id}/changes
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugIssuesByIssueIdChangesActor,
    inports::<100>(changes, type_, created_on, links, name, issue, user, message),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_issues_by_issue_id_changes(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/changes".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("changes") {
        body.insert("changes".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("type_") {
        body.insert("type".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("created_on") {
        body.insert("created_on".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("links") {
        body.insert("links".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("name") {
        body.insert("name".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("issue") {
        body.insert("issue".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("user") {
        body.insert("user".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("message") {
        body.insert("message".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/changes failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the list of all changes that have been made to the specified\nissue. Changes are returned in chronological order with the oldest\nchange first.\n\nEach time an issue is edited in the UI or through the API, an immutable\nchange record is created under the `/issues/123/changes` endpoint. It\nalso has a comment associated with the change.\n\nNote that this operation is changing significantly, due to privacy changes.\nSee the [announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/#changes-to-the-issue-changes-api)\nfor details.\n\n```\n$ curl -s https://api.bitbucket.org/2.0/repositories/evzijst/dogslow/issues/1/changes - | jq .\n\n{\n  \"pagelen\": 20,\n  \"values\": [\n    {\n      \"changes\": {\n        \"priority\": {\n          \"new\": \"trivial\",\n          \"old\": \"major\"\n        },\n        \"assignee\": {\n          \"new\": \"\",\n          \"old\": \"evzijst\"\n        },\n        \"assignee_account_id\": {\n          \"new\": \"\",\n          \"old\": \"557058:c0b72ad0-1cb5-4018-9cdc-0cde8492c443\"\n        },\n        \"kind\": {\n          \"new\": \"enhancement\",\n          \"old\": \"bug\"\n        }\n      },\n      \"links\": {\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/evzijst/dogslow/issues/1/changes/2\"\n        },\n        \"html\": {\n          \"href\": \"https://bitbucket.org/evzijst/dogslow/issues/1#comment-2\"\n        }\n      },\n      \"issue\": {\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/evzijst/dogslow/issues/1\"\n          }\n        },\n        \"type\": \"issue\",\n        \"id\": 1,\n        \"repository\": {\n          \"links\": {\n            \"self\": {\n              \"href\": \"https://api.bitbucket.org/2.0/repositories/evzijst/dogslow\"\n            },\n            \"html\": {\n              \"href\": \"https://bitbucket.org/evzijst/dogslow\"\n            },\n            \"avatar\": {\n              \"href\": \"https://bitbucket.org/evzijst/dogslow/avatar/32/\"\n            }\n          },\n          \"type\": \"repository\",\n          \"name\": \"dogslow\",\n          \"full_name\": \"evzijst/dogslow\",\n          \"uuid\": \"{988b17c6-1a47-4e70-84ee-854d5f012bf6}\"\n        },\n        \"title\": \"Updated title\"\n      },\n      \"created_on\": \"2018-03-03T00:35:28.353630+00:00\",\n      \"user\": {\n        \"username\": \"evzijst\",\n        \"nickname\": \"evzijst\",\n        \"display_name\": \"evzijst\",\n        \"type\": \"user\",\n        \"uuid\": \"{aaa7972b-38af-4fb1-802d-6e3854c95778}\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/users/evzijst\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/evzijst/\"\n          },\n          \"avatar\": {\n            \"href\": \"https://bitbucket.org/account/evzijst/avatar/32/\"\n          }\n        }\n      },\n      \"message\": {\n        \"raw\": \"Removed assignee, changed kind and priority.\",\n        \"markup\": \"markdown\",\n        \"html\": \"<p>Removed assignee, changed kind and priority.</p>\",\n        \"type\": \"rendered\"\n      },\n      \"type\": \"issue_change\",\n      \"id\": 2\n    }\n  ],\n  \"page\": 1\n}\n```\n\nChanges support [filtering and sorting](../../../meta/filtering) that\ncan be used to search for specific changes. For instance, to see\nwhen an issue transitioned to \"resolved\":\n\n```\n$ curl -s https://api.bitbucket.org/2.0/repositories/site/master/issues/1/changes \\\n   -G --data-urlencode='q=changes.state.new = \"resolved\"'\n```\n\nThis resource is only available on repositories that have the issue\ntracker enabled.\n\nN.B.\n\nThe `changes.assignee` and `changes.assignee_account_id` fields are not\na `user` object. Instead, they contain the raw `username` and\n`account_id` of the user. This is to protect the integrity of the audit\nlog even after a user account gets deleted.\n\nThe `changes.assignee` field is deprecated will disappear in the\nfuture. Use `changes.assignee_account_id` instead.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}/changes
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdChangesActor,
    inports::<100>(q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id_changes(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/changes".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/changes failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified issue change object.\n\nThis resource is only available on repositories that have the issue\ntracker enabled.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}/changes/{change_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdChangesByChangeIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id_changes_by_change_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/changes/{change_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/changes/{{change_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new issue comment.\n\n```\n$ curl https://api.bitbucket.org/2.0/repositories/atlassian/prlinks/issues/42/comments/ \\\n  -X POST -u evzijst \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"content\": {\"raw\": \"Lorem ipsum.\"}}'\n```
///
/// Method: POST /repositories/{username}/{repo_slug}/issues/{issue_id}/comments
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugIssuesByIssueIdCommentsActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_issues_by_issue_id_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/comments".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/comments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of all comments that were made on the\nspecified issue.\n\nThe default sorting is oldest to newest and can be overridden with\nthe `sort` query parameter.\n\nThis endpoint also supports filtering and sorting of the results. See\n[filtering and sorting](../../../../../../meta/filtering) for more details.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}/comments
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdCommentsActor,
    inports::<100>(username, repo_slug, q),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/comments".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/comments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Updates the content of the specified issue comment. Note that only\nthe `content.raw` field can be modified.\n\n```\n$ curl https://api.bitbucket.org/2.0/repositories/atlassian/prlinks/issues/42/comments/5728901 \\\n  -X PUT -u evzijst \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"content\": {\"raw\": \"Lorem ipsum.\"}'\n```
///
/// Method: PUT /repositories/{username}/{repo_slug}/issues/{issue_id}/comments/{comment_id}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugIssuesByIssueIdCommentsByCommentIdActor,
    inports::<100>(username, repo_slug, issue_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_issues_by_issue_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/comments/{comment_id}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issue_id") {
        endpoint = endpoint.replace("{{issue_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified issue comment object.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}/comments/{comment_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdCommentsByCommentIdActor,
    inports::<100>(username, repo_slug, issue_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/comments/{comment_id}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issue_id") {
        endpoint = endpoint.replace("{{issue_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes the specified comment.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/issues/{issue_id}/comments/{comment_id}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugIssuesByIssueIdCommentsByCommentIdActor,
    inports::<100>(username, repo_slug, issue_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_issues_by_issue_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/comments/{comment_id}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("issue_id") {
        endpoint = endpoint.replace("{{issue_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retract your vote.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/issues/{issue_id}/vote
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugIssuesByIssueIdVoteActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_issues_by_issue_id_vote(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/vote".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/vote failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Vote for this issue.\n\nTo cast your vote, do an empty PUT. The 204 status code indicates that\nthe operation was successful.
///
/// Method: PUT /repositories/{username}/{repo_slug}/issues/{issue_id}/vote
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugIssuesByIssueIdVoteActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_issues_by_issue_id_vote(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/vote".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/vote failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Check whether the authenticated user has voted for this issue.\nA 204 status code indicates that the user has voted, while a 404\nimplies they haven't.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}/vote
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdVoteActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id_vote(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/vote".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/vote failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Indicated whether or not the authenticated user is watching this\nissue.
///
/// Method: GET /repositories/{username}/{repo_slug}/issues/{issue_id}/watch
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugIssuesByIssueIdWatchActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_issues_by_issue_id_watch(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/watch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/watch failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Start watching this issue.\n\nTo start watching this issue, do an empty PUT. The 204 status code\nindicates that the operation was successful.
///
/// Method: PUT /repositories/{username}/{repo_slug}/issues/{issue_id}/watch
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugIssuesByIssueIdWatchActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_issues_by_issue_id_watch(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/watch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/watch failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Stop watching this issue.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/issues/{issue_id}/watch
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugIssuesByIssueIdWatchActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_issues_by_issue_id_watch(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/issues/{issue_id}/watch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/issues/{{issue_id}}/watch failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the milestones that have been defined in the issue tracker.\n\nThis resource is only available on repositories that have the issue\ntracker enabled.
///
/// Method: GET /repositories/{username}/{repo_slug}/milestones
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugMilestonesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_milestones(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/milestones".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/milestones failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified issue tracker milestone object.
///
/// Method: GET /repositories/{username}/{repo_slug}/milestones/{milestone_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugMilestonesByMilestoneIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_milestones_by_milestone_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/milestones/{milestone_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/milestones/{{milestone_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Produces a raw patch for a single commit (diffed against its first\nparent), or a patch-series for a revspec of 2 commits (e.g.\n`3a8b42..9ff173` where the first commit represents the source and the\nsecond commit the destination).\n\nIn case of the latter (diffing a revspec), a patch series is returned\nfor the commits on the source branch (`3a8b42` and its ancestors in\nour example). For Mercurial, a single patch is returned that combines\nthe changes of all commits on the source branch.\n\nWhile similar to diffs, patches:\n\n* Have a commit header (username, commit message, etc)\n* Do not support the `path=foo/bar.py` query parameter\n\nThe raw patch is returned as-is, in whatever encoding the files in the\nrepository use. It is not decoded into unicode. As such, the\ncontent-type is `text/plain`.
///
/// Method: GET /repositories/{username}/{repo_slug}/patch/{spec}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPatchBySpecActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_patch_by_spec(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/patch/{spec}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/patch/{{spec}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all pull requests on the specified repository.\n\nBy default only open pull requests are returned. This can be controlled\nusing the `state` query parameter. To retrieve pull requests that are\nin one of multiple states, repeat the `state` parameter for each\nindividual state.\n\nThis endpoint also supports filtering and sorting of the results. See\n[filtering and sorting](../../../../meta/filtering) for more details.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsActor,
    inports::<100>(username, repo_slug, state),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/pullrequests".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("state") {
        query_pairs.push(("state", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new pull request where the destination repository is\nthis repository and the author is the authenticated user.\n\nThe minimum required fields to create a pull request are `title` and\n`source`, specified by a branch name.\n\n```\ncurl https://api.bitbucket.org/2.0/repositories/my-username/my-repository/pullrequests \\\n    -u my-username:my-password \\\n    --request POST \\\n    --header 'Content-Type: application/json' \\\n    --data '{\n        \"title\": \"My Title\",\n        \"source\": {\n            \"branch\": {\n                \"name\": \"staging\"\n            }\n        }\n    }'\n```\n\nIf the pull request's `destination` is not specified, it will default\nto the `repository.mainbranch`. To open a pull request to a\ndifferent branch, say from a feature branch to a staging branch,\nspecify a `destination` (same format as the `source`):\n\n```\n{\n    \"title\": \"My Title\",\n    \"source\": {\n        \"branch\": {\n            \"name\": \"my-feature-branch\"\n        }\n    },\n    \"destination\": {\n        \"branch\": {\n            \"name\": \"staging\"\n        }\n    }\n}\n```\n\nReviewers can be specified by adding an array of user objects as the\n`reviewers` property.\n\n```\n{\n    \"title\": \"My Title\",\n    \"source\": {\n        \"branch\": {\n            \"name\": \"my-feature-branch\"\n        }\n    },\n    \"reviewers\": [\n        {\n            \"uuid\": \"{504c3b62-8120-4f0c-a7bc-87800b9d6f70}\"\n        }\n    ]\n}\n```\n\nOther fields:\n\n* `description` - a string\n* `close_source_branch` - boolean that specifies if the source branch should be closed upon merging
///
/// Method: POST /repositories/{username}/{repo_slug}/pullrequests
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugPullrequestsActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_pullrequests(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/pullrequests".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/pullrequests failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of the pull request's activity log.\n\nThis includes comments that were made by the reviewers, updates and\napprovals.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/activity
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsActivityActor,
    inports::<100>(username, repo_slug, pull_request_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_activity(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/pullrequests/activity".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pull_request_id") {
        endpoint = endpoint.replace("{{pull_request_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/activity failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Mutates the specified pull request.\n\nThis can be used to change the pull request's branches or description.\n\nOnly open pull requests can be mutated.
///
/// Method: PUT /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdActor,
    inports::<100>(username, repo_slug, pull_request_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pull_request_id") {
        endpoint = endpoint.replace("{{pull_request_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified pull request.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdActor,
    inports::<100>(username, repo_slug, pull_request_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pull_request_id") {
        endpoint = endpoint.replace("{{pull_request_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of the pull request's activity log.\n\nThis includes comments that were made by the reviewers, updates and\napprovals.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/activity
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdActivityActor,
    inports::<100>(username, repo_slug, pull_request_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_activity(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/activity".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pull_request_id") {
        endpoint = endpoint.replace("{{pull_request_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/activity failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Redact the authenticated user's approval of the specified pull\nrequest.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/approve
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdApproveActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_approve(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/approve".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/approve failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Approve the specified pull request as the authenticated user.
///
/// Method: POST /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/approve
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdApproveActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_approve(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/approve".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/approve failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of the pull request's comments.\n\nThis includes both global, inline comments and replies.\n\nThe default sorting is oldest to newest and can be overridden with\nthe `sort` query parameter.\n\nThis endpoint also supports filtering and sorting of the results. See\n[filtering and sorting](../../../../../../meta/filtering) for more\ndetails.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdCommentsActor,
    inports::<100>(username, repo_slug, pull_request_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pull_request_id") {
        endpoint = endpoint.replace("{{pull_request_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/comments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new pull request comment.\n\nReturns the newly created pull request comment.
///
/// Method: POST /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdCommentsActor,
    inports::<100>(username, repo_slug, pull_request_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pull_request_id") {
        endpoint = endpoint.replace("{{pull_request_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/comments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Updates a specific pull request comment.
///
/// Method: PUT /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments/{comment_id}
#[actor(
    BitbucketUpdateRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdCommentsByCommentIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments/{comment_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a specific pull request comment.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments/{comment_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdCommentsByCommentIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments/{comment_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes a specific pull request comment.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments/{comment_id}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdCommentsByCommentIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/comments/{comment_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of the pull request's commits.\n\nThese are the commits that are being merged into the destination\nbranch when the pull requests gets accepted.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/commits
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdCommitsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_commits(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/commits".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/commits failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Declines the pull request.
///
/// Method: POST /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/decline
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdDeclineActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_decline(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/decline".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/decline failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Redirects to the [repository diff](../../diff/%7Bspec%7D)\nwith the revspec that corresponds to the pull request.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/diff
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdDiffActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_diff(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/diff".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/diff failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Redirects to the [repository diffstat](../../diffstat/%7Bspec%7D)\nwith the revspec that corresponds to the pull request.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/diffstat
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdDiffstatActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_diffstat(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/diffstat".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/diffstat failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Merges the pull request.
///
/// Method: POST /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/merge
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdMergeActor,
    inports::<100>(type_, close_source_branch, merge_strategy, message),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_merge(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/merge".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut body = serde_json::Map::new();
    if let Some(val) = inputs.get("type_") {
        body.insert("type".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("close_source_branch") {
        body.insert("close_source_branch".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("merge_strategy") {
        body.insert("merge_strategy".to_string(), val.clone().into());
    }
    if let Some(val) = inputs.get("message") {
        body.insert("message".to_string(), val.clone().into());
    }
    if !body.is_empty() {
        builder = builder.json(&serde_json::Value::Object(body));
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/merge failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/patch
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdPatchActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_patch(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/patch".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/patch failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all statuses (e.g. build results) for the given pull\nrequest.
///
/// Method: GET /repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/statuses
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugPullrequestsByPullRequestIdStatusesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_pullrequests_by_pull_request_id_statuses(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/pullrequests/{pull_request_id}/statuses".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/pullrequests/{{pull_request_id}}/statuses failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the branches and tags in the repository.\n\nBy default, results will be in the order the underlying source control system returns them and identical to\nthe ordering one sees when running \"$ git show-ref\". Note that this follows simple\nlexical ordering of the ref names.\n\nThis can be undesirable as it does apply any natural sorting semantics, meaning for instance that refs are\nsorted [\"branch1\", \"branch10\", \"branch2\", \"v10\", \"v11\", \"v9\"] instead of [\"branch1\", \"branch2\",\n\"branch10\", \"v9\", \"v10\", \"v11\"].\n\nSorting can be changed using the ?sort= query parameter. When using ?sort=name to explicitly sort on ref name,\nBitbucket will apply natural sorting and interpret numerical values as numbers instead of strings.
///
/// Method: GET /repositories/{username}/{repo_slug}/refs
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugRefsActor,
    inports::<100>(username, repo_slug, q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_refs(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/refs".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/refs failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a list of all open branches within the specified repository.\nResults will be in the order the source control manager returns them.\n\n```\n$ curl -s https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/refs/branches | jq .\n{\n  \"pagelen\": 10,\n  \"values\": [\n    {\n      \"heads\": [\n        {\n          \"hash\": \"f1a0933ce59e809f190602655e22ae6ec107c397\",\n          \"type\": \"commit\",\n          \"links\": {\n            \"self\": {\n              \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397\"\n            },\n            \"html\": {\n              \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/f1a0933ce59e809f190602655e22ae6ec107c397\"\n            }\n          }\n        }\n      ],\n      \"type\": \"named_branch\",\n      \"name\": \"default\",\n      \"links\": {\n        \"commits\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commits/default\"\n        },\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/refs/branches/default\"\n        },\n        \"html\": {\n          \"href\": \"https://bitbucket.org/seanfarley/mercurial/branch/default\"\n        }\n      },\n      \"target\": {\n        \"hash\": \"f1a0933ce59e809f190602655e22ae6ec107c397\",\n        \"repository\": {\n          \"links\": {\n            \"self\": {\n              \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial\"\n            },\n            \"html\": {\n              \"href\": \"https://bitbucket.org/seanfarley/mercurial\"\n            },\n            \"avatar\": {\n              \"href\": \"https://bitbucket.org/seanfarley/mercurial/avatar/32/\"\n            }\n          },\n          \"type\": \"repository\",\n          \"name\": \"mercurial\",\n          \"full_name\": \"seanfarley/mercurial\",\n          \"uuid\": \"{73dcbd61-e506-4fc1-9ecd-31be2b6d6031}\"\n        },\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397\"\n          },\n          \"comments\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397/comments\"\n          },\n          \"patch\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/patch/f1a0933ce59e809f190602655e22ae6ec107c397\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/f1a0933ce59e809f190602655e22ae6ec107c397\"\n          },\n          \"diff\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/diff/f1a0933ce59e809f190602655e22ae6ec107c397\"\n          },\n          \"approve\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397/approve\"\n          },\n          \"statuses\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397/statuses\"\n          }\n        },\n        \"author\": {\n          \"raw\": \"Martin von Zweigbergk <martinvonz@google.com>\",\n          \"type\": \"author\",\n          \"user\": {\n            \"username\": \"martinvonz\",\n            \"nickname\": \"martinvonz\",\n            \"display_name\": \"Martin von Zweigbergk\",\n            \"type\": \"user\",\n            \"uuid\": \"{fdb0e657-3ade-4fad-a136-95f1ffe4a5ac}\",\n            \"links\": {\n              \"self\": {\n                \"href\": \"https://api.bitbucket.org/2.0/users/martinvonz\"\n              },\n              \"html\": {\n                \"href\": \"https://bitbucket.org/martinvonz/\"\n              },\n              \"avatar\": {\n                \"href\": \"https://bitbucket.org/account/martinvonz/avatar/32/\"\n              }\n            }\n          }\n        },\n        \"parents\": [\n          {\n            \"hash\": \"5523aabb85c30ebc2b8e29aadcaf5e13fa92b375\",\n            \"type\": \"commit\",\n            \"links\": {\n              \"self\": {\n                \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/5523aabb85c30ebc2b8e29aadcaf5e13fa92b375\"\n              },\n              \"html\": {\n                \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/5523aabb85c30ebc2b8e29aadcaf5e13fa92b375\"\n              }\n            }\n          }\n        ],\n        \"date\": \"2018-02-01T18:44:49+00:00\",\n        \"message\": \"config: replace a for-else by any()\",\n        \"type\": \"commit\"\n      }\n    },\n    {\n      \"heads\": [\n        {\n          \"hash\": \"1d60ad093792706e1dc7a52b20942593f2c19655\",\n          \"type\": \"commit\",\n          \"links\": {\n            \"self\": {\n              \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/1d60ad093792706e1dc7a52b20942593f2c19655\"\n            },\n            \"html\": {\n              \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/1d60ad093792706e1dc7a52b20942593f2c19655\"\n            }\n          }\n        }\n      ],\n      \"type\": \"named_branch\",\n      \"name\": \"stable\",\n      \"links\": {\n        \"commits\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commits/stable\"\n        },\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/refs/branches/stable\"\n        },\n        \"html\": {\n          \"href\": \"https://bitbucket.org/seanfarley/mercurial/branch/stable\"\n        }\n      },\n      \"target\": {\n        \"hash\": \"1d60ad093792706e1dc7a52b20942593f2c19655\",\n        \"repository\": {\n          \"links\": {\n            \"self\": {\n              \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial\"\n            },\n            \"html\": {\n              \"href\": \"https://bitbucket.org/seanfarley/mercurial\"\n            },\n            \"avatar\": {\n              \"href\": \"https://bitbucket.org/seanfarley/mercurial/avatar/32/\"\n            }\n          },\n          \"type\": \"repository\",\n          \"name\": \"mercurial\",\n          \"full_name\": \"seanfarley/mercurial\",\n          \"uuid\": \"{73dcbd61-e506-4fc1-9ecd-31be2b6d6031}\"\n        },\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/1d60ad093792706e1dc7a52b20942593f2c19655\"\n          },\n          \"comments\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/1d60ad093792706e1dc7a52b20942593f2c19655/comments\"\n          },\n          \"patch\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/patch/1d60ad093792706e1dc7a52b20942593f2c19655\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/1d60ad093792706e1dc7a52b20942593f2c19655\"\n          },\n          \"diff\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/diff/1d60ad093792706e1dc7a52b20942593f2c19655\"\n          },\n          \"approve\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/1d60ad093792706e1dc7a52b20942593f2c19655/approve\"\n          },\n          \"statuses\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/1d60ad093792706e1dc7a52b20942593f2c19655/statuses\"\n          }\n        },\n        \"author\": {\n          \"raw\": \"Augie Fackler <raf@durin42.com>\",\n          \"type\": \"author\",\n          \"user\": {\n            \"username\": \"durin42\",\n            \"nickname\": \"durin42\",\n            \"display_name\": \"Augie Fackler\",\n            \"type\": \"user\",\n            \"uuid\": \"{e07dc61f-bb05-4218-b43a-d991f26be65a}\",\n            \"links\": {\n              \"self\": {\n                \"href\": \"https://api.bitbucket.org/2.0/users/durin42\"\n              },\n              \"html\": {\n                \"href\": \"https://bitbucket.org/durin42/\"\n              },\n              \"avatar\": {\n                \"href\": \"https://bitbucket.org/account/durin42/avatar/32/\"\n              }\n            }\n          }\n        },\n        \"parents\": [\n          {\n            \"hash\": \"56a0da11bde519d79168e890df4bcf0da62f0a7b\",\n            \"type\": \"commit\",\n            \"links\": {\n              \"self\": {\n                \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/56a0da11bde519d79168e890df4bcf0da62f0a7b\"\n              },\n              \"html\": {\n                \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/56a0da11bde519d79168e890df4bcf0da62f0a7b\"\n              }\n            }\n          }\n        ],\n        \"date\": \"2018-02-01T19:13:41+00:00\",\n        \"message\": \"Added signature for changeset d334afc585e2\",\n        \"type\": \"commit\"\n      }\n    }\n  ],\n  \"page\": 1,\n  \"size\": 2\n}\n```\n\nBranches support [filtering and sorting](../../../../../meta/filtering)\nthat can be used to search for specific branches. For instance, to find\nall branches that have \"stab\" in their name:\n\n```\ncurl -s https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/refs/branches -G --data-urlencode 'q=name ~ \"stab\"'\n```\n\nBy default, results will be in the order the underlying source control system returns them and identical to\nthe ordering one sees when running \"$ hg branches\" or \"$ git branch --list\". Note that this follows simple\nlexical ordering of the ref names.\n\nThis can be undesirable as it does apply any natural sorting semantics, meaning for instance that tags are\nsorted [\"v10\", \"v11\", \"v9\"] instead of [\"v9\", \"v10\", \"v11\"].\n\nSorting can be changed using the ?q= query parameter. When using ?q=name to explicitly sort on ref name,\nBitbucket will apply natural sorting and interpret numerical values as numbers instead of strings.
///
/// Method: GET /repositories/{username}/{repo_slug}/refs/branches
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugRefsBranchesActor,
    inports::<100>(username, repo_slug, q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_refs_branches(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/refs/branches".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/refs/branches failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new branch in the specified repository.\n\nThe payload of the POST should consist of a JSON document that\ncontains the name of the tag and the target hash.\n\n```\ncurl https://api.bitbucket.org/2.0/repositories/seanfarley/hg/refs/branches \\\n-s -u seanfarley -X POST -H \"Content-Type: application/json\" \\\n-d '{\n    \"name\" : \"smf/create-feature\",\n    \"target\" : {\n        \"hash\" : \"default\",\n    }\n}'\n```\n\nThis call requires authentication. Private repositories require the\ncaller to authenticate with an account that has appropriate\nauthorization.\n\nFor Git, the branch name should not include any prefixes (e.g.\nrefs/heads). This endpoint does support using short hash prefixes for\nthe commit hash, but it may return a 400 response if the provided\nprefix is ambiguous. Using a full commit hash is the preferred\napproach.\n\nFor Mercurial, the authenticated user making this call is the author of\nthe new branch commit and the date is current datetime of the call.
///
/// Method: POST /repositories/{username}/{repo_slug}/refs/branches
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugRefsBranchesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_refs_branches(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/refs/branches".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/refs/branches failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete a branch in the specified repository.\n\nThe main branch is not allowed to be deleted and will return a 400\nresponse.\n\nFor Git, the branch name should not include any prefixes (e.g.\nrefs/heads). For Mercurial, this closes all open heads on the branch,\nsets the author of the commit to the authenticated caller, and changes\nthe date to the datetime of the call.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/refs/branches/{name}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugRefsBranchesByNameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_refs_branches_by_name(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/refs/branches/{name}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/refs/branches/{{name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a branch object within the specified repository.\n\n```\n$ curl -s https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/refs/branches/default | jq .\n{\n  \"heads\": [\n    {\n      \"hash\": \"f1a0933ce59e809f190602655e22ae6ec107c397\",\n      \"type\": \"commit\",\n      \"links\": {\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397\"\n        },\n        \"html\": {\n          \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/f1a0933ce59e809f190602655e22ae6ec107c397\"\n        }\n      }\n    }\n  ],\n  \"type\": \"named_branch\",\n  \"name\": \"default\",\n  \"links\": {\n    \"commits\": {\n      \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commits/default\"\n    },\n    \"self\": {\n      \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/refs/branches/default\"\n    },\n    \"html\": {\n      \"href\": \"https://bitbucket.org/seanfarley/mercurial/branch/default\"\n    }\n  },\n  \"target\": {\n    \"hash\": \"f1a0933ce59e809f190602655e22ae6ec107c397\",\n    \"repository\": {\n      \"links\": {\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial\"\n        },\n        \"html\": {\n          \"href\": \"https://bitbucket.org/seanfarley/mercurial\"\n        },\n        \"avatar\": {\n          \"href\": \"https://bitbucket.org/seanfarley/mercurial/avatar/32/\"\n        }\n      },\n      \"type\": \"repository\",\n      \"name\": \"mercurial\",\n      \"full_name\": \"seanfarley/mercurial\",\n      \"uuid\": \"{73dcbd61-e506-4fc1-9ecd-31be2b6d6031}\"\n    },\n    \"links\": {\n      \"self\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397\"\n      },\n      \"comments\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397/comments\"\n      },\n      \"patch\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/patch/f1a0933ce59e809f190602655e22ae6ec107c397\"\n      },\n      \"html\": {\n        \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/f1a0933ce59e809f190602655e22ae6ec107c397\"\n      },\n      \"diff\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/diff/f1a0933ce59e809f190602655e22ae6ec107c397\"\n      },\n      \"approve\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397/approve\"\n      },\n      \"statuses\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/f1a0933ce59e809f190602655e22ae6ec107c397/statuses\"\n      }\n    },\n    \"author\": {\n      \"raw\": \"Martin von Zweigbergk <martinvonz@google.com>\",\n      \"type\": \"author\",\n      \"user\": {\n        \"username\": \"martinvonz\",\n        \"nickname\": \"martinvonz\",\n        \"display_name\": \"Martin von Zweigbergk\",\n        \"type\": \"user\",\n        \"uuid\": \"{fdb0e657-3ade-4fad-a136-95f1ffe4a5ac}\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/users/martinvonz\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/martinvonz/\"\n          },\n          \"avatar\": {\n            \"href\": \"https://bitbucket.org/account/martinvonz/avatar/32/\"\n          }\n        }\n      }\n    },\n    \"parents\": [\n      {\n        \"hash\": \"5523aabb85c30ebc2b8e29aadcaf5e13fa92b375\",\n        \"type\": \"commit\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/mercurial/commit/5523aabb85c30ebc2b8e29aadcaf5e13fa92b375\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/seanfarley/mercurial/commits/5523aabb85c30ebc2b8e29aadcaf5e13fa92b375\"\n          }\n        }\n      }\n    ],\n    \"date\": \"2018-02-01T18:44:49+00:00\",\n    \"message\": \"config: replace a for-else by any()\",\n    \"type\": \"commit\"\n  }\n}\n```\n\nThis call requires authentication. Private repositories require the\ncaller to authenticate with an account that has appropriate\nauthorization.\n\nFor Git, the branch name should not include any prefixes (e.g.\nrefs/heads).\n\nFor Mercurial, the response will include an additional field that lists\nthe open heads.
///
/// Method: GET /repositories/{username}/{repo_slug}/refs/branches/{name}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugRefsBranchesByNameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_refs_branches_by_name(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/refs/branches/{name}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/refs/branches/{{name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the tags in the repository.\n\nBy default, results will be in the order the underlying source control system returns them and identical to\nthe ordering one sees when running \"$ hg tags\" or \"$ git tag --list\". Note that this follows simple\nlexical ordering of the ref names.\n\nThis can be undesirable as it does apply any natural sorting semantics, meaning for instance that tags are\nsorted [\"v10\", \"v11\", \"v9\"] instead of [\"v9\", \"v10\", \"v11\"].\n\nSorting can be changed using the ?sort= query parameter. When using ?sort=name to explicitly sort on ref name,\nBitbucket will apply natural sorting and interpret numerical values as numbers instead of strings.
///
/// Method: GET /repositories/{username}/{repo_slug}/refs/tags
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugRefsTagsActor,
    inports::<100>(username, repo_slug, q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_refs_tags(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{username}/{repo_slug}/refs/tags".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/refs/tags failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new tag in the specified repository.\n\nThe payload of the POST should consist of a JSON document that\ncontains the name of the tag and the target hash.\n\n```\ncurl https://api.bitbucket.org/2.0/repositories/jdoe/myrepo/refs/tags \\\n-s -u jdoe -X POST -H \"Content-Type: application/json\" \\\n-d '{\n    \"name\" : \"new-tag-name\",\n    \"target\" : {\n        \"hash\" : \"a1b2c3d4e5f6\",\n    }\n}'\n```\n\nThis endpoint does support using short hash prefixes for the commit\nhash, but it may return a 400 response if the provided prefix is\nambiguous. Using a full commit hash is the preferred approach.
///
/// Method: POST /repositories/{username}/{repo_slug}/refs/tags
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugRefsTagsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_refs_tags(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/refs/tags".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/refs/tags failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete a tag in the specified repository.\n\nFor Git, the tag name should not include any prefixes (e.g. refs/tags).\nFor Mercurial, this adds a commit to the main branch that removes the\nspecified tag.
///
/// Method: DELETE /repositories/{username}/{repo_slug}/refs/tags/{name}
#[actor(
    BitbucketDeleteRepositoriesByUsernameByRepoSlugRefsTagsByNameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repositories_by_username_by_repo_slug_refs_tags_by_name(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/refs/tags/{name}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{username}}/{{repo_slug}}/refs/tags/{{name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified tag.\n\n```\n$ curl -s https://api.bitbucket.org/2.0/repositories/seanfarley/hg/refs/tags/3.8 -G | jq .\n{\n  \"name\": \"3.8\",\n  \"links\": {\n    \"commits\": {\n      \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/commits/3.8\"\n    },\n    \"self\": {\n      \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/refs/tags/3.8\"\n    },\n    \"html\": {\n      \"href\": \"https://bitbucket.org/seanfarley/hg/commits/tag/3.8\"\n    }\n  },\n  \"tagger\": {\n    \"raw\": \"Matt Mackall <mpm@selenic.com>\",\n    \"type\": \"author\",\n    \"user\": {\n      \"username\": \"mpmselenic\",\n      \"nickname\": \"mpmselenic\",\n      \"display_name\": \"Matt Mackall\",\n      \"type\": \"user\",\n      \"uuid\": \"{a4934530-db4c-419c-a478-9ab4964c2ee7}\",\n      \"links\": {\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/users/mpmselenic\"\n        },\n        \"html\": {\n          \"href\": \"https://bitbucket.org/mpmselenic/\"\n        },\n        \"avatar\": {\n          \"href\": \"https://bitbucket.org/account/mpmselenic/avatar/32/\"\n        }\n      }\n    }\n  },\n  \"date\": \"2016-05-01T18:52:25+00:00\",\n  \"message\": \"Added tag 3.8 for changeset f85de28eae32\",\n  \"type\": \"tag\",\n  \"target\": {\n    \"hash\": \"f85de28eae32e7d3064b1a1321309071bbaaa069\",\n    \"repository\": {\n      \"links\": {\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg\"\n        },\n        \"html\": {\n          \"href\": \"https://bitbucket.org/seanfarley/hg\"\n        },\n        \"avatar\": {\n          \"href\": \"https://bitbucket.org/seanfarley/hg/avatar/32/\"\n        }\n      },\n      \"type\": \"repository\",\n      \"name\": \"hg\",\n      \"full_name\": \"seanfarley/hg\",\n      \"uuid\": \"{c75687fb-e99d-4579-9087-190dbd406d30}\"\n    },\n    \"links\": {\n      \"self\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/commit/f85de28eae32e7d3064b1a1321309071bbaaa069\"\n      },\n      \"comments\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/commit/f85de28eae32e7d3064b1a1321309071bbaaa069/comments\"\n      },\n      \"patch\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/patch/f85de28eae32e7d3064b1a1321309071bbaaa069\"\n      },\n      \"html\": {\n        \"href\": \"https://bitbucket.org/seanfarley/hg/commits/f85de28eae32e7d3064b1a1321309071bbaaa069\"\n      },\n      \"diff\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/diff/f85de28eae32e7d3064b1a1321309071bbaaa069\"\n      },\n      \"approve\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/commit/f85de28eae32e7d3064b1a1321309071bbaaa069/approve\"\n      },\n      \"statuses\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/commit/f85de28eae32e7d3064b1a1321309071bbaaa069/statuses\"\n      }\n    },\n    \"author\": {\n      \"raw\": \"Sean Farley <sean@farley.io>\",\n      \"type\": \"author\",\n      \"user\": {\n        \"username\": \"seanfarley\",\n        \"nickname\": \"seanfarley\",\n        \"display_name\": \"Sean Farley\",\n        \"type\": \"user\",\n        \"uuid\": \"{a295f8a8-5876-4d43-89b5-3ad8c6c3c51d}\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/users/seanfarley\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/seanfarley/\"\n          },\n          \"avatar\": {\n            \"href\": \"https://bitbucket.org/account/seanfarley/avatar/32/\"\n          }\n        }\n      }\n    },\n    \"parents\": [\n      {\n        \"hash\": \"9a98d0e5b07fc60887f9d3d34d9ac7d536f470d2\",\n        \"type\": \"commit\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/seanfarley/hg/commit/9a98d0e5b07fc60887f9d3d34d9ac7d536f470d2\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/seanfarley/hg/commits/9a98d0e5b07fc60887f9d3d34d9ac7d536f470d2\"\n          }\n        }\n      }\n    ],\n    \"date\": \"2016-05-01T04:21:17+00:00\",\n    \"message\": \"debian: alphabetize build deps\",\n    \"type\": \"commit\"\n  }\n}\n```
///
/// Method: GET /repositories/{username}/{repo_slug}/refs/tags/{name}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugRefsTagsByNameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_refs_tags_by_name(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/refs/tags/{name}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/refs/tags/{{name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// This endpoint redirects the client to the directory listing of the\nroot directory on the main branch.\n\nThis is equivalent to directly hitting\n[/2.0/repositories/{username}/{repo_slug}/src/{node}/{path}](src/%7Bnode%7D/%7Bpath%7D)\nwithout having to know the name or SHA1 of the repo's main branch.\n\nTo create new commits, [POST to this endpoint](#post)
///
/// Method: GET /repositories/{username}/{repo_slug}/src
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugSrcActor,
    inports::<100>(format),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_src(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/src".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("format") {
        query_pairs.push(("format", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/src failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// This endpoint is used to create new commits in the repository by\nuploading files.\n\nTo add a new file to a repository:\n\n```\n$ curl https://api.bitbucket.org/2.0/repositories/username/slug/src \\\n  -F /repo/path/to/image.png=@image.png\n```\n\nThis will create a new commit on top of the main branch, inheriting the\ncontents of the main branch, but adding (or overwriting) the\n`image.png` file to the repository in the `/repo/path/to` directory.\n\nTo create a commit that deletes files, use the `files` parameter:\n\n```\n$ curl https://api.bitbucket.org/2.0/repositories/username/slug/src \\\n  -F files=/file/to/delete/1.txt \\\n  -F files=/file/to/delete/2.txt\n```\n\nYou can add/modify/delete multiple files in a request. Rename/move a\nfile by deleting the old path and adding the content at the new path.\n\nThis endpoint accepts `multipart/form-data` (as in the examples above),\nas well as `application/x-www-form-urlencoded`.\n\n## multipart/form-data\n\nA `multipart/form-data` post contains a series of \"form fields\" that\nidentify both the individual files that are being uploaded, as well as\nadditional, optional meta data.\n\nFiles are uploaded in file form fields (those that have a\n`Content-Disposition` parameter) whose field names point to the remote\npath in the repository where the file should be stored. Path field\nnames are always interpreted to be absolute from the root of the\nrepository, regardless whether the client uses a leading slash (as the\nabove `curl` example did).\n\nFile contents are treated as bytes and are not decoded as text.\n\nThe commit message, as well as other non-file meta data for the\nrequest, is sent along as normal form field elements. Meta data fields\nshare the same namespace as the file objects. For `multipart/form-data`\nbodies that should not lead to any ambiguity, as the\n`Content-Disposition` header will contain the `filename` parameter to\ndistinguish between a file named \"message\" and the commit message field.\n\n## application/x-www-form-urlencoded\n\nIt is also possible to upload new files using a simple\n`application/x-www-form-urlencoded` POST. This can be convenient when\nuploading pure text files:\n\n```\n$ curl https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src \\\n  --data-urlencode \"/path/to/me.txt=Lorem ipsum.\" \\\n  --data-urlencode \"message=Initial commit\" \\\n  --data-urlencode \"author=Erik van Zijst <erik.van.zijst@gmail.com>\"\n```\n\nThere could be a field name clash if a client were to upload a file\nnamed \"message\", as this filename clashes with the meta data property\nfor the commit message. To avoid this and to upload files whose names\nclash with the meta data properties, use a leading slash for the files,\ne.g. `curl --data-urlencode \"/message=file contents\"`.\n\nWhen an explicit slash is omitted for a file whose path matches that of\na meta data parameter, then it is interpreted as meta data, not as a\nfile.\n\n## Executables and links\n\nWhile this API aims to facilitate the most common use cases, it is\npossible to perform some more advanced operations like creating a new\nsymlink in the repository, or creating an executable file.\n\nFiles can be supplied with a `x-attributes` value in the\n`Content-Disposition` header. For example, to upload an executable\nfile, as well as create a symlink from `README.txt` to `README`:\n\n```\n--===============1438169132528273974==\nContent-Type: text/plain; charset=\"us-ascii\"\nMIME-Version: 1.0\nContent-Transfer-Encoding: 7bit\nContent-ID: \"bin/shutdown.sh\"\nContent-Disposition: attachment; filename=\"shutdown.sh\"; x-attributes:\"executable\"\n\n#!/bin/sh\nhalt\n\n--===============1438169132528273974==\nContent-Type: text/plain; charset=\"us-ascii\"\nMIME-Version: 1.0\nContent-Transfer-Encoding: 7bit\nContent-ID: \"/README.txt\"\nContent-Disposition: attachment; filename=\"README.txt\"; x-attributes:\"link\"\n\nREADME\n--===============1438169132528273974==--\n```\n\nLinks are files that contain the target path and have\n`x-attributes:\"link\"` set.\n\nWhen overwriting links with files, or vice versa, the newly uploaded\nfile determines both the new contents, as well as the attributes. That\nmeans uploading a file without specifying `x-attributes=\"link\"` will\ncreate a regular file, even if the parent commit hosted a symlink at\nthe same path.\n\nThe same applies to executables. When modifying an existing executable\nfile, the form-data file element must include\n`x-attributes=\"executable\"` in order to preserve the executable status\nof the file.\n\nNote that this API does not support the creation or manipulation of\nsubrepos / submodules.
///
/// Method: POST /repositories/{username}/{repo_slug}/src
#[actor(
    BitbucketCreateRepositoriesByUsernameByRepoSlugSrcActor,
    inports::<100>(message, author, parents, files, branch),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repositories_by_username_by_repo_slug_src(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/src".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("message") {
        query_pairs.push(("message", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("author") {
        query_pairs.push(("author", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("parents") {
        query_pairs.push(("parents", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("files") {
        query_pairs.push(("files", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("files") {
        query_pairs.push(("files", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("branch") {
        query_pairs.push(("branch", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{username}}/{{repo_slug}}/src failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// This endpoints is used to retrieve the contents of a single file,\nor the contents of a directory at a specified revision.\n\n## Raw file contents\n\nWhen `path` points to a file, this endpoint returns the raw contents.\nThe response's Content-Type is derived from the filename\nextension (not from the contents). The file contents are not processed\nand no character encoding/recoding is performed and as a result no\ncharacter encoding is included as part of the Content-Type.\n\nThe `Content-Disposition` header will be \"attachment\" to prevent\nbrowsers from running executable files.\n\nIf the file is managed by LFS, then a 301 redirect pointing to\nAtlassian's media services platform is returned.\n\nThe response includes an ETag that is based on the contents of the file\nand its attributes. This means that an empty `__init__.py` always\nreturns the same ETag, regardless on the directory it lives in, or the\ncommit it is on.\n\n## File meta data\n\nWhen the request for a file path includes the query parameter\n`?format=meta`, instead of returning the file's raw contents, Bitbucket\ninstead returns the JSON object describing the file's properties:\n\n```javascript\n$ curl https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef/tests/__init__.py?format=meta\n{\n  \"links\": {\n    \"self\": {\n      \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef5d3df01aed629f650959d6706d54cd335/tests/__init__.py\"\n    },\n    \"meta\": {\n      \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef5d3df01aed629f650959d6706d54cd335/tests/__init__.py?format=meta\"\n    }\n  },\n  \"path\": \"tests/__init__.py\",\n  \"commit\": {\n    \"type\": \"commit\",\n    \"hash\": \"eefd5ef5d3df01aed629f650959d6706d54cd335\",\n    \"links\": {\n      \"self\": {\n        \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/commit/eefd5ef5d3df01aed629f650959d6706d54cd335\"\n      },\n      \"html\": {\n        \"href\": \"https://bitbucket.org/atlassian/bbql/commits/eefd5ef5d3df01aed629f650959d6706d54cd335\"\n      }\n    }\n  },\n  \"attributes\": [],\n  \"type\": \"commit_file\",\n  \"size\": 0\n}\n```\n\nFile objects contain an `attributes` element that contains a list of\npossible modifiers. Currently defined values are:\n\n* `link` -- indicates that the entry is a symbolic link. The contents\n    of the file represent the path the link points to.\n* `executable` -- indicates that the file has the executable bit set.\n* `subrepository` -- indicates that the entry points to a submodule or\n    subrepo. The contents of the file is the SHA1 of the repository\n    pointed to.\n* `binary` -- indicates whether Bitbucket thinks the file is binary.\n\nThis endpoint can provide an alternative to how a HEAD request can be\nused to check for the existence of a file, or a file's size without\nincurring the overhead of receiving its full contents.\n\n\n## Directory listings\n\nWhen `path` points to a directory instead of a file, the response is a\npaginated list of directory and file objects in the same order as the\nunderlying SCM system would return them.\n\nFor example:\n\n```javascript\n$ curl https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef/tests\n{\n  \"pagelen\": 10,\n  \"values\": [\n    {\n      \"path\": \"tests/test_project\",\n      \"type\": \"commit_directory\",\n      \"links\": {\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef5d3df01aed629f650959d6706d54cd335/tests/test_project/\"\n        },\n        \"meta\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef5d3df01aed629f650959d6706d54cd335/tests/test_project/?format=meta\"\n        }\n      },\n      \"commit\": {\n        \"type\": \"commit\",\n        \"hash\": \"eefd5ef5d3df01aed629f650959d6706d54cd335\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/commit/eefd5ef5d3df01aed629f650959d6706d54cd335\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/atlassian/bbql/commits/eefd5ef5d3df01aed629f650959d6706d54cd335\"\n          }\n        }\n      }\n    },\n    {\n      \"links\": {\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef5d3df01aed629f650959d6706d54cd335/tests/__init__.py\"\n        },\n        \"meta\": {\n          \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef5d3df01aed629f650959d6706d54cd335/tests/__init__.py?format=meta\"\n        }\n      },\n      \"path\": \"tests/__init__.py\",\n      \"commit\": {\n        \"type\": \"commit\",\n        \"hash\": \"eefd5ef5d3df01aed629f650959d6706d54cd335\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/commit/eefd5ef5d3df01aed629f650959d6706d54cd335\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/atlassian/bbql/commits/eefd5ef5d3df01aed629f650959d6706d54cd335\"\n          }\n        }\n      },\n      \"attributes\": [],\n      \"type\": \"commit_file\",\n      \"size\": 0\n    }\n  ],\n  \"page\": 1,\n  \"size\": 2\n}\n```\n\nWhen listing the contents of the repo's root directory, the use of a\ntrailing slash at the end of the URL is required.\n\nThe response by default is not recursive, meaning that only the direct contents of\na path are returned. The response does not recurse down into\nsubdirectories. In order to \"walk\" the entire directory tree, the\nclient can either parse each response and follow the `self` links of each\n`commit_directory` object, or can specify a `max_depth` to recurse to.\n\nThe max_depth parameter will do a breadth-first search to return the contents of the subdirectories\nup to the depth specified. Breadth-first search was chosen as it leads to the least amount of\nfile system operations for git. If the `max_depth` parameter is specified to be too\nlarge, the call will time out and return a 555.\n\nEach returned object is either a `commit_file`, or a `commit_directory`,\nboth of which contain a `path` element. This path is the absolute path\nfrom the root of the repository. Each object also contains a `commit`\nobject which embeds the commit the file is on. Note that this is merely\nthe commit that was used in the URL. It is *not* the commit that last\nmodified the file.\n\nDirectory objects have 2 representations. Their `self` link returns the\npaginated contents of the directory. The `meta` link on the other hand\nreturns the actual `directory` object itself, e.g.:\n\n```javascript\n{\n  \"path\": \"tests/test_project\",\n  \"type\": \"commit_directory\",\n  \"links\": {\n    \"self\": {\n      \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef5d3df01aed629f650959d6706d54cd335/tests/test_project/\"\n    },\n    \"meta\": {\n      \"href\": \"https://api.bitbucket.org/2.0/repositories/atlassian/bbql/src/eefd5ef5d3df01aed629f650959d6706d54cd335/tests/test_project/?format=meta\"\n    }\n  },\n  \"commit\": { ... }\n}\n```\n\n## Querying, filtering and sorting\n\nLike most API endpoints, this API supports the Bitbucket\nquerying/filtering syntax and so you could filter a directory listing\nto only include entries that match certain criteria. For instance, to\nlist all binary files over 1kb use the expression:\n\n`size > 1024 and attributes = \"binary\"`\n\nwhich after urlencoding yields the query string:\n\n`?q=size%3E1024+and+attributes%3D%22binary%22`\n\nTo change the ordering of the response, use the `?sort` parameter:\n\n`.../src/eefd5ef/?sort=-size`\n\nSee [filtering and sorting](../../../../../../meta/filtering) for more\ndetails.
///
/// Method: GET /repositories/{username}/{repo_slug}/src/{node}/{path}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugSrcByNodeByPathActor,
    inports::<100>(format, q, sort, max_depth),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_src_by_node_by_path(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/src/{node}/{path}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("format") {
        query_pairs.push(("format", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("max_depth") {
        query_pairs.push(("max_depth", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/src/{{node}}/{{path}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the versions that have been defined in the issue tracker.\n\nThis resource is only available on repositories that have the issue\ntracker enabled.
///
/// Method: GET /repositories/{username}/{repo_slug}/versions
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugVersionsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_versions(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/versions".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/versions failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specified issue tracker version object.
///
/// Method: GET /repositories/{username}/{repo_slug}/versions/{version_id}
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugVersionsByVersionIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_versions_by_version_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/versions/{version_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/versions/{{version_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of all the watchers on the specified\nrepository.
///
/// Method: GET /repositories/{username}/{repo_slug}/watchers
#[actor(
    BitbucketReadRepositoriesByUsernameByRepoSlugWatchersActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_username_by_repo_slug_watchers(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{username}/{repo_slug}/watchers".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{username}}/{{repo_slug}}/watchers failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve an application property value stored against a commit.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/commit/{commit}/properties/{app_key}/{property_name}
#[actor(
    BitbucketReadCommitHostedPropertyValueActor,
    inports::<100>(username, repo_slug, commit, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_commit_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/commit/{commit}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("commit") {
        endpoint = endpoint.replace("{{commit}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/commit/{{commit}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete an application property value stored against a commit.
///
/// Method: DELETE /repositories/{workspace}/{repo_slug}/commit/{commit}/properties/{app_key}/{property_name}
#[actor(
    BitbucketDeleteCommitHostedPropertyValueActor,
    inports::<100>(username, repo_slug, commit, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_commit_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/commit/{commit}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("commit") {
        endpoint = endpoint.replace("{{commit}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{workspace}}/{{repo_slug}}/commit/{{commit}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update an application property value stored against a commit.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/commit/{commit}/properties/{app_key}/{property_name}
#[actor(
    BitbucketUpdateCommitHostedPropertyValueActor,
    inports::<100>(username, repo_slug, commit, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_commit_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/commit/{commit}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("commit") {
        endpoint = endpoint.replace("{{commit}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/commit/{{commit}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of all pull requests as part of which this commit was reviewed.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/commit/{commit}/pullrequests
#[actor(
    BitbucketReadPullrequestsForCommitActor,
    inports::<100>(username, repo_slug, commit, page, pagelen),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pullrequests_for_commit(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/commit/{commit}/pullrequests".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("commit") {
        endpoint = endpoint.replace("{{commit}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("page") {
        query_pairs.push(("page", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("pagelen") {
        query_pairs.push(("pagelen", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/commit/{{commit}}/pullrequests failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Find deployments
///
/// Method: GET /repositories/{workspace}/{repo_slug}/deployments/
#[actor(
    BitbucketReadDeploymentsForRepositoryActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_deployments_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/deployments/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/deployments/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve a deployment
///
/// Method: GET /repositories/{workspace}/{repo_slug}/deployments/{deployment_uuid}
#[actor(
    BitbucketReadDeploymentForRepositoryActor,
    inports::<100>(username, repo_slug, deployment_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_deployment_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/deployments/{deployment_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("deployment_uuid") {
        endpoint = endpoint.replace("{{deployment_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/deployments/{{deployment_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Find environments
///
/// Method: GET /repositories/{workspace}/{repo_slug}/environments/
#[actor(
    BitbucketReadEnvironmentsForRepositoryActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_environments_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/environments/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/environments/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create an environment.
///
/// Method: POST /repositories/{workspace}/{repo_slug}/environments/
#[actor(
    BitbucketCreateEnvironmentActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_environment(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/environments/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{workspace}}/{{repo_slug}}/environments/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete an environment
///
/// Method: DELETE /repositories/{workspace}/{repo_slug}/environments/{environment_uuid}
#[actor(
    BitbucketDeleteEnvironmentForRepositoryActor,
    inports::<100>(username, repo_slug, environment_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_environment_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/environments/{environment_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("environment_uuid") {
        endpoint = endpoint.replace("{{environment_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{workspace}}/{{repo_slug}}/environments/{{environment_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve an environment
///
/// Method: GET /repositories/{workspace}/{repo_slug}/environments/{environment_uuid}
#[actor(
    BitbucketReadEnvironmentForRepositoryActor,
    inports::<100>(username, repo_slug, environment_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_environment_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/environments/{environment_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("environment_uuid") {
        endpoint = endpoint.replace("{{environment_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/environments/{{environment_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update an environment
///
/// Method: POST /repositories/{workspace}/{repo_slug}/environments/{environment_uuid}/changes/
#[actor(
    BitbucketUpdateEnvironmentForRepositoryActor,
    inports::<100>(username, repo_slug, environment_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_environment_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/environments/{environment_uuid}/changes/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("environment_uuid") {
        endpoint = endpoint.replace("{{environment_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{workspace}}/{{repo_slug}}/environments/{{environment_uuid}}/changes/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Endpoint to create and initiate a pipeline. \nThere are a couple of different options to initiate a pipeline, where the payload of the request will determine which type of pipeline will be instantiated.\n# Trigger a Pipeline for a branch\nOne way to trigger pipelines is by specifying the branch for which you want to trigger a pipeline. \nThe specified branch will be used to determine which pipeline definition from the `bitbucket-pipelines.yml` file will be applied to initiate the pipeline. The pipeline will then do a clone of the repository and checkout the latest revision of the specified branch.\n\n### Example\n\n```\n$ curl -X POST -is -u username:password \\\n  -H 'Content-Type: application/json' \\\n https://api.bitbucket.org/2.0/repositories/jeroendr/meat-demo2/pipelines/ \\\n  -d '\n  {\n    \"target\": {\n      \"ref_type\": \"branch\", \n      \"type\": \"pipeline_ref_target\", \n      \"ref_name\": \"master\"\n    }\n  }'\n```\n# Trigger a Pipeline for a commit on a branch or tag\nYou can initiate a pipeline for a specific commit and in the context of a specified reference (e.g. a branch, tag or bookmark).\nThe specified reference will be used to determine which pipeline definition from the bitbucket-pipelines.yml file will be applied to initiate the pipeline. The pipeline will clone the repository and then do a checkout the specified reference. \n\nThe following reference types are supported:\n\n* `branch` \n* `named_branch`\n* `bookmark` \n * `tag`\n\n### Example\n\n```\n$ curl -X POST -is -u username:password \\\n  -H 'Content-Type: application/json' \\\n  https://api.bitbucket.org/2.0/repositories/jeroendr/meat-demo2/pipelines/ \\\n  -d '\n  {\n    \"target\": {\n      \"commit\": {\n        \"type\": \"commit\", \n        \"hash\": \"ce5b7431602f7cbba007062eeb55225c6e18e956\"\n      }, \n      \"ref_type\": \"branch\", \n      \"type\": \"pipeline_ref_target\", \n      \"ref_name\": \"master\"\n    }\n  }'\n```\n# Trigger a specific pipeline definition for a commit\nYou can trigger a specific pipeline that is defined in your `bitbucket-pipelines.yml` file for a specific commit. \nIn addition to the commit revision, you specify the type and pattern of the selector that identifies the pipeline definition. The resulting pipeline will then clone the repository and checkout the specified revision.\n\n### Example\n\n```\n$ curl -X POST -is -u username:password \\\n  -H 'Content-Type: application/json' \\\n https://api.bitbucket.org/2.0/repositories/jeroendr/meat-demo2/pipelines/ \\\n -d '\n  {\n     \"target\": {\n      \"commit\": {\n         \"hash\":\"a3c4e02c9a3755eccdc3764e6ea13facdf30f923\",\n         \"type\":\"commit\"\n       },\n        \"selector\": {\n           \"type\":\"custom\",\n              \"pattern\":\"Deploy to production\"\n          },\n        \"type\":\"pipeline_commit_target\"\n   }\n  }'\n```\n# Trigger a specific pipeline definition for a commit on a branch or tag\nYou can trigger a specific pipeline that is defined in your `bitbucket-pipelines.yml` file for a specific commit in the context of a specified reference. \nIn addition to the commit revision, you specify the type and pattern of the selector that identifies the pipeline definition, as well as the reference information. The resulting pipeline will then clone the repository a checkout the specified reference.\n\n### Example\n\n```\n$ curl -X POST -is -u username:password \\\n  -H 'Content-Type: application/json' \\\n https://api.bitbucket.org/2.0/repositories/jeroendr/meat-demo2/pipelines/ \\\n -d '\n  {\n     \"target\": {\n      \"commit\": {\n         \"hash\":\"a3c4e02c9a3755eccdc3764e6ea13facdf30f923\",\n         \"type\":\"commit\"\n       },\n       \"selector\": {\n          \"type\": \"custom\",\n          \"pattern\": \"Deploy to production\"\n       },\n       \"type\": \"pipeline_ref_target\",\n       \"ref_name\": \"master\",\n       \"ref_type\": \"branch\"\n     }\n  }'\n```\n\n\n# Trigger a custom pipeline with variables\nIn addition to triggering a custom pipeline that is defined in your `bitbucket-pipelines.yml` file as shown in the examples above, you can specify variables that will be available for your build. In the request, provide a list of variables, specifying the following for each variable: key, value, and whether it should be secured or not (this field is optional and defaults to not secured).\n\n### Example\n\n```\n$ curl -X POST -is -u username:password \\\n  -H 'Content-Type: application/json' \\\n https://api.bitbucket.org/2.0/repositories/{workspace}/{repo_slug}/pipelines/ \\\n -d '\n  {\n    \"target\": {\n      \"type\": \"pipeline_ref_target\",\n      \"ref_type\": \"branch\",\n      \"ref_name\": \"master\",\n      \"selector\": {\n        \"type\": \"custom\",\n        \"pattern\": \"Deploy to production\"\n      }\n    },\n    \"variables\": [\n      {\n        \"key\": \"var1key\",\n        \"value\": \"var1value\",\n        \"secured\": true\n      },\n      {\n        \"key\": \"var2key\",\n        \"value\": \"var2value\"\n      }\n    ]\n  }'\n```\n
///
/// Method: POST /repositories/{workspace}/{repo_slug}/pipelines/
#[actor(
    BitbucketCreatePipelineForRepositoryActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_pipeline_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{workspace}}/{{repo_slug}}/pipelines/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Find pipelines
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/
#[actor(
    BitbucketReadPipelinesForRepositoryActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipelines_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve a specified pipeline
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}
#[actor(
    BitbucketReadPipelineForRepositoryActor,
    inports::<100>(username, repo_slug, pipeline_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipeline_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pipeline_uuid") {
        endpoint = endpoint.replace("{{pipeline_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// update repositories by workspace by repo slug pipelines by pipeline uuid remotetriggers by trigger key
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/remote-triggers/{trigger_key}
#[actor(
    BitbucketUpdateRepositoriesByWorkspaceByRepoSlugPipelinesByPipelineUuidRemotetriggersByTriggerKeyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repositories_by_workspace_by_repo_slug_pipelines_by_pipeline_uuid_remotetriggers_by_trigger_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/remote-triggers/{trigger_key}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/remote-triggers/{{trigger_key}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Find steps for the given pipeline.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/
#[actor(
    BitbucketReadPipelineStepsForRepositoryActor,
    inports::<100>(username, repo_slug, pipeline_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipeline_steps_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pipeline_uuid") {
        endpoint = endpoint.replace("{{pipeline_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/steps/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve a given step of a pipeline.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}
#[actor(
    BitbucketReadPipelineStepForRepositoryActor,
    inports::<100>(username, repo_slug, pipeline_uuid, step_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipeline_step_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pipeline_uuid") {
        endpoint = endpoint.replace("{{pipeline_uuid}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("step_uuid") {
        endpoint = endpoint.replace("{{step_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/steps/{{step_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve the log file for a given step of a pipeline.\n\nThis endpoint supports (and encourages!) the use of [HTTP Range requests](https://tools.ietf.org/html/rfc7233) to deal with potentially very large log files.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/log
#[actor(
    BitbucketReadPipelineStepLogForRepositoryActor,
    inports::<100>(username, repo_slug, pipeline_uuid, step_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipeline_step_log_for_repository(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/log".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pipeline_uuid") {
        endpoint = endpoint.replace("{{pipeline_uuid}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("step_uuid") {
        endpoint = endpoint.replace("{{step_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/steps/{{step_uuid}}/log failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// read repositories by workspace by repo slug pipelines by pipeline uuid steps by step uuid logs by log uuid
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/logs/{log_uuid}
#[actor(
    BitbucketReadRepositoriesByWorkspaceByRepoSlugPipelinesByPipelineUuidStepsByStepUuidLogsByLogUuidActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_workspace_by_repo_slug_pipelines_by_pipeline_uuid_steps_by_step_uuid_logs_by_log_uuid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/logs/{log_uuid}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/steps/{{step_uuid}}/logs/{{log_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// read repositories by workspace by repo slug pipelines by pipeline uuid steps by step uuid test reports
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/test_reports
#[actor(
    BitbucketReadRepositoriesByWorkspaceByRepoSlugPipelinesByPipelineUuidStepsByStepUuidTestReportsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_workspace_by_repo_slug_pipelines_by_pipeline_uuid_steps_by_step_uuid_test_reports(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/test_reports".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/steps/{{step_uuid}}/test_reports failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// read repositories by workspace by repo slug pipelines by pipeline uuid steps by step uuid test reports test cases
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/test_reports/test_cases
#[actor(
    BitbucketReadRepositoriesByWorkspaceByRepoSlugPipelinesByPipelineUuidStepsByStepUuidTestReportsTestCasesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_workspace_by_repo_slug_pipelines_by_pipeline_uuid_steps_by_step_uuid_test_reports_test_cases(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/test_reports/test_cases".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/steps/{{step_uuid}}/test_reports/test_cases failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// read repositories by workspace by repo slug pipelines by pipeline uuid steps by step uuid test reports test cases by test case uuid test case reasons
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/test_reports/test_cases/{test_case_uuid}/test_case_reasons
#[actor(
    BitbucketReadRepositoriesByWorkspaceByRepoSlugPipelinesByPipelineUuidStepsByStepUuidTestReportsTestCasesByTestCaseUuidTestCaseReasonsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repositories_by_workspace_by_repo_slug_pipelines_by_pipeline_uuid_steps_by_step_uuid_test_reports_test_cases_by_test_case_uuid_test_case_reasons(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/steps/{step_uuid}/test_reports/test_cases/{test_case_uuid}/test_case_reasons".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/steps/{{step_uuid}}/test_reports/test_cases/{{test_case_uuid}}/test_case_reasons failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Signal the stop of a pipeline and all of its steps that not have completed yet.
///
/// Method: POST /repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/stopPipeline
#[actor(
    BitbucketStopPipelineActor,
    inports::<100>(username, repo_slug, pipeline_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_stop_pipeline(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines/{pipeline_uuid}/stopPipeline".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pipeline_uuid") {
        endpoint = endpoint.replace("{{pipeline_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{workspace}}/{{repo_slug}}/pipelines/{{pipeline_uuid}}/stopPipeline failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve the repository pipelines configuration.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config
#[actor(
    BitbucketReadRepositoryPipelineConfigActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_config(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update the pipelines configuration for a repository.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/pipelines_config
#[actor(
    BitbucketUpdateRepositoryPipelineConfigActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repository_pipeline_config(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/pipelines_config failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update the next build number that should be assigned to a pipeline. The next build number that will be configured has to be strictly higher than the current latest build number for this repository.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/pipelines_config/build_number
#[actor(
    BitbucketUpdateRepositoryBuildNumberActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repository_build_number(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/build_number".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/build_number failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create a schedule for the given repository.
///
/// Method: POST /repositories/{workspace}/{repo_slug}/pipelines_config/schedules/
#[actor(
    BitbucketCreateRepositoryPipelineScheduleActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repository_pipeline_schedule(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/schedules/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/schedules/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve the configured schedules for the given repository.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config/schedules/
#[actor(
    BitbucketReadRepositoryPipelineSchedulesActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_schedules(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/schedules/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/schedules/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve a schedule by its UUID.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config/schedules/{schedule_uuid}
#[actor(
    BitbucketReadRepositoryPipelineScheduleActor,
    inports::<100>(username, repo_slug, schedule_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_schedule(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/schedules/{schedule_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("schedule_uuid") {
        endpoint = endpoint.replace("{{schedule_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/schedules/{{schedule_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update a schedule.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/pipelines_config/schedules/{schedule_uuid}
#[actor(
    BitbucketUpdateRepositoryPipelineScheduleActor,
    inports::<100>(username, repo_slug, schedule_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repository_pipeline_schedule(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/schedules/{schedule_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("schedule_uuid") {
        endpoint = endpoint.replace("{{schedule_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/schedules/{{schedule_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete a schedule.
///
/// Method: DELETE /repositories/{workspace}/{repo_slug}/pipelines_config/schedules/{schedule_uuid}
#[actor(
    BitbucketDeleteRepositoryPipelineScheduleActor,
    inports::<100>(username, repo_slug, schedule_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repository_pipeline_schedule(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/schedules/{schedule_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("schedule_uuid") {
        endpoint = endpoint.replace("{{schedule_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/schedules/{{schedule_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve the executions of a given schedule.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config/schedules/{schedule_uuid}/executions/
#[actor(
    BitbucketReadRepositoryPipelineScheduleExecutionsActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_schedule_executions(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/schedules/{schedule_uuid}/executions/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/schedules/{{schedule_uuid}}/executions/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create or update the repository SSH key pair. The private key will be set as a default SSH identity in your build container.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/pipelines_config/ssh/key_pair
#[actor(
    BitbucketUpdateRepositoryPipelineKeyPairActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repository_pipeline_key_pair(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/ssh/key_pair".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/ssh/key_pair failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete the repository SSH key pair.
///
/// Method: DELETE /repositories/{workspace}/{repo_slug}/pipelines_config/ssh/key_pair
#[actor(
    BitbucketDeleteRepositoryPipelineKeyPairActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repository_pipeline_key_pair(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/ssh/key_pair".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/ssh/key_pair failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve the repository SSH key pair excluding the SSH private key. The private key is a write only field and will never be exposed in the logs or the REST API.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config/ssh/key_pair
#[actor(
    BitbucketReadRepositoryPipelineSshKeyPairActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_ssh_key_pair(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/ssh/key_pair".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/ssh/key_pair failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Find repository level known hosts.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/
#[actor(
    BitbucketReadRepositoryPipelineKnownHostsActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_known_hosts(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/ssh/known_hosts/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create a repository level known host.
///
/// Method: POST /repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/
#[actor(
    BitbucketCreateRepositoryPipelineKnownHostActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repository_pipeline_known_host(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/ssh/known_hosts/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve a repository level known host.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/{known_host_uuid}
#[actor(
    BitbucketReadRepositoryPipelineKnownHostActor,
    inports::<100>(username, repo_slug, known_host_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_known_host(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/{known_host_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("known_host_uuid") {
        endpoint = endpoint.replace("{{known_host_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/ssh/known_hosts/{{known_host_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update a repository level known host.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/{known_host_uuid}
#[actor(
    BitbucketUpdateRepositoryPipelineKnownHostActor,
    inports::<100>(username, repo_slug, known_host_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repository_pipeline_known_host(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/{known_host_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("known_host_uuid") {
        endpoint = endpoint.replace("{{known_host_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/ssh/known_hosts/{{known_host_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete a repository level known host.
///
/// Method: DELETE /repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/{known_host_uuid}
#[actor(
    BitbucketDeleteRepositoryPipelineKnownHostActor,
    inports::<100>(username, repo_slug, known_host_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repository_pipeline_known_host(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/ssh/known_hosts/{known_host_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("known_host_uuid") {
        endpoint = endpoint.replace("{{known_host_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/ssh/known_hosts/{{known_host_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Find repository level variables.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config/variables/
#[actor(
    BitbucketReadRepositoryPipelineVariablesActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_variables(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/variables/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/variables/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create a repository level variable.
///
/// Method: POST /repositories/{workspace}/{repo_slug}/pipelines_config/variables/
#[actor(
    BitbucketCreateRepositoryPipelineVariableActor,
    inports::<100>(username, repo_slug),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_repository_pipeline_variable(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/variables/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/variables/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update a repository level variable.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketUpdateRepositoryPipelineVariableActor,
    inports::<100>(username, repo_slug, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repository_pipeline_variable(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete a repository level variable.
///
/// Method: DELETE /repositories/{workspace}/{repo_slug}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketDeleteRepositoryPipelineVariableActor,
    inports::<100>(username, repo_slug, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repository_pipeline_variable(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve a repository level variable.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketReadRepositoryPipelineVariableActor,
    inports::<100>(username, repo_slug, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_pipeline_variable(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete an application property value stored against a repository.
///
/// Method: DELETE /repositories/{workspace}/{repo_slug}/properties/{app_key}/{property_name}
#[actor(
    BitbucketDeleteRepositoryHostedPropertyValueActor,
    inports::<100>(username, repo_slug, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_repository_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{workspace}}/{{repo_slug}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve an application property value stored against a repository.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/properties/{app_key}/{property_name}
#[actor(
    BitbucketReadRepositoryHostedPropertyValueActor,
    inports::<100>(username, repo_slug, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_repository_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update an application property value stored against a repository.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/properties/{app_key}/{property_name}
#[actor(
    BitbucketUpdateRepositoryHostedPropertyValueActor,
    inports::<100>(username, repo_slug, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_repository_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve an application property value stored against a pull request.
///
/// Method: GET /repositories/{workspace}/{repo_slug}/pullrequests/{pullrequest_id}/properties/{app_key}/{property_name}
#[actor(
    BitbucketReadPullRequestHostedPropertyValueActor,
    inports::<100>(username, repo_slug, pullrequest_id, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pull_request_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pullrequests/{pullrequest_id}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pullrequest_id") {
        endpoint = endpoint.replace("{{pullrequest_id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /repositories/{{workspace}}/{{repo_slug}}/pullrequests/{{pullrequest_id}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update an application property value stored against a pull request.
///
/// Method: PUT /repositories/{workspace}/{repo_slug}/pullrequests/{pullrequest_id}/properties/{app_key}/{property_name}
#[actor(
    BitbucketUpdatePullRequestHostedPropertyValueActor,
    inports::<100>(username, repo_slug, pullrequest_id, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_pull_request_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pullrequests/{pullrequest_id}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pullrequest_id") {
        endpoint = endpoint.replace("{{pullrequest_id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /repositories/{{workspace}}/{{repo_slug}}/pullrequests/{{pullrequest_id}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete an application property value stored against a pull request.
///
/// Method: DELETE /repositories/{workspace}/{repo_slug}/pullrequests/{pullrequest_id}/properties/{app_key}/{property_name}
#[actor(
    BitbucketDeletePullRequestHostedPropertyValueActor,
    inports::<100>(username, repo_slug, pullrequest_id, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_pull_request_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/repositories/{workspace}/{repo_slug}/pullrequests/{pullrequest_id}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("pullrequest_id") {
        endpoint = endpoint.replace("{{pullrequest_id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /repositories/{{workspace}}/{{repo_slug}}/pullrequests/{{pullrequest_id}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all snippets. Like pull requests, repositories and teams, the\nfull set of snippets is defined by what the current user has access to.\n\nThis includes all snippets owned by the current user, but also all snippets\nowned by any of the teams the user is a member of, or snippets by other\nusers that the current user is either watching or has collaborated on (for\ninstance by commenting on it).\n\nTo limit the set of returned snippets, apply the\n`?role=[owner|contributor|member]` query parameter where the roles are\ndefined as follows:\n\n* `owner`: all snippets owned by the current user\n* `contributor`: all snippets owned by, or watched by the current user\n* `member`: owned by the user, their teams, or watched by the current user\n\nWhen no role is specified, all public snippets are returned, as well as all\nprivately owned snippets watched or commented on.\n\nThe returned response is a normal paginated JSON list. This endpoint\nonly supports `application/json` responses and no\n`multipart/form-data` or `multipart/related`. As a result, it is not\npossible to include the file contents.
///
/// Method: GET /snippets
#[actor(
    BitbucketReadSnippetsActor,
    inports::<100>(role),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("role") {
        query_pairs.push(("role", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new snippet under the authenticated user's account.\n\nSnippets can contain multiple files. Both text and binary files are\nsupported.\n\nThe simplest way to create a new snippet from a local file:\n\n    $ curl -u username:password -X POST https://api.bitbucket.org/2.0/snippets               -F file=@image.png\n\nCreating snippets through curl has a few limitations and so let's look\nat a more complicated scenario.\n\nSnippets are created with a multipart POST. Both `multipart/form-data`\nand `multipart/related` are supported. Both allow the creation of\nsnippets with both meta data (title, etc), as well as multiple text\nand binary files.\n\nThe main difference is that `multipart/related` can use rich encoding\nfor the meta data (currently JSON).\n\n\nmultipart/related (RFC-2387)\n----------------------------\n\nThis is the most advanced and efficient way to create a paste.\n\n    POST /2.0/snippets/evzijst HTTP/1.1\n    Content-Length: 1188\n    Content-Type: multipart/related; start=\"snippet\"; boundary=\"===============1438169132528273974==\"\n    MIME-Version: 1.0\n\n    --===============1438169132528273974==\n    Content-Type: application/json; charset=\"utf-8\"\n    MIME-Version: 1.0\n    Content-ID: snippet\n\n    {\n      \"title\": \"My snippet\",\n      \"is_private\": true,\n      \"scm\": \"hg\",\n      \"files\": {\n          \"foo.txt\": {},\n          \"image.png\": {}\n        }\n    }\n\n    --===============1438169132528273974==\n    Content-Type: text/plain; charset=\"us-ascii\"\n    MIME-Version: 1.0\n    Content-Transfer-Encoding: 7bit\n    Content-ID: \"foo.txt\"\n    Content-Disposition: attachment; filename=\"foo.txt\"\n\n    foo\n\n    --===============1438169132528273974==\n    Content-Type: image/png\n    MIME-Version: 1.0\n    Content-Transfer-Encoding: base64\n    Content-ID: \"image.png\"\n    Content-Disposition: attachment; filename=\"image.png\"\n\n    \n    --===============1438169132528273974==--\n\nThe request contains multiple parts and is structured as follows.\n\nThe first part is the JSON document that describes the snippet's\nproperties or meta data. It either has to be the first part, or the\nrequest's `Content-Type` header must contain the `start` parameter to\npoint to it.\n\nThe remaining parts are the files of which there can be zero or more.\nEach file part should contain the `Content-ID` MIME header through\nwhich the JSON meta data's `files` element addresses it. The value\nshould be the name of the file.\n\n`Content-Disposition` is an optional MIME header. The header's\noptional `filename` parameter can be used to specify the file name\nthat Bitbucket should use when writing the file to disk. When present,\n`filename` takes precedence over the value of `Content-ID`.\n\nWhen the JSON body omits the `files` element, the remaining parts are\nnot ignored. Instead, each file is added to the new snippet as if its\nname was explicitly linked (the use of the `files` elements is\nmandatory for some operations like deleting or renaming files).\n\n\nmultipart/form-data\n-------------------\n\nThe use of JSON for the snippet's meta data is optional. Meta data can\nalso be supplied as regular form fields in a more conventional\n`multipart/form-data` request:\n\n    $ curl -X POST -u credentials https://api.bitbucket.org/2.0/snippets               -F title=\"My snippet\"               -F file=@foo.txt -F file=@image.png\n\n    POST /2.0/snippets HTTP/1.1\n    Content-Length: 951\n    Content-Type: multipart/form-data; boundary=----------------------------63a4b224c59f\n\n    ------------------------------63a4b224c59f\n    Content-Disposition: form-data; name=\"file\"; filename=\"foo.txt\"\n    Content-Type: text/plain\n\n    foo\n\n    ------------------------------63a4b224c59f\n    Content-Disposition: form-data; name=\"file\"; filename=\"image.png\"\n    Content-Type: application/octet-stream\n\n    ?PNG\n\n    IHDR?1??I.....\n    ------------------------------63a4b224c59f\n    Content-Disposition: form-data; name=\"title\"\n\n    My snippet\n    ------------------------------63a4b224c59f--\n\nHere the meta data properties are included as flat, top-level form\nfields. The file attachments use the `file` field name. To attach\nmultiple files, simply repeat the field.\n\nThe advantage of `multipart/form-data` over `multipart/related` is\nthat it can be easier to build clients.\n\nEssentially all properties are optional, `title` and `files` included.\n\n\nSharing and Visibility\n----------------------\n\nSnippets can be either public (visible to anyone on Bitbucket, as well\nas anonymous users), or private (visible only to the owner, creator\nand members of the team in case the snippet is owned by a team). This\nis controlled through the snippet's `is_private` element:\n\n* **is_private=false** -- everyone, including anonymous users can view\n  the snippet\n* **is_private=true** -- only the owner and team members (for team\n  snippets) can view it\n\nTo create the snippet under a team account, just append the team name\nto the URL (see `/2.0/snippets/{username}`).
///
/// Method: POST /snippets
#[actor(
    BitbucketCreateSnippetsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_snippets(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /snippets failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Identical to `/snippets`, except that the result is further filtered\nby the snippet owner and only those that are owned by `{username}` are\nreturned.
///
/// Method: GET /snippets/{username}
#[actor(
    BitbucketReadSnippetsByUsernameActor,
    inports::<100>(role, username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("role") {
        query_pairs.push(("role", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Identical to `/snippets`, except that the new snippet will be\ncreated under the account specified in the path parameter `{username}`.
///
/// Method: POST /snippets/{username}
#[actor(
    BitbucketCreateSnippetsByUsernameActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_snippets_by_username(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /snippets/{{username}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Used to update a snippet. Use this to add and delete files and to\nchange a snippet's title.\n\nTo update a snippet, one can either PUT a full snapshot, or only the\nparts that need to be changed.\n\nThe contract for PUT on this API is that properties missing from the\nrequest remain untouched so that snippets can be efficiently\nmanipulated with differential payloads.\n\nTo delete a property (e.g. the title, or a file), include its name in\nthe request, but omit its value (use `null`).\n\nAs in Git, explicit renaming of files is not supported. Instead, to\nrename a file, delete it and add it again under another name. This can\nbe done atomically in a single request. Rename detection is left to\nthe SCM.\n\nPUT supports three different content types for both request and\nresponse bodies:\n\n* `application/json`\n* `multipart/related`\n* `multipart/form-data`\n\nThe content type used for the request body can be different than that\nused for the response. Content types are specified using standard HTTP\nheaders.\n\nUse the `Content-Type` and `Accept` headers to select the desired\nrequest and response format.\n\n\napplication/json\n----------------\n\nAs with creation and retrieval, the content type determines what\nproperties can be manipulated. `application/json` does not support\nfile contents and is therefore limited to a snippet's meta data.\n\nTo update the title, without changing any of its files:\n\n    $ curl -X POST -H \"Content-Type: application/json\" https://api.bitbucket.org/2.0/snippets/evzijst/kypj             -d '{\"title\": \"Updated title\"}'\n\n\nTo delete the title:\n\n    $ curl -X POST -H \"Content-Type: application/json\" https://api.bitbucket.org/2.0/snippets/evzijst/kypj             -d '{\"title\": null}'\n\nNot all parts of a snippet can be manipulated. The owner and creator\nfor instance are immutable.\n\n\nmultipart/related\n-----------------\n\n`multipart/related` can be used to manipulate all of a snippet's\nproperties. The body is identical to a POST. properties omitted from\nthe request are left unchanged. Since the `start` part contains JSON,\nthe mechanism for manipulating the snippet's meta data is identical\nto `application/json` requests.\n\nTo update one of a snippet's file contents, while also changing its\ntitle:\n\n    PUT /2.0/snippets/evzijst/kypj HTTP/1.1\n    Content-Length: 288\n    Content-Type: multipart/related; start=\"snippet\"; boundary=\"===============1438169132528273974==\"\n    MIME-Version: 1.0\n\n    --===============1438169132528273974==\n    Content-Type: application/json; charset=\"utf-8\"\n    MIME-Version: 1.0\n    Content-ID: snippet\n\n    {\n      \"title\": \"My updated snippet\",\n      \"files\": {\n          \"foo.txt\": {}\n        }\n    }\n\n    --===============1438169132528273974==\n    Content-Type: text/plain; charset=\"us-ascii\"\n    MIME-Version: 1.0\n    Content-Transfer-Encoding: 7bit\n    Content-ID: \"foo.txt\"\n    Content-Disposition: attachment; filename=\"foo.txt\"\n\n    Updated file contents.\n\n    --===============1438169132528273974==--\n\nHere only the parts that are changed are included in the body. The\nother files remain untouched.\n\nNote the use of the `files` list in the JSON part. This list contains\nthe files that are being manipulated. This list should have\ncorresponding multiparts in the request that contain the new contents\nof these files.\n\nIf a filename in the `files` list does not have a corresponding part,\nit will be deleted from the snippet, as shown below:\n\n    PUT /2.0/snippets/evzijst/kypj HTTP/1.1\n    Content-Length: 188\n    Content-Type: multipart/related; start=\"snippet\"; boundary=\"===============1438169132528273974==\"\n    MIME-Version: 1.0\n\n    --===============1438169132528273974==\n    Content-Type: application/json; charset=\"utf-8\"\n    MIME-Version: 1.0\n    Content-ID: snippet\n\n    {\n      \"files\": {\n        \"image.png\": {}\n      }\n    }\n\n    --===============1438169132528273974==--\n\nTo simulate a rename, delete a file and add the same file under\nanother name:\n\n    PUT /2.0/snippets/evzijst/kypj HTTP/1.1\n    Content-Length: 212\n    Content-Type: multipart/related; start=\"snippet\"; boundary=\"===============1438169132528273974==\"\n    MIME-Version: 1.0\n\n    --===============1438169132528273974==\n    Content-Type: application/json; charset=\"utf-8\"\n    MIME-Version: 1.0\n    Content-ID: snippet\n\n    {\n        \"files\": {\n          \"foo.txt\": {},\n          \"bar.txt\": {}\n        }\n    }\n\n    --===============1438169132528273974==\n    Content-Type: text/plain; charset=\"us-ascii\"\n    MIME-Version: 1.0\n    Content-Transfer-Encoding: 7bit\n    Content-ID: \"bar.txt\"\n    Content-Disposition: attachment; filename=\"bar.txt\"\n\n    foo\n\n    --===============1438169132528273974==--\n\n\nmultipart/form-data\n-----------------\n\nAgain, one can also use `multipart/form-data` to manipulate file\ncontents and meta data atomically.\n\n    $ curl -X PUT http://localhost:12345/2.0/snippets/evzijst/kypj             -F title=\"My updated snippet\" -F file=@foo.txt\n\n    PUT /2.0/snippets/evzijst/kypj HTTP/1.1\n    Content-Length: 351\n    Content-Type: multipart/form-data; boundary=----------------------------63a4b224c59f\n\n    ------------------------------63a4b224c59f\n    Content-Disposition: form-data; name=\"file\"; filename=\"foo.txt\"\n    Content-Type: text/plain\n\n    foo\n\n    ------------------------------63a4b224c59f\n    Content-Disposition: form-data; name=\"title\"\n\n    My updated snippet\n    ------------------------------63a4b224c59f\n\nTo delete a file, omit its contents while including its name in the\n`files` field:\n\n    $ curl -X PUT https://api.bitbucket.org/2.0/snippets/evzijst/kypj -F files=image.png\n\n    PUT /2.0/snippets/evzijst/kypj HTTP/1.1\n    Content-Length: 149\n    Content-Type: multipart/form-data; boundary=----------------------------ef8871065a86\n\n    ------------------------------ef8871065a86\n    Content-Disposition: form-data; name=\"files\"\n\n    image.png\n    ------------------------------ef8871065a86--\n\nThe explicit use of the `files` element in `multipart/related` and\n`multipart/form-data` is only required when deleting files.\nThe default mode of operation is for file parts to be processed,\nregardless of whether or not they are listed in `files`, as a\nconvenience to the client.
///
/// Method: PUT /snippets/{username}/{encoded_id}
#[actor(
    BitbucketUpdateSnippetsByUsernameByEncodedIdActor,
    inports::<100>(encoded_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_snippets_by_username_by_encoded_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /snippets/{{username}}/{{encoded_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes a snippet and returns an empty response.
///
/// Method: DELETE /snippets/{username}/{encoded_id}
#[actor(
    BitbucketDeleteSnippetsByUsernameByEncodedIdActor,
    inports::<100>(encoded_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_snippets_by_username_by_encoded_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /snippets/{{username}}/{{encoded_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieves a single snippet.\n\nSnippets support multiple content types:\n\n* application/json\n* multipart/related\n* multipart/form-data\n\n\napplication/json\n----------------\n\nThe default content type of the response is `application/json`.\nSince JSON is always `utf-8`, it cannot reliably contain file contents\nfor files that are not text. Therefore, JSON snippet documents only\ncontain the filename and links to the file contents.\n\nThis means that in order to retrieve all parts of a snippet, N+1\nrequests need to be made (where N is the number of files in the\nsnippet).\n\n\nmultipart/related\n-----------------\n\nTo retrieve an entire snippet in a single response, use the\n`Accept: multipart/related` HTTP request header.\n\n    $ curl -H \"Accept: multipart/related\" https://api.bitbucket.org/2.0/snippets/evzijst/1\n\nResponse:\n\n    HTTP/1.1 200 OK\n    Content-Length: 2214\n    Content-Type: multipart/related; start=\"snippet\"; boundary=\"===============1438169132528273974==\"\n    MIME-Version: 1.0\n\n    --===============1438169132528273974==\n    Content-Type: application/json; charset=\"utf-8\"\n    MIME-Version: 1.0\n    Content-ID: snippet\n\n    {\n      \"links\": {\n        \"self\": {\n          \"href\": \"https://api.bitbucket.org/2.0/snippets/evzijst/kypj\"\n        },\n        \"html\": {\n          \"href\": \"https://bitbucket.org/snippets/evzijst/kypj\"\n        },\n        \"comments\": {\n          \"href\": \"https://api.bitbucket.org/2.0/snippets/evzijst/kypj/comments\"\n        },\n        \"watchers\": {\n          \"href\": \"https://api.bitbucket.org/2.0/snippets/evzijst/kypj/watchers\"\n        },\n        \"commits\": {\n          \"href\": \"https://api.bitbucket.org/2.0/snippets/evzijst/kypj/commits\"\n        }\n      },\n      \"id\": kypj,\n      \"title\": \"My snippet\",\n      \"created_on\": \"2014-12-29T22:22:04.790331+00:00\",\n      \"updated_on\": \"2014-12-29T22:22:04.790331+00:00\",\n      \"is_private\": false,\n      \"files\": {\n        \"foo.txt\": {\n          \"links\": {\n            \"self\": {\n              \"href\": \"https://api.bitbucket.org/2.0/snippets/evzijst/kypj/files/367ab19/foo.txt\"\n            },\n            \"html\": {\n              \"href\": \"https://bitbucket.org/snippets/evzijst/kypj#file-foo.txt\"\n            }\n          }\n        },\n        \"image.png\": {\n          \"links\": {\n            \"self\": {\n              \"href\": \"https://api.bitbucket.org/2.0/snippets/evzijst/kypj/files/367ab19/image.png\"\n            },\n            \"html\": {\n              \"href\": \"https://bitbucket.org/snippets/evzijst/kypj#file-image.png\"\n            }\n          }\n        }\n      ],\n      \"owner\": {\n        \"username\": \"evzijst\",\n        \"nickname\": \"evzijst\",\n        \"display_name\": \"Erik van Zijst\",\n        \"uuid\": \"{d301aafa-d676-4ee0-88be-962be7417567}\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/users/evzijst\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/evzijst\"\n          },\n          \"avatar\": {\n            \"href\": \"https://bitbucket-staging-assetroot.s3.amazonaws.com/c/photos/2013/Jul/31/erik-avatar-725122544-0_avatar.png\"\n          }\n        }\n      },\n      \"creator\": {\n        \"username\": \"evzijst\",\n        \"nickname\": \"evzijst\",\n        \"display_name\": \"Erik van Zijst\",\n        \"uuid\": \"{d301aafa-d676-4ee0-88be-962be7417567}\",\n        \"links\": {\n          \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/users/evzijst\"\n          },\n          \"html\": {\n            \"href\": \"https://bitbucket.org/evzijst\"\n          },\n          \"avatar\": {\n            \"href\": \"https://bitbucket-staging-assetroot.s3.amazonaws.com/c/photos/2013/Jul/31/erik-avatar-725122544-0_avatar.png\"\n          }\n        }\n      }\n    }\n\n    --===============1438169132528273974==\n    Content-Type: text/plain; charset=\"us-ascii\"\n    MIME-Version: 1.0\n    Content-Transfer-Encoding: 7bit\n    Content-ID: \"foo.txt\"\n    Content-Disposition: attachment; filename=\"foo.txt\"\n\n    foo\n\n    --===============1438169132528273974==\n    Content-Type: image/png\n    MIME-Version: 1.0\n    Content-Transfer-Encoding: base64\n    Content-ID: \"image.png\"\n    Content-Disposition: attachment; filename=\"image.png\"\n\n    \n    --===============1438169132528273974==--\n\nmultipart/form-data\n-------------------\n\nAs with creating new snippets, `multipart/form-data` can be used as an\nalternative to `multipart/related`. However, the inherently flat\nstructure of form-data means that only basic, root-level properties\ncan be returned, while nested elements like `links` are omitted:\n\n    $ curl -H \"Accept: multipart/form-data\" https://api.bitbucket.org/2.0/snippets/evzijst/kypj\n\nResponse:\n\n    HTTP/1.1 200 OK\n    Content-Length: 951\n    Content-Type: multipart/form-data; boundary=----------------------------63a4b224c59f\n\n    ------------------------------63a4b224c59f\n    Content-Disposition: form-data; name=\"title\"\n    Content-Type: text/plain; charset=\"utf-8\"\n\n    My snippet\n    ------------------------------63a4b224c59f--\n    Content-Disposition: attachment; name=\"file\"; filename=\"foo.txt\"\n    Content-Type: text/plain\n\n    foo\n\n    ------------------------------63a4b224c59f\n    Content-Disposition: attachment; name=\"file\"; filename=\"image.png\"\n    Content-Transfer-Encoding: base64\n    Content-Type: application/octet-stream\n\n    \n    ------------------------------5957323a6b76--
///
/// Method: GET /snippets/{username}/{encoded_id}
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdActor,
    inports::<100>(encoded_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Used to retrieve a paginated list of all comments for a specific\nsnippet.\n\nThis resource works identical to commit and pull request comments.\n\nThe default sorting is oldest to newest and can be overridden with\nthe `sort` query parameter.
///
/// Method: GET /snippets/{username}/{encoded_id}/comments
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdCommentsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/comments".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/comments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new comment.\n\nThe only required field in the body is `content.raw`.\n\nTo create a threaded reply to an existing comment, include `parent.id`.
///
/// Method: POST /snippets/{username}/{encoded_id}/comments
#[actor(
    BitbucketCreateSnippetsByUsernameByEncodedIdCommentsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_snippets_by_username_by_encoded_id_comments(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/comments".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /snippets/{{username}}/{{encoded_id}}/comments failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes a snippet comment.\n\nComments can only be removed by their author.
///
/// Method: DELETE /snippets/{username}/{encoded_id}/comments/{comment_id}
#[actor(
    BitbucketDeleteSnippetsByUsernameByEncodedIdCommentsByCommentIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_snippets_by_username_by_encoded_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/comments/{comment_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /snippets/{{username}}/{{encoded_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Updates a comment.\n\nComments can only be updated by their author.
///
/// Method: PUT /snippets/{username}/{encoded_id}/comments/{comment_id}
#[actor(
    BitbucketUpdateSnippetsByUsernameByEncodedIdCommentsByCommentIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_snippets_by_username_by_encoded_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/comments/{comment_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /snippets/{{username}}/{{encoded_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the specific snippet comment.
///
/// Method: GET /snippets/{username}/{encoded_id}/comments/{comment_id}
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdCommentsByCommentIdActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_comments_by_comment_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/comments/{comment_id}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/comments/{{comment_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the changes (commits) made on this snippet.
///
/// Method: GET /snippets/{username}/{encoded_id}/commits
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdCommitsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_commits(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/commits".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/commits failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: GET /snippets/{username}/{encoded_id}/commits/{revision}
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdCommitsByRevisionActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_commits_by_revision(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/commits/{revision}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/commits/{{revision}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Convenience resource for getting to a snippet's raw files without the\nneed for first having to retrieve the snippet itself and having to pull\nout the versioned file links.
///
/// Method: GET /snippets/{username}/{encoded_id}/files/{path}
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdFilesByPathActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_files_by_path(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/files/{path}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/files/{{path}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Used to stop watching a specific snippet. Returns 204 (No Content)\nto indicate success.
///
/// Method: DELETE /snippets/{username}/{encoded_id}/watch
#[actor(
    BitbucketDeleteSnippetsByUsernameByEncodedIdWatchActor,
    inports::<100>(encoded_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_snippets_by_username_by_encoded_id_watch(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}/watch".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /snippets/{{username}}/{{encoded_id}}/watch failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Used to start watching a specific snippet. Returns 204 (No Content).
///
/// Method: PUT /snippets/{username}/{encoded_id}/watch
#[actor(
    BitbucketUpdateSnippetsByUsernameByEncodedIdWatchActor,
    inports::<100>(encoded_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_snippets_by_username_by_encoded_id_watch(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}/watch".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /snippets/{{username}}/{{encoded_id}}/watch failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Used to check if the current user is watching a specific snippet.\n\nReturns 204 (No Content) if the user is watching the snippet and 404 if\nnot.\n\nHitting this endpoint anonymously always returns a 404.
///
/// Method: GET /snippets/{username}/{encoded_id}/watch
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdWatchActor,
    inports::<100>(encoded_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_watch(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}/watch".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/watch failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes the snippet.\n\nNote that this only works for versioned URLs that point to the latest\ncommit of the snippet. Pointing to an older commit results in a 405\nstatus code.\n\nTo delete a snippet, regardless of whether or not concurrent changes\nare being made to it, use `DELETE /snippets/{encoded_id}` instead.
///
/// Method: DELETE /snippets/{username}/{encoded_id}/{node_id}
#[actor(
    BitbucketDeleteSnippetsByUsernameByEncodedIdByNodeIdActor,
    inports::<100>(encoded_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_snippets_by_username_by_encoded_id_by_node_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}/{node_id}".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /snippets/{{username}}/{{encoded_id}}/{{node_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Identical to `GET /snippets/encoded_id`, except that this endpoint\ncan be used to retrieve the contents of the snippet as it was at an\nolder revision, while `/snippets/encoded_id` always returns the\nsnippet's current revision.\n\nNote that only the snippet's file contents are versioned, not its\nmeta data properties like the title.\n\nOther than that, the two endpoints are identical in behavior.
///
/// Method: GET /snippets/{username}/{encoded_id}/{node_id}
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdByNodeIdActor,
    inports::<100>(encoded_id, node_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_by_node_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}/{node_id}".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("node_id") {
        endpoint = endpoint.replace("{{node_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/{{node_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Identical to `UPDATE /snippets/encoded_id`, except that this endpoint\ntakes an explicit commit revision. Only the snippet's \"HEAD\"/\"tip\"\n(most recent) version can be updated and requests on all other,\nolder revisions fail by returning a 405 status.\n\nUsage of this endpoint over the unrestricted `/snippets/encoded_id`\ncould be desired if the caller wants to be sure no concurrent\nmodifications have taken place between the moment of the UPDATE\nrequest and the original GET.\n\nThis can be considered a so-called \"Compare And Swap\", or CAS\noperation.\n\nOther than that, the two endpoints are identical in behavior.
///
/// Method: PUT /snippets/{username}/{encoded_id}/{node_id}
#[actor(
    BitbucketUpdateSnippetsByUsernameByEncodedIdByNodeIdActor,
    inports::<100>(encoded_id, node_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_snippets_by_username_by_encoded_id_by_node_id(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}/{node_id}".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("node_id") {
        endpoint = endpoint.replace("{{node_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /snippets/{{username}}/{{encoded_id}}/{{node_id}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieves the raw contents of a specific file in the snippet. The\n`Content-Disposition` header will be \"attachment\" to avoid issues with\nmalevolent executable files.\n\nThe file's mime type is derived from its filename and returned in the\n`Content-Type` header.\n\nNote that for text files, no character encoding is included as part of\nthe content type.
///
/// Method: GET /snippets/{username}/{encoded_id}/{node_id}/files/{path}
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdByNodeIdFilesByPathActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_by_node_id_files_by_path(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/snippets/{username}/{encoded_id}/{node_id}/files/{path}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/{{node_id}}/files/{{path}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the diff of the specified commit against its first parent.\n\nNote that this resource is different in functionality from the `patch`\nresource.\n\nThe differences between a diff and a patch are:\n\n* patches have a commit header with the username, message, etc\n* diffs support the optional `path=foo/bar.py` query param to filter the\n  diff to just that one file diff (not supported for patches)\n* for a merge, the diff will show the diff between the merge commit and\n  its first parent (identical to how PRs work), while patch returns a\n  response containing separate patches for each commit on the second\n  parent's ancestry, up to the oldest common ancestor (identical to\n  its reachability).\n\nNote that the character encoding of the contents of the diff is\nunspecified as Git and Mercurial do not track this, making it hard for\nBitbucket to reliably determine this.
///
/// Method: GET /snippets/{username}/{encoded_id}/{revision}/diff
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdByRevisionDiffActor,
    inports::<100>(path, encoded_id, revision),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_by_revision_diff(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}/{revision}/diff".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("revision") {
        endpoint = endpoint.replace("{{revision}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("path") {
        query_pairs.push(("path", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/{{revision}}/diff failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the patch of the specified commit against its first\nparent.\n\nNote that this resource is different in functionality from the `diff`\nresource.\n\nThe differences between a diff and a patch are:\n\n* patches have a commit header with the username, message, etc\n* diffs support the optional `path=foo/bar.py` query param to filter the\n  diff to just that one file diff (not supported for patches)\n* for a merge, the diff will show the diff between the merge commit and\n  its first parent (identical to how PRs work), while patch returns a\n  response containing separate patches for each commit on the second\n  parent's ancestry, up to the oldest common ancestor (identical to\n  its reachability).\n\nNote that the character encoding of the contents of the patch is\nunspecified as Git and Mercurial do not track this, making it hard for\nBitbucket to reliably determine this.
///
/// Method: GET /snippets/{username}/{encoded_id}/{revision}/patch
#[actor(
    BitbucketReadSnippetsByUsernameByEncodedIdByRevisionPatchActor,
    inports::<100>(encoded_id, revision),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_snippets_by_username_by_encoded_id_by_revision_patch(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/snippets/{username}/{encoded_id}/{revision}/patch".to_string();
    if let Some(val) = inputs.get("encoded_id") {
        endpoint = endpoint.replace("{{encoded_id}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("revision") {
        endpoint = endpoint.replace("{{revision}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /snippets/{{username}}/{{encoded_id}}/{{revision}}/patch failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all the teams that the authenticated user is associated\nwith.
///
/// Method: GET /teams
#[actor(
    BitbucketReadTeamsActor,
    inports::<100>(role),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("role") {
        query_pairs.push(("role", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Gets the public information associated with a team.\n\nIf the team's profile is private, `location`, `website` and\n`created_on` elements are omitted.
///
/// Method: GET /teams/{username}
#[actor(
    BitbucketReadTeamsByUsernameActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the list of accounts that are following this team.
///
/// Method: GET /teams/{username}/followers
#[actor(
    BitbucketReadTeamsByUsernameFollowersActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_followers(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/followers".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/followers failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the list of accounts this team is following.
///
/// Method: GET /teams/{username}/following
#[actor(
    BitbucketReadTeamsByUsernameFollowingActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_following(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/following".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/following failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of webhooks installed on this team.
///
/// Method: GET /teams/{username}/hooks
#[actor(
    BitbucketReadTeamsByUsernameHooksActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_hooks(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/hooks".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/hooks failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new webhook on the specified team.\n\nTeam webhooks are fired for events from all repositories belonging to\nthat team account.\n\nNote that only admins can install webhooks on teams.
///
/// Method: POST /teams/{username}/hooks
#[actor(
    BitbucketCreateTeamsByUsernameHooksActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_teams_by_username_hooks(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/hooks".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /teams/{{username}}/hooks failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Updates the specified webhook subscription.\n\nThe following properties can be mutated:\n\n* `description`\n* `url`\n* `active`\n* `events`
///
/// Method: PUT /teams/{username}/hooks/{uid}
#[actor(
    BitbucketUpdateTeamsByUsernameHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_teams_by_username_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /teams/{{username}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes the specified webhook subscription from the given team\naccount.
///
/// Method: DELETE /teams/{username}/hooks/{uid}
#[actor(
    BitbucketDeleteTeamsByUsernameHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_teams_by_username_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /teams/{{username}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the webhook with the specified id installed on the given\nteam account.
///
/// Method: GET /teams/{username}/hooks/{uid}
#[actor(
    BitbucketReadTeamsByUsernameHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all members of the specified team. Any member of any of the\nteam's groups is considered a member of the team. This includes users\nin groups that may not actually have access to any of the team's\nrepositories.\n\nThis operation has been deprecated due to privacy changes.\nSee the [announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/)\nfor details.
///
/// Method: GET /teams/{username}/members
#[actor(
    BitbucketReadTeamsByUsernameMembersActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_members(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/members".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/members failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns an object for each team permission a user on the team has.\n\nPermissions returned are effective permissions — if a user is a member of\nmultiple groups with distinct roles, only the highest level is returned.\n\nPermissions can be:\n\n* `admin`\n* `collaborator`\n\nOnly users with admin permission for the team may access this resource.\n\nExample:\n\n```\n$ curl https://api.bitbucket.org/2.0/teams/atlassian_tutorial/permissions\n\n{\n  \"pagelen\": 10,\n  \"values\": [\n    {\n      \"permission\": \"admin\",\n      \"type\": \"team_permission\",\n      \"user\": {\n        \"type\": \"user\",\n        \"username\": \"evzijst\",\n        \"nickname\": \"evzijst\",\n        \"display_name\": \"Erik van Zijst\",\n        \"uuid\": \"{d301aafa-d676-4ee0-88be-962be7417567}\"\n      },\n      \"team\": {\n        \"username\": \"bitbucket\",\n        \"display_name\": \"Atlassian Bitbucket\",\n        \"uuid\": \"{4cc6108a-a241-4db0-96a5-64347ac04f87}\"\n      }\n    },\n    {\n      \"permission\": \"collaborator\",\n      \"type\": \"team_permission\",\n      \"user\": {\n        \"type\": \"user\",\n        \"username\": \"seanaty\",\n        \"nickname\": \"seanaty\",\n        \"display_name\": \"Sean Conaty\",\n        \"uuid\": \"{504c3b62-8120-4f0c-a7bc-87800b9d6f70}\"\n      },\n      \"team\": {\n        \"username\": \"bitbucket\",\n        \"display_name\": \"Atlassian Bitbucket\",\n        \"uuid\": \"{4cc6108a-a241-4db0-96a5-64347ac04f87}\"\n      }\n    }\n  ],\n  \"page\": 1,\n  \"size\": 2\n}\n```\n\nResults may be further [filtered or sorted](../../../meta/filtering) by\nteam, user, or permission by adding the following query string\nparameters:\n\n* `q=user.username=\"evzijst\"` or `q=permission=\"admin\"`\n* `sort=team.display_name`\n\nNote that the query parameter values need to be URL escaped so that `=`\nwould become `%3D`.
///
/// Method: GET /teams/{username}/permissions
#[actor(
    BitbucketReadTeamsByUsernamePermissionsActor,
    inports::<100>(username, q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_permissions(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/permissions".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/permissions failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns an object for each repository permission for all of a\nteam’s repositories.\n\nIf the username URL parameter refers to a user account instead of\na team account, an object containing the repository permissions\nof all the username's repositories will be returned.\n\nPermissions returned are effective permissions — the highest level of\npermission the user has. This does not include public repositories that\nusers are not granted any specific permission in, and does not\ndistinguish between direct and indirect privileges.\n\nOnly users with admin permission for the team may access this resource.\n\nPermissions can be:\n\n* `admin`\n* `write`\n* `read`\n\nExample:\n\n```\n$ curl https://api.bitbucket.org/2.0/teams/atlassian_tutorial/permissions/repositories\n\n{\n  \"pagelen\": 10,\n  \"values\": [\n    {\n      \"type\": \"repository_permission\",\n      \"user\": {\n        \"type\": \"user\",\n        \"username\": \"evzijst\",\n        \"display_name\": \"Erik van Zijst\",\n        \"uuid\": \"{d301aafa-d676-4ee0-88be-962be7417567}\"\n      },\n      \"repository\": {\n        \"type\": \"repository\",\n        \"name\": \"geordi\",\n        \"full_name\": \"bitbucket/geordi\",\n        \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n      },\n      \"permission\": \"admin\"\n    },\n    {\n      \"type\": \"repository_permission\",\n      \"user\": {\n        \"type\": \"user\",\n        \"username\": \"seanaty\",\n        \"display_name\": \"Sean Conaty\",\n        \"uuid\": \"{504c3b62-8120-4f0c-a7bc-87800b9d6f70}\"\n      },\n      \"repository\": {\n        \"type\": \"repository\",\n        \"name\": \"geordi\",\n        \"full_name\": \"bitbucket/geordi\",\n        \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n      },\n      \"permission\": \"write\"\n    }\n  ],\n  \"page\": 1,\n  \"size\": 2\n}\n```\n\nResults may be further [filtered or sorted](../../../../meta/filtering)\nby repository, user, or permission by adding the following query string\nparameters:\n\n* `q=repository.name=\"geordi\"` or `q=permission>\"read\"`\n* `sort=user.display_name`\n\nNote that the query parameter values need to be URL escaped so that `=`\nwould become `%3D`.
///
/// Method: GET /teams/{username}/permissions/repositories
#[actor(
    BitbucketReadTeamsByUsernamePermissionsRepositoriesActor,
    inports::<100>(username, q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_permissions_repositories(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/permissions/repositories".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/permissions/repositories failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns an object for each repository permission of a given repository.\n\nIf the username URL parameter refers to a user account instead of\na team account, an object containing the repository permissions\nof the username's repository will be returned.\n\nPermissions returned are effective permissions — the highest level of\npermission the user has. This does not include public repositories that\nusers are not granted any specific permission in, and does not\ndistinguish between direct and indirect privileges.\n\nOnly users with admin permission for the repository may access this resource.\n\nPermissions can be:\n\n* `admin`\n* `write`\n* `read`\n\nExample:\n\n```\n$ curl https://api.bitbucket.org/2.0/teams/atlassian_tutorial/permissions/repositories/geordi\n\n{\n  \"pagelen\": 10,\n  \"values\": [\n    {\n      \"type\": \"repository_permission\",\n      \"user\": {\n        \"type\": \"user\",\n        \"username\": \"evzijst\",\n        \"display_name\": \"Erik van Zijst\",\n        \"uuid\": \"{d301aafa-d676-4ee0-88be-962be7417567}\"\n      },\n      \"repository\": {\n        \"type\": \"repository\",\n        \"name\": \"geordi\",\n        \"full_name\": \"bitbucket/geordi\",\n        \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n      },\n      \"permission\": \"admin\"\n    },\n    {\n      \"type\": \"repository_permission\",\n      \"user\": {\n        \"type\": \"user\",\n        \"username\": \"seanaty\",\n        \"display_name\": \"Sean Conaty\",\n        \"uuid\": \"{504c3b62-8120-4f0c-a7bc-87800b9d6f70}\"\n      },\n      \"repository\": {\n        \"type\": \"repository\",\n        \"name\": \"geordi\",\n        \"full_name\": \"bitbucket/geordi\",\n        \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n      },\n      \"permission\": \"write\"\n    }\n  ],\n  \"page\": 1,\n  \"size\": 2\n}\n```\n\nResults may be further [filtered or sorted](../../../../meta/filtering)\nby user, or permission by adding the following query string parameters:\n\n* `q=permission>\"read\"`\n* `sort=user.display_name`\n\nNote that the query parameter values need to be URL escaped so that `=`\nwould become `%3D`.
///
/// Method: GET /teams/{username}/permissions/repositories/{repo_slug}
#[actor(
    BitbucketReadTeamsByUsernamePermissionsRepositoriesByRepoSlugActor,
    inports::<100>(username, repo_slug, q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_permissions_repositories_by_repo_slug(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/permissions/repositories/{repo_slug}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("repo_slug") {
        endpoint = endpoint.replace("{{repo_slug}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/permissions/repositories/{{repo_slug}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Find account level variables.
///
/// Method: GET /teams/{username}/pipelines_config/variables/
#[actor(
    BitbucketReadPipelineVariablesForTeamActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipeline_variables_for_team(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/pipelines_config/variables/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/pipelines_config/variables/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create an account level variable.
///
/// Method: POST /teams/{username}/pipelines_config/variables/
#[actor(
    BitbucketCreatePipelineVariableForTeamActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_pipeline_variable_for_team(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/pipelines_config/variables/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /teams/{{username}}/pipelines_config/variables/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update a team level variable.
///
/// Method: PUT /teams/{username}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketUpdatePipelineVariableForTeamActor,
    inports::<100>(username, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_pipeline_variable_for_team(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /teams/{{username}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete a team level variable.
///
/// Method: DELETE /teams/{username}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketDeletePipelineVariableForTeamActor,
    inports::<100>(username, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_pipeline_variable_for_team(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /teams/{{username}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve a team level variable.
///
/// Method: GET /teams/{username}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketReadPipelineVariableForTeamActor,
    inports::<100>(username, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipeline_variable_for_team(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new project.\n\nNote that the avatar has to be embedded as either a data-url\nor a URL to an external image as shown in the examples below:\n\n```\n$ body=$(cat << EOF\n{\n    \"name\": \"Mars Project\",\n    \"key\": \"MARS\",\n    \"description\": \"Software for colonizing mars.\",\n    \"links\": {\n        \"avatar\": {\n            \"href\": \"data:image/gif;base64,R0lGODlhEAAQAMQAAORHHOVSKudfOulrSOp3WOyDZu6QdvCchPGolfO0o/...\"\n        }\n    },\n    \"is_private\": false\n}\nEOF\n)\n$ curl -H \"Content-Type: application/json\" \\\n       -X POST \\\n       -d \"$body\" \\\n       https://api.bitbucket.org/2.0/teams/teams-in-space/projects/ | jq .\n{\n  // Serialized project document\n}\n```\n\nor even:\n\n```\n$ body=$(cat << EOF\n{\n    \"name\": \"Mars Project\",\n    \"key\": \"MARS\",\n    \"description\": \"Software for colonizing mars.\",\n    \"links\": {\n        \"avatar\": {\n            \"href\": \"http://i.imgur.com/72tRx4w.gif\"\n        }\n    },\n    \"is_private\": false\n}\nEOF\n)\n$ curl -H \"Content-Type: application/json\" \\\n       -X POST \\\n       -d \"$body\" \\\n       https://api.bitbucket.org/2.0/teams/teams-in-space/projects/ | jq .\n{\n  // Serialized project document\n}\n```
///
/// Method: POST /teams/{username}/projects/
#[actor(
    BitbucketCreateTeamsByUsernameProjectsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_teams_by_username_projects(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/projects/".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /teams/{{username}}/projects/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: GET /teams/{username}/projects/
#[actor(
    BitbucketReadTeamsByUsernameProjectsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_projects(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/projects/".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/projects/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: GET /teams/{username}/projects/{project_key}
#[actor(
    BitbucketReadTeamsByUsernameProjectsByProjectKeyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_projects_by_project_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/projects/{project_key}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/projects/{{project_key}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// 
///
/// Method: DELETE /teams/{username}/projects/{project_key}
#[actor(
    BitbucketDeleteTeamsByUsernameProjectsByProjectKeyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_teams_by_username_projects_by_project_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/projects/{project_key}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /teams/{{username}}/projects/{{project_key}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Since this endpoint can be used to both update and to create a\nproject, the request body depends on the intent.\n\n### Creation\n\nSee the POST documentation for the project collection for an\nexample of the request body.\n\nNote: The `key` should not be specified in the body of request\n(since it is already present in the URL). The `name` is required,\neverything else is optional.\n\n### Update\n\nSee the POST documentation for the project collection for an\nexample of the request body.\n\nNote: The key is not required in the body (since it is already in\nthe URL). The key may be specified in the body, if the intent is\nto change the key itself. In such a scenario, the location of the\nproject is changed and is returned in the `Location` header of the\nresponse.
///
/// Method: PUT /teams/{username}/projects/{project_key}
#[actor(
    BitbucketUpdateTeamsByUsernameProjectsByProjectKeyActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_teams_by_username_projects_by_project_key(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/projects/{project_key}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /teams/{{username}}/projects/{{project_key}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// All repositories owned by a user/team. This includes private\nrepositories, but filtered down to the ones that the calling user has\naccess to.
///
/// Method: GET /teams/{username}/repositories
#[actor(
    BitbucketReadTeamsByUsernameRepositoriesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_teams_by_username_repositories(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/teams/{username}/repositories".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/repositories failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Search for code in the repositories of the specified team
///
/// Method: GET /teams/{username}/search/code
#[actor(
    BitbucketSearchAccountActor,
    inports::<100>(username, search_query, page, pagelen),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_search_account(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/teams/{username}/search/code".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("search_query") {
        query_pairs.push(("search_query", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("page") {
        query_pairs.push(("page", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("pagelen") {
        query_pairs.push(("pagelen", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /teams/{{username}}/search/code failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the currently logged in user.
///
/// Method: GET /user
#[actor(
    BitbucketReadUserActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/user".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /user failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all the authenticated user's email addresses. Both\nconfirmed and unconfirmed.
///
/// Method: GET /user/emails
#[actor(
    BitbucketReadUserEmailsActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_user_emails(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/user/emails".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /user/emails failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns details about a specific one of the authenticated user's\nemail addresses.\n\nDetails describe whether the address has been confirmed by the user and\nwhether it is the user's primary address or not.
///
/// Method: GET /user/emails/{email}
#[actor(
    BitbucketReadUserEmailsByEmailActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_user_emails_by_email(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/user/emails/{email}".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /user/emails/{{email}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns an object for each repository the caller has explicit access\nto and their effective permission — the highest level of permission the\ncaller has. This does not return public repositories that the user was\nnot granted any specific permission in, and does not distinguish between\ndirect and indirect privileges.\n\nPermissions can be:\n\n* `admin`\n* `write`\n* `read`\n\nExample:\n\n```\n$ curl https://api.bitbucket.org/2.0/user/permissions/repositories\n\n{\n  \"pagelen\": 10,\n  \"values\": [\n    {\n      \"type\": \"repository_permission\",\n      \"user\": {\n        \"type\": \"user\",\n        \"username\": \"evzijst\",\n        \"nickname\": \"evzijst\",\n        \"display_name\": \"Erik van Zijst\",\n        \"uuid\": \"{d301aafa-d676-4ee0-88be-962be7417567}\"\n      },\n      \"repository\": {\n        \"type\": \"repository\",\n        \"name\": \"geordi\",\n        \"full_name\": \"bitbucket/geordi\",\n        \"uuid\": \"{85d08b4e-571d-44e9-a507-fa476535aa98}\"\n      },\n      \"permission\": \"admin\"\n    }\n  ],\n  \"page\": 1,\n  \"size\": 1\n}\n```\n\nResults may be further [filtered or sorted](../../../meta/filtering) by\nrepository or permission by adding the following query string\nparameters:\n\n* `q=repository.name=\"geordi\"` or `q=permission>\"read\"`\n* `sort=repository.name`\n\nNote that the query parameter values need to be URL escaped so that `=`\nwould become `%3D`.
///
/// Method: GET /user/permissions/repositories
#[actor(
    BitbucketReadUserPermissionsRepositoriesActor,
    inports::<100>(q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_user_permissions_repositories(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/user/permissions/repositories".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /user/permissions/repositories failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns an object for each team the caller is a member of, and their\neffective role — the highest level of privilege the caller has. If a\nuser is a member of multiple groups with distinct roles, only the\nhighest level is returned.\n\nPermissions can be:\n\n* `admin`\n* `collaborator`\n\nExample:\n\n```\n$ curl https://api.bitbucket.org/2.0/user/permissions/teams\n\n{\n  \"pagelen\": 10,\n  \"values\": [\n    {\n      \"permission\": \"admin\",\n      \"type\": \"team_permission\",\n      \"user\": {\n        \"type\": \"user\",\n        \"username\": \"evzijst\",\n        \"nickname\": \"evzijst\",\n        \"display_name\": \"Erik van Zijst\",\n        \"uuid\": \"{d301aafa-d676-4ee0-88be-962be7417567}\"\n      },\n      \"team\": {\n        \"username\": \"bitbucket\",\n        \"display_name\": \"Atlassian Bitbucket\",\n        \"uuid\": \"{4cc6108a-a241-4db0-96a5-64347ac04f87}\"\n      }\n    }\n  ],\n  \"page\": 1,\n  \"size\": 1\n}\n```\n\nResults may be further [filtered or sorted](../../../meta/filtering) by\nteam or permission by adding the following query string parameters:\n\n* `q=team.username=\"bitbucket\"` or `q=permission=\"admin\"`\n* `sort=team.display_name`\n\nNote that the query parameter values need to be URL escaped so that `=`\nwould become `%3D`.
///
/// Method: GET /user/permissions/teams
#[actor(
    BitbucketReadUserPermissionsTeamsActor,
    inports::<100>(q, sort),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_user_permissions_teams(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/user/permissions/teams".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("q") {
        query_pairs.push(("q", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /user/permissions/teams failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Gets the public information associated with a user account.\n\nIf the user's profile is private, `location`, `website` and\n`created_on` elements are omitted.\n\nNote that the user object returned by this operation is changing significantly, due to privacy changes.\nSee the [announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/#changes-to-bitbucket-user-objects) for details.
///
/// Method: GET /users/{username}
#[actor(
    BitbucketReadUsersByUsernameActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_users_by_username(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the list of accounts that are following this user.\n\nThis operation has been deprecated due to privacy changes.\nSee the [announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/) for details.
///
/// Method: GET /users/{username}/followers
#[actor(
    BitbucketReadUsersByUsernameFollowersActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_users_by_username_followers(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/followers".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/followers failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the list of accounts this user is following.\n\nThis operation has been deprecated due to privacy changes.\nSee the [announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/) for details.
///
/// Method: GET /users/{username}/following
#[actor(
    BitbucketReadUsersByUsernameFollowingActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_users_by_username_following(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/following".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/following failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Creates a new webhook on the specified user account.\n\nAccount-level webhooks are fired for events from all repositories\nbelonging to that account.\n\nNote that one can only register webhooks on one's own account, not that\nof others.\n\nAlso, note that the username path parameter has been deprecated due to\n[privacy changes](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/#removal-of-usernames-from-user-referencing-apis).\nUse the account's UUID or account_id instead.
///
/// Method: POST /users/{username}/hooks
#[actor(
    BitbucketCreateUsersByUsernameHooksActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_users_by_username_hooks(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/users/{username}/hooks".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /users/{{username}}/hooks failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of webhooks installed on this user account.\n\nNote that the username path parameter has been deprecated due to\n[privacy changes](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/#removal-of-usernames-from-user-referencing-apis).\nUse the account's UUID or account_id instead.
///
/// Method: GET /users/{username}/hooks
#[actor(
    BitbucketReadUsersByUsernameHooksActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_users_by_username_hooks(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/users/{username}/hooks".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/hooks failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes the specified webhook subscription from the given user\naccount.\n\nNote that the username path parameter has been deprecated due to\n[privacy changes](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/#removal-of-usernames-from-user-referencing-apis).\nUse the account's UUID or account_id instead.
///
/// Method: DELETE /users/{username}/hooks/{uid}
#[actor(
    BitbucketDeleteUsersByUsernameHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_users_by_username_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /users/{{username}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns the webhook with the specified id installed on the given\nuser account.\n\nNote that the username path parameter has been deprecated due to\n[privacy changes](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/#removal-of-usernames-from-user-referencing-apis).\nUse the account's UUID or account_id instead.
///
/// Method: GET /users/{username}/hooks/{uid}
#[actor(
    BitbucketReadUsersByUsernameHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_users_by_username_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Updates the specified webhook subscription.\n\nThe following properties can be mutated:\n\n* `description`\n* `url`\n* `active`\n* `events`\n\nNote that the username path parameter has been deprecated due to\n[privacy changes](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/#removal-of-usernames-from-user-referencing-apis).\nUse the account's UUID or account_id instead.
///
/// Method: PUT /users/{username}/hooks/{uid}
#[actor(
    BitbucketUpdateUsersByUsernameHooksByUidActor,
    inports::<100>(uid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_users_by_username_hooks_by_uid(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/hooks/{uid}".to_string();
    if let Some(val) = inputs.get("uid") {
        endpoint = endpoint.replace("{{uid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /users/{{username}}/hooks/{{uid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns all members of the specified team. Any member of any of the\nteam's groups is considered a member of the team. This includes users\nin groups that may not actually have access to any of the team's\nrepositories.\n\nThis operation has been deprecated due to privacy changes.\nSee the [announcement](https://developer.atlassian.com/cloud/bitbucket/bitbucket-api-changes-gdpr/)\nfor details.
///
/// Method: GET /users/{username}/members
#[actor(
    BitbucketReadUsersByUsernameMembersActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_users_by_username_members(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/users/{username}/members".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/members failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Find user level variables.
///
/// Method: GET /users/{username}/pipelines_config/variables/
#[actor(
    BitbucketReadPipelineVariablesForUserActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipeline_variables_for_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/pipelines_config/variables/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/pipelines_config/variables/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Create a user level variable.
///
/// Method: POST /users/{username}/pipelines_config/variables/
#[actor(
    BitbucketCreatePipelineVariableForUserActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_pipeline_variable_for_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/pipelines_config/variables/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /users/{{username}}/pipelines_config/variables/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete an account level variable.
///
/// Method: DELETE /users/{username}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketDeletePipelineVariableForUserActor,
    inports::<100>(username, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_pipeline_variable_for_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /users/{{username}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve a user level variable.
///
/// Method: GET /users/{username}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketReadPipelineVariableForUserActor,
    inports::<100>(username, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_pipeline_variable_for_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update a user level variable.
///
/// Method: PUT /users/{username}/pipelines_config/variables/{variable_uuid}
#[actor(
    BitbucketUpdatePipelineVariableForUserActor,
    inports::<100>(username, variable_uuid),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_pipeline_variable_for_user(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/pipelines_config/variables/{variable_uuid}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("variable_uuid") {
        endpoint = endpoint.replace("{{variable_uuid}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /users/{{username}}/pipelines_config/variables/{{variable_uuid}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Retrieve an application property value stored against a user.
///
/// Method: GET /users/{username}/properties/{app_key}/{property_name}
#[actor(
    BitbucketReadPropertiesActor,
    inports::<100>(username, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_properties(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Delete an application property value stored against a user.
///
/// Method: DELETE /users/{username}/properties/{app_key}/{property_name}
#[actor(
    BitbucketDeleteUserHostedPropertyValueActor,
    inports::<100>(username, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_user_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /users/{{username}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Update an application property value stored against a user.
///
/// Method: PUT /users/{username}/properties/{app_key}/{property_name}
#[actor(
    BitbucketUpdateUserHostedPropertyValueActor,
    inports::<100>(username, app_key, property_name),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_user_hosted_property_value(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/properties/{app_key}/{property_name}".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("app_key") {
        endpoint = endpoint.replace("{{app_key}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("property_name") {
        endpoint = endpoint.replace("{{property_name}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /users/{{username}}/properties/{{app_key}}/{{property_name}} failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// All repositories owned by a user/team. This includes private\nrepositories, but filtered down to the ones that the calling user has\naccess to.
///
/// Method: GET /users/{username}/repositories
#[actor(
    BitbucketReadUsersByUsernameRepositoriesActor,
    inports::<100>(trigger),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_users_by_username_repositories(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/users/{username}/repositories".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/repositories failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Adds a new SSH public key to the specified user account and returns the resulting key.\n\nExample:\n```\n$ curl -X POST -H \"Content-Type: application/json\" -d '{\"key\": \"ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKqP3Cr632C2dNhhgKVcon4ldUSAeKiku2yP9O9/bDtY user@myhost\"}' https://api.bitbucket.org/2.0/users/markadams-atl/ssh-keys\n\n{\n    \"comment\": \"user@myhost\",\n    \"created_on\": \"2018-03-14T13:17:05.196003+00:00\",\n    \"key\": \"ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKqP3Cr632C2dNhhgKVcon4ldUSAeKiku2yP9O9/bDtY\",\n    \"label\": \"\",\n    \"last_used\": \"2018-03-20T13:18:05.196003+00:00\",\n    \"links\": {\n        \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/users/markadams-atl/ssh-keys/b15b6026-9c02-4626-b4ad-b905f99f763a\"\n        }\n    },\n    \"owner\": {\n        \"display_name\": \"Mark Adams\",\n        \"links\": {\n            \"avatar\": {\n                \"href\": \"https://bitbucket.org/account/markadams-atl/avatar/32/\"\n            },\n            \"html\": {\n                \"href\": \"https://bitbucket.org/markadams-atl/\"\n            },\n            \"self\": {\n                \"href\": \"https://api.bitbucket.org/2.0/users/markadams-atl\"\n            }\n        },\n        \"type\": \"user\",\n        \"username\": \"markadams-atl\",\n        \"nickname\": \"markadams-atl\",\n        \"uuid\": \"{d7dd0e2d-3994-4a50-a9ee-d260b6cefdab}\"\n    },\n    \"type\": \"ssh_key\",\n    \"uuid\": \"{b15b6026-9c02-4626-b4ad-b905f99f763a}\"\n}\n```
///
/// Method: POST /users/{username}/ssh-keys
#[actor(
    BitbucketCreateUsersByUsernameSshkeysActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_create_users_by_username_sshkeys(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/ssh-keys".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.post(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("POST /users/{{username}}/ssh-keys failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Returns a paginated list of the user's SSH public keys.\n\nExample:\n\n```\n$ curl https://api.bitbucket.org/2.0/users/markadams-atl/ssh-keys\n{\n    \"page\": 1,\n    \"pagelen\": 10,\n    \"size\": 1,\n    \"values\": [\n        {\n            \"comment\": \"user@myhost\",\n            \"created_on\": \"2018-03-14T13:17:05.196003+00:00\",\n            \"key\": \"ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKqP3Cr632C2dNhhgKVcon4ldUSAeKiku2yP9O9/bDtY\",\n            \"label\": \"\",\n            \"last_used\": \"2018-03-20T13:18:05.196003+00:00\",\n            \"links\": {\n                \"self\": {\n                    \"href\": \"https://api.bitbucket.org/2.0/users/markadams-atl/ssh-keys/b15b6026-9c02-4626-b4ad-b905f99f763a\"\n                }\n            },\n            \"owner\": {\n                \"display_name\": \"Mark Adams\",\n                \"links\": {\n                    \"avatar\": {\n                        \"href\": \"https://bitbucket.org/account/markadams-atl/avatar/32/\"\n                    },\n                    \"html\": {\n                        \"href\": \"https://bitbucket.org/markadams-atl/\"\n                    },\n                    \"self\": {\n                        \"href\": \"https://api.bitbucket.org/2.0/users/markadams-atl\"\n                    }\n                },\n                \"type\": \"user\",\n                \"username\": \"markadams-atl\",\n                \"nickname\": \"markadams-atl\",\n                \"uuid\": \"{d7dd0e2d-3994-4a50-a9ee-d260b6cefdab}\"\n            },\n            \"type\": \"ssh_key\",\n            \"uuid\": \"{b15b6026-9c02-4626-b4ad-b905f99f763a}\"\n        }\n    ]\n}\n```
///
/// Method: GET /users/{username}/ssh-keys
#[actor(
    BitbucketReadUsersByUsernameSshkeysActor,
    inports::<100>(username),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_read_users_by_username_sshkeys(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/ssh-keys".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("GET /users/{{username}}/ssh-keys failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Deletes a specific SSH public key from a user's account\n\nExample:\n```\n$ curl -X DELETE https://api.bitbucket.org/2.0/users/markadams-atl/ssh-keys/{b15b6026-9c02-4626-b4ad-b905f99f763a}\n```
///
/// Method: DELETE /users/{username}/ssh-keys/
#[actor(
    BitbucketDeleteUsersByUsernameSshkeysActor,
    inports::<100>(username, key_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_delete_users_by_username_sshkeys(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/ssh-keys/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("key_id") {
        endpoint = endpoint.replace("{{key_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.delete(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("DELETE /users/{{username}}/ssh-keys/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

/// Updates a specific SSH public key on a user's account\n\nNote: Only the 'comment' field can be updated using this API. To modify the key or comment values, you must delete and add the key again.\n\nExample:\n```\n$ curl -X PUT -H \"Content-Type: application/json\" -d '{\"label\": \"Work key\"}' https://api.bitbucket.org/2.0/users/markadams-atl/ssh-keys/{b15b6026-9c02-4626-b4ad-b905f99f763a}\n\n{\n    \"comment\": \"\",\n    \"created_on\": \"2018-03-14T13:17:05.196003+00:00\",\n    \"key\": \"ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKqP3Cr632C2dNhhgKVcon4ldUSAeKiku2yP9O9/bDtY\",\n    \"label\": \"Work key\",\n    \"last_used\": \"2018-03-20T13:18:05.196003+00:00\",\n    \"links\": {\n        \"self\": {\n            \"href\": \"https://api.bitbucket.org/2.0/users/markadams-atl/ssh-keys/b15b6026-9c02-4626-b4ad-b905f99f763a\"\n        }\n    },\n    \"owner\": {\n        \"display_name\": \"Mark Adams\",\n        \"links\": {\n            \"avatar\": {\n                \"href\": \"https://bitbucket.org/account/markadams-atl/avatar/32/\"\n            },\n            \"html\": {\n                \"href\": \"https://bitbucket.org/markadams-atl/\"\n            },\n            \"self\": {\n                \"href\": \"https://api.bitbucket.org/2.0/users/markadams-atl\"\n            }\n        },\n        \"type\": \"user\",\n        \"username\": \"markadams-atl\",\n        \"nickname\": \"markadams-atl\",\n        \"uuid\": \"{d7dd0e2d-3994-4a50-a9ee-d260b6cefdab}\"\n    },\n    \"type\": \"ssh_key\",\n    \"uuid\": \"{b15b6026-9c02-4626-b4ad-b905f99f763a}\"\n}\n```
///
/// Method: PUT /users/{username}/ssh-keys/
#[actor(
    BitbucketUpdateUsersByUsernameSshkeysActor,
    inports::<100>(username, key_id),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn bitbucket_update_users_by_username_sshkeys(context: ActorContext) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let mut endpoint = "/users/{username}/ssh-keys/".to_string();
    if let Some(val) = inputs.get("username") {
        endpoint = endpoint.replace("{{username}}", &super::message_to_str(val));
    }
    if let Some(val) = inputs.get("key_id") {
        endpoint = endpoint.replace("{{key_id}}", &super::message_to_str(val));
    }

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout(Duration::from_secs(30))
        .build()?;

    let mut builder = client.put(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp.headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value = serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert("response".to_string(), Message::object(EncodableValue::from(json!({
                "status": status,
                "headers": headers,
                "body": body_value,
            }))));
        }
        Err(e) => {
            output.insert("error".to_string(), Message::Error(format!("PUT /users/{{username}}/ssh-keys/ failed: {}", e).into()));
        }
    }

    Ok(output)
}

