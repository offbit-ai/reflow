#![allow(clippy::all, unused_imports, dead_code)]

//! Auto-generated API actors for Alpha Vantage
//!
//! Service: Alpha Vantage (alphavantage)
//! Stock market data and financial indicators
//!
//! Required env var: ALPHAVANTAGE_API_KEY (API key)
//!
//! Generated by api-schema-gen codegen — do not edit manually.

use crate::{Actor, ActorBehavior, ClientBuilderExt, Message, Port};
use anyhow::{Error, Result};
use reflow_actor::{message::EncodableValue, ActorContext};
use reflow_actor_macro::actor;
use serde_json::{json, Value};
use std::collections::HashMap;
use std::time::Duration;

const BASE_URL: &str = "https://www.alphavantage.co/query";
const ENV_KEY: &str = "ALPHAVANTAGE_API_KEY";

/// Apply authentication to the request builder.
fn apply_auth(
    config: &reflow_actor::ActorConfig,
    mut builder: reqwest::RequestBuilder,
) -> Result<reqwest::RequestBuilder> {
    let credential = config
        .get_config_or_env(ENV_KEY)
        .ok_or_else(|| anyhow::anyhow!("Missing env var: {}", ENV_KEY))?;
    builder = builder.query(&[("apikey", &credential)]);
    Ok(builder)
}

/// read time_series via Alpha Vantage API
///
/// Method: GET /
#[actor(
    AlphavantageReadTimeSeriesActor,
    inports::<100>(function, symbol, interval),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_time_series(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET / failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// search symbols via Alpha Vantage API
///
/// Method: GET /
#[actor(
    AlphavantageSearchSymbolsActor,
    inports::<100>(function, keywords),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_search_symbols(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("keywords") {
        query_pairs.push(("keywords", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET / failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get intraday OHLCV time series for a stock symbol
///
/// Method: GET /query
#[actor(
    AlphavantageReadTimeSeriesIntradayActor,
    inports::<100>(function, symbol, interval, adjusted, extended_hours, month, outputsize, datatype, entitlement, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_time_series_intraday(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("adjusted") {
        query_pairs.push(("adjusted", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("extended_hours") {
        query_pairs.push(("extended_hours", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("month") {
        query_pairs.push(("month", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("outputsize") {
        query_pairs.push(("outputsize", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("entitlement") {
        query_pairs.push(("entitlement", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get daily OHLCV time series for a stock symbol
///
/// Method: GET /query
#[actor(
    AlphavantageReadTimeSeriesDailyActor,
    inports::<100>(function, symbol, outputsize, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_time_series_daily(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("outputsize") {
        query_pairs.push(("outputsize", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get daily adjusted OHLCV time series with split/dividend adjustment
///
/// Method: GET /query
#[actor(
    AlphavantageReadTimeSeriesDailyAdjustedActor,
    inports::<100>(function, symbol, outputsize, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_time_series_daily_adjusted(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("outputsize") {
        query_pairs.push(("outputsize", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get weekly OHLCV time series for a stock symbol
///
/// Method: GET /query
#[actor(
    AlphavantageReadTimeSeriesWeeklyActor,
    inports::<100>(function, symbol, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_time_series_weekly(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get weekly adjusted OHLCV time series
///
/// Method: GET /query
#[actor(
    AlphavantageReadTimeSeriesWeeklyAdjustedActor,
    inports::<100>(function, symbol, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_time_series_weekly_adjusted(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get monthly OHLCV time series for a stock symbol
///
/// Method: GET /query
#[actor(
    AlphavantageReadTimeSeriesMonthlyActor,
    inports::<100>(function, symbol, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_time_series_monthly(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get monthly adjusted OHLCV time series
///
/// Method: GET /query
#[actor(
    AlphavantageReadTimeSeriesMonthlyAdjustedActor,
    inports::<100>(function, symbol, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_time_series_monthly_adjusted(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get the latest price and volume information for a symbol
///
/// Method: GET /query
#[actor(
    AlphavantageReadGlobalQuoteActor,
    inports::<100>(function, symbol, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_global_quote(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get realtime quotes for multiple symbols
///
/// Method: GET /query
#[actor(
    AlphavantageReadRealtimeBulkQuotesActor,
    inports::<100>(function, symbol, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_realtime_bulk_quotes(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get the current market open/close status of major trading venues
///
/// Method: GET /query
#[actor(
    AlphavantageReadMarketStatusActor,
    inports::<100>(function, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_market_status(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get realtime US options data for a specific symbol
///
/// Method: GET /query
#[actor(
    AlphavantageReadRealtimeOptionsActor,
    inports::<100>(function, symbol, contract, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_realtime_options(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("contract") {
        query_pairs.push(("contract", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get historical US options data for a specific symbol
///
/// Method: GET /query
#[actor(
    AlphavantageReadHistoricalOptionsActor,
    inports::<100>(function, symbol, date, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_historical_options(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("date") {
        query_pairs.push(("date", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get news and sentiment data for stocks, forex, and crypto
///
/// Method: GET /query
#[actor(
    AlphavantageReadNewsSentimentActor,
    inports::<100>(function, tickers, topics, time_from, time_to, sort, limit, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_news_sentiment(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("tickers") {
        query_pairs.push(("tickers", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("topics") {
        query_pairs.push(("topics", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_from") {
        query_pairs.push(("time_from", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_to") {
        query_pairs.push(("time_to", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("sort") {
        query_pairs.push(("sort", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("limit") {
        query_pairs.push(("limit", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get earnings call transcript for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadEarningsCallTranscriptActor,
    inports::<100>(function, symbol, quarter, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_earnings_call_transcript(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("quarter") {
        query_pairs.push(("quarter", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get the top 20 gainers, losers, and most active tickers in the US market
///
/// Method: GET /query
#[actor(
    AlphavantageReadTopGainersLosersActor,
    inports::<100>(function, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_top_gainers_losers(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get insider transactions (SEC Form 4 filings) for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadInsiderTransactionsActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_insider_transactions(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get institutional ownership data for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadInstitutionalHoldingsActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_institutional_holdings(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Compute analytics and statistics over a fixed time window
///
/// Method: GET /query
#[actor(
    AlphavantageAnalyzeAnalyticsFixedWindowActor,
    inports::<100>(function, SYMBOLS, RANGE, INTERVAL, OHLC, CALCULATIONS, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_analyze_analytics_fixed_window(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("SYMBOLS") {
        query_pairs.push(("SYMBOLS", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("RANGE") {
        query_pairs.push(("RANGE", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("INTERVAL") {
        query_pairs.push(("INTERVAL", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("OHLC") {
        query_pairs.push(("OHLC", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("CALCULATIONS") {
        query_pairs.push(("CALCULATIONS", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Compute analytics and statistics over a sliding time window
///
/// Method: GET /query
#[actor(
    AlphavantageAnalyzeAnalyticsSlidingWindowActor,
    inports::<100>(function, SYMBOLS, RANGE, INTERVAL, WINDOW_SIZE, CALCULATIONS, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_analyze_analytics_sliding_window(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("SYMBOLS") {
        query_pairs.push(("SYMBOLS", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("RANGE") {
        query_pairs.push(("RANGE", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("INTERVAL") {
        query_pairs.push(("INTERVAL", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("WINDOW_SIZE") {
        query_pairs.push(("WINDOW_SIZE", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("CALCULATIONS") {
        query_pairs.push(("CALCULATIONS", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get company overview, financial ratios, and other key metrics
///
/// Method: GET /query
#[actor(
    AlphavantageReadCompanyOverviewActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_company_overview(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get ETF profile and holdings information
///
/// Method: GET /query
#[actor(
    AlphavantageReadEtfProfileActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_etf_profile(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get historical and upcoming dividend data for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadDividendsActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_dividends(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get historical stock split events for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadSplitsActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_splits(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get annual and quarterly income statements for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadIncomeStatementActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_income_statement(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get annual and quarterly balance sheets for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadBalanceSheetActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_balance_sheet(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get annual and quarterly cash flow statements for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadCashFlowActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_cash_flow(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get historical shares outstanding data for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadSharesOutstandingActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_shares_outstanding(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get annual and quarterly earnings data for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadEarningsActor,
    inports::<100>(function, symbol, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_earnings(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get upcoming earnings dates for a company
///
/// Method: GET /query
#[actor(
    AlphavantageReadEarningsCalendarActor,
    inports::<100>(function, symbol, horizon, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_earnings_calendar(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("horizon") {
        query_pairs.push(("horizon", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get list of active or delisted US stocks and ETFs
///
/// Method: GET /query
#[actor(
    AlphavantageReadListingStatusActor,
    inports::<100>(function, date, state, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_listing_status(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("date") {
        query_pairs.push(("date", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("state") {
        query_pairs.push(("state", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get expected upcoming IPOs in the next 3 months
///
/// Method: GET /query
#[actor(
    AlphavantageReadIpoCalendarActor,
    inports::<100>(function, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_ipo_calendar(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get realtime currency exchange rate
///
/// Method: GET /query
#[actor(
    AlphavantageReadFxExchangeRateActor,
    inports::<100>(function, from_currency, to_currency, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_fx_exchange_rate(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("from_currency") {
        query_pairs.push(("from_currency", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("to_currency") {
        query_pairs.push(("to_currency", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get intraday time series for a currency pair
///
/// Method: GET /query
#[actor(
    AlphavantageReadFxIntradayActor,
    inports::<100>(function, from_symbol, to_symbol, interval, outputsize, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_fx_intraday(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("from_symbol") {
        query_pairs.push(("from_symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("to_symbol") {
        query_pairs.push(("to_symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("outputsize") {
        query_pairs.push(("outputsize", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get daily exchange rates for a currency pair
///
/// Method: GET /query
#[actor(
    AlphavantageReadFxDailyActor,
    inports::<100>(function, from_symbol, to_symbol, outputsize, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_fx_daily(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("from_symbol") {
        query_pairs.push(("from_symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("to_symbol") {
        query_pairs.push(("to_symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("outputsize") {
        query_pairs.push(("outputsize", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get weekly exchange rates for a currency pair
///
/// Method: GET /query
#[actor(
    AlphavantageReadFxWeeklyActor,
    inports::<100>(function, from_symbol, to_symbol, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_fx_weekly(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("from_symbol") {
        query_pairs.push(("from_symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("to_symbol") {
        query_pairs.push(("to_symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get monthly exchange rates for a currency pair
///
/// Method: GET /query
#[actor(
    AlphavantageReadFxMonthlyActor,
    inports::<100>(function, from_symbol, to_symbol, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_fx_monthly(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("from_symbol") {
        query_pairs.push(("from_symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("to_symbol") {
        query_pairs.push(("to_symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get realtime exchange rate for a digital currency
///
/// Method: GET /query
#[actor(
    AlphavantageReadCryptoExchangeRateActor,
    inports::<100>(function, from_currency, to_currency, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_crypto_exchange_rate(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("from_currency") {
        query_pairs.push(("from_currency", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("to_currency") {
        query_pairs.push(("to_currency", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get intraday time series for a cryptocurrency
///
/// Method: GET /query
#[actor(
    AlphavantageReadCryptoIntradayActor,
    inports::<100>(function, symbol, market, interval, outputsize, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_crypto_intraday(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("market") {
        query_pairs.push(("market", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("outputsize") {
        query_pairs.push(("outputsize", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get daily historical time series for a cryptocurrency
///
/// Method: GET /query
#[actor(
    AlphavantageReadCryptoDailyActor,
    inports::<100>(function, symbol, market, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_crypto_daily(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("market") {
        query_pairs.push(("market", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get weekly historical time series for a cryptocurrency
///
/// Method: GET /query
#[actor(
    AlphavantageReadCryptoWeeklyActor,
    inports::<100>(function, symbol, market, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_crypto_weekly(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("market") {
        query_pairs.push(("market", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get monthly historical time series for a cryptocurrency
///
/// Method: GET /query
#[actor(
    AlphavantageReadCryptoMonthlyActor,
    inports::<100>(function, symbol, market, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_crypto_monthly(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("market") {
        query_pairs.push(("market", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get historical WTI crude oil prices
///
/// Method: GET /query
#[actor(
    AlphavantageReadWtiCrudeOilActor,
    inports::<100>(function, interval, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_wti_crude_oil(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get historical Brent crude oil prices
///
/// Method: GET /query
#[actor(
    AlphavantageReadBrentCrudeOilActor,
    inports::<100>(function, interval, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_brent_crude_oil(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get historical Henry Hub natural gas prices
///
/// Method: GET /query
#[actor(
    AlphavantageReadNaturalGasActor,
    inports::<100>(function, interval, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_natural_gas(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get historical prices for various commodities (copper, aluminum, wheat, corn, cotton, sugar, coffee)
///
/// Method: GET /query
#[actor(
    AlphavantageReadCommodityPricesActor,
    inports::<100>(function, interval, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_commodity_prices(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get annual and quarterly Real GDP data for the US
///
/// Method: GET /query
#[actor(
    AlphavantageReadRealGdpActor,
    inports::<100>(function, interval, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_real_gdp(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get quarterly Real GDP per Capita for the US
///
/// Method: GET /query
#[actor(
    AlphavantageReadRealGdpPerCapitaActor,
    inports::<100>(function, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_real_gdp_per_capita(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get daily, weekly, and monthly US Treasury yield
///
/// Method: GET /query
#[actor(
    AlphavantageReadTreasuryYieldActor,
    inports::<100>(function, interval, maturity, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_treasury_yield(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("maturity") {
        query_pairs.push(("maturity", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get daily, weekly, and monthly federal funds rate
///
/// Method: GET /query
#[actor(
    AlphavantageReadFederalFundsRateActor,
    inports::<100>(function, interval, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_federal_funds_rate(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get monthly and semiannual Consumer Price Index data
///
/// Method: GET /query
#[actor(
    AlphavantageReadCpiActor,
    inports::<100>(function, interval, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_cpi(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get annual inflation rates for the US
///
/// Method: GET /query
#[actor(
    AlphavantageReadInflationActor,
    inports::<100>(function, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_inflation(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get monthly advance retail sales data
///
/// Method: GET /query
#[actor(
    AlphavantageReadRetailSalesActor,
    inports::<100>(function, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_retail_sales(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get monthly US unemployment rate data
///
/// Method: GET /query
#[actor(
    AlphavantageReadUnemploymentRateActor,
    inports::<100>(function, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_unemployment_rate(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get monthly US nonfarm payroll data
///
/// Method: GET /query
#[actor(
    AlphavantageReadNonfarmPayrollActor,
    inports::<100>(function, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_nonfarm_payroll(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Simple Moving Average (SMA) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorSmaActor,
    inports::<100>(function, symbol, interval, time_period, series_type, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_sma(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_period") {
        query_pairs.push(("time_period", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("series_type") {
        query_pairs.push(("series_type", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Exponential Moving Average (EMA) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorEmaActor,
    inports::<100>(function, symbol, interval, time_period, series_type, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_ema(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_period") {
        query_pairs.push(("time_period", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("series_type") {
        query_pairs.push(("series_type", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Relative Strength Index (RSI) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorRsiActor,
    inports::<100>(function, symbol, interval, time_period, series_type, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_rsi(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_period") {
        query_pairs.push(("time_period", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("series_type") {
        query_pairs.push(("series_type", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Moving Average Convergence/Divergence (MACD) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorMacdActor,
    inports::<100>(function, symbol, interval, series_type, fastperiod, slowperiod, signalperiod, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_macd(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("series_type") {
        query_pairs.push(("series_type", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fastperiod") {
        query_pairs.push(("fastperiod", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("slowperiod") {
        query_pairs.push(("slowperiod", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("signalperiod") {
        query_pairs.push(("signalperiod", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Bollinger Bands (BBANDS) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorBbandsActor,
    inports::<100>(function, symbol, interval, time_period, series_type, nbdevup, nbdevdn, matype, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_bbands(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_period") {
        query_pairs.push(("time_period", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("series_type") {
        query_pairs.push(("series_type", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("nbdevup") {
        query_pairs.push(("nbdevup", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("nbdevdn") {
        query_pairs.push(("nbdevdn", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("matype") {
        query_pairs.push(("matype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Stochastic Oscillator (STOCH) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorStochActor,
    inports::<100>(function, symbol, interval, fastkperiod, slowkperiod, slowdperiod, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_stoch(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("fastkperiod") {
        query_pairs.push(("fastkperiod", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("slowkperiod") {
        query_pairs.push(("slowkperiod", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("slowdperiod") {
        query_pairs.push(("slowdperiod", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Average Directional Movement Index (ADX) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorAdxActor,
    inports::<100>(function, symbol, interval, time_period, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_adx(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_period") {
        query_pairs.push(("time_period", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Commodity Channel Index (CCI) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorCciActor,
    inports::<100>(function, symbol, interval, time_period, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_cci(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_period") {
        query_pairs.push(("time_period", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get Aroon indicator values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorAroonActor,
    inports::<100>(function, symbol, interval, time_period, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_aroon(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("time_period") {
        query_pairs.push(("time_period", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}

/// Get On Balance Volume (OBV) values
///
/// Method: GET /query
#[actor(
    AlphavantageReadTechnicalIndicatorObvActor,
    inports::<100>(function, symbol, interval, datatype, apikey),
    outports::<50>(response, error),
    state(MemoryState)
)]
pub async fn alphavantage_read_technical_indicator_obv(
    context: ActorContext,
) -> Result<HashMap<String, Message>, Error> {
    let inputs = context.get_payload();
    let actor_config = context.get_config();

    let endpoint = "/query".to_string();

    let url = format!("{}{}", BASE_URL.trim_end_matches('/'), endpoint);

    let client = reqwest::Client::builder()
        .timeout_compat(Duration::from_secs(30))
        .build()?;

    let mut builder = client.get(&url);
    builder = builder.header("Content-Type", "application/json");
    builder = apply_auth(actor_config, builder)?;

    let mut query_pairs: Vec<(&str, String)> = Vec::new();
    if let Some(val) = inputs.get("function") {
        query_pairs.push(("function", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("symbol") {
        query_pairs.push(("symbol", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("interval") {
        query_pairs.push(("interval", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("datatype") {
        query_pairs.push(("datatype", super::message_to_str(val)));
    }
    if let Some(val) = inputs.get("apikey") {
        query_pairs.push(("apikey", super::message_to_str(val)));
    }
    if !query_pairs.is_empty() {
        builder = builder.query(&query_pairs);
    }

    let mut output = HashMap::new();
    match builder.send().await {
        Ok(resp) => {
            let status = resp.status().as_u16();
            let headers: HashMap<String, String> = resp
                .headers()
                .iter()
                .filter_map(|(k, v)| v.to_str().ok().map(|val| (k.to_string(), val.to_string())))
                .collect();
            let body_text = resp.text().await.unwrap_or_default();
            let body_value: Value =
                serde_json::from_str(&body_text).unwrap_or(Value::String(body_text));
            output.insert(
                "response".to_string(),
                Message::object(EncodableValue::from(json!({
                    "status": status,
                    "headers": headers,
                    "body": body_value,
                }))),
            );
        }
        Err(e) => {
            output.insert(
                "error".to_string(),
                Message::Error(format!("GET /query failed: {}", e).into()),
            );
        }
    }

    Ok(output)
}
